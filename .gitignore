# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Virtualenv (project-local, provisioned by @dev — NOT committed)
.venv/
venv/
requirements.lock

# Env / secrets
.env
.env.*
!.env.example
!.env.vespera.example
config_local.py
*.pem
*.key

# Vespera materialized data (parquet cache — regenerable from Sentinel DB)
# manifest.csv is committed separately (source of truth for hashes)
data/in_sample/
data/hold_out/
# Pre-cache layer (ADR-4; local mirror of raw Sentinel trades — regenerable)
data/cache/

# Scratch / WIP evidence (regenerable, large parquet artifacts NOT custodial)
data/_scratch/
data/baseline-run/scratch/
data/baseline-run/ra-20260428-1-decision-3-scratch/
data/manifest_PREVIEW.csv

# Tmp / scratch (build pilots, peak captures, PR body drafts)
.tmp/

# IDE
.vscode/
.idea/
*.swp

# ProfitDLL local copies (too big for git)
*.dll

# Runtime state (ATR/percentile caches, logs)
state/
logs/

# OS
Thumbs.db
.DS_Store

# AIOX runtime
.aiox/handoffs/
