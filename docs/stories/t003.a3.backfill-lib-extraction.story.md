# Story T003.A3 — Backfill lib structural extraction (post-bulk hardening)

**Epic:** T003 — Pre-2024 WDOFUT data acquisition for H_next preregistered window.
**Title:** DLL backfill structural extraction — promote Phase-2C prototype primitives into hardened `dll_backfill_lib.py` module surface (ChunkPlanner class, ManifestStore atomic-write class, ValidateGate Nova C1-C10, recovery tool, report generator, manifest schema bump 15→19) WITHOUT regressing prototype behaviour or acquisition viability.
**Wave:** Successor to T003.A2 Phase-2C prototype. Created 2026-05-03 BRT by Pax (@po) per HYBRID adjudication of Aria C3 BLOCKING-ish (Council 2026-05-03 R1 Amendment ratified 6/6 + user MWF cosign).
**Status:** **Draft** (gated on T003.A2 bulk-run completion + Quinn prototype QA verdict).
**Branch:** TBD (will be `t003-a3-backfill-lib-extraction` per River cosign post-T003.A2 bulk closure).
**Owner (Architecture):** Aria (@architect) — class boundary design + refactor plan + regression-prevention spec.
**Owner (Storage):** Dara (@data-engineer) — manifest schema 15→19 with backward compat read; history snapshot cadence; ManifestStore class API.
**Owner (Audit Gates):** Nova (@market-microstructure) — ValidateGate Nova C1-C10 hardening (intra-day trade_num monotonic, vol_brl consistency, sha256 manifest integrity, etc.) — auction-tolerant per Q-FIN-12-E carry-forward.
**Owner (DLL Quirks):** Nelo (@profitdll-specialist) — quirk anchors review; Q-RANGE-13-E and Q-FIN-12-E carry-forward into extracted classes.
**Owner (Impl):** Dex (@dev) — refactor commits; commit-per-class atomic discipline.
**Owner (Verify):** Quinn (@qa) — regression suite (prototype tests survive refactor); 7-check gate per `qa-gate.md`.
**Reviewers:** Sable (@governance) pre-push coherence audit; Pax cosign on AC verification.
**Source:** Pax adjudication 2026-05-03 (T003.A2 §0.5 amendment) — debt named, not hidden.
**Estimate:** ~3-5 squad sessions (extraction + tests + Quinn QA + Sable audit + Gage push).

---

## Context

T003.A2 was authored 2026-05-02 with 10 ACs predicated on extracted classes (`ChunkPlanner`, `ManifestStore`, `ValidateGate`) and tooling (`recover_parquet_from_jsonl.py`, `generate_backfill_report.py`) plus a 19-col manifest schema. Implementation pivoted during Phase-2A→2C to a **monolithic prototype** at HEAD `b1802ac` that delivers acquisition feasibility but defers structural extraction. The prototype is approved to proceed to bulk acquisition; T003.A3 captures the extraction debt as named work to prevent silent technical-debt accumulation. See `docs/stories/t003.a2.dll-backfill-orchestrator.story.md` §0.5 for the full status amendment.

---

## User story

Como **Aria (@architect) responsável pela manutenibilidade do orchestrator** e **Quinn (@qa) responsável por regression coverage**, preciso que os primitivos hoje monolíticos no `scripts/dll_backfill_orchestrator.py` (Phase-2C prototype) sejam **promovidos a classes extraídas** em `scripts/dll_backfill_lib.py`, mais o catálogo de tooling auxiliar (recovery + report) seja criado, e o manifest schema seja bumped 15→19 colunas com backward-compat read — **sem regredir o comportamento de aquisição** já validado pelo bulk run.

---

## Acceptance criteria (binary-verifiable)

- [ ] **AC1 (ChunkPlanner class extraction)** — Extract `plan_chunks` + `Chunk` dataclass + `B3_HOLIDAYS_2023_2024` from `scripts/dll_backfill_orchestrator.py` into `scripts/dll_backfill_lib.py::ChunkPlanner` class. API: `ChunkPlanner(calendar=B3_HOLIDAYS_2023_2024, max_business_days=5).plan(start, end, ticker) -> list[Chunk]`. Prototype call sites updated. Test `tests/dll_backfill/test_chunk_planner.py` (NEW) asserts: (i) parity with current `plan_chunks` output for 2023-01-01..2024-01-01 WDOFUT; (ii) determinism; (iii) holiday/weekend skip; (iv) window<=5 business days hard-rule; (v) calendar parameter swap honored.

- [ ] **AC2 (ManifestStore class extraction with atomic semantics)** — Extract `ManifestRow` + `ManifestStore` from orchestrator into `scripts/dll_backfill_lib.py::ManifestStore`. API: `store = ManifestStore(path); store.load(); store.upsert(row); store.commit()`. Atomic write via tmp+rename (PRESERVE current `_atomic_write` semantics). Test `tests/dll_backfill/test_manifest_atomic.py` (NEW) asserts: (i) header line v1.1 enforced; (ii) atomic rename (no partial writes under concurrent reader); (iii) backward-compat read of existing v1.1 15-col manifests produced by Phase-2C bulk run.

- [ ] **AC3 (ValidateGate class with Nova C1-C10 + data-completeness-first hybrid)** — Create `scripts/dll_backfill_lib.py::ValidateGate` class encapsulating BOTH the current `_classify_status` data-completeness-first heuristic AND the full Nova C1-C10 audit checks (C1 trade_num monotonic intra-day, C2 ts in window, C3 price bounds [VWAP×0.7, VWAP×1.3], C4 qty>0 int, C5 aggressor distribution auction<=25%, C6 vol_brl consistency, C7 sha256 manifest integrity, C8 first/last ts in chunk window, C9 zero queue_full_drops, C10 persisted_rows == total_trades). Verdict tuple `(status, failures: list[str])`. Auction-tolerant per Q-FIN-12-E (auction-heavy first 1000 does NOT downgrade if data-completeness signals are clean). Test `tests/dll_backfill/test_validate_gate.py` (NEW) asserts each C1-C10 individually + integration on real chunk produced by Phase-2C bulk run.

- [ ] **AC4 (Recovery tool `scripts/recover_parquet_from_jsonl.py`)** — Conditional AC: ONLY required if T003.A3 also rehabilitates the streaming `IncrementalParquetSink` path (orjson migration or background-thread flush). If sink stays deprecated and bulk path remains single-shot, this AC is **WAIVED with rationale** in story Change Log. If implemented: tool reads JSONL sidecar → reconstructs identical parquet; idempotent re-run. Test `tests/dll_backfill/test_recovery_tool.py` (NEW).

- [ ] **AC5 (Report generator `scripts/generate_backfill_report.py`)** — Consume `D:\Algotrader\dll-backfill\manifest.csv` (post-bulk) → render `D:\Algotrader\dll-backfill\backfill-report.md` with 6 sections: (i) coverage matrix (date × status); (ii) per-chunk metrics table; (iii) cross-chunk seam audit (intra-chunk monotonic; inter-chunk session-boundary expected breaks); (iv) retention floor map (which 2023 dates hit `retention_exhausted`); (v) baseline×0.5 alarm log if implemented; (vi) total_trades aggregate vs Nelo estimate ~145M. Test `tests/dll_backfill/test_reconciliation_report.py` (NEW) asserts markdown structure + sections present.

- [ ] **AC6 (Manifest schema bump 15→19 with backward compat)** — Add 4 columns to manifest schema: `validate_gate_status`, `validate_gate_failures`, `dll_outcome_class`, `error_msg` (per original T003.A2 AC2). Header bump `v1.1` → `v2.0`. ManifestStore.load() reads BOTH v1.1 (15-col, backward compat) AND v2.0 (19-col); writes v2.0 always. History snapshots `manifest-{YYYYMMDD-HHMMSS}.csv` per write. Test `tests/dll_backfill/test_manifest_schema_v2.py` (NEW) asserts (i) v1.1 read OK; (ii) v2.0 write OK; (iii) round-trip v1.1→v2.0 lossless for the 15 shared cols.

- [ ] **AC7 (Phase-2C prototype tests survive refactor — zero regression)** — Run full prototype test suite against extracted classes; ALL pre-existing tests green (no behaviour drift). `git diff` of `dll_backfill_orchestrator.py` shows only call-site updates (delegation to extracted classes); no semantic mutation of orchestrator main loop, abort triggers, quarantine logic, or Riven caps. Test `tests/dll_backfill/test_no_regression.py` (NEW) executes prototype-era smoke + integration tests against new lib API.

---

## Dependencies & gating

- **Hard prerequisite:** T003.A2 bulk-run for 2023-01-01..2024-01-01 must complete (full or Council-ratified partial coverage) and Quinn QA prototype gate must register a verdict (PASS | CONCERNS | WAIVED) before T003.A3 begins. Rationale: extraction over a moving prototype is unsafe; let bulk run establish the empirical reference behaviour first.
- **Soft prerequisite:** Council 2026-05-03 R1 Amendment §6.1 quarantine semantics (Riven C2) preserved verbatim — extraction must NOT mutate quarantine flagging logic.
- **Article II:** Push exclusive @devops Gage; same gate as T003.A2.
- **Article IV:** Every behaviour preserved must trace to T003.A2 prototype source-of-truth (`b1802ac`) or canonical quirks; no invented semantics during refactor.
- **R1 hard-rule:** `scripts/dll_probe_2023_12_wdofut.py` UNTOUCHED (verified via `git diff` empty post-T003.A3).

---

## Risks

1. **R-1: Refactor regression on quarantine logic.** Riven C2 quarantine flagging is intricate (post-abort flip of `chunks_ok_this_run` to `quarantined_pre_abort_<reason>`); extraction risks subtle drift. Mitigation: AC7 test_no_regression.py as binary gate; Sable pre-push audit explicitly verifies quarantine path parity.
2. **R-2: Manifest schema migration breaks downstream Mira consumers.** If Mira IC pipeline reads manifest pre-T003.A3, schema bump 15→19 must be backward-compat. Mitigation: AC6 backward-compat read explicitly tested; v1.1 historical manifests from bulk run remain readable.
3. **R-3: ValidateGate C1-C10 reactivation false-positives.** Nova C5 (aggressor distribution) was originally bypassed because auction-heavy first 1000 trades false-positive. Mitigation: AC3 explicitly preserves auction-tolerant data-completeness-first override; C5 raises `concerns` not `fail` when data-completeness is clean.

---

## Definition of Done

- [ ] All 7 ACs PASS (AC4 may be WAIVED with rationale if sink stays deprecated).
- [ ] All NEW unit test files green; coverage ≥90% on extracted classes.
- [ ] `dll_backfill_orchestrator.py` reduced to thin orchestration shell; primitives all sourced from `dll_backfill_lib.py`.
- [ ] Manifest schema v2.0 round-trip tested.
- [ ] Quinn QA 7-check PASS verdict in `docs/qa/gates/T003.A3-quinn-qa-gate.md`.
- [ ] Sable pre-push coherence audit PASS.
- [ ] Pax cosign on AC verification.
- [ ] Gage push EXCLUSIVE per Article II.

---

## Change Log

| Date (BRT) | Author | Change |
|------------|--------|--------|
| 2026-05-03 | Pax (@po) | Story created per HYBRID adjudication of Aria C3 BLOCKING-ish (Council 2026-05-03 R1 Amendment). Captures structural-extraction debt named in T003.A2 §0.5. Status: Draft pending T003.A2 bulk closure. |
