# Story T002.0h.1 — WarmUpGate per-phase dated paths (E1 harness amend)

**Epic:** [EPIC-T002.0](../prd/EPIC-T002.0.md)
**Title:** WarmUpGate per-phase dated paths — eliminate singleton + default-path binding (E1 harness amend)
**Wave:** Successor to T002.0h (non-blocking; Beckett N5 + Riven §9 HOLD #2 disarm gates)
**Status:** **Draft (Pax stub header — @sm formal draft downstream)**
**Owner (Dev):** Dex (@dev)
**Reviewers:** Aria (@architect — design authority for harness amendment + per-phase dated path resolvers); Mira (@ml-researcher — anti-leak invariants under per-phase WarmUpGate); Beckett (@backtester — N5 consumer); Dara (@data-engineer — audit chain integrity per-phase)
**QA:** Quinn (@qa) — integration test 2 distinct as_of coexisting + AC9 cache contract preserved + audit chain enriched per-phase; Riven (@risk-manager) cosign disarm gate #1
**Source:** [docs/councils/COUNCIL-2026-04-28-ESC-010-dual-track.md](../councils/COUNCIL-2026-04-28-ESC-010-dual-track.md) (ESC-010 mini-council 6/6 — Track A 5/6 MAJORITY APPROVE_E2 + Dara dissent E1; Track B F3 vote convergent on successor split) + [docs/backtest/T002-beckett-t11-bis-smoke-report-N4-2026-04-28.md](../backtest/T002-beckett-t11-bis-smoke-report-N4-2026-04-28.md) (§root-cause-empirical-track-A: WarmUpGate singleton + default-path binding empirical refutation)
**Estimate:** ~2-3 sessions squad (Aria design ~0.5 + Dex impl ~1-1.5 + Quinn re-QA ~0.5 + Riven cosign ~0.25)

---

## Spec ref

- **Parent spec:** [docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml](../ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml) v0.2.3 (post PRR-20260428-1+2) **UNTOUCHED** at `data_splits` / `feature_set` / `trading_rules` / `n_trials` levels — story-level R15 only if needed.
- **T002.0g + T002.0h contracts preserved:** orchestrator builder API (`ATR20dBuilder`, `Percentiles126dBuilder`) **immutable**; AC9 cache contract triple-key preserved.
- **ADR-1 v3 invariant:** CAP_v3 8.4 GiB **IMMUTABLE**.

---

## User story (preliminary)

Como **Beckett executando full pipeline empirical com smoke + full phases potencialmente requiring distinct as_of dates**, preciso que `WarmUpGate` em `scripts/run_cpcv_dry_run.py` instancie per-phase com dated path resolvers (não singleton + default-path lookup), de forma que smoke phase (que deriva `as_of` via `max(in_sample_start, in_sample_end - 30d)`) e full phase (que usa `--in-sample-start`) possam coexistir cleanly sem masking ou strict-equality false-failure. Sem este patch, ESC-010 Track A E2 narrowing (drop `--smoke` from AC8) é a workaround de curto prazo; E1 é o structurally correct fix preservando Dara data-integrity stance.

---

## Acceptance criteria (preliminary — @sm formal draft downstream)

### Per-phase WarmUpGate instantiation (Aria design authority)

- [ ] **AC1** — Refactor `scripts/run_cpcv_dry_run.py:848` (current ~L848 singleton instantiation) para per-phase `WarmUpGate` instance. Smoke phase + full phase obtêm distinct WarmUpGate instances (or shared instance with per-phase dated-path resolvers — Aria design call).
- [ ] **AC2** — Dated-path resolvers em `scripts/run_cpcv_dry_run.py:120-121` (current `_DEFAULT_ATR_PATH` / `_DEFAULT_PERCENTILES_PATH`): per-phase resolution to dated state files (`atr_20d_{as_of}.json` + `percentiles_126d_{as_of}.json`) instead of default-path symlink (which holds last write, root cause of N4 empirical fault).
- [ ] **AC3** — Smoke phase reads `--in-sample-start`-aware as_of (via Aria design — direct CLI binding OR continue current `max(in_sample_start, in_sample_end - 30d)` derivation BUT lookup dated path for derived as_of). Full phase reads `--in-sample-start` as_of as before. Both paths use dated-path lookup, not default-path symlink.

### AC9 cache contract preservation (Mira + Dara authority)

- [ ] **AC4** — AC9 cache contract triple-key `(as_of_date, source_sha256_from_manifest, builder_version_semver)` preserved per-phase. Each phase's WarmUpGate validates triple-key against its specific as_of's sidecar (`_cache_key_{as_of}.json`); fail-closed `StaleCacheError` semantics preserved per-phase.
- [ ] **AC5** — `cache_audit.jsonl` audit chain enriched per-phase: each phase emits `{computed_at_brt, as_of_date, phase ∈ {smoke, full}, status ∈ {hit,miss,stale,force_rebuild,write}, expected_key, found_key, note}`. Sable audit consumable; existing `cache_audit.jsonl` schema extended (add `phase` field) without breaking existing entries.

### Quinn integration test (mandatory)

- [ ] **AC6** — `tests/scripts/test_run_cpcv_dry_run.py` (or new `tests/integration/test_warmup_gate_per_phase.py`) cobre: 2 distinct as_of dates (e.g., `2024-08-22` for full + `2025-05-31` for smoke) coexisting cleanly in single `run_cpcv_dry_run.py` invocation; smoke phase loads `2025-05-31` dated path; full phase loads `2024-08-22` dated path; ZERO strict-equality false-failures.
- [ ] **AC7** — Backward-compat: existing AC8 invocation literal (post-PRR-20260428-1, no `--smoke`) continues to PASS unchanged. ZERO regressions in T002.0g + T002.0h test suites.

### Riven cosign disarm gate (custodial)

- [ ] **AC8** — Riven §9 HOLD #2 disarm gate #1 cleared post-T002.0h.1 PASS. Documented in `docs/qa/gates/T002.0g-riven-cosign.md` §9 (or successor doc). Disarm gates #2-#5 (T002.1.bis PASS + Beckett N5 PASS + Mira statistical clearance + Riven dual-sign) remain pending.

---

## Tasks (preliminary — @sm formal draft downstream)

- [ ] **T0** — Pre-flight handshakes: Aria + Mira + Dara sign-offs em comentários da story; Pax `*validate-story-draft T002.0h.1` 10-point gate.
- [ ] **T1** — Aria design call: per-phase WarmUpGate instance vs shared-instance-with-per-phase-resolvers. Dated-path resolver design.
- [ ] **T2** — Dex impl per Aria design (~1-1.5 sessions).
- [ ] **T3** — Quinn integration test (AC6) + backward-compat test (AC7).
- [ ] **T4** — Quinn re-QA: full T002.0g+T002.0h+T002.0h.1 suite ZERO regressions.
- [ ] **T5** — Riven cosign disarm gate #1 clearance.

---

## Dependencies

- **Done (blocking inputs):**
  - **T002.0h** — orchestrator + AC9 cache contract (preserved per-phase under T002.0h.1).
  - **ESC-010 mini-council 6/6** — Track A 5/6 MAJORITY APPROVE_E2 + Track B convergent F3 (E1 deferred to this successor).
- **Pending (downstream unblock):**
  - **Beckett N5 re-run** — full pipeline empirical with per-phase WarmUpGate (post T002.0h.1 PASS).
  - **Riven §9 HOLD #2 disarm gate #1** — clearance post T002.0h.1 PASS.
- **NOT blocking T002.0h closure:** T002.0h.1 is non-blocking for T002.0h closure (E2 narrowed AC8 via drop --smoke; T002.0h closure path empirically anchored by DIAGNOSTIC run_id 324c151f...).

---

## Article IV trace (Pax stub)

NO INVENTION — every clause traceable to:
1. Aria Track A E1 analysis (ESC-010 ledger §2.1) — per-phase dated paths design rationale.
2. Beckett N4 §root-cause-empirical-track-A (`run_cpcv_dry_run.py:848` singleton + L865 hardcoded smoke window + WarmUpGate default-path-only lookup; verbatim error log: "atr: stale as_of_date (file=2024-08-22, expected=2025-05-31)").
3. Dara dissent ESC-010 (§2.6) — data-integrity / auditability stance preserved as governance entity via this successor story.
4. ESC-009 condition #4 (Mira authority — already adopted 6/6) carry-forward.
5. AC9 cache contract triple-key (T002.0h L73-79) preserved per-phase.

---

## Reword/Authoring History

- **2026-04-28 BRT — Pax (@po) drafted stub header** from ESC-010 mini-council 6/6 outcome (Track A 5/6 MAJORITY APPROVE_E2 + Dara dissent E1 registered; Track B 6/6 functional convergence em F2-with-binding-F3-commitment + NEW §9 HOLD #2 armed). Stub header serves as governance-entity registration per Riven custodial veto authority ("F2 SEM F3 commitment → REJECT"; combined F2 + F3-commitment cleared via T002.0h.1 + T002.1.bis spawn). Status: **Draft** — @sm formal draft downstream replaces this stub with full ACs/tasks/DoD per template. Pax cosign 2026-04-28 BRT.

---

## Pax cosign (stub header)

```
Validator: Pax (@po) — Product Owner / Story Balancer / Council Steward
Story: T002.0h.1 — WarmUpGate per-phase dated paths (E1 harness amend)
Date: 2026-04-28 BRT
Status: Draft (stub header; @sm formal draft downstream)
Authority: ESC-010 mini-council 6/6 functional convergence Track B F3 + Track A 5/6 E2 + Dara E1 dissent
Article IV: NO INVENTION — every clause traceable to ESC-010 ledger + Beckett N4 report
            + cpcv_harness analysis (Aria M1) + Dara dissent registered.
R15 compliance: New successor story (NOT amend T002.0h Done — pending T002.0h closure
                first; this story registered as governance entity for Riven §9 HOLD #2
                disarm gate #1).
Spec compliance: parent spec UNTOUCHED at data_splits / feature_set / trading_rules /
                 n_trials levels.
Next gate: @sm formal draft downstream (replaces this stub with full ACs/tasks/DoD).
Cosign: Pax 2026-04-28 BRT (Autonomous Daily Session, council steward — stub registration only).
```

— Pax, balanceando ✨
