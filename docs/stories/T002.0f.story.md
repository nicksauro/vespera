# Story T002.0f — CPCV Harness Integration

**Epic:** [EPIC-T002.0](../prd/EPIC-T002.0.md)
**Status:** InProgress (T1 complete; T2/T3 pending Mira research-log delivery)
**Owner (Dev):** Dex (@dev)
**Reviewers:** Beckett (@backtester, R6 CPCV authority — interface + dry-run executor), Mira (@ml-researcher, metrics integration), Nova (@market-microstructure, cost-atlas wire)
**QA:** Quinn (@qa)
**Source:** Beckett HALT report 2026-04-25 (`*run-cpcv --dry-run` exposed 3 callables ausentes + glue ausente para PBO 5×45) + [docs/backtest/T002-mira-handshake-2026-04-25.md](../backtest/T002-mira-handshake-2026-04-25.md)
**Estimate:** 2 sessões (1 harness + glue, 1 dry-run runner + smoke)

---

## Spec ref

- **Parent spec:** [docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml](../ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml) (5 trials T1-T5 declarados em §trading_rules; CPCV `n_groups=10, k=2, n_paths=45`)
- **Metrics spec:** [docs/ml/specs/T002-vespera-metrics-spec.md](../ml/specs/T002-vespera-metrics-spec.md) v0.2.2 (compute_full_report contract)
- **Mira handshake:** [docs/backtest/T002-mira-handshake-2026-04-25.md](../backtest/T002-mira-handshake-2026-04-25.md) (closure que expôs T002.0f)
- **Beckett shape:** [docs/backtest/T002-backtest-result-shape.md](../backtest/T002-backtest-result-shape.md) §1 (BacktestResult contract)

---

## User story

Como Beckett (@backtester, R6 CPCV authority) executando `*run-cpcv --dry-run --spec docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml`, preciso de um **CPCV harness** que conecte os componentes já entregues por T002.0a-e + T002.1 — `{warmup gate → events_dataframe → backtest_fn (closure costs+calendar+P126 bands) → CPCVEngine 45-path × 5-trial fan-out → compute_full_report → KillDecision}` — para que eu produza um veredito **falsificável** sobre a tese T002, com `(T=5, N=45)` PBO matrix populada e K1/K2/K3 avaliados sobre dados reais (não stub), respeitando o protocolo de 6 GiB soft halt + telemetria psutil cadência ~30s. Sem este harness, todos os módulos T002.0a-e existem isolados e a Fase E não é executável.

---

## Acceptance criteria

### Harness module — events + backtest_fn factory (Beckett §1)

- [x] **AC1** `packages/t002_eod_unwind/cpcv_harness.py` exporta `build_events_dataframe(in_sample_start: date, in_sample_end: date, trials: tuple[str, ...]) -> pd.DataFrame` com:
  - Columns canônicas: `t_start, t_end, session, trial_id, entry_window`
  - **Warmup-gated:** chama `packages.t002_eod_unwind.warmup.gate.assert_warmup_satisfied(in_sample_start)` (T002.1) antes de emitir qualquer event; HALT se P126/ATR20 não estiverem prontos.
  - **Nova-session-validity gated:** chama validação de calendário Nova (rollover/half-day/holiday) — sessões inválidas para WDO são puladas (não emitidas como event).
  - Index monotonic increasing por `t_start` (engine `_validate_events` exige).
  - Dtype: `t_start, t_end` = `pd.Timestamp` BRT-naive (R2); `session` = `date`; `trial_id` ∈ `trials`; `entry_window` = string canônica per spec §trading_rules.
- [x] **AC2** `make_backtest_fn(costs: BacktestCosts, calendar: RolloverCalendar, percentiles_state: PercentilesState) -> Callable[[pd.DataFrame, pd.DataFrame, CPCVSplit], BacktestResult]` retorna **closure** sobre:
  - `costs` — `BacktestCosts.from_engine_config(docs/backtest/engine-config.yaml v1.0.0)` (lê Nova cost-atlas v1.0.0 ou TO-VERIFY default).
  - `calendar` — Nova rollover calendar (intra-fold session validity).
  - `percentiles_state` — **per-train-fold P126 bands** rebuilt for each split (NOT global P126 — leak vector). Closure encapsula `state` para que `backtest_fn` seja pure-function-style sem side-effects globais.
  - Closure retorna `BacktestResult` per fold conforme Beckett §1.1 shape (TradeRecord usa canonical `Fill` from `exec_backtest.py`).

### 5-trial fan-out (spec v0.2.0 §trading_rules)

- [x] **AC3** `cpcv_harness.py` orquestra **5-trial × 45-path fan-out** (T1-T5 declarados na spec v0.2.0):
  - Para cada `trial_id ∈ ('T1', 'T2', 'T3', 'T4', 'T5')`, executa `CPCVEngine.run(events_filtered_by_trial, backtest_fn)` → `list[BacktestResult]` len=45.
  - Total output: `dict[trial_id, list[BacktestResult]]` com 5 keys × 45 results = 225 fold-results.
  - Trial isolation: cada trial usa subset filtrado de `events` por `trial_id` column (não cross-contamination).
- [x] **AC4** **Determinism (carry-forward AC12 of T002.0c):** 3 runs do harness com mesmo seed + mesma window in-sample produzem 225 `BacktestResult` byte-identical (`content_sha256()` matching across runs).

### compute_full_report integration (Mira AC12/AC12.1/AC13 of T002.0d)

- [ ] **AC5** `packages/vespera_metrics/report.py::compute_full_report(cpcv_results: dict[str, list[BacktestResult]], n_trials_source: str, config: ReportConfig) -> FullReport` implementado (atualmente DEFERRED stub em `report.py:6-11`):
  - Consome dict 5-trial × 45-path de AC3.
  - Constrói `(T=5, N=45)` PBO matrix per BBLZ 2014 (linha = trial, coluna = path) usando per-fold OOS Sharpe.
  - Chama `pbo.probability_backtest_overfitting` sobre matrix → escalar PBO global.
  - Calcula DSR via `dsr.deflated_sharpe_ratio` com `n_trials = read_research_log_cumulative()` (NOT spec.n_trials.total isolado — AC12.1 of T002.0d).
  - Retorna `FullReport(metrics=MetricsResult, per_path_results=tuple, kill_decision=evaluate_kill_criteria(...))`.
  - `n_trials_source` field populated com `docs/ml/research-log.md@<git-sha>` (AC12.1 carry).
- [ ] **AC6** `compute_full_report` validado contra **synthetic fixture** reusando Mira toy benchmarks de [T002-vespera-metrics-spec.md](../ml/specs/T002-vespera-metrics-spec.md) §6 (T11 PBO 4×4 + T9/T10 DSR toys). Synthetic input: 5 trials × 45 fake `BacktestResult` com Sharpe distributions controladas → output `FullReport.metrics.pbo` e `.dsr` batem com Mira reference values dentro de `1e-6`.

### Dry-run CLI (Beckett invocation contract)

- [ ] **AC7** `scripts/run_cpcv_dry_run.py` CLI com:
  - `--spec PATH` — required, path para spec yaml v0.2.0.
  - `--dry-run` — flag boolean (default true; `--no-dry-run` desabilita psutil polling para perf).
  - `--in-sample-start YYYY-MM-DD` `--in-sample-end YYYY-MM-DD` — window override (default lê da spec).
  - `--smoke` — flag boolean: 1-month subset (`in_sample_end - 30d`) antes de full run.
  - `--run-id STR` — override default uuid hex.
  - Exit code 0 se PASS, 1 se HALT (mem soft-cap exceeded ou warmup failure), 2 se determinism violation.
- [ ] **AC8** **Memory protocol (feedback_cpcv_dry_run_memory_protocol.md):**
  - psutil polling cadência 30s (configurable `--mem-poll-interval-s`).
  - **6 GiB soft halt threshold** (`process.memory_info().rss > 6 * 1024**3`) → emit warning, persist current fold state, **HALT graceful** (não kill).
  - Telemetry CSV writer to `data/baseline-run/cpcv-dryrun-{run_id}/telemetry.csv` com columns `(timestamp_brt, rss_mb, vms_mb, cpu_pct, fold_index, trial_id)`.
- [ ] **AC9** **Hold-out lock guard:** Antes de iteration, `scripts/run_cpcv_dry_run.py` invoca `_holdout_lock.assert_holdout_safe(in_sample_start, in_sample_end)` — fail-closed se window cruzar `[2025-07-01, 2026-04-21]` sem `VESPERA_UNLOCK_HOLDOUT=1` (R1+R15(d)).

### Smoke test

- [ ] **AC10** `--smoke` run em **1-month subset** (configurable; default últimos 30 dias da `in_sample_end - 30d`) completa em <5min em hardware referencial (i5/16GB) sem exceder 6 GiB e produz `FullReport` válido (KillDecision verdict ∈ {GO, NO_GO}).
- [ ] **AC11** Smoke success é **pre-condition** documentada para 12-month full run; `--smoke` failure aborta full run automaticamente com exit code 1 + reason em telemetry CSV.

### Logging & traceability

- [ ] **AC12** Run artifacts persisted em `data/baseline-run/cpcv-dryrun-{run_id}/`:
  - `telemetry.csv` (AC8)
  - `full_report.md` (`FullReport.to_markdown()` output)
  - `full_report.json` (FullReport serialized)
  - `determinism_stamp.json` (DeterminismStamp from BacktestRunner)
  - `events_metadata.json` (in_sample window + n_events + n_trials + warmup gate timestamp)
- [ ] **AC13** Spec lock: `DeterminismStamp.spec_sha256` matches sha256 do `--spec` arquivo (Beckett §5.4); mismatch entre runs aborta com exit code 2.

---

## Tasks

- [ ] **T0** — **Pre-flight handshake (Beckett + Mira + Nova):**
  - Beckett: confirma shape `dict[trial_id, list[BacktestResult]]` em AC3; sign-off em comentário.
  - Mira: confirma `n_trials_source` semantics em AC5 (cumulative vs spec-isolated) — refresh AC12.1 do T002.0d ainda válido.
  - Nova: confirma cost-atlas v1.0.0 wire em `BacktestCosts.from_engine_config` (já entregue T002.0e — Quinn ready).
  - Sign-offs **antes** de Dex iniciar T1.
- [x] **T1** — `packages/t002_eod_unwind/cpcv_harness.py`:
  - `build_events_dataframe(in_sample_start, in_sample_end, trials) -> pd.DataFrame` (AC1) com warmup gate + Nova session validity.
  - `make_backtest_fn(costs, calendar, percentiles_state) -> Callable` (AC2) closure.
  - `run_5_trial_fanout(events, backtest_fn, engine) -> dict[str, list[BacktestResult]]` (AC3).
- [ ] **T2** — `packages/vespera_metrics/report.py`:
  - Implementar `compute_full_report(cpcv_results, n_trials_source, config) -> FullReport` (AC5).
  - Build (T=5, N=45) PBO matrix (linha trial, coluna path).
  - Read `n_trials` cumulative de `docs/ml/research-log.md` (Mira AC0.1 of T002.0d).
  - Wire `evaluate_kill_criteria` → KillDecision.
- [ ] **T3** — `scripts/run_cpcv_dry_run.py`:
  - argparse CLI (AC7): `--spec`, `--dry-run`, `--in-sample-start/end`, `--smoke`, `--run-id`, `--mem-poll-interval-s`.
  - Hold-out lock guard pre-iteration (AC9).
  - psutil polling thread cadência 30s + telemetry CSV writer (AC8).
  - 6 GiB soft halt with graceful state persist (AC8).
  - Run artifacts persistence em `data/baseline-run/cpcv-dryrun-{run_id}/` (AC12).
  - Spec sha256 lock check (AC13).
- [x] **T4** — `tests/cpcv_harness/test_build_events_dataframe.py`:
  - AC1 columns + warmup gate + Nova session validity gate.
  - Edge: warmup not satisfied → raise.
  - Edge: invalid session in window → skipped (not raised).
- [x] **T5** — `tests/cpcv_harness/test_make_backtest_fn.py`:
  - AC2 closure independence (no globals leaked).
  - Edge: per-train-fold P126 rebuild — passing 2 different train sets yields 2 different P126 bands.
- [x] **T6** — `tests/cpcv_harness/test_5_trial_fanout.py`:
  - AC3 dict shape (5 keys × 45 results = 225).
  - AC4 determinism: 3 runs same seed → 225 `content_sha256()` identical.
  - Trial isolation: filter `events_T1` ≠ `events_T2`.
- [ ] **T7** — `tests/vespera_metrics/test_compute_full_report.py`:
  - AC5 PBO matrix shape (T=5, N=45).
  - AC5 DSR uses `n_trials_cumulative` (mock `research-log.md` parse).
  - AC6 against Mira synthetic toy (T11 PBO 4×4 → reproduce with fake 5×45 input that maps to 4×4 reduced → match within 1e-6).
- [ ] **T8** — `tests/scripts/test_run_cpcv_dry_run.py`:
  - AC7 CLI parsing.
  - AC9 hold-out lock guard fires (subprocess invoke with hold-out window → exit 1).
  - AC8 telemetry CSV columns + 6 GiB soft halt mock (mock psutil to return 7 GiB → graceful HALT, no kill).
  - AC10 smoke 1-month subset completes (mock backtest_fn for speed).
  - AC11 smoke failure aborts full run.
- [ ] **T9** — `tests/contracts/test_cpcv_harness_contracts.py`:
  - AC13 spec_sha256 lock — modified spec → exit 2.
  - AC12 artifacts persisted (file existence check post-run).
- [ ] **T10** — Quinn QA gate: `pytest tests/cpcv_harness/ tests/vespera_metrics/test_compute_full_report.py tests/scripts/test_run_cpcv_dry_run.py tests/contracts/ -q` ≥ all PASS; `ruff check packages/t002_eod_unwind/cpcv_harness.py packages/vespera_metrics/report.py scripts/run_cpcv_dry_run.py` clean; `mypy` clean.
- [ ] **T11** — Beckett re-run `*run-cpcv --dry-run --spec docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml --smoke` against this audited pre-flight baseline → smoke PASS → unblock 12-month full.

---

## Dependencies

- **Done (blocking inputs):**
  - **T002.0a** (BacktestBroker contract) — Done.
  - **T002.0b** (`_holdout_lock.py` + `BacktestCosts.from_engine_config`) — Done.
  - **T002.0c** (`vespera_cpcv` engine + `CPCVEngine.run` + `BacktestResult`) — Ready for Review (Quinn QA pending).
  - **T002.0d** (`vespera_metrics` modules + dataclasses + `evaluate_kill_criteria`) — Ready for Review (`compute_full_report` deferred to this story).
  - **T002.0e** (Nova cost-atlas v1.0.0 + engine-config wire) — Ready-for-QA-Quinn.
  - **T002.1** (warmup gate + P126/ATR20 builders) — Done.
- **Blocks:** Task #36 (CPCV Gate Fase E), 12-month full run, T002 GO/NO-GO verdict.
- **Paralelizável com:** none (final integration km).

---

## File List (esperada após Dex impl)

**Created (T1 complete — Dex 2026-04-26):**
- `packages/t002_eod_unwind/cpcv_harness.py` (CREATED — 560 LoC; Beckett §1: build_events_dataframe + make_backtest_fn + run_5_trial_fanout + assert_warmup_satisfied wrapper per Pax gap #1)
- `tests/cpcv_harness/__init__.py` (CREATED)
- `tests/cpcv_harness/test_build_events_dataframe.py` (CREATED — 293 LoC; AC1 coverage: columns/dtypes, warmup gate fires (READY/IN_PROGRESS), invalid session skipped, monotonic index, trial subset, validation errors, entry window times)
- `tests/cpcv_harness/test_make_backtest_fn.py` (CREATED — 262 LoC; AC2 coverage: closure signature, no-globals independence, per-fold P126 rebuild observable, purity, costs+calendar anchored in hash)
- `tests/cpcv_harness/test_5_trial_fanout.py` (CREATED — 254 LoC; AC3+AC4 coverage: dict shape 5×45, trial isolation, 3 runs byte-identical, subset trials, missing trial_id raises, uniform DeterminismStamp via BacktestRunner per Beckett T0)

**Created (pending — T2/T3):**
- `scripts/run_cpcv_dry_run.py` (PENDING T3 — Beckett dry-run executor)
- `tests/vespera_metrics/test_compute_full_report.py` (PENDING T2 — un-defers T002.0d AC12/AC12.1/AC13)
- `tests/scripts/__init__.py` (PENDING T3, if not exists)
- `tests/scripts/test_run_cpcv_dry_run.py` (PENDING T3)
- `tests/contracts/test_cpcv_harness_contracts.py` (PENDING T3 — AC13 spec lock + AC12 artifacts)

**Modified (pending — T2/T3):**
- `packages/vespera_metrics/report.py` (PENDING T2 — `compute_full_report` impl replacing DEFERRED stub L6-11; gated on Mira `docs/ml/research-log.md` v0.1)
- `docs/stories/T002.0d.story.md` (PENDING T2 — mark T5 / AC12 / AC12.1 / AC13 as completed via T002.0f delivery; cross-ref)

**Run artifacts (output, gitignored):**
- `data/baseline-run/cpcv-dryrun-{run_id}/telemetry.csv` (runtime)
- `data/baseline-run/cpcv-dryrun-{run_id}/full_report.md` (runtime)
- `data/baseline-run/cpcv-dryrun-{run_id}/full_report.json` (runtime)
- `data/baseline-run/cpcv-dryrun-{run_id}/determinism_stamp.json` (runtime)
- `data/baseline-run/cpcv-dryrun-{run_id}/events_metadata.json` (runtime)

**Total:** 10 NEW + 2 MODIFIED + 5 runtime artifacts.

---

## Testing Strategy

| AC | Tipo | Arquivo | Comando | Output |
|----|------|---------|---------|--------|
| AC1 | Unit | `tests/cpcv_harness/test_build_events_dataframe.py::test_columns_and_dtypes` | `pytest -q` | columns canônicas + dtypes BRT-naive |
| AC1 | Unit (gate) | `tests/cpcv_harness/test_build_events_dataframe.py::test_warmup_gate_fires` | `pytest -q` | RuntimeError if P126 not ready |
| AC1 | Unit (gate) | `tests/cpcv_harness/test_build_events_dataframe.py::test_invalid_session_skipped` | `pytest -q` | session not in calendar → not emitted |
| AC2 | Unit | `tests/cpcv_harness/test_make_backtest_fn.py::test_closure_independence` | `pytest -q` | no module-level state mutation |
| AC2 | Unit | `tests/cpcv_harness/test_make_backtest_fn.py::test_per_fold_p126_rebuild` | `pytest -q` | 2 train sets → 2 distinct P126 bands |
| AC3 | Unit | `tests/cpcv_harness/test_5_trial_fanout.py::test_dict_shape_5x45` | `pytest -q` | `len(dict)==5 and all(len(v)==45)` |
| AC3 | Unit | `tests/cpcv_harness/test_5_trial_fanout.py::test_trial_isolation` | `pytest -q` | events_T1 ∩ events_T2 by trial_id == ∅ |
| AC4 | Determinism | `tests/cpcv_harness/test_5_trial_fanout.py::test_3_runs_byte_identical` | 3x run | 225 `content_sha256` identical |
| AC5 | Unit | `tests/vespera_metrics/test_compute_full_report.py::test_pbo_matrix_shape` | `pytest -q` | matrix shape (5, 45) |
| AC5 | Unit | `tests/vespera_metrics/test_compute_full_report.py::test_n_trials_cumulative` | `pytest -q` | `n_trials_source.startswith("docs/ml/research-log.md")` |
| AC6 | Synthetic | `tests/vespera_metrics/test_compute_full_report.py::test_against_mira_toy_benchmarks` | `pytest -q` | PBO + DSR within 1e-6 vs Mira §6 toys |
| AC7 | CLI | `tests/scripts/test_run_cpcv_dry_run.py::test_argparse_required` | `pytest -q` | required `--spec` validation |
| AC8 | Mock | `tests/scripts/test_run_cpcv_dry_run.py::test_6gb_soft_halt` | `pytest -q` | mock psutil 7 GiB → graceful HALT |
| AC8 | Mock | `tests/scripts/test_run_cpcv_dry_run.py::test_telemetry_csv_columns` | `pytest -q` | CSV columns match spec |
| AC9 | Contract | `tests/scripts/test_run_cpcv_dry_run.py::test_holdout_lock_guard_fires` | subprocess | exit 1 on hold-out window |
| AC10 | Smoke | `tests/scripts/test_run_cpcv_dry_run.py::test_smoke_1month_completes` | mock backtest | <5min sim time, FullReport emitted |
| AC11 | Smoke | `tests/scripts/test_run_cpcv_dry_run.py::test_smoke_failure_aborts_full` | `pytest -q` | smoke fail → full run not started |
| AC12 | Contract | `tests/contracts/test_cpcv_harness_contracts.py::test_artifacts_persisted` | post-run | 5 files exist in data/baseline-run/.../ |
| AC13 | Contract | `tests/contracts/test_cpcv_harness_contracts.py::test_spec_sha256_lock` | mod spec | exit code 2 |

**Determinism strict (AC4 + AC12 carry from T002.0c):** 3 runs byte-equal — 225 `BacktestResult.content_sha256()` matching across independent harness invocations same seed.

**Synthetic fixture (AC6):** reuse Mira toy benchmarks from `T002-vespera-metrics-spec.md` §6 (T11 PBO 4×4 anti-correlation → maps to 5×45 fake fold input that reduces to 4×4 → matches within 1e-6).

**Smoke 1-month subset (AC10):** mandatory pre-condition before 12-month full run — runs in <5min on i5/16GB hardware with mock backtest_fn (real backtest_fn smoke runs in ~30min and is Beckett's call to invoke separately).

**Hold-out lock guard (AC9):** fires antes de iteration — subprocess invocation with hold-out window forced exits with code 1 before any partition I/O.

---

## Risks

1. **R1 — `compute_full_report` integration may surface BacktestResult shape gaps not visible in T002.0c isolated tests.**
   *Mitigation:* T0 handshake with Beckett before T2 to lock dict[trial_id, list[BacktestResult]] shape; if gap surfaces, escalate to @aiox-master for cross-story patch (don't silently mutate T002.0c).
2. **R2 — Per-train-fold P126 rebuild cost may dominate 12-month run time (45 paths × 5 trials = 225 rebuilds).**
   *Mitigation:* AC10 smoke 1-month subset gives empirical timing; if >5min smoke, optimize before 12-month full (memoize P126 by train index hash, since CPCV splits often share train events across paths).
3. **R3 — 6 GiB soft halt false-positive on Windows 10 Pro (psutil RSS includes shared libs differently).**
   *Mitigation:* AC8 cadência 30s gives early signal; threshold configurable via `--mem-poll-interval-s` and `--mem-soft-cap-gb`. Document baseline RSS post-warmup in run_id metadata for tuning.
4. **R4 — `n_trials_cumulative` from `docs/ml/research-log.md` may be stale (Mira AC0.1 of T002.0d still PENDENTE).**
   *Mitigation:* AC5 reads file at run-time + DeterminismStamp captures `docs/ml/research-log.md@<git-sha>`; if file missing, fail-closed with explicit error pointing to T002.0d Task T0 (Mira deliverable). Soft-fallback to spec.n_trials.total=5 with WARN-level log explicitly forbidden (Article IV: no invented baseline).
5. **R5 — Spec v0.2.0 §trading_rules trial enumeration may not exactly map to harness `trials` tuple (T1-T5 vs T_long/T_short/T_neutral semantic split).**
   *Mitigation:* T0 handshake clarifies trial_id → spec mapping; AC3 trial isolation test catches mis-mapping early. Document mapping inline in `cpcv_harness.py` docstring.

---

## Architecture Notes

- **6 GiB soft halt protocol:** per `feedback_cpcv_dry_run_memory_protocol.md`, harness must respect 6 GiB threshold with graceful state persist (NOT kill -9). psutil polling cadência 30s. Telemetry CSV path: `data/baseline-run/cpcv-dryrun-{run_id}/telemetry.csv`.
- **Beckett R6 CPCV exclusive authority:** Beckett owns `*run-cpcv` and `*run-cpcv --dry-run` invocation; Dex implements the harness Beckett calls. River + Pax + Quinn workflow this story; Beckett re-runs T11 to close.
- **AC14 Trade(ts, price, qty) parity preserved:** TradeRecord in `BacktestResult` continues using canonical `Fill` from `exec_backtest.py` per Beckett §1.1 (T002.0c invariant). Harness does NOT introduce new trade dataclass.
- **5-trial × 45-path fan-out shape:** 225 fold-results total. Output dict `{T1: [r0..r44], T2: [...], ..., T5: [...]}` — flat list explicitly rejected (loses trial isolation needed for PBO matrix construction).
- **Per-train-fold P126 rebuild:** critical anti-leak invariant — global P126 over in-sample is leak vector (test set P126 contaminates train decisions). Closure encapsulates `percentiles_state` rebuild per fold; verified by AC2 test.
- **Determinism (R6):** 3-run byte-equal carry-forward from T002.0c AC12. DeterminismStamp from `BacktestRunner` propagated to all 225 results.
- **Hold-out lock fail-closed (R1+R15(d)):** `_holdout_lock.assert_holdout_safe` called at script entry (AC9) AND inside `CPCVEngine.generate_splits` (T002.0c AC12) — defense-in-depth.

---

## Definition of Done

- [ ] AC1-AC13 todos verde (proof mecânico na tabela Testing Strategy)
- [ ] T0 handshake completo: Beckett + Mira + Nova sign-offs em comentários
- [ ] T11 Beckett re-run `*run-cpcv --dry-run --smoke` PASS — smoke FullReport válido + telemetry CSV produzida
- [ ] Quinn PASS: `pytest tests/cpcv_harness/ tests/scripts/ tests/vespera_metrics/test_compute_full_report.py tests/contracts/ -q` PASS; `ruff` + `mypy` clean
- [ ] Cross-ref: T002.0d Tasks T4/T5 + AC12/AC12.1/AC13 marked Done (compute_full_report no longer DEFERRED)
- [ ] Hold-out intocado: AC9 test verde + grep `VESPERA_UNLOCK_HOLDOUT` em `scripts/run_cpcv_dry_run.py` ≥ 1 match
- [ ] Pax `*validate-story-draft T002.0f` → GO antes de Dev start
- [ ] Gage pushes (R12) post-Quinn PASS

---

## Handshakes required (pré-Dev start)

| Agente | Valida | Sign-off |
|--------|--------|----------|
| **Beckett** | Shape `dict[trial_id, list[BacktestResult]]` (AC3) + dry-run CLI contract (AC7) + smoke 1-month subset semantics (AC10) | Comentário na story |
| **Mira** | `n_trials_source` cumulative semantics em AC5 (refresh AC12.1 of T002.0d) + synthetic fixture mapping (AC6) | Comentário na story |
| **Nova** | Cost-atlas v1.0.0 wire em `BacktestCosts.from_engine_config` (já entregue T002.0e — confirma só) | Comentário na story |
| **Pax** | `*validate-story-draft` 10-point | Status Draft → Ready |

---

## Reword/Authoring History

- **2026-04-25 BRT — River drafted from Beckett HALT report** (5 ACs proposed expanded to 13 ACs) + Mira handshake closure (`docs/backtest/T002-mira-handshake-2026-04-25.md`). Status: Draft.
- Sources consolidated: Beckett HALT mission text, T002.0c/0d existing story templates, spec T002 v0.2.0 §trading_rules (5 trials), Mira metrics-spec v0.2.2, Nova cost-atlas v1.0.0, `feedback_cpcv_dry_run_memory_protocol.md` (6 GiB soft halt + telemetry CSV).
- Authority preserved: Beckett R6 CPCV (T11 re-run gate); Mira metrics ownership (AC5/AC6 fixture); Nova cost-atlas (AC2 closure costs); Pax validation gate (DoD).

---

## T0 Handshakes (pre-Dex start)

### Beckett T0 Handshake — 2026-04-26 BRT

- **AC3 shape:** APPROVED — `dict[trial_id, list[BacktestResult]]` com 5 keys × 45 results = 225 fold-results bate exatamente com Beckett shape spec §1.6 + §3.2 contract. Trial isolation por `trial_id` filtering em `events` (column canônica AC1) preserva PBO matrix `(T=5, N=45)` per BBLZ 2014 (linha=trial, coluna=path). Engine `CPCVEngine.run` retorna `list[BacktestResult]` len=45 ordenado por `path_index` ascending — fan-out wrapper só itera 5 trials e empilha em dict. **Nota:** harness DEVE chamar via `BacktestRunner.run` (não `CPCVEngine.run` direto) para injetar `DeterminismStamp` uniforme nos 225 results via `dataclasses.replace` (runner.py L131-133) — sem isso AC4 byte-identical falha.
- **AC7 CLI contract:** APPROVED — Solicito adição não-bloqueante: `--seed INT` (override default 42, necessário para reproducibilidade explícita CI vs ad-hoc) e `--mem-soft-cap-gb FLOAT` (já em R3 mitigation; configurabilidade evita falso-positivo Windows). Defaults preservam comportamento atual.
- **AC10 smoke semantics:** APPROVED — 1-month subset com mock backtest_fn em <5min é gate pre-12-month correto. Mock evita Roll-spread compute real (~30min). Smoke valida pipeline end-to-end (events → fan-out → compute_full_report → KillDecision), NÃO estatística — esta é responsabilidade do 12-month full subsequente.
- **Verdict:** HANDSHAKE_OK_FOR_DEX_START

— Beckett, reencenando o passado 🎞️

### Mira T0 Handshake — 2026-04-26 BRT

- **AC5 `n_trials_source` semantics: APPROVED** — Semântica é **squad-cumulative across ALL research stories**, NÃO T002-isolado. Algoritmo de parse esperado:
  1. `read_research_log_cumulative(path="docs/ml/research-log.md") -> tuple[int, str]` retorna `(n_trials_total, source_ref)`.
  2. Parse: somar campo `n_trials` (int) de TODAS entries YAML-frontmatter, independente de story_id (T002.0d, T002.0f, T003.x futuras). DSR penaliza multiple-testing GLOBAL (Bailey-LdP 2014 §3).
  3. `source_ref = f"docs/ml/research-log.md@{git_rev_parse_HEAD()}"`.
  4. **Fail-closed se file missing** (R4): `FileNotFoundError("docs/ml/research-log.md not found — Mira T002.0d AC0.1 deliverable required")`. **Forbid soft-fallback** para `spec.n_trials.total=5` (Article IV).
  5. **Pre-condition Mira:** Mira emite `docs/ml/research-log.md` v0.1 com seed entry T002.0d (n_trials=5: T1-T5) + entry T002.0f (n_trials=0, harness only) ANTES de Dex iniciar T2. Não bloqueia T1.
- **AC6 synthetic fixture mapping: APPROVED com clarificação** — Mapping 5×45 → 4×4 via **projection-based reduction**:
  1. Construir 5 trials × 45 paths onde **trials 1-4 replicam linhas T11 §6.5** (`[10,9,1,0.5]`, `[8,7,3,2]`, `[3,2,7,8]`, `[1,0.5,9,10]`) repetidas 11.25× ao longo dos 45 paths em **blocos contíguos de 11-12 paths cada** (trial 5 = noise/throwaway).
  2. Reduce: `pbo_matrix[0:4, 0:4]` extraído como sub-matriz. Assert `pbo_value(reduced_matrix) ≈ 1.0` within `1e-6` (T11 expected).
  3. DSR T9/T10 toys validam isolados sobre `sr_distribution` derivado de paths Sharpe artificiais — `sr_observed=2.5`, `sr_distribution=np.array([0.5,0.7,...,2.8])`, `n_trials=10` (mock `read_research_log_cumulative`). Expected DSR ≈ 1.0 ±1e-6.
  4. Trial 5 valida shape (T=5, N=45), não participa do PBO assertion.
- **Verdict:** HANDSHAKE_OK_FOR_DEX_START — condicionado a Mira entregar `docs/ml/research-log.md` v0.1 antes de Dex T2 (não bloqueia T1).

— Mira, mapeando o sinal 📊

### Nova T0 Handshake — 2026-04-26 BRT

- **AC1 session-validity gate:** APPROVED — API existente em `packages/t002_eod_unwind/warmup/calendar_loader.py::CalendarData.is_valid_sample_day(d: date) -> bool` cobre o gate. Cobertura: weekends, BR holidays, Copom, rollover D-3..D-1 antes de cada `wdo_expirations`. Carregamento via `CalendarLoader.load(Path("config/calendar/2024-2027.yaml"))`. **Sem gap blocker** — Dex invoca `is_valid_sample_day(session_date)` no loop de emissão, pulando (não raising) sessões False. Half-day NÃO coberto explicitamente — refinar atlas v1.0.1 se ofício B3 publicar half-day no window 12mo.
- **AC2 cost-atlas wire:** APPROVED — `BacktestCosts.from_engine_config(Path)` em `packages/t002_eod_unwind/adapters/exec_backtest.py:78` faz contrato `engine-config.yaml::consumer_contract` (load YAML → resolve atlas → SHA256 LF check → version lock → map cost_components/slippage_model). Atlas v1.0.0 sha `acf44941…` lockado. Article IV preserved. Já é entrega T002.0e — Dex apenas chama.
- **Per-fold P126 anti-leak:** APPROVED — Closure `make_backtest_fn(..., percentiles_state)` rebuildando per train fold é microestruturalmente correto. Global P126 sobre in-sample contamina test fold via volatilidade futura (look-ahead) — vetor de leak clássico. AC2 test `test_per_fold_p126_rebuild` (T5) verifica empiricamente. Memoize por train-index hash é otimização válida pós-smoke (não anti-leak crime).
- **Verdict:** HANDSHAKE_OK_FOR_DEX_START

— Nova, lendo o feed cru 🔭

### T0 Handshake summary

| Agent | Verdict | Pre-condition |
|-------|---------|---------------|
| Beckett | HANDSHAKE_OK_FOR_DEX_START | Optional `--seed` + `--mem-soft-cap-gb` flags (non-blocking) |
| Mira | HANDSHAKE_OK_FOR_DEX_START | `docs/ml/research-log.md` v0.1 emitido antes de Dex T2 (não bloqueia T1) |
| Nova | HANDSHAKE_OK_FOR_DEX_START | (none) |

**Net:** Dex CLEARED para `*develop-story T002.0f` — pode iniciar T1 (`cpcv_harness.py`) imediatamente. T2 (`compute_full_report`) aguarda Mira `research-log.md` v0.1.

---

## Validation Status

**Validator:** Pax (@po) — Balancer ♎
**Date:** 2026-04-25 BRT
**Command:** `*validate-story-draft T002.0f`
**Verdict:** **GO** — Status promoted `Draft → Ready`
**Score:** 10/10 PASS, zero ❌ críticos

### 10-point checklist breakdown

| # | Critério | Verdict | Evidência |
|---|----------|---------|-----------|
| 1 | Scope clarity | ✅ | User story §22-24 explicita propósito + boundary; 13 ACs bounded ao harness + glue + dry-run runner |
| 2 | AC testability | ✅ | Todos AC1-AC13 binary-verifiable; tabela Testing Strategy mapeia 19 testes AC→arquivo→comando |
| 3 | Dependencies | ✅ | Cross-refs spec v0.2.0 + metrics v0.2.2 + handshake doc + Beckett shape — paths existem na arvore; T002.0a/0b/2.1 Done; 0c/0d/0e Ready-for-QA |
| 4 | File paths | ✅ | 10 NEW + 2 MODIFIED + 5 runtime; verifiquei `packages/vespera_metrics/report.py` stub L6-11 referenciado em AC5 está coerente com código real |
| 5 | Agent responsibilities | ✅ | T0 handshakes Beckett/Mira/Nova → Dex T1-T9 → Quinn T10 → Beckett T11; Beckett R6 CPCV exclusivo respeitado |
| 6 | Validation criteria | ✅ | Quinn gate explícito (T10): pytest 4 trees + ruff + mypy clean; determinism strict (3 runs byte-equal) |
| 7 | Edge cases | ✅ | Warmup gate fires, invalid session skipped, per-fold P126 rebuild, trial isolation, 6 GiB mock, holdout lock fires, smoke fail aborta full |
| 8 | Error handling | ✅ | Fail-closed AC9; graceful HALT AC8 (não kill); R4 forbid soft-fallback (Article IV); exit codes 0/1/2 distintos |
| 9 | Risks | ✅ | 5 risks com mitigations realistic (R1 cross-story patch, R2 memoize, R3 configurable, R4 fail-closed, R5 T0 mapping) |
| 10 | Definition of Done | ✅ | 7 critérios DoD: ACs + handshakes + T11 re-run + Quinn PASS + cross-ref 0d + holdout intocado + Pax GO + Gage push |

### Gaps menores (não-bloqueantes — anotações para Dex)

1. **API naming AC1:** story cita `assert_warmup_satisfied(in_sample_start)`; API real em `packages/t002_eod_unwind/warmup/gate.py` é `WarmUpGate().check(as_of_date) -> GateCheckResult`. Dex deve criar wrapper trivial OU ajustar AC1 para referenciar API existente.
2. **Telemetry CSV columns AC8:** divergência cosmética entre story (`rss_mb, vms_mb, cpu_pct, fold_index, trial_id`) e `feedback_cpcv_dry_run_memory_protocol.md` (`peak_commit_bytes, peak_wset_bytes, sample_size, duration_ms`). Dex sugerido: superset que satisfaça ambos.

### Authority sources cross-checked

- ✅ `docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` (5 trials confirmed)
- ✅ `docs/ml/specs/T002-vespera-metrics-spec.md` v0.2.2 (compute_full_report contract)
- ✅ `docs/backtest/T002-mira-handshake-2026-04-25.md` (closed, 8/8 alignment PASS)
- ✅ `docs/prd/EPIC-T002.0-EXECUTION.yaml` (T002.0f wave W4 + Pax gate documented)
- ✅ `feedback_cpcv_dry_run_memory_protocol.md` (6 GiB soft halt + psutil 30s)
- ✅ `packages/vespera_metrics/report.py` L6-11 DEFERRED stub presente (target AC5)
- ✅ `packages/t002_eod_unwind/adapters/_holdout_lock.py` (target AC9)
- ✅ `packages/vespera_cpcv/engine.py` (CPCVEngine.run target AC3)

### Pax cosign

Pax (@po) — 2026-04-25 BRT — Status `Draft → Ready`. Dex unblocked para `*develop-story T002.0f` após T0 handshakes Beckett+Mira+Nova.

— Pax, equilibrando prioridades 🎯
