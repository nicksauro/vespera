# Story T002.0c — CPCV Engine (Purged Combinatorial CV)

**Epic:** [EPIC-T002.0](../prd/EPIC-T002.0.md)
**Status:** Ready for Review (Quinn QA gate pending) — handshake closed (Mira purge formula §8 + Beckett shape §1 fechadas 2026-04-25)
**Owner (Dev):** Dex (@dev)
**Reviewers:** Mira (@ml-researcher, algoritmo + config), Beckett (@backtester, interface consumível)
**QA:** Quinn (@qa)
**Source audit:** [docs/ml/plans/T002-phase-E-readiness-gaps.md](../ml/plans/T002-phase-E-readiness-gaps.md) gap #18
**Estimate:** 2 sessões de trabalho

---

## User story

Como Beckett executando a Fase E, preciso de um engine **Purged Combinatorial Cross-Validation** (Lopez de Prado, *Advances in Financial Machine Learning* Ch.12) parametrizado pela spec T002 v0.2.0 (N=10 grupos, k=2 test groups per split, 45 paths = C(10,2), `embargo_sessions=1`), seed-determinístico e parallel-safe, para que eu rode `*run-cpcv --spec T002-v0.2.0.yaml` e obtenha 45 backtest paths sem information leakage entre train/test e sem overlap temporal induzindo PBO falso.

---

## Acceptance criteria

### Engine core

- [ ] **AC1** Módulo `packages/vespera_cpcv/__init__.py` exporta:
  - `CPCVConfig(n_groups: int, k: int, embargo_sessions: int, seed: int)`
  - `CPCVEngine(config: CPCVConfig)`
  - `CPCVEngine.generate_splits(events: pd.DataFrame) -> Iterator[CPCVSplit]`
  - `CPCVSplit(path_id: int, group_ids_test: tuple[int, ...], train_events: pd.DataFrame, test_events: pd.DataFrame)`
- [ ] **AC2** Para `n_groups=10, k=2`, `generate_splits` produz exatamente `C(10, 2) = 45` `CPCVSplit`s com `path_id ∈ [0, 44]`
- [ ] **AC3** Grupos são particionamento contíguo temporal de `events` (não aleatório) — critério padrão AFML §12.3
- [ ] **AC4** **Purging:** para cada split, eventos de treino cujo `t_end` (label deadline) cai dentro do test window são removidos (AFML §7.4.1 + §12.4). Implementar via `_purge(train, test)`.
- [ ] **AC5** **Embargo:** após cada test group, `embargo_sessions=1` sessão adicional de treino é removida para impedir leakage por autocorrelação (AFML §7.4.2). Implementar via `_embargo(train, test_end, embargo_sessions)`.

### Determinismo e paralelismo

- [ ] **AC6** Seed-determinístico: mesmo `seed` + mesma `events` ⇒ mesma sequência de 45 splits, byte-a-byte (hash de cada split idêntico entre execuções)
- [ ] **AC7** Parallel-safe: `CPCVEngine` é stateless pós-construção; splits podem ser consumidos em paralelo via `joblib.Parallel` ou `multiprocessing.Pool` sem race condition (AC9 valida)

### Benchmark reprodutível

- [ ] **AC8** Unit test `test_cpcv_against_afml_benchmark.py` reproduz o benchmark numérico de AFML Ch.12 §12.4 Figure 12.2/12.3: dado um toy dataset de N=6/k=2 (= 15 paths), valores esperados de eventos purgados + embargados batem com a referência dentro de tolerância 0 (comparação exata).
- [ ] **AC9** Paralelismo test: `joblib.Parallel(n_jobs=4)(delayed(hash_split)(s) for s in engine.generate_splits(...))` produz lista de 45 hashes idêntica a versão serial.

### Integração spec T002

- [ ] **AC10** `CPCVConfig.from_spec_yaml(path)` constrói config lendo `cv_scheme` da spec T002 v0.2.0 L147-154 (n_groups=10, k=2, embargo_sessions=1, seed via `SPEC_SEED` env ou default 42). Validation: rejeita spec com `cv_scheme.type != "CPCV"`.
- [ ] **AC11** Interface para Beckett: `CPCVEngine.run(events, backtest_fn: Callable[[train, test], BacktestResult]) -> list[BacktestResult]` — shape consumível pelo `*run-cpcv` de Beckett (45 results, 1 por path).

### Safety

- [ ] **AC12** Se `events` contém qualquer timestamp ∈ `[2025-07-01, 2026-04-21]` e `VESPERA_UNLOCK_HOLDOUT != 1`, `generate_splits` lança `HoldoutLockError` antes de retornar qualquer split.
- [ ] **AC13** Zero lookahead interno: purging + embargo validados por assertions em runtime (train.index.max() < test.index.min() - embargo OR train.index.min() > test.index.max() + embargo).

---

## Tasks

- [ ] **T0** — **Handshake Mira + Beckett (pré-T1)**: Mira confirma fórmula de purging (train events com `t_end >= test.start AND t_start <= test.end` removidos) + embargo (próxima `embargo_sessions` sessões pós-test removidas). Beckett confirma shape de `BacktestResult` (proposto: `@dataclass BacktestResult(path_id: int, pnl_series: pd.Series, trades: list[Fill], metrics_raw: dict)`). Sign-off na story **antes** de T1.
- [ ] **T1** — `packages/vespera_cpcv/config.py`: `CPCVConfig` pydantic-like dataclass + `from_spec_yaml(path: str) -> CPCVConfig` que lê `cv_scheme` das linhas 148-154. Rejection: `cv_scheme.type != "CPCV"` → `ValueError("Unsupported cv_scheme.type: {type}")`.
- [ ] **T2** — `packages/vespera_cpcv/splits.py`: `CPCVSplit` dataclass (frozen) + `hash_split(split) -> str` (sha256 sobre `(path_id, group_ids_test, train_index_tuple, test_index_tuple)`).
- [ ] **T3** — `packages/vespera_cpcv/engine.py`:
  - `CPCVEngine(config)` com `np.random.Generator(PCG64(config.seed))` (determinismo AC6)
  - `_partition_groups(events)` — split contíguo em `n_groups` via `np.array_split(events.index, n_groups)`
  - `_enumerate_test_combos()` — `itertools.combinations(range(n_groups), k)` em ordem canônica (determinismo)
  - `_purge(train, test_start, test_end)` + `_embargo(train, test_end, embargo_sessions)` separados para testabilidade
  - `generate_splits(events)` — yield `CPCVSplit` com `path_id` = índice na ordem de `combinations`
  - `run(events, backtest_fn)` — consome `generate_splits`, invoca `backtest_fn(train, test)` por split, retorna `list[BacktestResult]` (len=45)
- [ ] **T4** — `packages/vespera_cpcv/__init__.py` re-export: `CPCVConfig`, `CPCVEngine`, `CPCVSplit`, `HoldoutLockError` (reuse de T002.0b `_holdout_lock`)
- [ ] **T5** — `tests/vespera_cpcv/test_cpcv_config.py` (AC10): parse spec v0.2.0 ok; reject `cv_scheme.type=KFold`; `SPEC_SEED` env respeitado
- [ ] **T6** — `tests/vespera_cpcv/test_cpcv_engine.py` (AC2, AC3, AC6, AC7, AC9): `len(list(splits)) == 45`; 2 runs com mesmo seed → hashes idênticos; `joblib.Parallel(n_jobs=4)` produz mesma lista de hashes que serial
- [ ] **T7** — `tests/vespera_cpcv/test_cpcv_against_afml_benchmark.py` (AC8): toy dataset 6 events × 6 groups × k=2 (15 paths), valores **exatos** esperados de eventos purgados/embargados — Mira fornece a tabela de referência na `metrics-spec.md` ou sign-off comentário (se atraso de T002.0d, Mira pode entregar a tabela inline aqui)
- [ ] **T8** — `tests/vespera_cpcv/test_cpcv_purge_embargo.py` (AC4, AC5, AC13): invariante `train.t_end.max() < test.t_start.min() - embargo` OR `train.t_start.min() > test.t_end.max() + embargo`
- [ ] **T9** — `tests/contracts/test_cpcv_holdout_lock.py` (AC12): events com `ts=2025-07-15` → `HoldoutLockError` se flag ≠ 1; PASS com flag=1
- [ ] **T10** — Quinn: `pytest tests/contracts/` ≥ 28/28; `pytest tests/vespera_cpcv/ -q` PASS; `ruff` + `mypy` clean

---

## File list (previsto)

- `packages/vespera_cpcv/__init__.py` (novo)
- `packages/vespera_cpcv/config.py` (novo)
- `packages/vespera_cpcv/engine.py` (novo)
- `packages/vespera_cpcv/splits.py` (novo)
- `tests/vespera_cpcv/test_cpcv_config.py` (novo)
- `tests/vespera_cpcv/test_cpcv_engine.py` (novo)
- `tests/vespera_cpcv/test_cpcv_against_afml_benchmark.py` (novo)
- `tests/vespera_cpcv/test_cpcv_purge_embargo.py` (novo)
- `tests/contracts/test_cpcv_holdout_lock.py` (novo)

---

## Dependencies

- **Blocks:** Task #36 (CPCV Gate Fase E)
- **Blocked by:** T002.0b (CPCV consome adapter via `backtest_fn`)
- **Paralelizável com T002.0d** se Mira define API de `vespera_metrics` antes da impl

---

## Dev notes

- **Referência canônica:** Lopez de Prado, Marcos. *Advances in Financial Machine Learning*, Wiley 2018, Ch. 7 (purging/embargo) + Ch. 12 (Combinatorial CV).
- **Spec T002 v0.2.0 L147-154:** `cv_scheme.type=CPCV, n_groups=10, k=2, n_paths=45, embargo_sessions=1, purging=true`
- **R6:** determinismo (seed) é invariante — CPCV sem determinismo é PBO falso.
- **R1 + R15(d):** hold-out intocado (AC12).
- **Não usar scikit-learn `KFold`** — não implementa purging nem embargo.
- **Mira spec alignment:** Mira valida fórmula de purging + embargo contra seu entendimento do spec antes de Dex implementar.
- **Beckett interface:** shape de `BacktestResult` + `run()` negociado com Beckett no handshake (para `*run-cpcv` consumir direto).

---

## Testing strategy (Quinn)

| AC | Tipo | Arquivo | Comando | Output |
|----|------|---------|---------|--------|
| AC1 | Unit | `tests/vespera_cpcv/test_cpcv_config.py::test_api_surface` | `pytest ... -q` | API exports presentes |
| AC2 | Unit | `tests/vespera_cpcv/test_cpcv_engine.py::test_generates_45_splits` | `pytest ... -q` | `len == 45` |
| AC3 | Unit | `tests/vespera_cpcv/test_cpcv_engine.py::test_contiguous_groups` | `pytest ... -q` | groups contíguos em `events.index` |
| AC4, AC5 | Unit | `tests/vespera_cpcv/test_cpcv_purge_embargo.py` | `pytest ... -q` | invariantes PASS |
| AC6 | Unit determinismo | `tests/vespera_cpcv/test_cpcv_engine.py::test_determinism` | run 2x | hashes idênticos |
| AC7, AC9 | Parallel | `tests/vespera_cpcv/test_cpcv_engine.py::test_parallel_safe` | joblib n_jobs=4 | mesma lista hashes |
| AC8 | Benchmark | `tests/vespera_cpcv/test_cpcv_against_afml_benchmark.py` | `pytest ... -q` | tolerância 0 vs tabela Mira |
| AC10 | Unit | `tests/vespera_cpcv/test_cpcv_config.py` | `pytest ... -q` | parse spec + reject |
| AC11 | Integration | `tests/integration/test_cpcv_run_dummy.py` | `pytest ... -q` | `len(results)==45` |
| AC12 | Contract | `tests/contracts/test_cpcv_holdout_lock.py` | `pytest tests/contracts/ -q` | ≥ 28/28 |
| AC13 | Unit | `tests/vespera_cpcv/test_cpcv_purge_embargo.py::test_no_lookahead` | `pytest ... -q` | assertions runtime PASS |

---

## Definition of Done

- [ ] AC1-AC13 verde (proof mecânico na tabela Testing Strategy)
- [ ] Mira valida algoritmo + fórmulas em comentário na story com referências explícitas (AFML Ch.12 §12.4 Figure 12.2/12.3 para 45 paths; §7.4.1 purging; §7.4.2 embargo). **Mira fornece tabela de referência numérica para AC8** (6/k=2 = 15 paths toy) antes de T7 rodar.
- [ ] Beckett valida interface `CPCVEngine.run` com snippet de uso em `*run-cpcv`:
  ```python
  from vespera_cpcv import CPCVEngine, CPCVConfig
  cfg = CPCVConfig.from_spec_yaml("docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml")
  eng = CPCVEngine(cfg)
  results = eng.run(events_in_sample, backtest_fn=beckett_backtest_fn)
  assert len(results) == 45
  ```
- [ ] Quinn PASS: `pytest tests/contracts/ -q` ≥ 28/28; `pytest tests/vespera_cpcv/ -q` PASS; benchmark AFML exato
- [ ] Hold-out intocado: test AC12 verde; grep code de `VESPERA_UNLOCK_HOLDOUT` em `packages/vespera_cpcv/` ≥ 1 match
- [ ] Cross-ref: T002.0d atualizada (`compute_full_report` pode consumir `list[BacktestResult]`)
- [ ] Pax `*validate-story-draft` → GO antes de Dev
- [ ] Gage pushes (R12)

---

## Handshakes required (pré-Dev start)

| Agente | Valida | Sign-off |
|--------|--------|----------|
| **Mira** | Fórmulas purging+embargo vs AFML Ch.7/12 + config CPCV alinhado spec L147-154 + **tabela numérica toy AC8** | ASSINADO — `docs/ml/specs/T002-cpcv-purge-formula.md` §6.4 + §8 |
| **Beckett** | Interface `CPCVEngine.run(events, backtest_fn) -> list[BacktestResult]` consumível + shape `BacktestResult` (AC11) | ASSINADO — `docs/backtest/T002-backtest-result-shape.md` §1 |
| **Pax** | `*validate-story-draft` 10-point | Status Draft → Ready |

---

## Dev Agent Record

**Agent:** Dex (@dev) — implementation 2026-04-25 BRT.

### Files created

- `packages/vespera_cpcv/__init__.py` — public re-exports (CPCVEngine, CPCVConfig, CPCVSplit, BacktestRunner, 6 result dataclasses, HoldoutLockError).
- `packages/vespera_cpcv/config.py` — `CPCVConfig` frozen dataclass + `from_spec_yaml`.
- `packages/vespera_cpcv/purge.py` — `decompose_test_blocks` + `purge_train` (AFML §7.4.1 eq. 7.1) + `embargo_train` (AFML §7.4.2 + Mira §4.2 dataset-calendar).
- `packages/vespera_cpcv/engine.py` — `CPCVEngine.generate_splits` + `run` + `CPCVSplit` frozen dataclass with `hash_split()`.
- `packages/vespera_cpcv/result.py` — 6 frozen dataclasses per Beckett shape spec §1 (TradeRecord, FoldMetadata, AggregateMetrics, DeterminismStamp, FoldDiagnostics, BacktestResult) + `content_sha256()`.
- `packages/vespera_cpcv/runner.py` — `BacktestRunner` orchestrator (engine + DeterminismStamp).
- `tests/cpcv/__init__.py` — pytest package marker.
- `tests/cpcv/test_engine_against_afml_benchmark.py` — AC8 byte-exact match against Mira §6.4 toy table (15 splits, 20 total embargoed, 0 purged).
- `tests/cpcv/test_engine_determinism.py` — AC2/6/7/9/10/13 (45 splits, 3-run identical hashes, parallel-safe, spec parsing, no-lookahead).
- `tests/cpcv/test_purge_formula.py` — purge + embargo formula isolated unit tests including dataset-calendar gap (Mira §5.5).
- `tests/cpcv/test_holdout_guard.py` — AC12 (HoldoutLockError BEFORE iteration; unlock flag honored).
- `tests/cpcv/test_result_dataclasses.py` — frozen invariants + AC12 content_sha256 byte-identical across 3 constructions.

### Test results

`pytest tests/cpcv/ tests/contracts/ -q`: **107 passed in 6.70s** (59 cpcv + 48 contracts). `ruff check` clean.

| AC | Status | Evidence |
|----|--------|----------|
| AC1 (API surface) | PASS | `from packages.vespera_cpcv import ...` resolves all 7 names |
| AC2 (45 splits) | PASS | `test_two_runs_same_seed_produce_identical_split_hashes` |
| AC3 (contiguous partition) | PASS | `np.array_split` over `events.index` (engine `_assign_groups`) |
| AC4 (purge AFML §7.4.1) | PASS | `test_purge_drops_train_with_label_overlap` + AFML benchmark sum-purged=0 |
| AC5 (embargo AFML §7.4.2) | PASS | `test_embargo_drops_next_session_after_block` + AFML benchmark sum-embargoed=20 |
| AC6 (seed determinism) | PASS | `test_three_runs_byte_identical_hashes` |
| AC7 (parallel safe) | PASS | `test_parallel_consumption_produces_same_hashes_as_serial` |
| AC8 (AFML toy benchmark byte-exact) | PASS | 15/15 parametrized splits match Mira §6.4 table |
| AC9 (joblib parallel) | PASS | shuffle-then-sort hashes identical to serial |
| AC10 (`from_spec_yaml`) | PASS | parses T002 v0.2.0; rejects KFold; honors `SPEC_SEED` |
| AC11 (run interface) | PASS | `CPCVEngine.run(events, backtest_fn) -> list[BacktestResult]` len=45 sorted by path_index |
| AC12 (hold-out lock) | PASS | `test_holdout_guard_fires_before_iteration` raises on first `next()` |
| AC13 (no-lookahead) | PASS | `test_no_lookahead_invariant_holds_for_all_splits` over all 45 splits |

### Determinism (AC12 strict — 3 runs)

Manual verification: same seed + same events ⇒ identical 45-tuple of `hash_split()` byte-for-byte across 3 independent engine constructions. First hash `db0cdb6eb988576d…` reproduced 3/3 runs.

### Issues encontradas

1. **TestBlock NamedTuple pytest collection warning** — resolved by renaming canonical class to `CPCVTestBlock` (kept `TestBlock` as backwards-compat alias). Tests use local alias `_Block` to avoid pytest auto-collection of `TestBlock`.
2. **Mission specified `tests/cpcv/`** while story specified `tests/vespera_cpcv/`. Followed mission directive — tests live under `tests/cpcv/`. Story's File List should be reconciled by Pax (cosmetic — file naming).
3. **AC14 in mission text** does not exist in the story (story has AC1-AC13 only). Mission's AC14 (`Trade(ts, price, qty)` parity) is interpreted as Beckett shape spec §1.1 invariant (TradeRecord uses canonical `Fill` from `exec_backtest.py`) — validated by `test_trade_uses_same_fill_type_as_live_broker`.
4. **`run()` `spec_snapshot` parameter** carried as reserved-for-future per Beckett §3.1 (param accepted but not yet consumed; will plumb through `BacktestRunner` once Mira finalizes `vespera_metrics` interface in T002.0d).
5. **DeterminismStamp `engine_config_sha256` / `cost_atlas_sha256`** flagged `[TO-VERIFY]` per Beckett §5.4 — `BacktestRunner` accepts paths and hashes them when supplied; defaults to empty/None until engine-config wiring lands.

### Next handoff

Quinn (@qa) `*qa-gate T002.0c` — quality gate over:
- `pytest tests/contracts/ -q` (must remain ≥ 28/28 → currently 48/48)
- `pytest tests/cpcv/ -q` (currently 59/59 PASS)
- `ruff check packages/vespera_cpcv/ tests/cpcv/` (currently clean)
- AFML byte-exact match against Mira §6.4 (verified)
- 3-run determinism evidence (verified)
- File List completeness (above)

### Change Log

- 2026-04-25: Implemented T1-T9 + extended scope per mission directive (CPCVEngine + 6 result dataclasses + BacktestRunner + 5 test modules). Status: Draft → Ready for Review.
