# PRR-20260430-1 — T002.7 Phase G OOS Confirmation Pre-Committed Disposition Rule

> **prr_id:** PRR-20260430-1
> **title:** T002.7 Phase G OOS confirmation pre-committed disposition rule
> **date_brt:** 2026-04-30
> **author:** Pax (@po)
> **council_authority:** ESC-012 ratified APPROVE_PATH_B 5/6 SUPERMAJORITY (`docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md` §1 + §4.1 R3 + §4.4 R15/R16/R17)
> **spec_amendment:** `docs/ml/specs/T002-gate-4b-real-tape-clearance.md` v1.2.0 §15.13 (Mira authority — Phase G OOS Unlock Protocol; sub-sections §15.13.1..§15.13.13 drafted in parallel under same ESC-012 R2 mandate)
> **parent_spec_yaml:** `docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` v0.2.3 (thresholds UNMOVABLE per Anti-Article-IV Guard #4)
> **status:** REGISTERED (pre-Phase-G-N8-run hash-frozen)

---

## §1 Purpose

This PRR registers the **pre-committed disposition rule** for the upcoming Phase G N8 OOS confirmation run, **hash-frozen BEFORE** any data is consumed, per ESC-012 R3 mandate (Kira C-K1 + Mira C-M4 source). The disposition rule binds the interpretation of every possible Phase G outcome to a deterministic action path, eliminating post-hoc reasoning and outcome-based threshold reinterpretation. This enforces strict §0 falsifiability discipline (Bailey-LdP 2014 §6 canonical reproducible-backtest gating triplet) and preserves Article IV (No Invention) at the verdict layer.

The rule has **4 explicit branches** covering the full outcome space — no implicit fall-through, no "we'll decide when we see the data" branch, no threshold relaxation.

---

## §2 Scope

| Field | Value |
|---|---|
| **OOS window** | 2025-07-01..2026-04-21 (10 months hold-out, parent spec yaml `data_splits.hold_out_virgin`) |
| **Single-shot** | YES per ESC-012 R9 (Kira C-K3 + Mira C-M6); result binding regardless of outcome; CANNOT be re-run with adjusted parameters |
| **Reusability invariant** | IDENTICAL `engine_config_sha256` (`ccfb575a…`), spec yaml v0.2.3 thresholds, cost atlas v1.0.0 (`bbe1ddf7…`), rollover calendar (`c6174922…`), Bonferroni n_trials=5 (T1..T5 verbatim), latency_dma2_profile, RLP detection, microstructure flags (ESC-012 R6) |
| **No mutation** | NO refinement / no parameter tweak / no Bonferroni expansion / no cost mutation / no spec yaml threshold mutation (Anti-Article-IV Guard #4) / no hold-out lock policy mutation (Anti-Article-IV Guard #3 — Phase G unlock IS authorized §15.10 transition, NOT a guard violation per ESC-012 R10) |
| **Predictor↔label** | IDENTICAL Phase F2 — predictor=`-intraday_flow_direction`, label=`ret_forward_to_17:55_pts`, paired-resample bootstrap n=10000 PCG64 seed=42 (parent spec §15.4; ESC-012 R7) |

---

## §3 Disposition rule — 4 explicit branches (verbatim)

The four branches below ARE the full outcome partition. No 5th branch may be invented post-hoc; no branch may be relaxed; thresholds in §4 below are UNMOVABLE.

### §3.1 Branch R15 — PASS K1+K2+K3 (surprise PASS)

| Field | Value |
|---|---|
| **Outcome label** | `PASS_OOS_surprise` |
| **Trigger** | DSR > 0.95 OOS **AND** PBO < 0.5 OOS **AND** IC_holdout > 0 (in-sample binding clause) **AND** IC_holdout > 0.5 × IC_in_sample (decay sub-clause; full evaluation per Mira spec §15.13.5) |
| **Action** | ESC-013 council convene; Aria + Mira + Riven + Pax + Sable + Kira; adjudicate K1 strict bar interpretation under OOS surprise PASS; Gate 5 still requires paper-mode Phase G/H |
| **Carry-forward** | ESC-012 R10 Gate 5 fence preservation — Phase G PASS does NOT pre-disarm Gate 5 alone under K1 strict bar (Anti-Article-IV Guard #6); Gate 5 capital ramp dual-sign requires K1+K2+K3 ALL PASS + paper-mode Phase G/H |
| **Verdict label discipline** | `PASS` (parent spec §9 verdict label discipline; ESC-012 R13) |

### §3.2 Branch R16 — FAIL K3 collapse (clean falsification)

| Field | Value |
|---|---|
| **Outcome label** | `FAIL_K3_collapse_strategy_edge_clean` |
| **Trigger** | IC_holdout < 0.3 (significant drop from in-sample IC) — strategy edge collapses on OOS tape |
| **Action** | T002 hypothesis falsified clean per spec §0 falsifiability discipline; Riven 3-bucket reclassify N8 → `strategy_edge` clean negative; T002 retire ceremony; epic deprecate; squad bandwidth freed for H_next research |
| **Carry-forward** | ESC-012 R11 Riven 3-bucket post-mortem Round 3 entry; ESC-012 R12 Mira F2-T9 re-clearance authority post-Phase-G; ESC-012 R13 Pax T002.7 close + outcome decision |
| **Verdict label discipline** | `FAIL_K3_collapse` (NOT `costed_out_edge` — distinguishes signal absence from cost foreclosure) |

### §3.3 Branch R17 — FAIL K1+K3 sustains (cost-foreclosed edge confirmed OOS)

| Field | Value |
|---|---|
| **Outcome label** | `FAIL_K1_DSR_strict_bar_K3_signal_sustains` |
| **Trigger** | IC_holdout ∈ [0.3, 0.5 × IC_in_sample) (signal real but decayed) **AND** DSR_OOS < 0.95 (K1 strict bar still FAIL on OOS tape) |
| **Action** | `costed_out_edge` OOS-confirmed; T002 retire ceremony with refined diagnostic (signal real OOS-stable but cost-foreclosed under conservative slippage + transaction cost regime); Riven 3-bucket reclassify N8 → `costed_out_edge_oos_confirmed`; institutional knowledge documented for H_next research |
| **Carry-forward** | ESC-012 R11/R12/R13; F2 round 2 `costed_out_edge` Round-2 verdict gets Round-3 OOS confirmation overlay (does NOT overwrite — preserves governance integrity) |
| **Verdict label discipline** | `FAIL_costed_out_edge_confirmed_OOS` |

### §3.4 Branch INCONCLUSIVE — borderline thresholds OR regime mismatch

| Field | Value |
|---|---|
| **Outcome label** | `INCONCLUSIVE_regime_mismatch` |
| **Trigger** | Any of: (a) DSR_OOS ∈ [0.90, 0.95] borderline AND IC_holdout ≥ 0.5 × IC_in_sample (K1 borderline + K3 PASS); (b) PBO_OOS ∈ [0.45, 0.55] borderline; (c) data-quality bucket fires (Riven 3-bucket §7.1 Bucket A — circuit-breaker contamination, RLP-gap, <70% session coverage, rollover-gap) — does NOT cleanly fall into R15/R16/R17 |
| **Action** | ESC escalation council (ESC-013); defer to council per Riven custodial veto authority + Mira ML statistical authority; possible outcomes (council decides): (i) re-classify under R16/R17 with new evidence; (ii) extend hold-out window to 2026-04-22..present (would require new PRR + new ESC ratification); (iii) data-quality remediation + re-run authorization (would require new PRR explicitly waiving R9 single-shot discipline + new council ratification — DEFAULT REJECT per Anti-Article-IV Guard #4 spirit) |
| **Carry-forward** | ESC-012 R13 Pax T002.7 close path = ESC escalate; verdict label `INCONCLUSIVE_regime_mismatch` (parent spec §9 verdict label discipline ESC-012 R13) |

---

## §4 Numerical thresholds (per parent spec yaml v0.2.3 + Mira spec §15.13.5 — UNMOVABLE)

### §4.1 K1/K2/K3 binding thresholds (cited verbatim from parent spec §1 + spec yaml v0.2.3 L207-209)

| Criterion | Threshold | Source anchor |
|---|---|---|
| **K1 PASS** | DSR > 0.95 | spec yaml v0.2.3 §kill_criteria.k1_dsr (L207); parent spec §1 K1 row; Bailey-LdP 2014 §3 |
| **K2 PASS** | PBO < 0.5 | spec yaml v0.2.3 §kill_criteria.k2_pbo (L208); parent spec §1 K2 row; BBLP 2014 |
| **K3 in-sample binding clause** | IC_in_sample > 0 with CI95 lower bound > 0 | spec yaml v0.2.3 §kill_criteria.k3_ic (L209); parent spec §1 K3 row; AFML §8.6 |
| **K3 decay sub-clause (Phase G full evaluation per Mira spec §15.13.5)** | IC_holdout > 0.5 × IC_in_sample | parent spec §1 K3 row "rolling-window decay curve documented; decay < 1 σ rolling stdev preserves in-sample sign"; Mira spec §15.13.5 (full decay evaluation unlocked under Phase G `holdout_locked=False` parameter path) |

**Anti-Article-IV Guard #4 (UNMOVABLE):** This PRR cites SPEC thresholds verbatim. NO mutation. NO per-run waiver. NO post-hoc relaxation. If a future situation appears to require a different threshold, this PRR FAILS and the post-Phase-G action MUST be ESC escalation (Branch INCONCLUSIVE), not threshold reinterpretation.

### §4.2 INCONCLUSIVE branch threshold partition (Branch §3.4 explicit boundaries)

| Condition | Maps to branch |
|---|---|
| DSR_OOS > 0.95 AND PBO_OOS < 0.5 AND IC_holdout ≥ 0.5 × IC_in_sample | **R15** PASS surprise |
| IC_holdout < 0.3 (regardless of DSR/PBO) | **R16** FAIL K3 collapse clean |
| IC_holdout ∈ [0.3, 0.5 × IC_in_sample) AND DSR_OOS < 0.95 | **R17** FAIL K1+K3 sustains |
| DSR_OOS ∈ [0.90, 0.95] borderline AND IC_holdout ≥ 0.5 × IC_in_sample | **INCONCLUSIVE** — K1 borderline + K3 PASS |
| PBO_OOS ∈ [0.45, 0.55] borderline | **INCONCLUSIVE** — K2 borderline |
| Riven 3-bucket §7.1 Bucket A (data_quality) fires | **INCONCLUSIVE** — regime mismatch / data quality |
| Other combinations not cleanly matching R15/R16/R17 | **INCONCLUSIVE** — ESC escalate |

**Bonferroni invariant:** n_trials=5 (T1..T5 verbatim) PRESERVED across all branches. K1 strict bar applies to per-trial DSR (NOT family-wise relaxed); ESC-012 R6 reusability invariant binding.

---

## §5 Anti-Article-IV Guards 1-8 carry-forward

| Guard | Status | Carry-forward source |
|---|---|---|
| **#1** Dex impl gated em Mira spec PASS | PRESERVED | Mira spec amendment v1.2.0 §15.13 + ESC-012 R1 (F2-T5-OBS-1 fix prereq) + ESC-012 R2 (spec amendment prereq) precede Phase G N8 run |
| **#3** Hold-out lock UNMOVABLE | PRESERVED | Phase G unlock IS authorized §15.10 transition per Mira spec, NOT a guard violation per ESC-012 R10 K1 strict bar UNMOVABLE preservation. Hold-out window 2025-07-01..2026-04-21 IS the canonical OOS test for THIS strategy specifically (parent spec yaml v0.2.0 `data_splits.hold_out_virgin`). Single-shot consumption per R9. |
| **#4** Thresholds UNMOVABLE | PRESERVED | This PRR §4 cites SPEC thresholds verbatim; NO mutation; INCONCLUSIVE branch routes borderline to ESC, NOT to threshold relaxation |
| **#5** No subsample | PRESERVED | OOS window consumed in full single-shot per R9; no subsample selection; no walk-forward optimization on hold-out |
| **#6** Gate 5 fence (Gate 5 sem Gate 4 = REJECT) | PRESERVED | R15 branch action explicitly states Gate 5 still requires paper-mode Phase G/H; ESC-012 R10 verbatim |
| **#7** No push (Gage exclusive) | PRESERVED | Article II — Gage exclusive `git push`; this PRR is local-write artifact only; commit + push delegated to @devops at story close per ESC-012 §7 closure conditions |
| **#8** IC field default 0.0 reserved for pre-compute state | PRESERVED | `ic_holdout_status='computed'` propagation per Mira spec §15.13.2; NEVER silently default to 0.0 in Phase G run; `evaluate_kill_criteria` raises `InvalidVerdictReport` if status != `computed` per parent spec §15.5 + §15.6 invariant; default 0.0 reserved for Phase F2 `deferred` state ONLY (now superseded under Phase G `holdout_locked=False` parameter path) |

---

## §6 Pax cosign hash registration

### §6.1 Hashing protocol

The canonical `pax_cosign.py` script (`scripts/pax_cosign.py`) computes SHA256 over `yaml.safe_dump(payload, sort_keys=True, allow_unicode=True)` for `preregistration_revisions[]` array entries inside spec yaml files (8 fields: revision_id, timestamp_brt, from_version, to_version, breaking_fields, justification, data_constraint_evidence, pax_cosign_hash). This PRR is a standalone markdown file (NOT a spec yaml entry); the canonical revision hash will be computed and registered when the parent spec yaml v0.2.3 → v0.2.4 PRR-20260430-1 entry is appended at T002.7 close (per ESC-012 R13 Pax close authority + R15 governance ledger MANIFEST R15 append-only invariant).

### §6.2 Pre-Phase-G-run interim hash (this PRR file content)

Because the parent spec yaml entry is deferred to story close, an interim integrity hash is computed over this PRR file's `frontmatter + §1..§9` sections (UTF-8 encoded, LF line endings, no trailing whitespace) for tamper-evidence purposes. The canonical `preregistration_revisions[]` hash will replace this interim hash at T002.7 close.

| Field | Value |
|---|---|
| **Hash protocol** | SHA256 over UTF-8 encoded file content (this PRR) frozen state |
| **Computation command** | `python -c "import hashlib; print(hashlib.sha256(open('docs/governance/PRR-20260430-1-T002.7-phase-g-disposition.md','rb').read()).hexdigest())"` |
| **Computed at** | T002.7 PRR registration step (this commit, by Pax @po) |
| **Interim hash — content-before-this-line SHA256** | `1d4a42cb19a661aacda26109a194fb7a965ea794636d4a30a96e9ba058f28557` (parent-spec-yaml `mira_signature_sha256_of_file_before_this_line` convention; SHA256 over UTF-8 bytes from file start through end of "| **Computed at**" row inclusive) |
| **Canonical hash (parent spec yaml entry)** | _to be computed via `python scripts/pax_cosign.py compute docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml --index 5` after PRR-20260430-1 entry appended to spec yaml at T002.7 close (will supersede interim hash above)_ |

### §6.3 Parent spec yaml `preregistration_revisions[]` entry (planned shape — to be appended at T002.7 close)

```yaml
- revision_id: PRR-20260430-1
  timestamp_brt: "2026-04-30T00:00:00-03:00"
  from_version: "0.2.3"
  to_version: "0.2.4"
  breaking_fields: []   # NO spec yaml field mutation — Anti-Article-IV Guard #4 UNMOVABLE
                        # PRR-20260430-1 is a disposition-rule registration, NOT a threshold/data-split mutation;
                        # parent spec yaml v0.2.3 thresholds + hold-out lock UNCHANGED; patch bump reflects
                        # preregistration_revisions[] append only (analogous to PRR-20260427-1, PRR-20260428-1/2/3 pattern).
  justification: |
    Phase G OOS confirmation pre-committed disposition rule registration per ESC-012 R3 mandate
    (5/6 SUPERMAJORITY APPROVE_PATH_B; Aria + Kira + Mira + Beckett + Pax APPROVE; Riven minority Path C).
    Hash-frozen BEFORE Phase G N8 OOS run to enforce strict §0 falsifiability discipline (Bailey-LdP 2014 §6
    canonical reproducible-backtest gating triplet) and prevent post-hoc reasoning / outcome-based threshold
    reinterpretation. 4 explicit branches cover full outcome space (R15 PASS surprise / R16 FAIL_K3_collapse /
    R17 FAIL_K1+K3_sustains / INCONCLUSIVE) — see docs/governance/PRR-20260430-1-T002.7-phase-g-disposition.md.
    Anti-Article-IV Guards #1/#3/#4/#5/#6/#7/#8 PRESERVED (this PRR §5 explicit table).
    Spec yaml data_splits.in_sample (2024-07-01..2025-06-30) UNCHANGED. Hold-out lock
    [2025-07-01, 2026-04-21] UNCHANGED — Phase G consumption IS authorized §15.10 transition
    (NOT guard violation per ESC-012 R10 K1 strict bar UNMOVABLE preservation). Bonferroni n_trials=5
    PRESERVED across all 4 branches. Patch bump 0.2.3 -> 0.2.4 reflects preregistration_revisions[] append only.
  data_constraint_evidence: |
    1. ESC-012 council resolution docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md §4.1 R3 verbatim:
       "Pre-committed disposition rule (PRR-20260430-1) hash-frozen BEFORE running: written in advance with
       4 explicit branches (PASS K1+K2+K3 -> ESC escalate; FAIL K3 collapse -> strategy_edge clean falsification;
       FAIL K1+K3 sustains -> costed_out_edge OOS-confirmed clean refutation; INCONCLUSIVE -> ESC escalation).
       Pax cosign hash via python scripts/pax_cosign.py register. NO OOS run before disposition rule registered."
    2. ESC-012 §4.4 decision tree R15/R16/R17 verbatim mapped to PRR §3.1/§3.2/§3.3 branches; INCONCLUSIVE
       branch §3.4 derived from ESC-012 R3 4th branch ("INCONCLUSIVE -> ESC escalation") Kira C-K1 + Mira C-M4 source.
    3. Parent spec yaml v0.2.3 thresholds (K1 DSR>0.95 / K2 PBO<0.5 / K3 IC>0) cited verbatim in PRR §4.1;
       Mira spec §1 K3 row decay sub-clause cited for K3 full-evaluation Phase G unlock per §15.13.5.
    4. Mira spec amendment v1.2.0 §15.13 (Phase G OOS Unlock Protocol) under same ESC-012 R2 mandate provides
       canonical authority for `holdout_locked=False` parameter path + `ic_holdout_status='computed'` propagation
       (referenced in PRR §5 Guard #8 carry-forward).
    5. Anti-Article-IV Guards 1-8 carry-forward audited explicitly in PRR §5 table; Guard #4 UNMOVABLE
       reaffirmed in PRR §4.1 + §4.2 INCONCLUSIVE-route-to-ESC clause (NO threshold relaxation path).
    6. Single-shot OOS discipline per ESC-012 R9 (Kira C-K3 + Mira C-M6) — Phase G window 2025-07-01..2026-04-21
       single-shot; result binding regardless of outcome; CANNOT be re-run with adjusted parameters.
       PRR §2 scope row + §3.4 INCONCLUSIVE re-run clause explicitly DEFAULT REJECT.
  council_ref: docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md
  authority: ESC-012 mini-council 5/6 SUPERMAJORITY APPROVE_PATH_B (Aria + Kira + Mira + Beckett + Pax APPROVE; Riven minority Path C dissenting fallback)
  pax_cosign_date: "2026-04-30 BRT"
  pax_cosign_hash: "<TBD-at-spec-yaml-append-time-via-pax_cosign.py-compute>"
```

---

## §7 ESC-012 R3 compliance audit

| ESC-012 R3 sub-clause | Status | Evidence |
|---|---|---|
| Hash-frozen BEFORE Phase G N8 run | ✅ | This PRR registered pre-Phase-G-N8; Phase G N8 run blocked by ESC-012 R1/R2/R4/R5 prereqs (F2-T5-OBS-1 fix + Mira spec amendment v1.2.0 + Dara hold-out tape pre-req + Sable Phase G coherence audit) — NONE complete yet, so N8 cannot run. Branch `t002-7-phase-g-prep` is pre-N8. |
| 4 explicit branches | ✅ | PRR §3.1 R15 PASS surprise + §3.2 R16 FAIL K3 collapse + §3.3 R17 FAIL K1+K3 sustains + §3.4 INCONCLUSIVE — all 4 verbatim from ESC-012 R3 mandate text |
| Pax cosign hash | ⏳ | Interim file-content SHA256 computation deferred to commit step §6.2; canonical `preregistration_revisions[]` hash deferred to T002.7 close per §6.3 (parent spec yaml append-only); both registered via `pax_cosign.py compute` per existing protocol |
| Article IV trace | ✅ | Each branch traces to spec §1 thresholds + ESC-012 §4.4 + Mira spec §15.13 (parallel draft); §5 Anti-Article-IV Guards 1-8 carry-forward table; §6.3 spec yaml entry justification + data_constraint_evidence anchored to ESC-012 council resolution + parent spec yaml + Mira spec amendment |
| NO OOS run before disposition rule registered | ✅ | This PRR is the disposition rule registration; Phase G N8 run blocked by ESC-012 §7 prereqs anyway; T002.7 story will gate N8 invocation on PRR-20260430-1 status=REGISTERED check |

---

## §8 Article IV self-audit

| Claim | Source / trace |
|---|---|
| ESC-012 R3 mandate verbatim text | `docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md` §4.1 R3 row |
| 4 branches R15/R16/R17 verbatim | `docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md` §4.4 R15/R16/R17 rows |
| 4th INCONCLUSIVE branch | ESC-012 R3 mandate text 4th alternative ("INCONCLUSIVE -> ESC escalation") + Riven custodial veto authority pattern (parent spec PRR-20260428-2 cosign source) |
| K1 DSR > 0.95 threshold | parent spec yaml v0.2.3 §kill_criteria.k1_dsr (L207); Mira spec §1 K1 row; Bailey-LdP 2014 §3 |
| K2 PBO < 0.5 threshold | parent spec yaml v0.2.3 §kill_criteria.k2_pbo (L208); Mira spec §1 K2 row; BBLP 2014 |
| K3 IC > 0 in-sample binding clause | parent spec yaml v0.2.3 §kill_criteria.k3_ic (L209); Mira spec §1 K3 row |
| K3 decay sub-clause IC_holdout > 0.5 × IC_in_sample | Mira spec §1 K3 row "rolling-window decay curve documented" + Mira spec §15.13.5 (Phase G full evaluation unlocked under `holdout_locked=False`) |
| Reusability invariant (engine_config_sha256 + cost atlas + rollover calendar + Bonferroni n_trials=5) | ESC-012 R6 (Aria C-A10 + Mira C-M5 + Riven implicit) |
| Predictor↔label semantics IDENTICAL F2 | ESC-012 R7 (Mira C-M5); Mira spec §15.4 |
| Single-shot OOS discipline | ESC-012 R9 (Kira C-K3 + Mira C-M6) |
| Anti-Article-IV Guards 1-8 carry-forward | PRR §5 table; original guards from Mira spec §11.2 + ESC-011 R5/R6 + ESC-012 R10 |
| `holdout_locked=False` parameter path | Mira spec amendment v1.2.0 §15.13.2 (parallel draft) |
| `ic_holdout_status='computed'` propagation | Mira spec amendment v1.2.0 §15.13.2 + parent spec §15.5 + §15.6 invariant |
| `pax_cosign.py` script behavior | `scripts/pax_cosign.py` L40-47 (canonical_hash function) + L28-37 (REQUIRED_REVISION_FIELDS tuple) |
| Patch-bump precedent (PRR registration without spec yaml field mutation) | parent spec yaml v0.2.3 PRR-20260427-1, PRR-20260428-1/2/3 (all `breaking_fields: []` or story-level path only; data_splits + thresholds UNCHANGED; spec yaml justification cite "patch bump reflects preregistration_revisions[] append only" pattern) |

**Anchor count:** 14 source anchors verified — every K1/K2/K3 numerical threshold, every branch action, every guard, every protocol claim traces to either ESC-012 council resolution / parent spec yaml v0.2.3 / Mira spec v1.2.0 §1 + §15 / `pax_cosign.py` script / parent spec PRR precedent. Zero invented features. Zero invented thresholds. Zero invented branches.

---

## §9 Pax cosign 2026-04-30 BRT

```
Cosign: Pax (@po)
Date (BRT): 2026-04-30
Authority: PO 10-point checklist guardian + ESC-012 5/6 SUPERMAJORITY APPROVE_PATH_B ratifying voter + R15.2 governance ledger keeper
Action: REGISTER PRR-20260430-1 hash-frozen pre-Phase-G-N8-run per ESC-012 R3 mandate

Cosign confirms:
  [x] 4 explicit branches (R15 PASS surprise + R16 FAIL K3 collapse + R17 FAIL K1+K3 sustains + INCONCLUSIVE)
  [x] All thresholds cited verbatim from parent spec yaml v0.2.3 (Anti-Article-IV Guard #4 UNMOVABLE)
  [x] Anti-Article-IV Guards 1-8 carry-forward audited (§5 table)
  [x] Article IV trace policy honored (§8 — 14 source anchors verified)
  [x] No threshold mutation; no spec yaml field mutation under this PRR (planned breaking_fields: [])
  [x] No hold-out lock policy mutation (Phase G unlock IS authorized §15.10 transition per ESC-012 R10)
  [x] Single-shot OOS discipline preserved (ESC-012 R9)
  [x] Pax cosign hash registration protocol §6 (interim file-content + canonical spec-yaml-entry deferred to T002.7 close)
  [x] Article II preserved (no push during PRR registration; Gage exclusive at story close)

Status: REGISTERED (pre-Phase-G-N8-run hash-frozen)
Next step: Phase G N8 run blocked until ESC-012 §7 closure conditions met (R1 F2-T5-OBS-1 fix + R2 Mira spec v1.2.0 §15.13 body + R4 Dara hold-out tape pre-req + R5 Sable Phase G coherence audit)
```

— Pax @po, 2026-04-30 BRT
