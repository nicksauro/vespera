# QA Gate — T002.1 Warm-up infrastructure (ATR_20d + Percentis 126d)

**Story:** `docs/stories/T002.1.story.md`
**Status (pre-gate):** Ready for Review
**Spec:** `docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` (mira_signature_sha256 `4b5624ad…dc3fc`)
**Design:** `docs/architecture/T002-end-of-day-inventory-unwind-design.md` §3.1 / §3.2 / §3.3 / §13 (ALN-20260425-1)
**Reviewer:** Quinn (@qa) — Test Architect
**Date:** 2026-04-25 BRT
**Authority:** Quality gate per `qa-gate.md` task; advisory verdict, may flag CONCERNS without blocking.

---

## Verdict

**PASS — with 1 LOW (informational) flag for downstream stories.**

All 7 mandatory QA checks passed; AC1–AC12 traceable to evidence; rename consistency 252→126 verified; anti-leakage strict in source code; edge cases covered. The 2 stale references Dex flagged (core/feature_computer.py docstring + T002.2/T002.3 dependency lines) are out-of-scope for T002.1 — flagged as LOW for SM/Pax cleanup before T002.2/T002.3 execute. They do **not** block T002.1 push.

---

## 7-point QA matrix

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | `pytest tests/t002_eod_unwind/warmup/ -v` | ✅ PASS | **31/31 passed** in 0.33s. Breakdown: atr=6, calendar=10, gate=7, percentiles_126d=8. |
| 2 | `pytest tests/ --ignore=tests/integration -q` (full regression) | ✅ PASS | **433 passed, 1 skipped** in 14.32s. Zero failures, zero collection errors. |
| 3 | `ruff check packages/t002_eod_unwind/warmup/ tests/t002_eod_unwind/warmup/` | ✅ PASS | "All checks passed!" — clean. |
| 4 | AC1–AC12 traceability | ✅ PASS | **12/12 ACs** mapped to specific tests + grep evidence. See § AC traceability matrix below. |
| 5 | Rename consistency 252d → 126d | ✅ PASS | Source files: zero `252` token in `*.py` under `packages/t002_eod_unwind/warmup/` and `tests/t002_eod_unwind/warmup/` except 2 docstring lines in `percentiles_126d_builder.py:13-14` referencing PRR-20260421-1 P252 infeasibility rationale (intentional historical context). State path `state/T002/percentiles_126d.json` matches AC4. Deleted: `percentiles_252d_builder.py` (production) + `test_percentiles_252d_builder.py` (only `.pyc` caches remain in `__pycache__/`, regenerated by next pytest run, harmless). `__init__.py` exports point to 126d module. |
| 6 | Anti-leakage strict | ✅ PASS | Both builders use strict inequality `d.day < as_of_date` in `_select_window` (atr_20d_builder.py:147, percentiles_126d_builder.py:134) — current day NEVER read. Test `test_atr_never_includes_current_day` + `test_percentiles_excludes_current_day` cover this explicitly. State files only persisted via builder output; gate.py reads `as_of_date` field and rejects mismatches via `_check_file` stale-date branch. Anti-UTC grep on `tz_convert\|utcnow\|pytz.UTC\|tzinfo=`: zero matches (AC8 honored). |
| 7 | Edge case coverage | ✅ PASS | 7 edge cases verified. See § Edge case matrix below. |

---

## AC traceability matrix

| AC | Statement | Evidence |
|----|-----------|----------|
| AC1 | `ATR20dBuilder.build()` returns float over 20 valid business days | `atr_20d_builder.py:101-119`; tests `test_atr_constant_range_equals_range`, `test_atr_uses_true_range_with_gap`. |
| AC2 | DLL fetched in worker thread, never callback (Nelo quirk) | Module docstring `atr_20d_builder.py:10-14` declares contract: builder is pure compute, caller (T002.4) handles thread-worker. Honored — no DLL calls in builder. |
| AC3 | `Percentiles126dBuilder.build()` computes P20/P60/P80 over 126 days | `percentiles_126d_builder.py:91-126`, `WINDOW=126` constant; test `test_p20_p60_p80_on_uniform_distribution` validates ground-truth values 0.198 / 0.595 / 0.794. |
| AC4 | State files at `state/T002/atr_20d.json` and `state/T002/percentiles_126d.json` with `as_of_date` + `computed_at_brt` | `atr_20d_builder.py:64-79` (to_json); `percentiles_126d_builder.py:71-88` (to_json); `state/T002/.gitkeep` exists; tests `test_persist_and_load_roundtrip` × 2 and `test_persist_writes_expected_schema`. |
| AC5 | `WarmUpGate.check()` returns ∈ {READY_TO_TRADE, WARM_UP_IN_PROGRESS, WARM_UP_FAILED} | `gate.py:24-27` enum; `gate.py:52-83` state machine; tests `test_both_files_valid_returns_ready`, `test_both_files_missing_returns_in_progress`, `test_only_atr_missing_returns_failed`. |
| AC6 | Corrupt or missing state ⇒ FAILED, never propagates exception | `gate.py:85-108` `_check_file` catches `JSONDecodeError`+`OSError`+`ValueError`; tests `test_corrupt_atr_returns_failed`, `test_missing_as_of_field_returns_failed`, `test_check_never_raises_on_bad_inputs`. |
| AC7 | Calendar `config/calendar/2024-2027.yaml` is single source for holidays / Copom / expirations | `calendar_loader.py` loads YAML → frozensets; `is_valid_sample_day()` combines all 4 filters; test `test_loads_real_project_calendar` reads actual project file. |
| AC8 | All BRT naive (R2) — no tz_convert, utcnow, pytz.UTC, tzinfo= | Grep over `packages/t002_eod_unwind/warmup/`: zero matches (re-verified during gate). |
| AC9 | Trades-only (R7) — no book/snapshot calls | Grep `book\|snapshot\|order_book\|GetBook\|MarketBook` (case-insensitive): only match is the explicit R7 negation comment `atr_20d_builder.py:15`. |
| AC10 | Coverage ≥ 85% on new modules | **95% measured** (atr=100, calendar=94, gate=90, percentiles=96, init=100). Exceeds threshold by 10 pp. |
| AC11 | Lint clean; typecheck (AST/ruff) clean | ruff: "All checks passed!"; mypy not in venv per Dex (acknowledged — ruff covers static + smoke). Acceptable for advisory level; would be CONCERNS-flag only if mypy were a constitutional requirement, which it is not (AC11 explicitly says "typecheck = AST parse OK"). |
| AC12 | Deterministic synthetic fixtures with known ground truth (±0.01 tolerance) | `test_atr_constant_range_equals_range` (constant range = expected ATR); `test_p20_p60_p80_on_uniform_distribution` (linspace 126 → ground-truth P20=0.198, P60=0.595, P80=0.794 within ±0.01). |

**Traceability score: 12/12 ACs satisfied with concrete evidence.**

---

## Edge case matrix

| Edge case | Coverage | Test |
|-----------|----------|------|
| Insufficient history (<20 ATR days, <126 percentile days) | ✅ | `test_atr_insufficient_history_raises`, `test_insufficient_history_raises` (percentiles) |
| Copom day excluded from sample | ✅ | `test_copom_days_excluded` (percentiles), `test_atr_excludes_copom_and_rollover` (atr) |
| Rollover window D-3..D-1 of WDO expiration excluded | ✅ | `test_rollover_window_excluded` (percentiles), `test_atr_excludes_copom_and_rollover` (atr) |
| Current day NEVER included (anti-leakage) | ✅ | `test_atr_never_includes_current_day`, `test_percentiles_excludes_current_day` |
| Persist/load roundtrip integrity | ✅ | `test_persist_and_load_roundtrip` × 2 + `test_persist_writes_expected_schema` |
| State file corrupt JSON ⇒ FAILED (no propagation) | ✅ | `test_corrupt_atr_returns_failed` + `test_check_never_raises_on_bad_inputs` |
| State file missing ⇒ IN_PROGRESS (not FAILED) | ✅ | `test_both_files_missing_returns_in_progress` |
| Stale `as_of_date` in state file ⇒ FAILED | ✅ | `test_stale_as_of_returns_failed` |
| Calendar YAML missing required key ⇒ raise | ✅ | `test_missing_required_key_raises` + `test_invalid_date_format_raises` + `test_missing_file_raises` |
| Calendar combines weekend + holiday + copom + rollover | ✅ | `test_is_valid_sample_day_combines_all_filters` + `test_is_business_day_excludes_weekend` |

**All 10 edge case classes covered. No coverage gap detected.**

---

## Rename consistency audit (252d → 126d)

| Layer | Result |
|-------|--------|
| `packages/t002_eod_unwind/warmup/*.py` source files | ✅ Only `percentiles_126d_builder.py` exists; `percentiles_252d_builder.py` removed. |
| `tests/t002_eod_unwind/warmup/*.py` source files | ✅ Only `test_percentiles_126d_builder.py` exists; `test_percentiles_252d_builder.py` removed. |
| `__init__.py` exports | ✅ `Percentiles126dBuilder`, `Percentiles126dState` exported; zero `Percentiles252d*` symbols. |
| State path AC4 | ✅ `state/T002/percentiles_126d.json` (matches schema test). |
| `WINDOW` constant | ✅ `Percentiles126dBuilder.WINDOW = 126` (line 94). |
| `__pycache__/` stale `.pyc` from old test file | ⚠️ LOW — `test_percentiles_252d_builder.cpython-*.pyc` files persist in `tests/t002_eod_unwind/warmup/__pycache__/`. Harmless (no source = won't be loaded), auto-cleaned on next pytest cache rebuild. Not a blocker. |
| Docstring P252 mentions in `percentiles_126d_builder.py:13-14` | ✅ Intentional — these explain PRR-20260421-1 rationale (why P252 was infeasible and P126 chosen). Historical context, not a stale ref. |

---

## Findings

### CRITICAL: 0
None.

### HIGH: 0
None.

### MEDIUM: 0
None.

### LOW: 3 (all informational, none block T002.1 push)

1. **Stale ref in `packages/t002_eod_unwind/core/feature_computer.py:29`** — docstring still says `"Rolling 252d bands injected by the caller (from Percentiles252dState)"`. **Out of T002.1 scope** (file belongs to T002.2 core layer). Flag for SM/Pax to update before T002.2 starts. Dex correctly did NOT modify this per authority boundary.
2. **Story T002.2 dependency line `docs/stories/T002.2.story.md:72`** references `Percentiles252dState`. **Out of T002.1 scope** — needs SM (@sm) reword pass on T002.2 before that story executes. Same semantic class as T002.1 reword event of 2026-04-25.
3. **Story T002.3 dependency line `docs/stories/T002.3.story.md:114`** references `Percentiles252dState`. Same as #2.

**Cumulative LOW disposition:** All 3 are mechanical text updates in downstream artifacts; none affect T002.1 deliverables. Recommend tracking as a single SM cleanup task before T002.2 starts. Quinn will not block T002.1 push on these per advisory authority.

### Informational

- T7 (integration test on 2025-Q1 real dataset) is **DEFERRED** by Dex per scope (requires materialized parquet + DLL adapter). Acceptable — T7 belongs to T002.4 integration phase, not T002.1 unit-level. No verdict impact.
- mypy not installed in venv — AC11 explicitly accepts "AST parse OK" as substitute, satisfied by ruff. No verdict impact.

---

## Spec / design alignment

- **Spec v0.2.0 binding (mira_signature `4b5624ad…dc3fc`)**: Story header pinned correctly; PRR-20260421-1 lookback rationale embedded in `percentiles_126d_builder.py` module docstring (lines 13-19). Pax cosign hash (`c34c201c…79e5a`) referenced in story L120. **Aligned.**
- **Design §3.2 (ALN-20260425-1)**: `state/T002/percentiles_126d.json` path matches design; `Percentiles rolling 126d` semantics match. **Aligned.**
- **MANIFEST invariants**: R2 (BRT naive) ✅, R5 (DLL via worker, doc-contract honored) ✅, R7 (trades-only) ✅, R11 (DOMAIN/FRAMEWORK separation: pure compute in warmup, no orchestration) ✅.

---

## Files written by this gate

- `docs/qa/gates/T002.1-qa-gate.md` (this file)

No story file modifications (Quinn authority limited to QA Results section; T002.1 story already has comprehensive Dev Agent Record + Validation Status — Pax owns final status promotion).

---

## Next handoff

1. **@devops (Gage)** — push T002.1 (warmup infrastructure 252→126 rename + Aria align). Branch + commit message per Dex File List. Constitutional gate clear; no CRITICAL/HIGH blockers.
2. **@sm (River) / @po (Pax)** — schedule SM cleanup pass on:
   - `packages/t002_eod_unwind/core/feature_computer.py:29` docstring
   - `docs/stories/T002.2.story.md:72` Dependencies line
   - `docs/stories/T002.3.story.md:114` Dependencies line

   All 3 are mechanical `Percentiles252dState` → `Percentiles126dState` rename, same semantic class as T002.1 reword event. Block T002.2/T002.3 development on this cleanup.
3. **Story T002.1 status**: recommend Pax promote `Ready for Review` → `Done` after Gage push completes. QA gate evidence: this file.

---

**Verdict signed by:** Quinn (@qa) — 2026-04-25 BRT
**Authority:** `qa-gate.md` task; advisory PASS/CONCERNS/FAIL decision per `core_principles[Gate Governance]`.

— Quinn, guardião da qualidade 🛡️
