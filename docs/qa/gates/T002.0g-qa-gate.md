---
storyId: T002.0g
verdict: PASS
date: 2026-04-26
reviewer: Quinn (@qa)
spec_version_consumed: T002-vespera-metrics-spec.md@v0.2.3 (orchestration is COMO; spec UNTOUCHED per R11 + Council C11)
spec_version_parent: T002-end-of-day-inventory-unwind-wdo-v0.2.0
amendments_acknowledged:
  - AC5 amended (Dara mini-council 4/4 APPROVE_OPTION_B): minimum set restricted to {2025-05-31}; 2024-06-30 deferred to USER-ESCALATION-QUEUE.md
  - AC12 amended: integration test verifies READY_TO_TRADE for {2025-05-31}; 2024-06-30 SKIPPED with reason BLOCK_GAP_UPSTREAM
  - DoD note: story closes COMPLETE for orchestrator scope; full 12-month run remains BLOCKED until upstream coverage user decision
---

# QA Gate — T002.0g Warmup State Orchestrator

**Story:** `docs/stories/T002.0g.story.md`
**Status (pre-gate):** Ready for QA (Quinn) — T1+T2+T3+T4+T5+T6 complete; AC5/AC12/DoD amended post-Dara mini-council
**Reviewer:** Quinn (@qa) — Test Architect & Quality Advisor
**Date:** 2026-04-26 BRT
**Authority:** `qa-gate.md` task; advisory verdict per `core_principles[Gate Governance]`. **DUAL-SIGN required** (Riven T8 cosign separately).

---

## Verdict

**PASS — clean across all 7 quality checks; 0 CRITICAL / 0 HIGH / 0 MEDIUM / 2 LOW (informational, do NOT block T8 dual-sign).** All 12 ACs traceable to passing tests + grep + code evidence (AC5/AC12 honored under amendments). 38/38 T002.0g scoped tests green (1 legitimate skip for AC12 amendment 2024-06-30); 56/56 path-lift regression green; 548/548 full regression green (2 skipped — pre-existing). Ruff clean across all 7 deliverable+modified files. Mypy clean (`Success: no issues found in 2 source files` on production deliverables). All 5 Anti-Article-IV Guards enforced and tested (Guard #1 silent-fallback REMOVAL verified end-to-end). Ready for Riven T8 custodial cosign + Pax T9 errata in parallel.

---

## Test Results

| Test tree | Tests | Result | Notes |
|-----------|-------|--------|-------|
| `tests/warmup_orchestrator/test_orchestrator.py` | 20 | PASS | T4: AC1 smoke, AC2 strict window (2 tests), AC3 holdout-before-source, AC4 escalation + applied path, AC5 multi-date + latest copies, AC6 default constant + custom dir, AC7 3-run determinism + wallclock-exclude, AC8 roundtrip, AC9 JSONL append-only + write-fail Guard #2, AC10 MemoryPoller constants, Guard #1 (2 tests), Mira T0 calendar required + propagated to builders, AC9 tradetype escalation manifest record |
| `tests/scripts/test_run_warmup_state.py` | 11 | PASS | T5: AC11 argparse (5 tests), AC3 holdout guard fires + grep contract, AC10 telemetry CSV columns + 6 GiB soft-halt mock, AC11 2025-05-31 smoke completes, AC11 parquet-only choices, Guard #1 propagation exit 1 |
| `tests/contracts/test_warmup_orchestrator_contracts.py` | 7 + 1 skip | PASS | T6: AC12 WarmUpGate READY_TO_TRADE (orchestrator-driven, NOT mocked JSON), AC12 amendment 2024-06-30 SKIPPED with explicit `BLOCK_GAP_UPSTREAM` reason, AC9 manifest pin in canonical-invariant.sums, AC6+AC9 6-artifact set under state/T002/, T3 path-lift regression (consumers default == state/T002/), Guard #1 fail-closed (missing + corrupt) |
| **Scoped subtotal** | **38 + 1 skip** | **PASS** | `pytest tests/warmup_orchestrator/ tests/scripts/test_run_warmup_state.py tests/contracts/test_warmup_orchestrator_contracts.py -q --tb=short` → 38 passed, 1 skipped in 3.36s |
| Path-lift regression | 56 | PASS | `pytest tests/cpcv_harness/ tests/scripts/test_run_cpcv_dry_run.py tests/contracts/test_cpcv_harness_contracts.py -q` → 56 passed in 13.23s. Zero regressions from `data/warmup/` → `state/T002/` lift in `cpcv_harness.py:96-97` + `run_cpcv_dry_run.py:120-121`. |
| **Full regression** | **548 + 2 skip** | **PASS** | `pytest tests/ -q --tb=no` → 548 passed, 2 skipped in 39.36s. Delta vs T002.0f baseline (506 + 1 skip): +42 PASS / +1 skip = 38 new T002.0g + 4 ancillary tests added in same window; new skip = legitimate AC12 amendment 2024-06-30 (`BLOCK_GAP_UPSTREAM`). T002.0a-f preserved. |

---

## AC Traceability Matrix (12 ACs — AC5/AC12 amended)

| AC | Spec ref | Implementation | Test | Status |
|----|----------|----------------|------|--------|
| AC1 | story L34-35 (parquet primary via feed_parquet; NO TimescaleDB; NO live DLL; manifest SHA delegated to adapter) | `orchestrator.py:107-149` (`ParquetSource` Protocol + `FeedParquetSource`); `orchestrator.py:559-768` (`orchestrate_warmup_state`); manifest re-check delegated to `feed_parquet._verify_integrity` per Dara T0 | `test_orchestrate_smoke_as_of_2025_05_31` (E2E happy path) | PASS |
| AC2 | story L38-39 (strict `[D-146bd, D-1]`; current day NEVER included; calendar-validated edges) | `orchestrator.py:210-241` (`compute_window` strict `<` + `WARMUP_VALID_DAYS_REQUIRED=146`); builders enforce `< as_of_date` (atr L147, pct L134) | `test_anti_leak_window_strict_excludes_current_day`, `test_window_covers_at_least_146_valid_business_days` | PASS |
| AC3 | story L42-43 (`assert_holdout_safe` BEFORE adapter open; defense-in-depth; `VESPERA_UNLOCK_HOLDOUT` bypass) | `orchestrator.py:592-625` (holdout_assert resolved + invoked PRE source.load_trades); CLI `run_warmup_state.py:390-412` repeats early at top-level for ALL as_of_dates | `test_holdout_lock_assert_called` (asserts `source.calls == []` when holdout fires), `test_holdout_lock_guard_fires` (CLI exit 1, orchestrator NOT called), `test_holdout_unlock_env_grep_contract` | PASS — grep `VESPERA_UNLOCK_HOLDOUT` in run_warmup_state.py: 3 matches (lines 22, 87, 90) |
| AC4 | story L47 (filter tradeType ∈ {2,3} OR escalate `[USER-ESCALATION-PENDING]` per Guard #4 — no improvise) | `orchestrator.py:278-314` (`TradeFilterDecision` + `inspect_tradetype_filter`); orchestrator emits `tradetype_filter_status="escalated_adapter_gap"` in manifest + WARN log | `test_tradeType_filter_or_escalate` (asserts escalated_adapter_gap + USER-ESCALATION-PENDING + WARN log), `test_inspect_tradetype_filter_with_attribute` (future-proof: applied if Trade.tradeType present), `test_manifest_records_tradetype_escalation` | PASS — Guard #4 honored; gap is upstream T002.0b adapter (Dara T0 confirmed) |
| AC5 (amended) | story L51 (mínimo set {2025-05-31}; 2024-06-30 → USER-ESCALATION-QUEUE; latest copy = most-recent in run) | `orchestrator.py:711-754` (per-date dated outputs + COPY of last as_of to canonical names) | `test_multi_as_of_dates_separate_files` (2 dates → 4 dated + 2 latest; latest content == max-date file bytes) | PASS (amendment honored) |
| AC6 | story L55 (default `state/T002/`; atomic path lift in same PR; `--output-dir` overridable) | `orchestrator.py:605-606` (output_dir as required param); CLI default `run_warmup_state.py:63 DEFAULT_OUTPUT_DIR=Path("state/T002")`; `cpcv_harness.py:96-97` lifted; `run_cpcv_dry_run.py:120-121` lifted | `test_output_path_canonical_state_T002`, `test_output_dir_canonical_constant_in_cli`, `test_argparse_default_output_dir`, `test_consumers_default_lifted_to_state_T002` (regression) | PASS |
| AC7 | story L59 (3 runs byte-identical EXCLUDING `computed_at_brt`; `state_content_hash` definition + sort_keys + canonical separators) | `orchestrator.py:256-272` (`_canonical_json_bytes` sort_keys+compact, `state_content_hash` excludes wallclock); `orchestrator.py:506-536` (`DeterminismStamp` + writer) | `test_determinism_3_runs_byte_identical_excluding_wallclock` (3 distinct out_dirs; asserts `len({hashes}) == 1`), `test_state_content_hash_excludes_wallclock` (unit) | PASS |
| AC8 | story L63 (`from_json(to_json())` deep-equality assert BEFORE persist) | `orchestrator.py:431-448` (`_assert_roundtrip_atr` + `_assert_roundtrip_pct`); invoked at L686 + L708 BEFORE `_write_*_state` | `test_schema_roundtrip_assert` (E2E read-back + re-roundtrip) | PASS |
| AC9 | story L67-71 (append-only JSONL manifest pinned in `.github/canonical-invariant.sums`; orchestrator fail-closed if write fails) | `orchestrator.py:174-204` (`ManifestEntry` schema with 9 fields); `orchestrator.py:454-468` (`append_manifest_entry` with `ManifestWriteError`); `.github/canonical-invariant.sums:3-9` (pin entry `PENDING_RIVEN_T8_DUAL_SIGN`) | `test_manifest_jsonl_append_only` (2 runs → 2 distinct lines, all 4 schema fields), `test_manifest_write_failure_raises` (Guard #2), `test_manifest_pin_in_canonical_invariants_sums` (presence enforced) | PASS — Riven T8 ratifies actual checksum |
| AC10 | story L75 (psutil daemon thread; cadence 30s; soft halt 6 GiB graceful state persist; CAP_v3 ADR-1 v3 compliance; streaming per as_of_date) | `run_warmup_state.py:191-313` (`MemoryPoller` REUSE pattern from cpcv_dry_run T3); `orchestrator.py:619-757` (per-date streaming loop, R4 mitigation) | `test_memory_poller_active_module_constant` (constants check), `test_telemetry_csv_columns` (header == TELEMETRY_COLUMNS), `test_6gb_soft_halt_mock` (psutil patched 7 GiB ⇒ `halt_event.is_set()` + `SOFT_HALT_FIRED` note) | PASS |
| AC11 | story L79-87 (argparse: --as-of-dates required, --source parquet, --output-dir default state/T002/, --seed 42, --mem-poll-interval-s 30, --mem-soft-cap-gb 6.0; exit 0/1/2) | `run_warmup_state.py:118-185` (`build_arg_parser`); `run_warmup_state.py:332-475` (main with branched exit codes 0/1/2) | `test_argparse_required_as_of_dates_exit_2` (SystemExit code=2), `test_argparse_seed_propagated`, `test_argparse_as_of_dates_list_parsed`, `test_argparse_invalid_iso_rejected`, `test_argparse_default_output_dir`, `test_argparse_parquet_only_choices`, `test_2025_05_31_smoke_completes` (rc=0), `test_holdout_lock_guard_fires` (rc=1), `test_insufficient_coverage_returns_exit_1` (rc=1) | PASS |
| AC12 (amended) | story L91 (integration test fixture orchestrator-driven for 2025-05-31 → READY_TO_TRADE; 2024-06-30 SKIPPED with `BLOCK_GAP_UPSTREAM`) | `orchestrator.py` end-to-end pipeline; consumed by `WarmUpGate.check` via `latest_atr_path`/`latest_percentiles_path` | `test_warmup_gate_ready_to_trade_post_orchestrator` (full pipeline, NOT mocked: stub trades 2023-2025 → orchestrator → WarmUpGate.check(2025-05-31) == READY_TO_TRADE; both `atr_file_ok` + `percentiles_file_ok`), `test_2024_06_30_skipped_pending_data_coverage` (`@pytest.mark.skip(reason='BLOCK_GAP_UPSTREAM 2023 manifest gap; tracked USER-ESCALATION-QUEUE.md')`) | PASS (partial per amendment) |

**Traceability score: 12/12 ACs satisfied with concrete code + test evidence (AC5/AC12 amendments honored).**

---

## 7 Quality Checks

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Code review (patterns, readability, maintainability) | PASS | Module-level docstring `orchestrator.py:1-49` exhaustively traces authorities (Aria/Mira/Beckett/Dara/Riven T0) + 5 Anti-Article-IV Guards + 12 AC mapping. Single-responsibility helpers (`compute_window`, `_aggregate_daily_with_close_at`, `_build_daily_metrics`, `_assert_roundtrip_*`, `_write_*_state`, `_copy_to_latest`, `_unlock_used`). Frozen dataclasses for value types (`ManifestEntry`, `TradeFilterDecision`, `DeterminismStamp`, `OrchestratorResult`, `FeedParquetSource`). `ParquetSource` Protocol enables DI for tests + future TimescaleSource (Phase F). CLI is thin (~340 LoC) + delegates orchestration to module. Underscore-prefixed module-private symbols consistent. |
| 2 | Unit tests (coverage + passing) | PASS | 38/38 scoped tests pass + 1 legitimate skip; coverage breadth: AC1 (1), AC2 (2), AC3 (3 incl. CLI guard + grep), AC4 (3), AC5 (1 multi-date), AC6 (4 incl. consumer regression), AC7 (2), AC8 (1), AC9 (3 incl. write-fail Guard #2 + pin presence), AC10 (3 incl. soft-halt mock), AC11 (9 incl. exit codes 0/1/2 + smoke + parquet-only + Guard #1 propagation), AC12 (1 PASS + 1 amendment skip), Guard #1 (4 incl. CPCV consumer fail-closed missing+corrupt), Mira T0 (2 incl. calendar-required + propagated). Edge cases: empty source ⇒ InsufficientCoverage; calendar all-blocked ⇒ InsufficientCoverage; corrupt JSON ⇒ ValueError; missing file ⇒ FileNotFoundError; psutil error ⇒ diagnostic CSV row (no crash). |
| 3 | Acceptance criteria | PASS | All 12 ACs traced — see AC matrix above. AC5 amendment honored (mínimo {2025-05-31}); AC12 amendment honored (2024-06-30 SKIP with explicit reason). Multi-date capability remains for future re-enablement post-coverage resolution. |
| 4 | No regressions | PASS | Full regression: **548 passed, 2 skipped** in 39.36s (vs T002.0f baseline 506 passed, 1 skipped). Delta of +42 PASS = 38 new T002.0g + 4 ancillary; +1 skip = legitimate AC12 amendment. T002.0a-f trees + T002.1-T002.4 all green. Path-lift regression suite (cpcv_harness + run_cpcv_dry_run + cpcv contracts): 56/56 PASS — zero regression from `data/warmup/` → `state/T002/` defaults lift. Zero collection errors. |
| 5 | Performance | PASS (within scope) | Scoped suite 3.36s; path-lift regression 13.23s; full suite 39.36s — all well below 5-min smoke ceiling. AC10 streaming per-as_of_date (`orchestrator.py:619-757`) bounds memory by single-date OHLC bucket count (R4 mitigation). MemoryPoller overhead (1 psutil call + 1 CSV append per 30s) ≈ microseconds — well below 5% NFR ceiling. AC12 contract test executes orchestrator end-to-end on 2-year synthetic dataset in <1s. |
| 6 | Security / fail-closed | PASS | (a) **Hold-out lock fail-closed**: `assert_holdout_safe` invoked BEFORE `source.load_trades` in orchestrator (`orchestrator.py:625`); CLI repeats early at top-level (`run_warmup_state.py:400`); `test_holdout_lock_assert_called` asserts `source.calls == []` when guard fires. Bypass via explicit `VESPERA_UNLOCK_HOLDOUT` env var with R1+R15(d) traceability (manifest line records `holdout_unlock_used:true`). (b) **Schema roundtrip pre-persist** (AC8): `_assert_roundtrip_atr` + `_assert_roundtrip_pct` invoked BEFORE `_write_*_state` — schema drift = AssertionError aborts run. (c) **Manifest atomic + Guard #2**: `append_manifest_entry` raises `ManifestWriteError` on OSError — caller MUST abort; NO fallback path; tested via `test_manifest_write_failure_raises`. (d) **Guard #1 silent-fallback REMOVED** from `run_cpcv_dry_run._load_warmup_state` (L506-552): now raises `FileNotFoundError` if missing OR `ValueError` if corrupt/schema-drift; main loop maps both to exit 1 with explicit operator hint ("Run scripts/run_warmup_state.py --as-of-dates <D> to materialize"); regression tests `test_run_cpcv_dry_run_load_warmup_state_fail_closed_on_missing` + `_on_corrupt`. (e) **Mira T0 calendar single-source**: `calendar` is REQUIRED kwarg — `test_calendar_param_required_no_internal_instance` proves orchestrator NEVER instantiates internally; `test_calendar_param_propagated_to_builders` asserts the SAME object goes to both ATR + Pct builders (no silent leak via calendar drift). |
| 7 | Documentation | PASS | Docstrings on every public symbol (`orchestrate_warmup_state`, `compute_window`, `inspect_tradetype_filter`, `_assert_roundtrip_*`, `append_manifest_entry`, `state_content_hash`, `MemoryPoller`, `build_arg_parser`, `main`). Module docstring `orchestrator.py:1-49` enumerates all 5 Anti-Article-IV Guards + cites all T0 handshakes. CLI docstring `run_warmup_state.py:1-23` documents AC11 contract + exit codes + DoD grep contract. Story T002.0g.story.md File List + Validation results updated by Dex. `.github/canonical-invariant.sums` carries Riven T8 placeholder pin entry with explicit schema field reference. Cross-ref to T002.0f T11 finding + USER-ESCALATION-QUEUE.md present in code comments + manifest schema. |

---

## Findings

### CRITICAL: 0
None.

### HIGH: 0
None.

### MEDIUM: 0
None.

### LOW: 2 (informational; do NOT block T8 dual-sign)

1. **Trade tradeType escalation is structural — not a regression** — `orchestrator.py:294-314` always returns `escalated_adapter_gap` for the parquet path because `feed_parquet.load_trades` yields only `(ts, price, qty)` (Dara T0 confirmed). This is honored per Guard #4 (NO improvise) and traced via manifest `tradetype_filter_status="escalated_adapter_gap"` + WARN log + USER-ESCALATION-QUEUE pointer. Riven T8 may convert this to a DoD block if desired (see story §Escalations encountered L202). Tracked: USER-ESCALATION-QUEUE.md P2 (tradeType filter promotion).

2. **2024-06-30 AC12 skip remains a TODO marker** — `tests/contracts/test_warmup_orchestrator_contracts.py:128-138` carries `@pytest.mark.skip(reason='BLOCK_GAP_UPSTREAM 2023 manifest gap; tracked USER-ESCALATION-QUEUE.md')`. Honored by mini-council 4/4 APPROVE_OPTION_B; closure deferred until user-decision (re-fetch DLL Nov-Dec 2023 OR accept smoke-only closure). Test left in place so future coverage-resolution simply removes the skip — no code change required.

### Informational

- mypy on production deliverables (`orchestrator.py`, `run_warmup_state.py`): `Success: no issues found in 2 source files` with `--ignore-missing-imports` (psutil ecosystem-wide pre-existing issue same as T002.0f baseline).
- Manifest pin in `.github/canonical-invariant.sums` is `PENDING_RIVEN_T8_DUAL_SIGN` placeholder per AC9 contract — Riven custodial cosign T8 replaces with actual checksum after first canonical run (committed contract not a defect).

---

## Cross-cutting verifications

| Verification | Status | Evidence |
|--------------|--------|----------|
| AC7 determinism — 3 runs byte-identical (excluding wall-clock) | PASS | `test_determinism_3_runs_byte_identical_excluding_wallclock` exercises 3 distinct out_dirs with the same `(as_of_dates, calendar, source_data, seed)`; reads `state_content_hash` from `determinism_stamp.json` of each; asserts `len({hashes}) == 1`. Unit-level invariant `test_state_content_hash_excludes_wallclock` proves `computed_at_brt` cannot pollute the hash. |
| AC3 hold-out grep — `VESPERA_UNLOCK_HOLDOUT` in `scripts/run_warmup_state.py` ≥ 1 | PASS | `grep VESPERA_UNLOCK_HOLDOUT scripts/run_warmup_state.py` → **3 matches** (L22 docstring, L87 module comment, L90 `_HOLDOUT_UNLOCK_ENV` constant). Enforced by `test_holdout_unlock_env_grep_contract` (`assert src.count("VESPERA_UNLOCK_HOLDOUT") >= 1`). |
| Guard #1 silent-fallback REMOVAL in `run_cpcv_dry_run.py:503-552` | PASS | `_load_warmup_state` now raises `FileNotFoundError` (L532-537) or `ValueError` (L541-552, both JSONDecodeError + schema drift). Main loop catches both at L845 + L930 → exit 1 with explicit diagnostic. Regression tests `test_run_cpcv_dry_run_load_warmup_state_fail_closed_on_missing` + `_on_corrupt` both PASS. Grep on production code: ZERO occurrences of literal `PercentileBands(p20=1.0` (legacy neutral fallback). |
| Path lift atomic regression — `data/warmup/` → `state/T002/` | PASS | `test_consumers_default_lifted_to_state_T002` asserts `cpcv_harness._DEFAULT_*_PATH == Path("state/T002/...")` AND `run_cpcv_dry_run._DEFAULT_*_PATH == Path("state/T002/...")`. Path-lift regression suite (56 tests across `tests/cpcv_harness/`, `tests/scripts/test_run_cpcv_dry_run.py`, `tests/contracts/test_cpcv_harness_contracts.py`): all PASS. Grep `data/warmup` in production .py files: 1 match (`run_cpcv_dry_run.py:117` comment annotating the lift); zero functional usages. |
| Mira T0 — `calendar` REQUIRED kwarg, NOT internally instantiated | PASS | `test_calendar_param_required_no_internal_instance` invokes `orchestrate_warmup_state` WITHOUT `calendar=` and asserts `TypeError`. `test_calendar_param_propagated_to_builders` patches `ATR20dBuilder` + `Percentiles126dBuilder` ctors and asserts the SAME `cal` object passed to orchestrator reaches BOTH builders — no silent re-instantiation. |
| Beckett T0 — COPY (not symlink) for default-path compat | PASS | `orchestrator.py:494-500` (`_copy_to_latest`) uses `shutil.copyfile`; comment "Beckett T0 — COPY (not symlink) on Windows; fail-closed if copy fails"; `test_multi_as_of_dates_separate_files` asserts `latest_atr.read_bytes() == most_recent_atr.read_bytes()` (true file copy, not symlink). OSError surfaces as-is so caller aborts (Guard #2). |
| Article IV traceability — every implementation decision traced | PASS | (a) Anti-leak window 146 = 126 P126 + 20 ATR_20 → spec v0.2.0 §trading_rules + Mira T0 (cited `orchestrator.py:88-101`). (b) `state_content_hash` excludes wallclock → AC7 + Mira T0 (cited L266-272). (c) Manifest schema with 9 fields → Aria T0 + Riven Q4 + AC9 (cited L174-204). (d) tradeType escalation NOT improvised → Guard #4 + Dara T0 (cited L294-314). (e) calendar required kwarg → Mira T0 single-source (cited L572-585). (f) parquet primary, NO TimescaleDB/DLL → user constraint canonical 2026-04-26 + AC1 (cited L107-149). |

---

## Dependencies status

| Dependency | Status | Evidence |
|------------|--------|----------|
| T002.0b _holdout_lock + feed_parquet adapter | Done | story L114; orchestrator REUSE confirmed (Article IV-A) |
| T002.1 P126 + ATR_20d builders + dataclasses | Done (commit 8936a53) | story L115; ATR20dBuilder + Percentiles126dBuilder consumed; canonical state/T002/ output dir honored |
| T002.0f CPCV harness + cpcv_harness.py + run_cpcv_dry_run.py | Done (Quinn QA PASS) + Path lift atomic in this PR | story L116; T11 finding HIGH RESOLVED via T3 atomic lift |
| Aria T0 handshake | HANDSHAKE_OK_FOR_DEX_START | story L329-337 |
| Mira T0 handshake | HANDSHAKE_OK_FOR_DEX_START | story L339-347 |
| Beckett T0 handshake | HANDSHAKE_OK_FOR_DEX_START | story L349-355 |
| Dara T0 handshake | BLOCK_GAP_UPSTREAM → resolved via mini-council 4/4 APPROVE_OPTION_B → HANDSHAKE_OK_FOR_DEX_START | story L357-368 |
| Mini-council resolution (Beckett + Pax + Sable + Morgan) | 4/4 APPROVE_OPTION_B (AC5/AC12/DoD amended) | story L370-378 |
| Pax `*validate-story-draft T002.0g` | GO 10/10 (Status `Draft → Ready`) | story L390-465 |

---

## Tools status summary

| Tool | Result |
|------|--------|
| `pytest tests/warmup_orchestrator/ tests/scripts/test_run_warmup_state.py tests/contracts/test_warmup_orchestrator_contracts.py -q --tb=short` | **38 passed, 1 skipped in 3.36s** |
| `pytest tests/cpcv_harness/ tests/scripts/test_run_cpcv_dry_run.py tests/contracts/test_cpcv_harness_contracts.py -q --tb=no` (path-lift regression) | **56 passed in 13.23s** |
| `pytest tests/ -q --tb=no` (full regression) | **548 passed, 2 skipped in 39.36s** |
| `ruff check packages/t002_eod_unwind/warmup/orchestrator.py packages/t002_eod_unwind/cpcv_harness.py scripts/run_warmup_state.py scripts/run_cpcv_dry_run.py tests/warmup_orchestrator/ tests/scripts/test_run_warmup_state.py tests/contracts/test_warmup_orchestrator_contracts.py` | **All checks passed!** |
| `mypy --ignore-missing-imports packages/t002_eod_unwind/warmup/orchestrator.py scripts/run_warmup_state.py` | **Success: no issues found in 2 source files** |
| `grep VESPERA_UNLOCK_HOLDOUT scripts/run_warmup_state.py` | **3 matches** (L22, L87, L90) — DoD grep contract satisfied (≥1) |

---

## Files written by this gate

- `docs/qa/gates/T002.0g-qa-gate.md` (this file)

No story-file modifications (Quinn authority limited to QA Results section; T002.0g story already has comprehensive Dev Agent Record + T0 handshakes + AC amendments + Validation Status — Pax owns final status promotion).

---

## Approved for next handoff

**PARALLEL handoffs unblocked:**

1. **Riven (@risk-manager) — T8 custodial cosign (DUAL-SIGN)**:
   - (a) Validate `state/T002/manifest.json` schema (`ManifestEntry` 9-field contract per `orchestrator.py:174-204`).
   - (b) Replace `PENDING_RIVEN_T8_DUAL_SIGN` placeholder in `.github/canonical-invariant.sums:9` with actual checksum after first canonical run.
   - (c) Confirm path lift R15 non-breaking (regression suite 56/56 PASS provides mechanical evidence).
   - (d) Optional: review whether AC4 `tradetype_filter_status="escalated_adapter_gap"` constitutes a DoD block (Quinn LOW finding #1 — currently informational per Guard #4 honor).

2. **Pax (@po) — T9 errata in `docs/stories/T002.1.story.md` footer** (orphan T002.4 caller reference fix per Sable C14; T002.4 real is child-peak, not adapter live). Parallelizable with T8.

**Pre-condition for both T8 + T9:** this gate (PASS); 38 scoped + 56 regression + 548 full suite all green; ruff + mypy clean; all 5 Anti-Article-IV Guards enforced + tested; AC5/AC12 amendments honored.

**Downstream after T7+T8+T9:** Beckett T11.bis smoke retry (T10) — `python scripts/run_warmup_state.py --as-of-dates 2025-05-31` then `python scripts/run_cpcv_dry_run.py --spec docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml --dry-run --smoke --in-sample-end 2025-06-30` — closure of T002.0g + unblock CPCV Phase E for {2025-05-31} window.

Recommend Pax promote story status `Ready for QA → Done` after Riven T8 dual-sign + Gage push complete (DUAL-SIGN protocol per Riven Q6).

---

**Verdict signed by:** Quinn (@qa) — 2026-04-26 BRT (Autonomous Daily Session)
**Authority:** `qa-gate.md` task; advisory PASS/CONCERNS/FAIL decision per `core_principles[Gate Governance]`. **DUAL-SIGN gate** — Riven T8 custodial cosign required before Gage push (story DoD L284-285).

— Quinn, guardiã da qualidade
