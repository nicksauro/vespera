# T002.0a Sable Gates — Canonical Checklist

**Owner:** Sable (The Seal ♉)
**Date:** 2026-04-23 BRT
**Scope:** T002.0a story — custodial + parquet materialization (warm-up 2024-01..06 + in-sample 2024-07..2025-06)
**Supersedes:** Dex's ad-hoc `G1..G6` vocabulary (2026-04-22)
**Related constitutional articles:** Art. I (CLI First), Art. IV (No Invention), Art. V (Quality First)
**Story:** `docs/stories/T002.0a.story.md`

---

## Reconciliation decision

**ADDITIVE.** Dex's `G1/G3/G6` (parity sample + post-relaunch rehash) and `G4` (memory telemetry) are **complementary** to the original 7-gate — they exercise a different failure mode (refactor drift + memory ceiling) than the original checklist (schema/continuity/hold-out grep). Absorbed as new canonical gates rather than rejected, because Dex's refactor of `materialize_parquet.py` introduced real parity risk that the pre-existing 7 gates did not cover, and the May+Jun 2025 relaunch is inherently new territory. Dex's naming (`G1..G6`) is **formally retired** — all gates below use canonical `G##-NOUN-NOUN` form. Dex's in-situ "passes" (Mar + Sep 2024 parity) are accepted as pre-evidence but must be **re-run formally by Sable** after any further edits to `materialize_parquet.py` on branch `t002-refactor-next-run`; pass claims predating this checklist are re-classified as `pending (pre-evidence)`. The result is a **10-gate checklist**.

---

## Canonical gate list

### G01 — MANIFEST-HEADER-SCHEMA
- **Objective:** Manifest row-0 (header) matches AC5 column order exactly.
- **Input required:** `data/manifest.csv` exists, non-empty.
- **Procedure:** `head -n 1 data/manifest.csv` — compare against canonical header `path,rows,sha256,start_ts_brt,end_ts_brt,ticker,phase,generated_at_brt`.
- **Pass criterion:** Byte-exact header match.
- **Fail action:** BLOCK. No downstream gate runs. Escalate to Sable for header rewrite via R10-signed MC entry.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g01-manifest-header.txt`.
- **Status:** [ ] pending
- **Owner:** Sable

### G02 — MANIFEST-ROW-COUNT-PHASE
- **Objective:** Manifest row count and `phase` distribution match spec.
- **Input required:** G01 passed.
- **Procedure:** `wc -l data/manifest.csv` + `awk -F',' 'NR>1 {print $7}' data/manifest.csv | sort | uniq -c`.
- **Pass criterion (16 rows):** `warmup=6, in_sample=10, hold_out=0`.
- **Pass criterion (18 rows post-G09):** `warmup=6, in_sample=12, hold_out=0`.
- **Fail action:** BLOCK. Dispatch to Riven.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g02-rowcount-phase.txt`.
- **Status:** [x] passed (16-row state, evidence: `MANIFEST_CHANGES.md` MC-20260423-1)
- **Owner:** Sable

### G03 — MANIFEST-HOLDOUT-LEAK-GREP
- **Objective:** Zero hold-out timestamps referenced in manifest.
- **Input required:** G01 passed.
- **Procedure:** `awk -F',' 'NR>1 && $4 ~ /^2025-0[789]|^2025-1[012]|^2026-/ { print }' data/manifest.csv | wc -l`.
- **Pass criterion:** Count = `0`.
- **Fail action:** BLOCK + immediate Riven+Beckett escalation (custodial breach).
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g03-holdout-grep.txt`.
- **Status:** [x] passed (evidence: `MANIFEST_CHANGES.md` `holdout_leak_scan: PASS`)
- **Owner:** Sable

### G04 — PARQUET-SHA256-REVERIFY
- **Objective:** Each manifest row's sha256 matches live bytes on disk.
- **Input required:** G01, G02 passed.
- **Procedure:** One-shot Python iterating `csv.DictReader` + `hashlib.sha256` against file bytes.
- **Pass criterion:** All rows match.
- **Fail action:** BLOCK. Quarantine drifted file; Riven records drift in new MC entry.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g04-sha256-reverify.txt`.
- **Status:** [x] passed (pre-evidence: MC-20260423-1 `sha256_drifts: 0`) — formal re-run required post-G09.
- **Owner:** Sable

### G05 — PARQUET-SCHEMA-STRICT
- **Objective:** Strict schema `(timestamp, ticker, price, qty, aggressor, buy_agent, sell_agent)` in exact order across all parquets.
- **Input required:** G01 passed.
- **Procedure:** Iterate `data/in_sample/**/*.parquet`, `pyarrow.parquet.read_schema`, assert `schema.names == EXPECTED_SCHEMA`.
- **Pass criterion:** Zero anomalies.
- **Fail action:** BLOCK. Non-conforming parquet re-materialize.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g05-schema-strict.txt`.
- **Status:** [x] passed (pre-evidence: `backfill_manifest.py` 2026-04-22 run logged zero schema anomalies)
- **Owner:** Sable

### G06 — PARQUET-ORPHAN-SCAN
- **Objective:** No `.tmp`/`.part`/`.bak` artifacts under `data/in_sample/`.
- **Input required:** None.
- **Procedure:** `find data/in_sample -type f ! -name '*.parquet' -print`.
- **Pass criterion:** Empty stdout.
- **Fail action:** BLOCK. Gage investigates; Riven co-signs removal.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g06-orphan-scan.txt`.
- **Status:** [ ] pending
- **Owner:** Sable

### G07 — PARITY-SAMPLE-WARMUP (absorbs Dex's "G1")
- **Objective:** Re-materialize warm-up month (`2024-03`) into scratch; prove sha256 == canonical. Guards against refactor drift on `materialize_parquet.py`.
- **Input required:** `scripts/verify_parity.py` present + Quinn concerns resolved; canonical parquet present.
- **Procedure:** `python scripts/verify_parity.py --month 2024-03 --ticker WDO 2>&1 | tee docs/qa/gates/evidence/T002.0a/g07-parity-mar2024.txt`.
- **Pass criterion:** Exit `0` + `[parity] PASS` in stdout.
- **Fail action:** BLOCK edits to `materialize_parquet.py` on `t002-refactor-next-run`; revert last patch; re-run.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g07-parity-mar2024.txt`.
- **Status:** [ ] pending (pre-evidence: Dex reported PASS 2026-04-22; NOT ACCEPTED as official — Quinn issued CONCERNS on `verify_parity.py`)
- **Owner:** Sable

### G08 — PARITY-SAMPLE-INSAMPLE (absorbs Dex's "G3")
- **Objective:** Re-materialize in-sample month (`2024-09`); prove sha256 == canonical.
- **Input required:** Same as G07 but for `2024-09`.
- **Procedure:** `python scripts/verify_parity.py --month 2024-09 --ticker WDO 2>&1 | tee docs/qa/gates/evidence/T002.0a/g08-parity-sep2024.txt`.
- **Pass criterion:** Exit `0` + `[parity] PASS`.
- **Fail action:** Same as G07.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g08-parity-sep2024.txt`.
- **Status:** [ ] pending (pre-evidence Dex; not accepted)
- **Owner:** Sable

### G09 — MAY-JUN-2025-RELAUNCH-TELEMETRY (supersedes Dex's "G4")
- **Objective:** Execute remaining in-sample materialization (`2025-05-01 → 2025-06-30`, 2 parquets) under memory-ceiling telemetry; produce rows 17-18 of manifest.
- **Input required:**
  - G07 + G08 passed (refactor non-drifting).
  - Quinn FAIL-BLOCK on `_telemetry_wrapper.py` **resolved** (wrapper fixed or replaced).
  - Riven R10 pre-sign-off as `MC-20260424-1` (per MC-20260423-1 `next_action` step 2).
- **Procedure:** Gage executes wrapper (or Quinn-approved replacement); Sable observes; telemetry CSV persisted.
- **Pass criterion (all must hold):**
  1. Exit code `0` (ceiling not breached).
  2. Peak commit memory ≤ ROM budget Aria/Riven defined.
  3. Two new parquets exist for months 2025-05 and 2025-06.
  4. Manifest grew 16 → 18 rows; both new rows `phase=in_sample`.
- **Fail action:** Exit code `3` (ceiling tripped) → Mira decides (accept chunked run / raise ceiling / streaming rewrite). Any other non-zero → Dex per Quinn's remediation plan.
- **Evidence artifact:** `data/materialize-may-jun-2025.log` + `data/materialize-may-jun-2025-telemetry.csv` + `docs/qa/gates/evidence/T002.0a/g09-may-jun-relaunch-summary.md`.
- **Status:** [ ] pending — **BLOCKED** on Quinn FAIL-BLOCK resolution.
- **Owner:** Gage (executor); Sable (verifier); Riven (pre-sign-off).

### G10 — POST-RELAUNCH-REHASH-18 (absorbs Dex's "G6")
- **Objective:** After G09, re-hash all 18 parquets; verify 16 pre-existing hashes match MC-20260423-1 `files_verified_sha256` (frozen 2026-04-23) + 2 new rows match manifest entries.
- **Input required:** G09 passed; frozen hash list in MC-20260423-1.
- **Procedure:** Extended rehash script compares rows 1-16 vs frozen list + rows 17-18 vs manifest.
- **Pass criterion:** 16 frozen matches + 2 self-matches = 18/18.
- **Fail action:** BLOCK T002.0a Done. Any drift in rows 1-16 = custodial incident → Riven + new MC drift entry.
- **Evidence artifact:** `docs/qa/gates/evidence/T002.0a/g10-rehash-18.txt`.
- **Status:** [ ] pending (depends on G09)
- **Owner:** Sable

---

## Current execution status

| Gate | Name | Executed? | When | By | Evidence | Status |
|------|------|-----------|------|-----|----------|--------|
| G01 | MANIFEST-HEADER-SCHEMA | No | — | — | — | pending |
| G02 | MANIFEST-ROW-COUNT-PHASE | Yes (pre-evid) | 2026-04-23 | Riven | MC-20260423-1 | passed (16-row) |
| G03 | MANIFEST-HOLDOUT-LEAK-GREP | Yes (pre-evid) | 2026-04-23 | Riven | MC-20260423-1 holdout_leak_scan | passed |
| G04 | PARQUET-SHA256-REVERIFY | Yes (pre-evid) | 2026-04-23 | Riven | MC-20260423-1 files_verified_sha256 | passed (16-row) |
| G05 | PARQUET-SCHEMA-STRICT | Yes (pre-evid) | 2026-04-22 | Dex | backfill_manifest zero-anomaly | passed |
| G06 | PARQUET-ORPHAN-SCAN | No | — | — | — | pending |
| G07 | PARITY-SAMPLE-WARMUP | Informal | 2026-04-22 | Dex | rejected (Quinn CONCERNS) | pending formal |
| G08 | PARITY-SAMPLE-INSAMPLE | Informal | 2026-04-22 | Dex | rejected (Quinn CONCERNS) | pending formal |
| G09 | MAY-JUN-2025-RELAUNCH-TELEMETRY | No | — | — | — | **BLOCKED** (Quinn FAIL-BLOCK) |
| G10 | POST-RELAUNCH-REHASH-18 | No | — | — | — | pending (depends G09) |

---

## Gaps to close before T002.0a can be marked Done

1. Create `docs/qa/gates/evidence/T002.0a/` directory; back-fill pre-evidence pointers for G02, G03, G04, G05.
2. G01, G06 never run — trivial; Sable executes immediately.
3. G07/G08 formal re-run after Dex addresses Quinn CONCERNS on `verify_parity.py`.
4. G09 FAIL-BLOCK: Dex remediates `_telemetry_wrapper.py` per Quinn findings + Sable drafts MC-20260424-1 for Riven.
5. G10 depends on G09.
6. Post-18-row: re-run G02 + G03 against updated manifest.

---

## Precedent

Dex's `G1-G6` vocabulary is formally superseded by this checklist (G01-G10). No agent other than **Sable** may declare a gate passed. No agent other than **Riven** may authorize a manifest mutation, and every authorization traces to an `MC-YYYYMMDD-N` entry in `docs/MANIFEST_CHANGES.md`.

"Ran it and it looked green" is not evidence. A gate is passed only when:
1. Gate procedure executed verbatim, AND
2. Stdout/stderr captured to designated evidence artifact, AND
3. Sable counter-signed in story Dev comments.

Dex's 2026-04-22 claims on G07/G08 are respected as good-faith pre-evidence but cannot mark gates closed. Formal re-run required.
