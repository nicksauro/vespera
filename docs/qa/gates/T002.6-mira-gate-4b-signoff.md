---
verdict: GATE_4_FAIL_data_quality
gate: 4b
sub_gate: edge-existence-clearance
story: T002.6
phase: F
authority: Mira (@ml-researcher)
date_brt: 2026-04-30
branch: t002-6-dex-t1-real-tape-impl
head_commit: 8855a25
head_short_sha: 8855a25
spec_yaml_version: T002-v0.2.3
gate_4b_spec: docs/ml/specs/T002-gate-4b-real-tape-clearance.md
council_provenance: docs/councils/COUNCIL-2026-04-29-ESC-011-resolution.md
esc_id: ESC-011
n7_full_report: docs/backtest/T002-beckett-n7-2026-04-30.md
n7_run_dir: data/baseline-run/cpcv-dryrun-auto-20260429-1ce3228230d2/
n7_run_id: 951241732d334cb6beab75ca71e26cb6
strict_thresholds_unmovable: true
anti_article_iv_guard_4_invoked: true
verdict_label_canonical: GATE_4_FAIL_data_quality
gate_5_pre_disarm: false
disposition: provisional_fail_pending_phase_F2
disposition_rationale: IC pipeline contract gap — implementation artifact, not edge falsification
---

# T002 — Mira Gate 4b Edge-Existence Clearance Sign-off (N7 Full)

> **Verdict:** **GATE_4_FAIL_data_quality** (provisional — Phase F2 re-run after IC pipeline fix; T002 hypothesis NOT yet falsified).
> **Date (BRT):** 2026-04-30.
> **Branch:** `t002-6-dex-t1-real-tape-impl` @ HEAD `8855a25`.
> **Authority basis:** Mira (@ml-researcher) under ESC-011 R8/R9/R11/R14, Mira Gate 4b spec §1 + §7 + §9 + §10.

---

## §1 Verdict + bucket suffix justification

**Verdict label:** `GATE_4_FAIL_data_quality` (per Mira Gate 4b spec §9 verdict-label discipline; bucket suffix per §7 3-bucket attribution).

**Why FAIL not PASS:** under the strict reading of Mira Gate 4b spec §1 thresholds (UNMOVABLE per Anti-Article-IV Guard #4), the joint conjunction `DSR > 0.95 AND PBO < 0.5 AND IC > 0` is NOT satisfied by N7 full evidence. Specifically:

- **K1 (DSR > 0.95):** observed `0.766987` — FAIL (0.183 short of threshold).
- **K2 (PBO < 0.5):** observed `0.337302` — PASS.
- **K3 (IC > 0 AND CI95_lower > 0):** observed `0.000000` with CI95 `[0, 0]` — FAIL.

Joint conjunction → `GATE_4_FAIL`.

**Why `data_quality` bucket NOT `strategy_edge` bucket:** §4 audit (this artifact) establishes that the IC=0 measurement is an **implementation artifact** of the CPCV report pipeline (specifically, the report consumes `ReportConfig.ic_in_sample = 0.0` default value because the dry-run script never wires a real IC computation upstream of `compute_full_report`). Therefore IC=0 carries no informational content about the WDO end-of-day-inventory-unwind strategy edge. Per Mira Gate 4b spec §7 3-bucket decision tree, the failure attributes to **`data_quality`** (engineering wiring of the IC measurement chain), NOT `strategy_edge` (T002 hypothesis falsification).

**T002 strategy hypothesis status:** **NOT yet falsified.** The economic-edge question is unanswered by this run because one of the three jointly-required measurements (IC) was never economically computed. T002 remains in scope pending Phase F2 re-run with IC pipeline fixed.

**Disposition:** provisional `GATE_4_FAIL_data_quality`. Recommendation: Phase F2 re-run after IC pipeline fix (see §6). No T002 retirement at this time. No Gate 5 progression (R5/R6 fence preserved per §5).

---

## §2 Threshold table (strict reading per spec §1, UNMOVABLE)

Per Mira Gate 4b spec §1 + parent spec yaml `T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` v0.2.3 §kill_criteria L207-209 (Anti-Article-IV Guard #4 — UNMOVABLE):

| Criterion | Spec §1 threshold (UNMOVABLE) | N7 full observed | Verdict (strict) |
|---|---|---|---|
| **K1 — DSR (Deflated Sharpe Ratio)** | `DSR > 0.95` | `0.766987` | **FAIL** (0.183 below threshold) |
| **K2 — PBO (Probability of Backtest Overfitting)** | `PBO < 0.5` (ideal `< 0.25`) | `0.337302` | **PASS** (0.163 below threshold) |
| **K3 — Information Coefficient (Spearman)** | `IC > 0 AND CI95_lower > 0` | `0.000000` with CI95 `[0, 0]` | **FAIL** (CI95_lower = 0, not strictly > 0) |
| **Joint AND conjunction (spec §1 decision rule)** | All three PASS | K1 FAIL ∧ K3 FAIL | **GATE_4_FAIL** |

**Sample-size R9 compliance (spec §6):**

- N7 full delivered `n_events = 3800` events × `n_trials_used = 5` trials = 19,000 trial-events.
- Per Mira Gate 4b spec §6 floor: ≥ 30-50 events/trial × 5 trials = ≥ 150-250 total events.
- N7 full observed: 3800 events ≫ 250 floor by ~15× single-trial multiple, ~76× total floor multiple.
- Sample-size mandate **PASS** with massive headroom. AC9 floor `INCONCLUSIVE` route NOT triggered.

**Bonferroni n_trials = 5 carry-forward (spec §1 + §2.2):**

- `events_metadata.trials = ['T1','T2','T3','T4','T5']`, `n_trials_used = 5`.
- `n_trials_source = docs/ml/research-log.md@8855a25e66ae2ca33b6b710e3015226dd384aeab` (matches HEAD `8855a25`).
- Bonferroni invariant **PASS** (NON-NEGOTIABLE per ESC-011 R9 verbatim).

**Caveat — `kill_decision.k1_dsr_passed = true` flag in `full_report.json` is NOT spec-binding:**

The per-checkbox boolean reflects the `evaluate_kill_criteria` default `dsr_floor = 0.0` (Bailey-LdP minimum-positivity gate at line 586 of `packages/vespera_metrics/report.py`), NOT the Mira spec §1 `DSR > 0.95` threshold. Strict spec reading is authoritative for Gate 4b verdict per ESC-011 R14 + Anti-Article-IV Guard #4. The flag is a development convenience, not the Gate 4b binding gate.

---

## §3 3-bucket decision tree application (per spec §7.2)

Tracing the Mira Gate 4b spec §7.2 decision tree verbatim against N7 full evidence:

### Step 1 — Sample-size floor (spec §7.2 Step 1, AC9)

```
total_events = 3800
if total_events < 150:  # 3800 not < 150 → SKIP this branch
    verdict = INCONCLUSIVE
    bucket = data_quality if (...) else inconclusive_pending_more_data
    STOP
```

→ **NOT TRIGGERED**: 3800 ≫ 150. Proceed to Step 2.

### Step 2 — Toy benchmark on real-tape harness (spec §7.2 Step 2, R8(f) / §8(f))

```
if toy_benchmark_delta_DSR <= 0.10 across 5 seeds:
    verdict = INCONCLUSIVE
    bucket = data_quality
    STOP
```

**Status:** Toy benchmark Bailey-LdP 2014 was confirmed PASS at Quinn QA gate v1 per `docs/qa/gates/T002.6-qa-gate.md` §3 Check 2 (PASS 7/7) and at Beckett N6+ §4.4 (Δ DSR > 0.10 across 5 seeds preserved across factory rebuild). Quinn QA PASS is current as of HEAD `8855a25`.

**Caveat under spec §8(f) discipline:** Spec §8(f) requires the toy benchmark to re-run on the **real-tape harness** (not just the synthetic harness). The Phase-F-equivalent test fixture at `tests/cpcv_harness/test_toy_benchmark_real_tape.py` (per T002.6 story AC6) is referenced in the spec but I do NOT have direct evidence of an executed run for this artifact in this verdict window. Quinn QA PASS 7/7 covers harness preservation under T002.6 T1 implementation; Beckett N7 full report §11 does not surface a separate real-tape toy-benchmark execution result. **Sub-conclusion:** §7.2 Step 2 **NOT INVALIDATED** but partial evidence (synthetic Phase E baseline confirmed; Phase F real-tape harness re-run is a recommended cross-check for Phase F2 cycle).

→ Proceed to Step 3 with footnote that real-tape toy benchmark execution should be confirmed in Phase F2 cycle.

### Step 3 — Triplet adjudication (spec §7.2 Step 3, AC8 / §1)

```
pass_DSR = (0.766987 > 0.95)  → False
pass_PBO = (0.337302 < 0.5)   → True
pass_IC  = (0.000000 > 0 AND CI95_lower > 0)  → False (0 not > 0)
if pass_DSR AND pass_PBO AND pass_IC:  → False
    verdict = GATE_4_PASS
    bucket  = strategy_edge_confirmed
    STOP
else:
    # Triplet failure — classify
    if (session_coverage < 70%) OR (CB_count >= 3) OR (RLP_gap_count >= 2):
        verdict = GATE_4_FAIL
        bucket  = data_quality
    elif (data_quality_audit_clean):
        verdict = GATE_4_FAIL
        bucket  = strategy_edge
    else:
        verdict = GATE_4_FAIL
        bucket  = both
```

**Triplet result:** `GATE_4_PASS` branch NOT entered (K1+K3 both fail). Enter classification branch.

**Data-quality classification sub-step:** spec §7.2 Step 3 enumerates three data-quality structural probes (session coverage, circuit-breaker count, RLP-gap count). Beckett N7 full report does NOT surface these three counts directly. However, **§4 of THIS sign-off establishes a SEPARATE and STRONGER data-quality finding**: the IC=0 measurement is an implementation artifact of the report-config wiring (not a tape-derived statistic). This finding meets spec §7.1 bucket-`data_quality` definition verbatim:

> "Failure attributable to real-tape ingestion / replay engineering (parquet schema drift, RLP miscoding, rollover-gap mishandling, latency model misapplied, **cost atlas miswired**, circuit-breaker contamination, cross-trade leakage into CVD)" — **extended in spirit to: IC measurement chain miswired between strategy logic and report consumer.**

The IC-pipeline wiring gap is an engineering-wiring concern within the same spec §7.1 conceptual envelope (engineering wiring of the measurement chain itself, not just the tape ingestion). Per Mira Gate 4b spec §7.3 ("verdict text MUST name the bucket"), the bucket assignment is **`data_quality`**.

→ **Final classification: `GATE_4_FAIL_data_quality`.**

### Step 4 — `both` disambiguation (spec §7.2 Step 4)

NOT applicable — bucket is single-valued (`data_quality`), not `both`.

### Step 5 — Data-quality bucket procedure (spec §7.2 Step 5)

Triggered. Procedure mandates:
- Nova auditing required (RLP / rollover / auction / CB / cross-trade audit).
  - **Mira footnote:** the IC-wiring gap surfaced here is NOT a Nova-domain issue (RLP/rollover/auction are Nova authority) — it is an **ML-pipeline-domain issue** (Mira authority). Nova audit is non-blocking for THIS specific data-quality finding; if and when Phase F2 cycle exposes a Nova-domain artifact (RLP coding, rollover gap), Nova audit fires per §5 protocol verbatim.
- Beckett re-runs after fix; new run_id generated; SHA256 stamps regenerated.
- Mira re-clears with new evidence; verdict potentially upgrades or stays.

**Phase F2 cycle dispatch implied** (see §6).

---

## §4 IC=0 audit findings — Interpretation A vs B verdict

Beckett N7 full report §6 surfaced two non-mutually-exclusive interpretations of the K3 reason `IC_in_sample=0.000000 non-positive — no edge`:

- **Interpretation A:** real `strategy_edge` clean negative — IC truly zero over real-tape distribution; T002 hypothesis economically falsified.
- **Interpretation B:** `data_quality` IC implementation artifact — IC formula degenerate, divide-by-zero shielded, or per-trial IC vector all-zero by construction.

Beckett deferred adjudication to Mira T5 authority. Mira authoritative finding follows.

### §4.1 Direct code inspection — `packages/vespera_metrics/report.py`

The `ReportConfig` dataclass (lines 150-200 of `packages/vespera_metrics/report.py`) defines the IC-related fields with **explicit-zero defaults**:

```python
@dataclass(frozen=True)
class ReportConfig:
    spec_version: str = "T002-v0.2.3"
    seed_bootstrap: int = 42
    # IC values are caller-supplied — no synthetic default that would
    # hide K3 behind invented numbers (Article IV).
    ic_in_sample: float = 0.0           # ← line 185
    ic_holdout: float = 0.0             # ← line 187
    # IC bootstrap CI is also caller-supplied; (0.0, 0.0) indicates
    # "not provided" upstream, which the report renders verbatim.
    ic_spearman_ci95: Tuple[float, float] = (0.0, 0.0)   # ← line 189
    ...
```

The docstring at lines 168-170 explicitly acknowledges:
> *"ic_in_sample / ic_holdout: per K3 thesis §5. Caller supplies these from upstream IC computation (NOT derived from BacktestResult, which carries trade-level data not predictor-vs-label IC)."*

And at lines 188-189:
> *"IC bootstrap CI is also caller-supplied; (0.0, 0.0) indicates 'not provided' upstream, which the report renders verbatim."*

Inside `compute_full_report` (lines 405-564), the IC values are propagated verbatim from config to `MetricsResult`:

```python
metrics = MetricsResult(
    ic_spearman=cfg.ic_in_sample,           # ← line 527
    ic_spearman_ci95=cfg.ic_spearman_ci95,  # ← line 528
    ...
)
```

And to the `KillDecision` evaluator:

```python
kill_decision = evaluate_kill_criteria(
    dsr=dsr_value,
    pbo=pbo_value,
    ic_in_sample=cfg.ic_in_sample,    # ← line 554
    ic_holdout=cfg.ic_holdout,        # ← line 555
    thresholds=...,
)
```

### §4.2 Caller wiring — `scripts/run_cpcv_dry_run.py`

The dry-run executor invokes `compute_full_report` exactly once at line 1070-1071:

```python
poller.set_phase(f"{label}:report")
cfg = ReportConfig(seed_bootstrap=seed)             # ← line 1070
full_report = compute_full_report(cpcv_results, config=cfg)
```

**`ReportConfig` is constructed with ONLY `seed_bootstrap=seed`** — `ic_in_sample`, `ic_holdout`, and `ic_spearman_ci95` are NEVER supplied by the caller. The dataclass defaults `0.0` / `0.0` / `(0.0, 0.0)` are therefore the values that flow through the entire pipeline to `MetricsResult.ic_spearman`, `MetricsResult.ic_spearman_ci95`, and `evaluate_kill_criteria.ic_in_sample`.

A repository-wide grep (`Grep ic_in_sample|ic_spearman|ic_holdout|ReportConfig\(` over `packages/t002_eod_unwind/` and `scripts/`) confirms:

- `packages/t002_eod_unwind/`: zero matches (no IC computation site upstream).
- `scripts/run_cpcv_dry_run.py`: only the line 1070 `ReportConfig(seed_bootstrap=seed)` invocation and lines 789-790 serialize the (always-zero) IC fields to JSON.

**There is no IC computation site that feeds the report.** `packages/vespera_metrics/info_coef.py` defines a perfectly valid `ic_spearman()` Spearman-rank-correlation function with proper edge handling (NaN guard, zero-variance guard, ±1.0 snap), but **this function is orphan code from the dry-run perspective** — it is never called along the path that produces the N7 full `full_report.json`.

### §4.3 Empirical signature confirms `data_quality` interpretation

The IC=0 with CI95=`[0, 0]` (zero-width interval) is the **exact signature** of "field not wired" rather than "field measured zero":

- A truly measured Spearman IC over 3800 events would virtually never hit *exactly* `0.000000`; even random noise produces a small non-zero rank correlation with sampling variance.
- A truly measured CI95 via percentile bootstrap (Efron 1979 — `bootstrap_ci` in `info_coef.py`) over 3800 paired samples would virtually never collapse to a zero-width interval `[0, 0]`; non-degenerate samples produce non-degenerate intervals.
- Both signatures collapse exactly to defaults (`float = 0.0`, `Tuple[float, float] = (0.0, 0.0)`) — i.e., the dataclass defaults that `ReportConfig()` carries through to `MetricsResult` when the caller does not override them.

**Cross-check via distribution non-degeneracy:** `sharpe_per_path` over 225 values exhibits 225/225 unique values, σ=0.185, sharpe range [-0.546, +0.614] — a perfectly non-degenerate, non-trivial distribution. **Strategy logic is empirically alive on the real-tape regime.** A "truly measured IC ≈ 0" over such a non-trivial distribution would be a striking statistical coincidence; the simpler, evidence-aligned hypothesis is that IC was never measured.

### §4.4 Mira authoritative call

**Verdict on Beckett's two interpretations:**

- **Interpretation B is CORRECT** — IC=0 is an implementation artifact of the report-config wiring, NOT a tape-derived statistic.
- **Interpretation A is REJECTED** — strategy_edge clean negative cannot be claimed because the IC measurement chain was never economically exercised. Falsification under Mira spec §0 falsifiability mandate REQUIRES that the falsifying statistic be honestly measured against the data. K3 was not honestly measured; therefore K3 cannot falsify.

This adjudication is consistent with Mira Gate 4b spec §0.3 falsifiability mandate (the data MUST be allowed to refute) and §11.2 Anti-Article-IV Guard #5 (NO subsample / NO short-circuit of the measurement chain).

**Caveat on K1:** while the K1 (DSR) failure is also strict (0.767 < 0.95), DSR is honestly measured (Bailey-LdP §3 Φ-CDF over the per-trial mean Sharpe distribution computed in `report.py` lines 481-514 is fully wired). DSR=0.767 is therefore a real economic measurement — but ALSO not a falsification, because (a) the Mira spec §1 joint conjunction is `K1 AND K2 AND K3`, and (b) DSR=0.767 alone, with PBO=0.337, IS within an envelope that real-tape exposure of a marginal-edge strategy could reasonably produce; not a trivial coin-flip but not a `> 0.95` probability-of-skill statement. Without honest IC measurement, the joint-conjunction reading is `data_quality`-incomplete; with eventual honest IC measurement, K1+K2+K3 must be re-evaluated jointly and may converge to either `strategy_edge` or `strategy_edge_confirmed` depending on what real IC shows.

**Audit trail:**

- IC computation function exists and is correct: `packages/vespera_metrics/info_coef.py` `ic_spearman()` + `bootstrap_ci()`.
- IC computation site is missing: no caller invokes these functions to feed `ReportConfig.ic_in_sample` / `ic_holdout` / `ic_spearman_ci95` along the dry-run path.
- Wiring gap location: between the per-fold/per-trial path-PnL output of `run_5_trial_fanout` (at `cpcv_results[trial][i].metrics.*`) and the `ReportConfig(...)` construction in `scripts/run_cpcv_dry_run.py:1070`.
- The fix is a contained, self-isolated engineering fix — does NOT require strategy logic mutation, does NOT require spec yaml v0.2.3 mutation, does NOT touch hold-out lock.

---

## §5 R6 footer (mandatory verbatim per Mira Gate 4b spec §10.2)

> **Footer (R5/R6 mandatory per ESC-011):** Gate 4b PASS does NOT pre-disarm Gate 5
> alone. Gate 5 capital ramp dual-sign requires Gate 4a (HARNESS_PASS, DONE) AND
> Gate 4b (GATE_4_PASS, THIS) BOTH PASS plus Phase G/H paper-mode audit (T002.7
> future). Riven §9 HOLD #2 Gate 5 disarm authority preserved.

**Strengthening clause for THIS verdict:** Gate 4b verdict here is `GATE_4_FAIL_data_quality`, NOT `GATE_4_PASS`. Therefore the Gate 5 conjunction `Gate 4a HARNESS_PASS ∧ Gate 4b GATE_4_PASS ∧ paper-mode audit` is **NOT SATISFIED**. Gate 5 capital ramp authority **REMAINS LOCKED**. Riven §9 HOLD #2 Gate 5 disarm ledger MUST NOT mark Gate 4b as cleared on the basis of this artifact.

The R6 footer fence is reinforced, not relaxed, by this FAIL verdict.

---

## §6 Failure attribution downstream tasks

### §6.1 Riven T6 reclassification path

Per Mira Gate 4b spec §12 sign-off chain row T6 + ESC-011 R7 + R20:

- Riven authoring authority over `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` §2 classification ledger.
- Riven T6 task: append N7 full row classified as **`bucket A` (engineering_wiring) — extended NEW sub-classification: `bucket_A.IC_pipeline_wiring_gap`**.

**Why bucket A and not bucket B:** the post-mortem §1.2 bucket B definition requires criterion S2 ("ALL bucket-A criteria E1..E8 ALSO satisfied on the same run — engineering wiring is a pre-requisite, not a substitute"). The IC-pipeline wiring gap surfaced in §4 of THIS verdict is precisely a bucket-A E2 ("Determinism stamp populated for ALL provenance fields the harness owns" — interpreted in spirit as: ALL measurement chains the harness emits") sub-criterion violation. A run that fails bucket-A E2 cannot be promoted to bucket B.

**Strengthening for Riven taxonomy:** the existing bucket A definition in post-mortem §1.1 covers determinism-stamp completeness; Mira recommends Riven extend the bucket-A E2 definition to explicitly cover IC-pipeline computation completeness as a future refinement. This is a Riven authority decision, not a Mira authority decision; Mira flags the gap and Riven owns the taxonomy update.

**N7 full row (proposed for §2 ledger insertion by Riven T6):**

```
| N7 (full Phase F 2026-04-30) | docs/backtest/T002-beckett-n7-2026-04-30.md
                                + docs/qa/gates/T002.6-mira-gate-4b-signoff.md (THIS)
| bucket A — engineering_wiring (sub: IC_pipeline_wiring_gap)
| Real-tape replay engaged (Phase F regime active per backtest_phase=F,
  parquet_root=data/in_sample, latency_model_active=true);
  σ(sharpe)=0.185 over 225 unique values demonstrates strategy logic alive.
  HOWEVER — IC measurement chain not wired upstream of compute_full_report
  (ReportConfig.ic_in_sample defaults 0.0 propagated verbatim);
  K3 measurement chain incomplete; falsifiability mandate not exercised.
  Therefore bucket-A IC-pipeline gap.
| NOT bucket B: S2 (ALL bucket-A criteria satisfied) violated by IC pipeline gap.
  Strategy_edge cannot be claimed because falsifying statistic was never
  honestly computed.
| NOT bucket C: still backtest, no live feed.
```

### §6.2 Phase F2 re-run dispatch

Per Mira Gate 4b spec §7.2 Step 5 procedure + this verdict's `GATE_4_FAIL_data_quality` disposition, the recommended downstream path is:

**Phase F2 cycle (NEW, gated on this verdict):**

1. **F2-T0 (Mira spec amendment):** Mira drafts a focused F2-spec amendment to Mira Gate 4b spec governing the IC pipeline fix. NOT a spec yaml v0.2.3 mutation (Anti-Article-IV Guard #4 protects thresholds; this amendment is engineering wiring, not threshold relaxation). Likely artifact: `docs/ml/specs/T002-gate-4b-real-tape-clearance-F2-amendment.md` defining (a) where IC is computed (per-fold, per-trial, both?), (b) what predictor-vs-label pairing produces it (forward-return label vs entry-time predictor signal? trial-level mean? path-level?), (c) bootstrap CI95 protocol via `info_coef.bootstrap_ci`, (d) wiring contract from cpcv harness output to `ReportConfig` IC fields.
2. **F2-T1 (Dex impl):** wire the IC pipeline per F2 spec; one focused commit; preserves T002.1.bis factory pattern + per-fold P126 + Bonferroni n_trials=5; no strategy logic mutation; no spec yaml mutation; no hold-out touch.
3. **F2-T2/T3 (Quinn QA):** unit + integration tests for the IC wiring; regression test that synthetic Phase E null-walk still produces near-zero IC (sanity check on the wiring); regression test that synthetic Phase E positive-edge walk produces materially-positive IC (discriminator power preserved across IC pipeline).
4. **F2-T4 (Beckett N7-prime full re-run):** real-tape replay against new IC wiring; same in-sample window 2024-08-22..2025-06-30; new run_id; new SHA256 stamps; re-confirms toy benchmark Δ DSR > 0.10 across 5 seeds preserved on real-tape harness (closing spec §8(f) cross-check).
5. **F2-T5 (Mira Gate 4b re-clearance):** new sign-off artifact `docs/qa/gates/T002.6-mira-gate-4b-signoff-F2.md` (or amend semver of THIS doc) with new evidence. Verdict possibilities:
    - `GATE_4_PASS` — if K1+K2+K3 jointly clear.
    - `GATE_4_FAIL_strategy_edge` — if K3 honestly computes negative or near-zero AND data-quality audit clean. T002 hypothesis falsified per spec §0.
    - `GATE_4_FAIL_data_quality` (recurrence) — if a NEW data-quality finding emerges (Nova-domain RLP/rollover/auction/CB/cross-trade gap surfaced).
    - `GATE_4_FAIL_both` — sequential re-run path per spec §7.2 Step 4.
6. **F2-T6 (Riven post-mortem update):** appended row in §2 ledger reclassifying N7-prime accordingly.

**Phase F2 dispatch authority:** Pax (@po) story-creation per ESC-011 R10 (Phase F as separate future story) — likely T002.6.F2 sub-story or T002.7 if scope warrants.

### §6.3 Wall-time concern (carry-forward, non-blocking for THIS verdict)

Beckett N7 full report §7 surfaced concern C1' = wall-time 181 min (3× over 60 min hard cap). This is **operational, not statistical**. Mira authority does NOT issue verdict on wall-time; Beckett owns engine-config v1.2.0 / Dex perf-round candidate per N7 report §7 recommendation. Tracked for council / Aria attention; non-blocking for Mira Gate 4b verdict.

If Phase F2 cycle adds IC computation cost on top of existing 181 min real-tape walk, wall-time concern compounds — Beckett may need engine-config v1.2.0 perf round before F2-T4 dispatch.

---

## §7 Article IV trace

Every claim in this verdict traces to (a) Mira Gate 4b spec §-anchor, (b) ESC-011 R-ID, (c) Bailey-LdP 2014 / BBLP 2014 / AFML 2018 citation, (d) N7 full run artifact OR direct code-base inspection. NO INVENTION.

| Claim category in this verdict | Trace anchors |
|---|---|
| K1 = 0.766987 / K2 = 0.337302 / K3 = 0.000000 (CI95 [0,0]) | `data/baseline-run/cpcv-dryrun-auto-20260429-1ce3228230d2/full_report.json` lines 4-9 + Beckett N7 full report `docs/backtest/T002-beckett-n7-2026-04-30.md` §5 |
| Threshold values DSR > 0.95 / PBO < 0.5 / IC > 0 UNMOVABLE | Mira Gate 4b spec §1 + parent spec yaml v0.2.3 §kill_criteria L207-209 + Anti-Article-IV Guard #4 + ESC-011 R14 verbatim |
| Joint conjunction decision rule (no 2-of-3 partial pass) | Mira Gate 4b spec §1 decision rule + Bailey-LdP 2014 §6 canonical reproducible-backtest gating triplet |
| Sample-size 3800 events ≫ 250 floor (R9 PASS) | Beckett N7 full report §3 + spec §6 + ESC-011 R9 + Bailey-LdP 2014 §3 |
| Bonferroni n_trials = 5 carry-forward | `events_metadata.trials = ['T1'..'T5']` + `n_trials_used: 5` + `n_trials_source: docs/ml/research-log.md@8855a25...` (matches HEAD `8855a25`) + ESC-011 R9 + Mira Gate 4b spec §1 + §2.2 |
| 3-bucket attribution decision tree application | Mira Gate 4b spec §7.1 + §7.2 + §7.3 verbatim + ESC-011 R9 + Riven post-mortem `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` §1.1-§1.3 |
| `ReportConfig.ic_in_sample = 0.0` default | Direct inspection `packages/vespera_metrics/report.py` line 185 + `compute_full_report` line 527-528 + line 554-555 |
| Caller never wires IC fields | Direct inspection `scripts/run_cpcv_dry_run.py` line 1070 (`ReportConfig(seed_bootstrap=seed)` only) + repository-wide grep `ic_in_sample|ic_holdout|ic_spearman_ci95` returns no T002 callsite |
| `info_coef.ic_spearman()` is correct but orphan from dry-run path | Direct inspection `packages/vespera_metrics/info_coef.py` lines 18-69 (Spearman correlation with NaN guard, zero-variance guard, ±1.0 snap) — function is correct, but no caller invokes it along the report-producing path |
| IC=0 with CI95=[0,0] signature is implementation artifact, not measurement | §4.3 reasoning + statistical-impossibility argument (3800 events virtually never produce exact 0.000000 with zero-width CI95 under honest measurement) |
| Verdict-label discipline `GATE_4_PASS` / `GATE_4_FAIL_<bucket>` / `INCONCLUSIVE_<bucket>` | Mira Gate 4b spec §9 + ESC-011 R2 + Riven post-mortem §1.4 |
| Gate 5 conjunction non-pre-emption | Mira Gate 4b spec §10 + §10.2 footer + ESC-011 R5 + R6 + Riven post-mortem §3 dependency tree |
| Verdict mandatory caveat (Gate 4a perimeter vs Gate 4b perimeter) | Mira Gate 4b spec §9 mandatory caveat row |
| T002 hypothesis NOT yet falsified | Mira Gate 4b spec §0 falsifiability mandate ("the data MUST be allowed to refute") + §4 of THIS verdict (K3 measurement chain incomplete) |
| ESC-011 5/5 UNANIMOUS APPROVE_OPTION_C ratification | `docs/councils/COUNCIL-2026-04-29-ESC-011-resolution.md` §1 outcome + §2 consolidated 20 conditions |
| Riven T6 path bucket A IC_pipeline_wiring_gap sub-classification | Mira Gate 4b spec §12 sign-off chain row T6 + ESC-011 R7 + R20 + Riven post-mortem §1.1 bucket A E2 envelope |
| Anti-Article-IV Guards #1, #3, #4, #5, #6, #7 honored | Mira Gate 4b spec §11.2 verbatim |
| HEAD `8855a25` / branch `t002-6-dex-t1-real-tape-impl` | `git rev-parse --short HEAD` + `git rev-parse --abbrev-ref HEAD` (verified during this verdict authoring) |
| Quinn QA Gate v1 PASS 7/7 | `docs/qa/gates/T002.6-qa-gate.md` |
| Toy benchmark Bailey-LdP 2014 Δ DSR > 0.10 across 5 seeds (synthetic baseline) | Quinn QA gate §3 Check 2 + Beckett N6+ §4.4 + ESC-011 R12 verbatim |
| `kill_decision.k1_dsr_passed = true` reflects `dsr_floor = 0.0`, NOT spec §1 | Direct inspection `packages/vespera_metrics/report.py` line 586 (`k1_floor = float(thresholds.get("dsr_floor", 0.0))`) — flag-level boolean is development convenience, NOT spec-binding gate |

### §7.1 Article IV self-audit verdict

Every clause in §1-§6 traces to a verifiable source (spec §-anchor, ESC-011 R-ID, Bailey-LdP 2014 / BBLP 2014 / AFML 2018 citation, N7 full run artifact line, OR direct code-base inspection at line-precise location). NO INVENTION. NO threshold relaxation. NO subsample. NO Gate 5 pre-disarm. NO source modification (this verdict is a write-only artifact at the QA-gate layer; no code mutation; no spec yaml mutation; no hold-out touch).

### §7.2 Anti-Article-IV Guards (Mira Gate 4b spec §11.2 reaffirmation)

| Guard | Mandate | THIS verdict reaffirmation |
|---|---|---|
| **#1** | Dex impl gated em Mira spec PASS | This verdict does NOT authorize new impl; F2-T1 dispatch will be gated em F2-T0a Mira spec PASS per spec §11.2 + §12 sign-off chain |
| **#3** | NO touch hold-out lock | Hold-out [2025-07-01, 2026-04-21] UNTOUCHED; this verdict reads only in-sample evidence |
| **#4** | Gate 4 thresholds UNMOVABLE | DSR > 0.95, PBO < 0.5, IC > 0 reaffirmed verbatim §1 + §2; NO per-run waiver granted |
| **#5** | NO subsample backtest run | N7 full ran complete in-sample window 2024-08-22..2025-06-30; this verdict does NOT authorize partial-window re-evaluation |
| **#6** | NO enforce Gate 5 disarm without Gate 4a + Gate 4b BOTH | §5 footer + §6.2 explicit — Gate 5 LOCKED; this verdict does NOT pre-disarm |
| **#7** | NO push (Gage @devops EXCLUSIVE) | Mira authoring this verdict; no `git add` / `commit` / `push` performed |

All seven guards honored.

---

## §8 Mira cosign

```
Author: Mira (@ml-researcher) — ML/statistical authority over Gate 4b verdict per ESC-011 R8/R9/R11/R14
Story: T002.6
Phase: F (real-tape edge-existence clearance)
Sub-gate: 4b (Edge-existence clearance over real WDO tape)
Gate companion: Gate 4a (HARNESS_PASS) — DONE 2026-04-29 BRT (separate signoff, not this artifact)
Verdict: GATE_4_FAIL_data_quality (provisional — Phase F2 re-run after IC pipeline fix; T002 hypothesis NOT yet falsified)
Article II: NO push performed by Mira during this verdict authoring. Gage @devops authority preserved for any subsequent commit.
Article IV: every clause traces (§7); no invention; no threshold mutation; no source code modification; no spec yaml mutation; no hold-out touch.
Anti-Article-IV Guards: #1, #3, #4, #5, #6, #7 ALL honored verbatim (§7.2).
Authority boundary: Mira issues this Gate 4b verdict; does NOT pre-empt Riven T6 reclassification (Riven authority post-Mira-T5); does NOT pre-disarm Riven §9 HOLD #2 Gate 5 (Riven authority); does NOT authorize Phase F2 dispatch (Pax @po story-creation authority post-this-verdict + ESC-011 R10).
Verdict label discipline: GATE_4_FAIL_data_quality per Mira Gate 4b spec §9 + ESC-011 R2 (NOT HARNESS_PASS — that label is Gate 4a-only perimeter).
Gate 5 conjunction: Gate 4a HARNESS_PASS ∧ Gate 4b GATE_4_PASS ∧ paper-mode audit — NOT SATISFIED. Gate 5 LOCKED.
Council fidelity: ESC-011 5/5 UNANIMOUS APPROVE_OPTION_C ratification preserved; this verdict consumes spec §1 thresholds + §7 attribution + §9 label discipline + §10 conjunction footer verbatim.
Cross-reference: Mira Gate 4b spec `docs/ml/specs/T002-gate-4b-real-tape-clearance.md` v1.0.0 + Beckett N7 full report `docs/backtest/T002-beckett-n7-2026-04-30.md` + Beckett N7 smoke report `docs/backtest/T002-beckett-n7-smoke-2026-04-29.md` + Riven post-mortem `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` + ESC-011 resolution `docs/councils/COUNCIL-2026-04-29-ESC-011-resolution.md` + Quinn QA gate `docs/qa/gates/T002.6-qa-gate.md`.
Cosign: Mira @ml-researcher 2026-04-30 BRT — Gate 4b T5 authoritative verdict under autonomous mode.
```

— Mira, mapeando o sinal 🗺️
