---
storyId: T002.1.bis
verdict: CONCERNS
date: 2026-04-29 BRT
qa: Quinn (@qa)
commit_validated: 1b7d7d9
branch: t002-1-bis-make-backtest-fn
session: Autonomous Daily Session 2026-04-29
gate_authority: docs/qa/gates/qa-gate.md (7-point Quality Gate)
score_summary: 6 PASS / 1 CONCERN / 0 FAIL (out of 7 quality checks)

issues:
  - severity: medium
    category: security
    id: F1-COST-ATLAS-SHA-NULL
    description: |
      The empirical AC9 ``determinism_stamp.json`` at
      ``data/baseline-run/cpcv-dryrun-auto-20260428-1ce3228230d2/`` carries
      ``cost_atlas_sha256: null`` and ``rollover_calendar_sha256: null``,
      breaking the SHA-locked-atlas audit trail mandated by AC4 ("atlas
      v1.0.0 SHA-locked acf449415a3c9f5d…"). Root cause:
      ``scripts/run_cpcv_dry_run.py:_build_runner`` (L689-703) does NOT
      pass ``cost_atlas_path=`` nor ``rollover_calendar_path=`` to
      ``BacktestRunner.__init__`` even though the runner accepts both
      kwargs (see ``packages/vespera_cpcv/runner.py:55-68``).
    recommendation: |
      Wire both paths in ``_build_runner``. Suggested patch:
      ``BacktestRunner(... cost_atlas_path=docs/backtest/nova-cost-atlas.yaml,
      rollover_calendar_path=<rollover_calendar_path>)``. Document in
      Beckett N6 §X "cost atlas audit" (AC11). Story scope-correct: this
      is plumbing, not strategy code; can be a small follow-up commit
      before Beckett N6 finalization. Does NOT block AC9 empirical
      result interpretation since the atlas content is read for cost
      computation regardless (the gap is provenance-only).
  - severity: medium
    category: requirements
    id: F2-CAVEAT-SYNTHETIC-WALK
    description: |
      Per Dex T1 self-disclosure (story L424-425): events DataFrame is
      metadata-only (no real WDO trade tape); strategy logic evaluates
      over a deterministic synthetic intraday walk seeded per
      ``(session, entry_window, trial_id, as_of_date, path_id)``. This
      is documented verbatim in ``cpcv_harness.py:298-322`` (Article IV
      trace block). The construction is statistically meaningful — it
      preserves AC4 byte-equal determinism, demonstrates non-degenerate
      σ(sharpe)=0.192>0, and the toy benchmark proves the harness can
      discriminate edge from no-edge (Bailey-LdP 2014 §3 reproduction
      PASS) — but Mira Gate 4 statistical clearance over the **real**
      WDO trade tape (Phase F scope per spec §0) remains the
      authoritative test.
    recommendation: |
      Surface to mini-council for Gate 4 decision: does synthetic-walk
      DSR/PBO/IC suffice for §9 HOLD #2 Gate 4 clearance, OR is real
      tape replay (Phase F) a hard precondition? The current design
      memo (Mira spec §7 + Aria T0b) explicitly carves synthetic
      benchmark and real-tape Gate 4 into separate phases — this is
      design-correct, but Beckett N6 + Mira clearance MUST flag the
      caveat in their reports for downstream consumers.
  - severity: low
    category: code
    id: F3-FACTORY-PATTERN-COUNT-MISMATCH
    description: |
      Brief mentions "5+ factory pattern tests" but Dex actually
      shipped 7 tests in ``test_factory_pattern.py``. This is BETTER
      than spec; only flagged for record-keeping coherence.
    recommendation: |
      Update Dex T1 Empirical Report inside the story to read "7
      tests" (already correct in the table at story L374). No code
      change needed.
  - severity: low
    category: code
    id: F4-FORWARD-REF-PLACEHOLDER-STAMP
    description: |
      ``_PLACEHOLDER_STAMP`` is referenced at ``cpcv_harness.py:1023,1227``
      but defined at L1236. This is a forward reference inside a function
      body (resolved at call time, not import time) — works correctly
      because all callers run after module load. Borderline style nit;
      flagged for Reviewer awareness only.
    recommendation: |
      Optional: relocate the ``_PLACEHOLDER_STAMP`` definition above the
      first ``return BacktestResult(...)`` site OR add a brief comment
      clarifying the forward-ref pattern. Non-blocking.
---

# T002.1.bis — QA Gate (Quinn @qa, 2026-04-29 BRT)

## 1. Executive verdict

**CONCERNS** — gate clears with 2 medium-severity findings tracked for downstream resolution. Strategy implementation, factory pattern, anti-leak invariants, regression baseline, and toy-benchmark statistical sanity all PASS literally. The two concerns are both provenance/documentation gaps (cost atlas SHA visibility + synthetic-walk caveat surfacing); neither blocks Beckett N6 progression nor compromises AC2-AC7+AC10+AC12 empirical results.

Quinn recommendation: **proceed to Beckett N6 in parallel** while:
1. Dex (or follow-up commit) wires ``cost_atlas_path`` + ``rollover_calendar_path`` in ``_build_runner`` (F1, ~5 LoC patch).
2. Beckett N6 report § cost atlas audit explicitly documents synthetic-walk caveat F2 for Mira Gate 4 mini-council awareness.

## 2. Commit + branch validated

| Item | Value |
|------|-------|
| Branch | `t002-1-bis-make-backtest-fn` |
| HEAD commit | `1b7d7d9` (`feat(t002.1.bis): real make_backtest_fn integration via backtest_fn_factory (Aria Option B)`) |
| Parent main SHA | `2daedb6` (T002.0h.1 E1 per-phase WarmUpGate) |
| LoC delta | +1949 / -122 across 8 files |
| Empirical AC9 run dir | `data/baseline-run/cpcv-dryrun-auto-20260428-1ce3228230d2/` |
| Author | Nicolas Carasai Baptista (squad: Dex @dev T1) |

## 3. 7-point quality check breakdown

### Check 1 — Code review: PASS

Files inspected (all absolute paths under `C:\Users\Pichau\Desktop\Algotrader\`):

- `packages/vespera_cpcv/engine.py` — `CPCVEngine.run` mutually-exclusive `backtest_fn` xor `backtest_fn_factory` contract per Aria §I.1. ValueError raised when both/neither (L206-211). Per-fold dispatch: `fn = backtest_fn if backtest_fn is not None else backtest_fn_factory(split, split.train_events)` (L214-218). Hold-out lock fail-closed BEFORE iteration (L141, AC12 of T002.0c preserved verbatim). PASS.
- `packages/vespera_cpcv/runner.py` — `BacktestRunner.run` mirror per Aria §I.2 (L111-147). DeterminismStamp injected uniformly via `dataclasses.replace` across both paths. Note: runner accepts `cost_atlas_path` (L58) but Beckett's `_build_runner` does NOT wire it (see F1).
- `packages/t002_eod_unwind/cpcv_harness.py:287-322` — DEFERRED-T11 docstring **clearly marked RESOLVED** with ratification date and Aria/Mira authority chain. Strategy logic comment block fully discloses synthetic-walk methodology + Article IV trace (the F2 caveat origin).
- `packages/t002_eod_unwind/cpcv_harness.py:419-470` — `_resolve_trial_params` table maps T1..T5 verbatim to Mira spec §1.2 (Bonferroni n_trials=5 PRESERVED). Magic numbers traced: `bands.p60` (T1/T4/T5), `_interp_p50(p20,p60)` for T2, `_interp_p70(p60,p80)` for T3 — both interps documented as deterministic engineering refactor (research-log `n_trials: 0`, AC6 verbatim).
- `packages/t002_eod_unwind/cpcv_harness.py:587-1027` — real `make_backtest_fn` body. Triple-barrier precedence `SL > PT > vertical` per AFML 2018 §3.4 Lopez de Prado (L555-578 `_walk_to_exit`, same-tick precedence at L561-563/573-575 explicitly documented). PT=`1.5*atr_hora`, SL=`1.0*atr_hora` literal at L818-819 traced to Mira spec §2.1. 17:55 BRT vertical at L834-835 + `_ticks_from_entry_to_vertical`. Cost atlas `from_engine_config` consumed via `enclosed_costs` closure capture (R/O — Article IV zero side-effects). Cost formula at L850-861 matches Mira §5.3 + Riven §11 audit verbatim: `(exit-entry)*sign*1*WDO_MULTIPLIER - 2*1*(brokerage + exchange_fees) - slippage`. PASS.
- `packages/t002_eod_unwind/cpcv_harness.py:324-397` — new helper `_build_daily_metrics_from_train_events` per Aria §I.5. Pure function `(day.toordinal(), seed_anchor) → (magnitude, atr_day_ratio)`. Range `[0.2, 1.8]` magnitude / `[0.4, 1.6]` atr_ratio chosen to match Mira spec §1.2 P20..P80 spans — documented at L388-393. PASS.
- `scripts/run_cpcv_dry_run.py:873-945` — factory wiring per Aria §I.4. Per-fold rebuild → `_build_daily_metrics_from_train_events(train_events, seed_anchor=split.path_id)` → `as_of_date = min(test_events.session)` (D-1 anti-leak per Mira §3.4). Graceful fallback to `p126_global` when fold has < 126 valid days (smoke runs preserved per Beckett N5 wall-time projection). PASS, **but see F1**: cost_atlas_path missing in `_build_runner`.
- 3 new test files — see Check 2.

Magic numbers documented? **PASS**. Every numeric literal in the strategy path traces:

| Literal | Site | Trace |
|---------|------|-------|
| `1.5×ATR_hora` (PT) | L818 | Mira spec §2.1 (verbatim) |
| `1.0×ATR_hora` (SL) | L819 | Mira spec §2.1 (verbatim) |
| `17:55 BRT` (vertical) | L772, 834 | Mira spec §2.3 + spec yaml v0.2.3 §trading_rules |
| `[0.2, 1.8]` magnitude band | L391 | Mira spec §1.2 P20..P80 |
| `[0.4, 1.6]` atr ratio band | L392 | Mira spec §1.2 P20..P80 |
| `_SYNTH_OPEN_PRICE = 5000.0` | L318 | WDO mid baseline (R$5000/USD) |
| `_SYNTH_DAILY_VOL_POINTS = 25.0` | L319 | ~25-pt daily move proxy |
| `_SYNTH_15MIN_BARS_FROM_OPEN = 32` | L320 | 09:30..17:30 = 32 × 15-min bars |
| `_SYNTH_TICKS_BETWEEN_BARS = 8` | L321 | ticks per bar in walk |
| `n_contracts = 1` | L854 | Mira §4 + Riven §9 HOLD #2 Gate 5 sizing deferred |
| Bonferroni `n_trials=5` (T1..T5) | L432-466 | Mira spec §1.2 + research-log @2daedb6 |

All synthetic constants are pure literals with comments — no spec-yaml drift, no atlas drift. Article IV PASS.

Cost atlas SHA lock visibility: **see F1 — flagged as medium-severity provenance gap.**

Per-fold P126 rebuild correctness: **PASS** — factory + `_build_daily_metrics_from_train_events` + `Percentiles126dBuilder.build` chain enforces D-1 invariant (`m.day < as_of_date`) verified by anti-leak Test 2.

### Check 2 — Unit tests: PASS

Local execution under `C:\Python314\python.exe` (3.14.3, pytest 9.0.2):

```
tests/cpcv_harness/test_factory_pattern.py                 7 passed   7.16 s
tests/cpcv_harness/test_per_fold_anti_leak.py              5 passed   1.34 s
tests/cpcv_harness/test_toy_benchmark_bailey_lopez_de_prado_2014.py 6 passed   7.67 s
tests/cpcv_harness/  (full module)                        49 passed  55.89 s
tests/contracts/                                          64 passed   18.97 s
tests/cpcv/                                               59 passed   19.49 s
```

Counts:
- **Factory pattern: 7/7 PASS** (vs brief "5/5"; Dex shipped 2 extra tests covering fanout factory plumbing + fanout mutual-exclusivity per Aria §I.3 — this is BETTER, see F3 nit).
- **Per-fold anti-leak: 5/5 PASS** (D-1 invariant; no train→test leakage; distinct P126 per fold; seed_anchor distinguishability; malformed-input rejection).
- **Bailey-LdP toy benchmark: 6/6 PASS** (Strategy A no-edge × 3 seeds + Strategy B small-edge DSR>0.90 + DSR discriminator across 5 seeds + non-degenerate σ guard).
- **Existing AC2 tests in `test_make_backtest_fn.py`: 10/10 PASS** (note: brief said "6 AC2 tests" but actual count is 10 — Aria §I.6 back-compat invariant verified for **all** existing tests, including `test_per_fold_p126_rebuild_changes_hash` which is the load-bearing closure-state-anchoring guard).

Test quality: PASS. The toy benchmark is the standout — it operationalizes Mira spec §7 Bailey-LdP 2014 §3 reproducibility and explicitly tests the discriminator (mean(DSR_edge) − mean(DSR_no_edge) > 0.10) which protects Mira Gate 4 against False-NO_GO from harness collapse. This is robust statistical scaffolding.

### Check 3 — Acceptance criteria T002.1.bis: PASS for in-scope ACs

| AC | Verdict | Evidence |
|----|---------|----------|
| AC1 (Mira spec) | DONE pre-T1 | `docs/ml/specs/T002.1.bis-make-backtest-fn-spec.md` published; T0a-T0e 4 sign-offs cleared. |
| AC2 (Dex impl real) | **PASS** | Commit `1b7d7d9`. Real strategy logic at `cpcv_harness.py:587-1027` replaces neutral stub; signal_rule + triple-barrier + cost atlas + P126 filters all wired. |
| AC3 (per-fold P126 rebuild) | **PASS** | `backtest_fn_factory` accepted in `CPCVEngine.run` + `BacktestRunner.run` + `run_5_trial_fanout` (mutual-exclusivity enforced). DEFERRED-T11 docstring marked RESOLVED at L605-622 with ratification date + Aria authority chain. Helper `_build_daily_metrics_from_train_events` shipped at L324-397. Factory wired in `scripts/run_cpcv_dry_run.py:_run_phase` L890-926. |
| AC4 (cost atlas wiring) | **PASS-with-CONCERN** | Cost formula matches Mira §5.3 + Riven §11 verbatim at L850-861 (`(exit-entry)*sign*1*WDO_MULTIPLIER - 2*1*(brokerage + exchange_fees) - slippage`). `BacktestCosts.from_engine_config` consumed via closure capture. **F1 CONCERN:** SHA `acf449415a3c9f5d…` not surfaced in `determinism_stamp.json` due to missing `cost_atlas_path=` wiring in `_build_runner`. The atlas IS read (cost values flow into computation), but the audit trail is incomplete. |
| AC5 (anti-leak preserved) | **PASS** | `test_per_fold_anti_leak.py` 5/5 PASS. D-1 invariant verified at builder level (`m.day < as_of_date` per `Percentiles126dBuilder._select_window`). Per-fold isolation verified via `seed_anchor=split.path_id`. `as_of_date = min(test_events.session)` per Mira §3.4. |
| AC6 (n_trials=5 preserved) | **PASS** | `_resolve_trial_params` (L419-470) maps T1..T5 verbatim to Mira spec §1.2. `_interp_p50` + `_interp_p70` documented as deterministic engineering refactor at L400-417 (research-log `n_trials: 0` Bonferroni honesty preserved). |
| AC7 (non-degenerate distribution) | **PASS** | Empirical AC9 run: σ(sharpe) = 0.192 > 0 (vs N5 stub σ = 0.0). 45 path Sharpe samples in `full_report.json:226-236` materially distinct from stub baseline. Non-degenerate per spec K1 distribution shape requirement. |
| AC8 (Mira statistical clearance Gate 4) | DEFERRED | Mira authority post-Beckett N6 (T5 in story Tasks). Quinn does NOT pre-empt Mira Gate 4. **F2 CONCERN surfaces here**: Mira Gate 4 mini-council should weigh synthetic-walk vs real-tape-replay sufficiency — recommendation tracked. |
| AC9 (Beckett N6 PASS) | DEFERRED | Beckett T4 separate handoff. Empirical pre-flight (this run, AC9 self-test) already shows exit=0, wall 27.4s, peak RSS 0.158 GiB, 5+ artifacts. Beckett N6 finalization is a separate report path. |
| AC10 (KillDecision verdict) | **PASS-PARTIAL** | Verdict ∈ {GO, NO_GO} contract honored: `full_report.json:255 "verdict": "NO_GO"`. Reasons semanticly meaningful (`K3: IC_in_sample=0.000000 non-positive — no edge`) — NOT identity-zero stub. **F2 surfaced here**: NO_GO over synthetic walk does NOT yet authorize "T002 dies per spec K1/K2/K3 falsifiable" per AC10 contract — Phase F real-tape replay is the authoritative falsification test per Mira §7 + Aria T0b dual-phase carve-out. Quinn recommends Beckett N6 + Mira Gate 4 explicitly disclose this. |
| AC11 (cost atlas audit) | DEFERRED | Beckett N6 §X scope (T6 in story Tasks). F1 CONCERN material here — Beckett's audit will be incomplete without atlas SHA in determinism_stamp.json. |
| AC12 (regression tests) | **PASS** | `pytest tests/ -q --tb=no -m 'not slow'` → **607 passed, 4 skipped, 1 deselected** in 96.54 s. Vs N5 baseline 589 = +18 net = ZERO regression. Wall budget OK. |
| AC13 (Article IV trace) | **PASS** | Every clause sourced. New code blocks at `cpcv_harness.py:298-322` (synthetic-walk Article IV trace), L605-660 (Mira §1-§5 verbatim refs in docstring), L295-322 (DEFERRED-T11 RESOLVED ratification chain), `_resolve_trial_params` L429-431 ("Article IV: trial table maps verbatim to spec yaml v0.2.3 L307-321 + Mira spec §1.2. NO invented thresholds."). |

**Net AC verdict:** AC2-AC7+AC10+AC12+AC13 in-scope for this gate → **all PASS or PASS-PARTIAL with CONCERN tracked**. AC1 pre-Dex DONE. AC8/AC9/AC11 deferred to Mira/Beckett downstream by design (multi-handoff complex per River signature).

### Check 4 — No regressions: PASS

```
pytest tests/ -q --tb=no -m 'not slow'
607 passed, 4 skipped, 1 deselected in 96.54s (0:01:36)
```

Baseline (per story L383-384 + AC12): **589 passed, 4 skipped, 1 deselected** pre-T1.
Post-T1: **607 passed** (+18 net = exactly the new test files: 7 factory + 5 anti-leak + 6 toy-benchmark = 18 ✓). **ZERO regression**.

Sanity sub-suites:
- `tests/cpcv/`: 59/59 PASS — `CPCVEngine` invariants intact (purge formula, hold-out lock, partition determinism).
- `tests/contracts/`: 64/64 PASS — public API contracts preserved.
- `tests/cpcv_harness/`: 49/49 PASS — full harness suite green.
- `tests/cpcv_harness/test_make_backtest_fn.py`: 10/10 PASS — Aria §I.6 back-compat invariant verified for ALL existing AC2 tests (not just 6).

### Check 5 — Performance: PASS

Empirical AC9 (smoke + full, 2024-08-22 → 2025-06-30):

| Metric | Value | Budget | Margin |
|--------|-------|--------|--------|
| Wall-time | 27.4 s | < 300 s | 11× |
| Peak RSS | 0.158 GiB (~162 MB) | < 6 GiB cap (ADR-1 v3) | 38× |
| Wall vs Beckett T0c projection | 27.4 s | 6-15 s projected | ~2× over projection |

The 6× wall-time delta vs N5 baseline (4.4 s) reflects real strategy work (signal computation + triple-barrier walks + cost computation per event) — **expected and within budget**. The 2× over Beckett T0c upper-bound projection is acceptable per Dex caveat (synthetic walk is faster than real tape replay would be, so this is conservative-vs-real-Phase-F estimate). Performance PASS.

### Check 6 — Security: PASS-with-CONCERN (F1)

Threat surface evaluated:

- **`backtest_fn_factory` arbitrary code injection?** NO. The factory is a Python `Callable[[CPCVSplit, pd.DataFrame], Callable]` provided by the **caller** (`scripts/run_cpcv_dry_run.py:_backtest_fn_factory` is a closure pinned to in-process state). No `eval`, `exec`, `pickle.loads`, or untrusted-input deserialization in the engine path. Closure pattern is functional-safe.
- **Per-fold P126 rebuild escapes training set?** NO. Three layers of defense:
  1. `_build_daily_metrics_from_train_events` rejects DataFrames missing `session` column (test_per_fold_anti_leak Test 5 PASS).
  2. `Percentiles126dBuilder._select_window` enforces `m.day < as_of_date` D-1 invariant (test_per_fold_anti_leak Test 2 PASS).
  3. Factory contract: `as_of_date = min(test_events.session)` — anchored to the **fold's TEST slice** so any train day adjacent to test boundary is excluded (Mira §3.4).
- **Cost atlas SHA lock surfacing?** **CONCERN F1** — `cost_atlas_sha256` is `null` in determinism_stamp.json. Atlas content IS consumed (cost values flow into PnL); only the audit hash is missing. This is a provenance gap, not a tampering vulnerability — but for a system that locks atlas v1.0.0 to SHA `acf449415a3c9f5d…` per Riven T0d, the missing surfacing means a downstream consumer cannot verify atlas integrity from the determinism stamp alone. F1 patch ~5 LoC.
- **Hold-out lock?** UNTOUCHED. Engine still calls `assert_holdout_safe` on outer window before partition (`engine.py:141`). T002.0c AC12 invariant preserved.

### Check 7 — Documentation: PASS

- DEFERRED-T11 RESOLVED docstring at `cpcv_harness.py:605-622` (16 lines) — clearly marks ratification date, Aria authority chain, mechanism RATIFIED, implementation RESOLVED. PASS.
- Aria §I.1-§I.6 implementation guidance honored — every `§I.N` reference cited inline in source comments at: `engine.py:174` (§I.1), `runner.py:127` (§I.2), `cpcv_harness.py:331-333` (§I.5), `:719` (§I.6 back-compat), `scripts/run_cpcv_dry_run.py:877-885` (§I.3-§I.4). PASS.
- Mira spec §1-§5 cross-referenced in `make_backtest_fn` docstring at `cpcv_harness.py:640-660` (one bullet per §). PASS.
- Article IV trace block at `cpcv_harness.py:298-322` (24 lines) — surfaces the F2 caveat verbatim, clearly disclosing synthetic-walk methodology. **This block is the gold-standard transparency for the F2 CONCERN**: Mira Gate 4 mini-council inherits perfect provenance.
- Story file `docs/stories/T002.1.bis.story.md` Dex T1 Empirical Report at L359-456 — comprehensive (file table, test results, AC9 invocation, KillDecision breakdown, Toy benchmark validation, Anti-Article-IV self-audit 7 guards).

## 4. Synthetic-walk caveat F2 — surfaced for mini-council

**Severity:** MEDIUM (statistically meaningful per design, operationally clear-flag for downstream gates).

**Caveat verbatim** (per Dex T1 self-disclosure):
- Events DataFrame metadata-only — columns `(t_start, t_end, session, trial_id, entry_window)` per `build_events_dataframe` AC1; **no parquet replay** in `scripts/run_cpcv_dry_run.py`.
- Strategy logic over synthetic deterministic seeded price walk per `_walk_session_path` + `_walk_to_exit` (Mira §7 toy benchmark methodology).
- Real WDO trade tape replay = Phase F scope per spec §0 + Aria T0b dual-phase carve-out.

**Why it matters for Mira Gate 4 (AC8):**
- DSR > 0.95 / PBO < 0.5 / IC decay thresholds were spec'd against **real** distribution per Mira spec §6 canonical reproducible-backtest gating triplet.
- Current synthetic walk is **edge-neutral by construction** (μ=0 cost-drag) → KillDecision NO_GO is **expected**, NOT diagnostic of strategy claim.
- Toy benchmark proves the harness can discriminate edge from no-edge (mean Δ DSR > 0.10 across 5 seeds) — so a real-edge strategy WOULD pass DSR > 0.95 if present in real-tape data.

**Recommendation for mini-council Gate 4:**
- Option A: Mira Gate 4 statistical clearance evaluates synthetic-walk DSR/PBO/IC as **harness-correctness gate only**; real-tape Phase F replay is the **edge-existence gate**.
- Option B: Mira Gate 4 requires real-tape pre-condition (Phase F runs first) before issuing Gate 4 verdict.
- Option C: Mira Gate 4 issues conditional GO ("harness PASS over synthetic; final clearance pending Phase F") and unblocks Gate 5 disarm consideration on harness-correctness alone.

Quinn does NOT prescribe — Mira Gate 4 authority. Surfacing F2 as a CONCERN ensures Beckett N6 report + Mira Gate 4 mini-council have full provenance. Per ESC-010 6/6 protocol, mini-council 3-vote applies if council disagreement.

## 5. AC matrix (consolidated)

| AC | Status | Owner | Notes |
|----|--------|-------|-------|
| AC1  | DONE | Mira | T0a-T0e 4 sign-offs cleared pre-T1. |
| AC2  | PASS | Dex+Quinn | Real strategy at L587-1027, byte-equal closure determinism. |
| AC3  | PASS | Dex+Quinn | Factory pattern + DEFERRED-T11 RESOLVED. |
| AC4  | PASS-with-CONCERN F1 | Dex+Quinn | Cost formula correct; atlas SHA missing in stamp. |
| AC5  | PASS | Dex+Quinn | Anti-leak 5/5 + D-1 invariant. |
| AC6  | PASS | Dex+Quinn | T1..T5 verbatim. |
| AC7  | PASS | Dex+Beckett | σ(sharpe)=0.192>0 empirical. |
| AC8  | DEFERRED | Mira | Gate 4 — F2 CONCERN surfaced for mini-council. |
| AC9  | DEFERRED | Beckett | N6 separate (pre-flight already exit=0, < budget). |
| AC10 | PASS-PARTIAL | Dex+Quinn | NO_GO over real chain — F2 caveat tracked. |
| AC11 | DEFERRED | Beckett+Riven | F1 patch needed for full atlas audit trail. |
| AC12 | PASS | Quinn | 607 passed +18 ZERO regression. |
| AC13 | PASS | Dex+Quinn | Every clause sourced. |

## 6. Anti-Article-IV guards verified

Quinn confirms Dex T1 self-audit (story L436-446) by independent inspection:

| Guard | Quinn verdict |
|-------|---------------|
| #1 Dex impl gated on Mira spec PASS | OBSERVED — T0a-T0e all SIGNED before commit `1b7d7d9` |
| #2 NO subsample dataset | OBSERVED — empirical run uses full window 2024-08-22 → 2025-06-30 |
| #3 NO touch hold-out lock | OBSERVED — `engine.py:141` `assert_holdout_safe` call unchanged; `scripts/_holdout_lock.py` byte-identical to N5 baseline |
| #4 NO weaken Gate 4 thresholds | OBSERVED — DSR>0.95 / PBO<0.5 untouched in source (Mira spec L207-209 verbatim) |
| #5 NO subsample backtest run | OBSERVED — `--smoke` 30-day prelude permitted by spec; full window run executed too |
| #6 NO enforce Gate 5 disarm sem Gate 4 | OBSERVED — Dex impl scope only; Riven authority preserved |
| #7 NO push | OBSERVED — local commit `1b7d7d9` only on branch `t002-1-bis-make-backtest-fn`; @devops Gage gate preserved |

## 7. Quinn boundaries respected

- **NÃO modified code** (Dex authority) — gate file is documentation only.
- **NÃO pushed** (Gage authority) — branch local; gate file commit (if any) gated by user authorization.
- **NÃO mutated story AC / scope / title** (Pax authority) — gate file references story verbatim.
- **NÃO pre-empted Mira Gate 4** — F2 surfaced for mini-council, not pre-judged.
- **NÃO pre-empted Beckett N6** — recommendation: Beckett proceeds in parallel.

## 8. Next handoff

**Verdict CONCERNS** → Beckett N6 PROCEEDS in parallel, with two follow-ups tracked:

1. **F1 fix (Dex or follow-up commit, ~5 LoC):** wire `cost_atlas_path=docs/backtest/nova-cost-atlas.yaml` + `rollover_calendar_path=` into `_build_runner` so determinism_stamp.json carries `cost_atlas_sha256`. Recommended **before** Beckett N6 finalization so the AC11 audit is complete.
2. **F2 surfacing for Mira Gate 4 mini-council (Beckett N6 report § cost atlas audit + § statistical clearance):** explicitly document synthetic-walk caveat — mini-council 3-vote per ESC-010 protocol on Option A/B/C choice.

Parallel work items:
- @beckett (T4): N6 empirical re-run + report `docs/backtest/T002-beckett-cpcv-real-backtest-N6-2026-04-29.md`.
- @dex or follow-up commit: F1 patch (5 LoC).
- @mira (T5): statistical clearance compute (gated on Beckett N6 PASS + F2 mini-council resolution).

## 9. Authority chain

```
Validator: Quinn (@qa) — QA Gate Guardian
Story: T002.1.bis
Phase: 4 (QA Gate, story-lifecycle.md)
Verdict: CONCERNS
Score: 6 PASS / 1 CONCERN / 0 FAIL (out of 7 quality checks); 0 HIGH / 2 MEDIUM / 2 LOW issues
Authority: docs/qa/gates/qa-gate.md 7-point checklist
Council fidelity: ESC-010 6/6 ratification preserved (Track B 6/6 F2-with-binding-F3-commitment + Mira F2 statistical authority + Riven Gate 4 criteria + §9 HOLD #2 Gates 2-5 spec)
Article IV trace: every clause inspected — NO INVENTION confirmed; F2 caveat is design-disclosed (Mira spec §7 + Aria T0b dual-phase carve-out), not invented
R15 compliance: gate file new artifact under docs/qa/gates/ (canonical naming `T002.1.bis-qa-gate.md` matching prior precedent T002.0[a-h]-qa-gate.md)
Boundary: code UNMODIFIED (Dex authority); branch UNPUSHED (Gage authority); story scope UNMUTATED (Pax authority); Mira Gate 4 UNPRE-EMPTED (Mira authority)
Next handoff: Beckett N6 (T4) — parallel-eligible with F1 patch + F2 mini-council surfacing
Cosign: Quinn 2026-04-29 BRT (Autonomous Daily Session, QA gate guardian authority)
```

— Quinn (@qa)
