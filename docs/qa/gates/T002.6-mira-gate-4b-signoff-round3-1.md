---
verdict: GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed
verdict_sub_classification: costed_out_edge_oos_confirmed_K3_passed
gate: 4b
sub_gate: edge-existence-clearance
phase: F2-T9.1 (Round 3.1 FINAL — post-N8.2 Phase G real-tape PROPER unlock with --phase G + holdout_locked=False wired per R18)
story: T002.7 (close trigger per ESC-013 §5(c))
authority: Mira (@ml-researcher)
date_brt: 2026-05-01
voter: Mira @ml-researcher
branch: t002-7-phase-g-prep
n8_2_run_dir: data/baseline-run/cpcv-dryrun-auto-20260501-3fce65dab8f8/
n8_2_run_id: 3da49a82343e4650abfd0d4921ee1897
n8_2_backtest_phase_observed: G (PROPER, not F)
n8_2_holdout_locked_observed: False (Phase G unlock wired per ESC-013 R18)
n8_2_ic_holdout_status_observed: 'computed' (verdict reasons empty; K3 PASS COMPUTED)
n8_2_wall_minutes: 141.32
n8_2_timestamp_brt: 2026-05-01T02:55:55
spec_yaml_version: T002-v0.2.3
gate_4b_spec: docs/ml/specs/T002-gate-4b-real-tape-clearance.md@v1.2.0
council_provenance_round1: docs/councils/COUNCIL-2026-04-29-ESC-011-resolution.md
council_provenance_round2: docs/councils/COUNCIL-2026-04-30-ESC-012-resolution.md
council_provenance_round3_1: docs/councils/COUNCIL-2026-04-30-ESC-013-resolution.md
prr_provenance: docs/governance/PRR-20260430-1-T002.7-phase-g-disposition.md
esc_011_ref: COUNCIL-2026-04-29-ESC-011 (Round 1 framing)
esc_012_ref: COUNCIL-2026-04-30-ESC-012 (Round 2 → 3 path adjudication; 5/6 SUPERMAJORITY APPROVE_PATH_B)
esc_013_ref: COUNCIL-2026-04-30-ESC-013 (Round 3.1 protocol-corrected re-run; 5/5 UNANIMOUS APPROVE_PATH_IV)
esc_013_outcome_branch: c (K1_FAIL_OOS_marginal — DSR drops back below 0.95 with K3 PASS)
esc_014_trigger: false
strict_thresholds_unmovable: true
anti_article_iv_guard_4_invoked: true
anti_article_iv_guard_8_invoked: true
gate_5_pre_disarm: false
disposition: final_fail_strategy_edge_oos_confirmed_K3_passed
disposition_rationale: |
  N8.2 executed under PROPER Phase G protocol (--phase G CLI flag + holdout_locked=False propagated per
  ESC-013 R18 Dex caller wiring). Verdict reasons empty, K1+K2+K3 all engine-PASS but PRR R15 strict
  trigger NOT met because K1 strict (DSR > 0.95) FAILS at DSR_OOS=0.205731 (0.744 below threshold).
  K3 decay COMPUTED-PASS (verdict reasons empty; ic_decay_passed=True; IC magnitude ~identical across
  windows IS=0.866010 / OOS=0.865933, ratio=0.99991 ≫ 0.5 decay floor). PRR §3.3 R17 NOT triggered
  because IC_holdout=0.865933 ≫ 0.5 × IC_in_sample=0.433005 (R17 requires IC_holdout ∈ [0.3, 0.5×IC_IS)).
  R15 fails (DSR<0.95). R16 fails (IC_holdout > 0.3). R17 fails (IC_holdout > 0.5×IC_IS). The K3
  COMPUTED-PASS + K1 OOS-FAIL combination matches ESC-013 §5 outcome (c) exactly: "K1_FAIL_OOS_marginal
  (DSR drops back below 0.95 with K3 PASS) → costed_out_edge_oos_confirmed_K3_passed (refined from Round 2);
  T002 retire with refined diagnostic". The N8 surprise DSR=0.965085 was a Phase F-protocol-over-holdout
  -window artifact (in-sample-DSR-over-OOS-window measurement under hardcoded holdout_locked=True);
  N8.2 PROPER unlock reveals the real OOS DSR=0.205731. K3 decay COMPUTED-PASS confirms predictor↔label
  rank-stability cross-window (IC robust 0.866 IS/OOS), but the realized PnL distribution under cost +
  friction + triple-barrier early exits is materially worse OOS: sharpe_mean -0.053 (NEGATIVE),
  hit_rate 0.472 (sub-50% trade-level), profit_factor 0.929 (LOSING money OOS gross-of-deployment).
  This is the canonical costed_out_edge signature CONFIRMED out-of-sample with K3 PASSED — refines
  Round 2 same-bucket diagnostic with stronger OOS evidence. ESC-013 §5(c) action: T002 retire with
  refined diagnostic; cost reduction R&D path Path A explicitly OFF per user constraint; Path C
  (ESC-012 fallback) auto-trigger for T002.7 close ceremony. ESC-014 NOT triggered (K1 strict FAIL
  pre-empts surprise PASS scenario; INCONCLUSIVE branch §3.4 NOT applicable because verdict computed
  not deferred). Gate 5 R10 fence preserved verbatim (K1 strict bar UNMOVABLE; Gate 5 capital ramp
  authority remains LOCKED).
supersedes: |
  docs/qa/gates/T002.6-mira-gate-4b-signoff-round3.md (Round 3 GATE_4_INCONCLUSIVE_pending_phase_g_proper —
  superseded by Round 3.1 post-N8.2 PROPER Phase G run; Round 3 sign-off integrity preserved per spec §15
  append-only revision discipline);
  docs/qa/gates/T002.6-mira-gate-4b-signoff-round2.md (Round 2 GATE_4_FAIL_strategy_edge / costed_out_edge —
  refined Round 3.1 with OOS-confirmed K3-passed evidence; same bucket, stronger diagnostic);
  docs/qa/gates/T002.6-mira-gate-4b-signoff.md (Round 1 GATE_4_FAIL_data_quality — superseded Round 2
  upon IC pipeline wiring close; Round 1 integrity preserved)
final_round: true
---

# T002.7 — Mira Gate 4b Edge-Existence Clearance Sign-off (Round 3.1 FINAL — Phase G OOS PROPER Re-clearance)

> **Verdict:** **`GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed`** (per ESC-013 §5 outcome (c)).
> **Date (BRT):** 2026-05-01.
> **Branch:** `t002-7-phase-g-prep` @ HEAD post-R18 Dex wiring + post-N8.2 artifacts landed.
> **N8.2 run dir:** `data/baseline-run/cpcv-dryrun-auto-20260501-3fce65dab8f8/` (run_id `3da49a82343e4650abfd0d4921ee1897`; timestamp_brt `2026-05-01T02:55:55`; wall 141 min 19 s).
> **Authority basis:** Mira (@ml-researcher) under ESC-013 R12 (F2-T9.1 re-clearance authority post-PROPER-Phase-G), Mira Gate 4b spec v1.2.0 §1 + §9 + §10 + §15.13 + §15.13.5 + §15.13.7 + §15.13.8, PRR-20260430-1 §3.1-§3.4 4-branch disposition rule + ESC-013 §5 outcome decision tree.
> **Supersedes:** Round 3 sign-off `signoff-round3.md` (INCONCLUSIVE_pending_phase_g_proper); Round 2 sign-off `signoff-round2.md` (FAIL_strategy_edge / costed_out_edge); Round 1 sign-off `signoff.md` (FAIL_data_quality). All prior rounds preserved per spec §15 append-only revision discipline. Round 3.1 FINAL.
> **Final round:** YES — closure of T002.7 Gate 4b adjudication chain.

---

## §1 Executive verdict + ESC-013 outcome (c) match

**Verdict label:** `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed` (per Mira Gate 4b spec v1.2.0 §9 verdict-label discipline + §15.13.8 disposition rule + PRR-20260430-1 §3.3 R17 strict reading + ESC-013 §5(c) outcome match).

**Sub-classification (Round 3.1 refinement):** `costed_out_edge_oos_confirmed_K3_passed` — refines Round 2 `costed_out_edge` diagnostic with explicit OOS confirmation under K3 COMPUTED-PASS. The bucket attribution remains `strategy_edge` (Riven 3-bucket §7.1 Bucket B); the sub-classification adds `_oos_confirmed_K3_passed` to capture: (a) K3 decay test was COMPUTED in PROPER Phase G unlock and PASSED (IC_holdout/IC_in_sample = 0.99991 ≫ 0.5 threshold); (b) K1 strict deployment bar (DSR > 0.95) FAILED at DSR_OOS = 0.206 (0.744 below threshold); (c) costed-out edge hypothesis Round 2 is OOS-confirmed: predictor-rank stability holds across windows (IC robust ~0.866 IS+OOS) but realized PnL distribution under cost + friction + triple-barrier layer is materially worse OOS than IS (sharpe_mean +0.046 IS → -0.053 OOS; profit_factor 1.075 IS → 0.929 OOS). This is the strongest possible diagnostic for the `costed_out_edge` paradigm: the predictor IS valid (rank stability OOS confirmed via K3 decay); the strategy IS NOT deployable (DSR OOS materially below 0.95 threshold).

**ESC-013 §5 outcome decision tree application** (verbatim from `docs/councils/COUNCIL-2026-04-30-ESC-013-resolution.md` §5):

| Outcome | Trigger condition | N8.2 evidence | Match? |
|---|---|---|---|
| (a) **K1+K2+K3 ALL COMPUTED PASS** (DSR>0.95 + PBO<0.5 + IC_holdout > 0.5×IC_in_sample) | Triple PASS → ESC-014 council convene to adjudicate K1 strict bar interpretation under OOS-PROPER surprise PASS | DSR_OOS = 0.206 < 0.95 → K1 STRICT FAIL | **NO MATCH** (K1 OOS strict FAIL pre-empts surprise PASS) |
| (b) **K3_FAIL_decay_collapse** (IC_holdout < 0.5 × IC_in_sample) | T002 falsified clean per spec §0; Riven 3-bucket reclassify N8.2 → strategy_edge_oos_K3_falsified; T002 retire ceremony | IC_holdout=0.865933 ≫ 0.5 × IC_in_sample=0.433005; K3 PASS computed | **NO MATCH** (K3 COMPUTED-PASS not collapse) |
| (c) **K1_FAIL_OOS_marginal** (DSR drops back below 0.95 with K3 PASS) | costed_out_edge_oos_confirmed_K3_passed (refined from Round 2); T002 retire with refined diagnostic; cost reduction R&D path (BLOCKED by user constraint Path A OFF) | DSR_OOS=0.206 < 0.95 (K1 FAIL OOS) AND K3 PASS COMPUTED (decay test 0.99991 > 0.5 threshold) | **MATCH** ✓ |
| (d) **INCONCLUSIVE_again** | ESC-014 escalation (procedural double-failure); deeper protocol audit; T002 retire as default | Verdict computed (kill_decision.verdict='GO' engine-level; verdict.reasons empty); not deferred | **NO MATCH** (verdict computed under PROPER Phase G unlock) |

**Outcome (c) match: ESC-013 §5 prescribes** — (i) verdict label `costed_out_edge_oos_confirmed_K3_passed` (refined from Round 2); (ii) T002 retire with refined diagnostic; (iii) cost reduction R&D path BLOCKED by user constraint Path A OFF; (iv) Path C (ESC-012 §5 fallback) auto-trigger for T002.7 close ceremony; (v) Riven F2-T6.1 Round 3.1 reclassification trigger; (vi) Pax T002.7 close trigger.

**Why FAIL not PASS:** Mira Gate 4b spec §1 thresholds are UNMOVABLE per Anti-Article-IV Guard #4 (ESC-011 R14 + ESC-012 R3 hash-frozen + ESC-013 R20 contract test). The joint conjunction `(DSR > 0.95) AND (PBO < 0.5) AND (IC_in_sample > 0 AND CI95_lower > 0) AND (IC_holdout > 0.5 × IC_in_sample)` per §1 + §15.13.5 decision rule is NOT satisfied:

- **K1 (DSR > 0.95):** observed N8.2 OOS `0.205731` → **STRICT FAIL** (0.744 below threshold; ~3.6× worse than IS).
- **K2 (PBO < 0.5):** observed N8.2 OOS `0.059524` → **PASS** (0.441 below threshold; well below 0.25 ideal sub-threshold).
- **K3 in-sample binding (IC > 0 AND CI95_lower > 0):** observed N8.2 OOS IC=`0.865933`, CI95=`[0.864880, 0.866025]`, lower=`0.864880 > 0` → **PASS strict**.
- **K3 decay sub-clause (IC_holdout > 0.5 × IC_in_sample) — Phase G full evaluation:** observed N8.2 IC_holdout=0.865933 vs F2 IC_in_sample=0.866010; decay ratio = 0.99991 > 0.5 threshold → **COMPUTED PASS**. Verdict reasons array empty; ic_holdout_status='computed' propagated; OBS-1 short-circuit DID NOT fire.

**Joint AND conjunction PRR §3.1 R15 strict** (`K1 OOS-PASS ∧ K2 OOS-PASS ∧ K3_in_sample_PASS ∧ K3_decay_COMPUTED_PASS`): K1 OOS FAIL ∧ K2 OOS PASS ∧ K3_in_sample PASS ∧ K3_decay COMPUTED PASS → **R15 trigger NOT met** (K1 fails). Verdict routes to PRR §3.3 R17 inspection.

**PRR §3.3 R17 inspection** (`IC_holdout ∈ [0.3, 0.5 × IC_in_sample) AND DSR_OOS < 0.95`): IC_holdout=0.865933 NOT in [0.3, 0.433005); DSR_OOS=0.206 < 0.95. R17 condition (a) fails (IC_holdout above 0.5×IC_IS). R17 trigger NOT met. Verdict routes to PRR §3.4 INCONCLUSIVE? **No** — INCONCLUSIVE is residual partition for borderline thresholds (DSR_OOS ∈ [0.90, 0.95]); DSR_OOS=0.206 is materially below borderline. Per PRR §3.4 Branch INCONCLUSIVE row: "any combination not cleanly matching R15/R16/R17 → INCONCLUSIVE". The N8.2 outcome is **a clean partition residual**: DSR_OOS << 0.95 strict + IC_holdout > 0.5×IC_IS. This combination is NOT a "borderline thresholds" or "regime mismatch" failure mode (PRR §3.4 INCONCLUSIVE description). It is the ESC-013 §5(c) explicit outcome — **K1_FAIL_OOS_marginal with K3 PASS** — which post-dates PRR-20260430-1 §3.4 INCONCLUSIVE branch enumeration.

**Mira Round 3.1 authoritative reading:** PRR §3 4-branch partition was hash-frozen pre-N8 over the assumed K3-status-deferred state space. ESC-013 §5 4-branch decision tree post-dates the PROPER-Phase-G capability and explicitly enumerates the (c) outcome `K1_FAIL_OOS_marginal + K3 PASS`. The two governance documents are sequentially consistent: PRR §3 frames the strict-trigger conjunction; ESC-013 §5 specifies the disposition under each strict-trigger-failure decomposition. Outcome (c) is the binding disposition — **`GATE_4_FAIL_strategy_edge` bucket with sub-classification `costed_out_edge_oos_confirmed_K3_passed`**.

**Disposition:** FINAL `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed`. Recommendation per ESC-013 §5(c):
- (a) **T002 retire ceremony** triggered (Path C ESC-012 §5 fallback auto-activate);
- (b) **Riven F2-T6.1 Round 3.1 reclassify** ledger entry in `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` per §15.13.8 disposition rule + ESC-013 §5(c) explicit outcome;
- (c) **Pax T002.7 close** trigger (story status DONE | ARCHIVED per ESC-013 §6 closure conditions row 6);
- (d) **ESC-014 NOT triggered** (K1 strict OOS FAIL pre-empts surprise PASS scenario per ESC-013 §5(a); INCONCLUSIVE_again §5(d) NOT applicable because verdict computed not deferred);
- (e) **Gate 5 R10 fence preserved verbatim** (K1 strict bar UNMOVABLE; Gate 5 capital ramp authority LOCKED; Anti-Article-IV Guard #4 honored).

No Gate 5 progression Round 3.1 (R5/R6 + ESC-012 R10 fence preserved per §7 below).

---

## §2 Threshold table strict (K1 FAIL, K2 PASS, K3 COMPUTED PASS, all reasons empty)

Per Mira Gate 4b spec v1.2.0 §1 + §15.13.5 + parent spec yaml `T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` v0.2.3 §kill_criteria L207-209 (Anti-Article-IV Guard #4 — UNMOVABLE):

| Criterion | Spec §1 + §15.13.5 threshold (UNMOVABLE) | F2 N7-prime in-sample (2024-08-22..2025-06-30) | N8 Phase F-over-holdout (artifact) | **N8.2 Phase G PROPER OOS (2025-07-01..2026-04-21)** | Verdict Round 3.1 (strict §15.13.8 4-branch reading) |
|---|---|---|---|---|---|
| **K1 — DSR (Deflated Sharpe Ratio)** | `DSR > 0.95` | `0.766987` | `0.965085` (artifact) | **`0.205731`** (`full_report.json:8`) | **STRICT FAIL** (0.744 below threshold; honestly measured under PROPER Phase G unlock) |
| **K2 — PBO (Probability of Backtest Overfitting)** | `PBO < 0.5` (ideal `< 0.25`) | `0.337302` | `0.162698` | **`0.059524`** (`full_report.json:9`) | **PASS** strict (0.441 below threshold; far below 0.25 ideal) |
| **K3 in-sample binding (`IC > 0 AND CI95_lower > 0`)** | strict §1 + §15.10 + §15.13 carry-forward | IC=`0.866010`, CI95_lower=`0.865288 > 0` (PASS) | IC=`0.865933` (over holdout window, F-protocol artifact) | IC=`0.865933`, CI95=`[0.864880, 0.866025]`, lower=`0.864880 > 0` (`full_report.json:3-7`) | **PASS** strict (IC magnitudes near-IDENTICAL across windows; Δ 0.000077, ratio 0.99991) |
| **K3 decay sub-clause (`IC_holdout > 0.5 × IC_in_sample`)** — Phase G full evaluation per §15.13.5 | `IC_holdout > 0.5 × 0.866010 = 0.433005` | n/a (in-sample reference) | NOT COMPUTED (deferred sentinel) | **IC_holdout=0.865933 > 0.433005** | **COMPUTED PASS** strict (decay ratio 0.99991 ≫ 0.5; verdict.reasons=[] empty; ic_holdout_status='computed' propagated; OBS-1 short-circuit DID NOT fire) |
| **Joint AND conjunction (PRR §3.1 R15 strict)** | `K1 OOS-PASS ∧ K2 OOS-PASS ∧ K3_in_sample_PASS ∧ K3_decay_COMPUTED_PASS` | n/a | n/a | K1 OOS FAIL ∧ K2 OOS PASS ∧ K3_in_sample PASS ∧ K3_decay COMPUTED PASS | **R15 trigger NOT met** (K1 strict FAIL); routes to ESC-013 §5 outcome decision tree |
| **ESC-013 §5 outcome (c) match** | K1_FAIL_OOS_marginal + K3 PASS → costed_out_edge_oos_confirmed_K3_passed; T002 retire with refined diagnostic | n/a | n/a | K1 OOS FAIL (DSR 0.206 < 0.95) AND K3 PASS COMPUTED | **MATCH** → final verdict `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed` |

**Sample-size R9 compliance (spec §6 + §15.13.9):** N8.2 delivered the same n_paths=45 CPCV combinatorial × n_trials_used=5 = 225 path-trial outcomes. Sample-size headroom preserved per F2 N7-prime + N8 carry-forward (74-127× over 250 floor). AC9 floor `INCONCLUSIVE` route NOT triggered. Bonferroni n_trials=5 carry-forward PASS (`n_trials_source: docs/ml/research-log.md@fadacf482633c4d23cdaf21edc986ca1f879752b` per N8.2 metrics; ESC-012 R6 reusability invariant binding).

**Verdict reasons array — Round 3.1 critical evidence:** `full_report.json:256` shows `"reasons": []` (empty array). This is the canonical Phase G unlock signature: K3 decay test was COMPUTED (not deferred); K1 + K2 + K3 all engine-PASS at the per-checkbox level (`k1_dsr_passed=true, k2_pbo_passed=true, k3_ic_decay_passed=true` per `full_report.json:257-259`). The empty reasons array confirms (a) ic_holdout_status='computed' propagated correctly per ESC-013 R18 Dex wiring; (b) OBS-1 short-circuit DID NOT fire (would have produced reason text `K3: DEFERRED — ic_holdout_status='deferred'`); (c) PROPER Phase G protocol honored.

**Caveat — `kill_decision.verdict = "GO"` is engine-level not spec-binding:** the engine-level verdict aggregates K1+K2+K3 into a Boolean conjunction with `dsr_floor=0.0` (Bailey-LdP minimum-positivity gate at `packages/vespera_metrics/report.py:586`). Mira spec §1 strict bar `DSR > 0.95` is materially stricter and is the binding deployment threshold per Anti-Article-IV Guard #4 + ESC-012 R3 hash-frozen + ESC-013 R20 contract test. Engine `verdict=GO` is informational; PRR §3.1 R15 trigger requires DSR>0.95 strict; N8.2 DSR=0.206 fails strict by 0.744. The Round 3.1 binding reading is **K1 STRICT FAIL** despite engine-level GO emission.

**Auxiliary distribution diagnostics (N8.2 Phase G OOS PROPER vs F2 N7-prime in-sample):**

| Statistic | F2 N7-prime in-sample | N8 (F-protocol-over-holdout artifact) | **N8.2 Phase G OOS PROPER** | Δ vs F2 IS | Δ vs N8 |
|---|---|---|---|---|---|
| `sharpe_mean` | +0.046251 | +0.101222 (artifact) | **-0.053056** (line 237) | -0.099307 (NEGATIVE OOS) | -0.154278 (artifact correction) |
| `sharpe_median` | +0.050216 | +0.094842 | -0.059359 (line 238) | -0.109575 (NEGATIVE) | -0.154201 |
| `sharpe_std` | 0.185048 | 0.177315 | 0.184340 (line 239) | -0.000708 (≈ identical) | +0.007025 |
| `sortino` | +0.109395 | +0.184163 | -0.121502 (line 240) | -0.230897 (NEGATIVE) | -0.305665 |
| `mar` | n/a | n/a | 0.0 (line 241) | n/a | n/a |
| `ulcer_index` | n/a | n/a | 0.888747 (line 242) | n/a | n/a |
| `max_drawdown` | 0.995481 | 0.753794 | **1.273941** (line 243) | +0.278460 (worse) | +0.520147 |
| `profit_factor` | 1.075107 | 1.155304 | **0.929058** (line 244) | -0.146049 (BELOW 1.0) | -0.226246 |
| `hit_rate` | 0.496698 | 0.472377 | 0.472377 (line 245) | -0.024321 (sub-50%) | IDENTICAL N8 (same OOS sample) |
| `n_paths` | 225 (45 × 5 trials) | 225 | 225 (line 246) | IDENTICAL | IDENTICAL |

**Sample-of-sharpe distribution N8.2 OOS** (per `full_report.json:10-235`): 225 path-Sharpe values; range [-0.7249, +0.6422]; mean -0.053 (NEGATIVE); median -0.0594 (NEGATIVE); >50% of paths show negative OOS Sharpe. The IS distribution (F2 N7-prime) was barely positive (mean +0.046); the OOS distribution is barely negative (mean -0.053). The shift from +0.046 IS to -0.053 OOS is exactly 0.099 — within one standard deviation of either distribution (~0.185), but the consistent sign-flip across mean and median + the profit_factor falling from 1.075 IS to 0.929 OOS + the Sortino flipping from +0.109 IS to -0.122 OOS demonstrates the strategy is materially worse OOS than IS at the realized PnL layer. This is the strongest possible empirical signature of `costed_out_edge` confirmed OOS with K3 PASSED: predictor-rank stability holds (IC ~0.866 IS+OOS) but realized PnL distribution under cost+friction+triple-barrier exits CONFIRMS the cost-foreclosure hypothesis cleanly out-of-sample.

---

## §3 N8 surprise DSR=0.965 artifact disambiguation (Phase F-over-holdout vs Phase G unlock proper)

This §3 is the **central Round 3.1 evidence**: the Round 3 N8 surprise DSR=0.965085 was an artifact of running Phase F protocol (`holdout_locked=True` hardcoded) over the Phase G holdout-window parquet. N8.2 PROPER unlock with `--phase G` CLI flag + `holdout_locked=False` propagated reveals the real OOS DSR=0.205731.

### §3.1 N8 protocol-violation signature

`scripts/run_cpcv_dry_run.py:1088-1102` pre-R18 (the production callsite for `compute_ic_from_cpcv_results`):

```python
if phase == "F":
    ic_result = compute_ic_from_cpcv_results(
        cpcv_results,
        seed_bootstrap=seed,
        n_resamples=10_000,
        holdout_locked=True,        # <-- HARDCODED True; §15.13.2 unfulfilled in N8
    )
```

The `--phase` CLI flag pre-R18 accepted only `{"E", "F"}` — Phase G was NOT a recognized choice. N8 ran under Phase F semantics: `holdout_locked=True`, `ic_holdout_status='deferred'`, `ic_holdout=0.0` field default; OBS-1 short-circuit fired in `evaluate_kill_criteria` per Dex `fadacf4`; verdict-layer reported `k3_ic_decay_passed=true` with reason `K3: DEFERRED — ic_holdout_status='deferred' — decay test pending Phase G unlock per Mira spec §15.10 (Phase F2 binding: IC_in_sample=0.865933 > 0 PASS)`.

**N8 DSR=0.965085 interpretation (Round 3.1 retrospective):** the N8 DSR was computed as the in-sample-DSR-over-holdout-window measurement under the Phase F protocol. Bailey-LdP DSR is computed from per-trial mean Sharpe distribution at `packages/vespera_metrics/report.py:481-514`; in Phase F the n_trials=5 carry-forward + the holdout-window CPCV path-PnL produced a relatively strong N8 mean Sharpe (+0.101 vs F2 IS +0.046) which propagated into a high DSR estimate (0.965 vs F2 IS 0.767). This was NOT an OOS-DSR measurement; it was an IS-DSR measurement over the OOS-window data under the F-protocol short-circuit semantics.

### §3.2 N8.2 PROPER Phase G protocol

Post-R18 Dex caller wiring (per ESC-013 R18 mandate, ~5-30 LoC `scripts/run_cpcv_dry_run.py`):

```python
if phase == "G":
    ic_result = compute_ic_from_cpcv_results(
        cpcv_results,
        seed_bootstrap=seed,
        n_resamples=10_000,
        holdout_locked=False,       # <-- Phase G unlock: holdout=AVAILABLE per §15.13.2
    )
    # Propagation: ic_holdout_status='computed' (was 'deferred' Phase F2)
    # Decay test full evaluation: IC_holdout > 0.5 × IC_in_sample becomes BINDING K3 sub-clause
```

N8.2 ran under Phase G semantics: `holdout_locked=False`, `ic_holdout_status='computed'`, IC_holdout populated with measured rank-correlation = 0.865933; OBS-1 short-circuit DID NOT fire (verdict.reasons=[] empty); decay test fully evaluated; K3 decay PASS at decay ratio 0.99991. R20 contract test (NEW per ESC-013) verifies the verdict-vs-reason invariant: K3 PASS reason text MUST contain computed numbers (or be empty for the unanimous-PASS case), NOT sentinel strings. N8.2 satisfies R20 with empty reasons.

### §3.3 The DSR sign-flip 0.965 → 0.206 (Round 3.1 forensic)

The DSR drop from N8 0.965 to N8.2 0.206 is **not a re-computation artifact**: both runs use the SAME holdout-window parquet (per ESC-013 R19 strict re-run discipline; only `dataset_sha256` may differ if parquet root or trade content shifts; engine_config / spec / atlas / calendar / cpcv_config / Bonferroni / latency / RLP / microstructure ALL IDENTICAL). The drop is the **artifact-correction signature**: N8 DSR was the F-protocol short-circuit (IS-DSR-over-OOS-window mode); N8.2 DSR is the proper OOS-DSR under Phase G unlock semantics.

The mechanism: Bailey-LdP DSR factors in (a) per-trial mean Sharpe; (b) skewness; (c) kurtosis; (d) n_trials Bonferroni correction. Under PROPER Phase G unlock, the DSR computation respects the holdout-window's actual PnL distribution properties — which now show NEGATIVE sharpe_mean (-0.053), suppressed Sortino (-0.122), elevated max_drawdown (1.274 vs 0.995 IS), and profit_factor below 1 (0.929 vs 1.075 IS). The DSR drops to 0.206 because the underlying inputs now reflect the realized OOS PnL distribution, not the F-protocol artifact.

**Mira Round 3.1 forensic reading:** the N8 DSR=0.965 was a procedural artifact, not real signal. ESC-013 §5(c) outcome match is evidence-grounded: K1 strict deployment bar STRICT FAILS at OOS DSR=0.206 << 0.95 threshold. The Round 2 verdict label `costed_out_edge` is OOS-confirmed. The Round 3 INCONCLUSIVE verdict is now resolved.

### §3.4 Why the artifact disambiguation matters for Article IV

Anti-Article-IV Guard #4 (ESC-011 R14 + ESC-012 R3 + ESC-013 R20): "K1 strict bar UNMOVABLE — DSR > 0.95 / PBO < 0.5 / IC > 0 thresholds CANNOT be relaxed under outcome pressure". The N8 DSR=0.965 was a near-miss surprise (above 0.95 threshold by 0.015); had Round 3 ratified `GATE_4_PASS_oos_confirmed` based on the artifact, it would have constituted a soft Article IV violation (passing at threshold based on an unreliable measurement). Mira Round 3 INCONCLUSIVE verdict correctly held the strict reading: K3 status DEFERRED-sentinel ≠ COMPUTED-PASS, so R15 trigger NOT met regardless of DSR appearing above threshold. ESC-013 §3 ratified Mira reading 5/5 UNANIMOUS APPROVE_PATH_IV. N8.2 PROPER Phase G run vindicates the Mira Round 3 strict reading: under PROPER unlock the DSR collapses to 0.206 — the K1 strict bar holds.

The Round 3 → Round 3.1 protocol-correction chain is **Article IV exemplary**: a near-miss surprise was held to strict-reading scrutiny; the strict reading prescribed protocol-corrected re-run; the re-run revealed the artifact and confirmed the Round 2 costed_out_edge diagnostic with stronger OOS evidence. NO threshold relaxation occurred; NO post-hoc disposition rule mutation occurred; NO favorable interpretation of artifact data was retained. The strict reading was preserved through three rounds.

---

## §4 IC=0.866 robust OOS interpretation (signal real but costs eat it)

### §4.1 Cross-window IC stability evidence

The N8.2 OOS IC measurement is the canonical Phase G K3 decay test:

| Window | Period | IC_spearman | CI95 | n_events |
|---|---|---|---|---|
| F2 N7-prime in-sample | 2024-08-22..2025-06-30 | 0.866010 | [0.865288, 0.866026] (width 0.000738) | 3800 |
| **N8.2 OOS PROPER** | **2025-07-01..2026-04-21** | **0.865933** | **[0.864880, 0.866025]** (width 0.001145) | **3700** |

**Decay ratio:** 0.865933 / 0.866010 = 0.99991 ≫ 0.5 threshold. K3 decay test PASS by ~99% margin. Predictor-rank stability holds across windows under the same predictor↔label binding (`predictor=-intraday_flow_direction`, `label=ret_forward_to_17:55_pts`; both rank-correlations computed identically; both pre-filter on `filter_active(event)`).

### §4.2 Why the IC stability is statistically valid

1. **Bootstrap CI95 non-degeneracy preserved cross-window:** F2 IS width = 0.000738; N8.2 OOS width = 0.001145. Both consistent with paired-resample bootstrap n=10000 PCG64 seed=42 paired-index strategy per spec §15.4. CI widths are tight, comparable order of magnitude (~50% wider OOS reflects slightly smaller n_events 3700 vs 3800).
2. **CI95_lower bound > 0 cross-window:** F2 IS lower 0.865288; N8.2 OOS lower 0.864880. Both >> 0; both PASS the Phase F2 + Phase G K3 in-sample binding clause strictly.
3. **Sample size:** N8.2 OOS n_events=3700 (≫ 30 minimum per §15.7 dedup invariant; matches `events_metadata.n_events=3700`). 123× over 30 floor.
4. **Decay test verbatim:** PRR §3.1 R15 conjunct #4 "IC_holdout > 0.5 × IC_in_sample (decay sub-clause; full evaluation per Mira spec §15.13.5)" → 0.865933 > 0.433005 PASS by 0.433 (orders of magnitude above threshold).

### §4.3 IC=0.866 interpretation under cross-window confirmation

The Round 2 §3.4 reasoning chain (engineering correctness vs strategy edge interpretation) is **OOS-CONFIRMED**:

- **Pure leakage:** would predict hit_rate ≈ IC saturating near 0.95+. N8.2 hit_rate=0.472. **REJECTED OOS** as in IS.
- **Spurious correlation noise:** would predict CI95 widening or sign-flip OOS. N8.2 CI95=[0.865, 0.866] tight, sign preserved. **REJECTED OOS** as in IS.
- **Real edge fully realized:** would predict OOS hit_rate tracking IC at deployable rate + DSR>0.95 OOS. N8.2 hit_rate=0.472, DSR=0.206. **REJECTED OOS**.
- **Real edge costed-out:** predictor-rank predicts forward return; magnitude doesn't survive cost+friction+early-exit layer. **CONFIRMED OOS** — IC=0.866, hit_rate=0.472, DSR=0.206, sharpe_mean=-0.053 (NEGATIVE OOS), profit_factor=0.929 (BELOW 1).

**Mira Round 3.1 reading:** the IC=0.866 OOS measurement is **engineering-correct measurement of real prediction-level discriminator power across windows**, OOS-confirmed under proper Phase G unlock. It is **NOT a leakage artifact** (rejected by hit_rate paradox cross-window). It is **NOT measurement noise** (rejected by CI95 tightness OOS). It is **NOT regime-mismatched signal** (rejected by IC magnitude near-IDENTICAL across windows; sign preserved; CI95 lower bounds both >> 0). It IS the canonical `costed_out_edge_oos_confirmed_K3_passed` signature.

### §4.4 What the K3 PASS adds vs Round 2

Round 2 made a `costed_out_edge` attribution based on (a) F2 IS IC=0.866 strong; (b) F2 IS DSR=0.767 strict FAIL; (c) F2 IS hit_rate=0.497 inconsistent with leakage. The K3 decay test was deferred (Phase G locked at the time). Round 2 conclusion was provisional: "IC magnitude IS preserved across windows under the same predictor↔label binding ... but is OOS UNCONFIRMED until Phase G unlock".

Round 3.1 PROVES the OOS confirmation cleanly:
- IC stability cross-window: 0.99991 ≫ 0.5 (K3 decay PASS COMPUTED).
- Sign preservation: positive IC IS, positive IC OOS.
- Magnitude preservation: 0.866 IS → 0.866 OOS (Δ 0.000077).
- CI95_lower bound > 0 cross-window: 0.865 IS / 0.865 OOS (PASS strict).

**Round 3.1 strengthens Round 2 from `costed_out_edge` to `costed_out_edge_oos_confirmed_K3_passed`** — the predictor-rank stability hypothesis IS cross-window confirmed; the cost-foreclosure hypothesis IS cross-window confirmed by NEGATIVE sharpe + sub-50% hit_rate + sub-1 profit_factor OOS. The diagnostic is COMPLETE.

---

## §5 hit_rate < 50% + sharpe negative + profit_factor < 1 evidence

### §5.1 The OOS PnL collapse

| Metric | F2 IS | **N8.2 OOS** | Δ | Interpretation |
|---|---|---|---|---|
| sharpe_mean | +0.046 | **-0.053** | -0.099 | Strategy LOSES money OOS gross-of-deployment |
| sharpe_median | +0.050 | **-0.059** | -0.110 | Median path is losing OOS |
| hit_rate | 0.497 | **0.472** | -0.025 | Sub-50% trade-level OOS (random walk worse than random) |
| profit_factor | 1.075 | **0.929** | -0.146 | gross_winners / gross_losers BELOW 1.0 OOS — losers exceed winners |
| sortino | +0.109 | **-0.122** | -0.231 | Downside-deviation Sortino NEGATIVE OOS |
| max_drawdown | 0.995 | **1.274** | +0.278 | Drawdown 28% worse OOS |

### §5.2 Costed_out_edge OOS confirmation logic

In an `_oos_confirmed_K3_passed` scenario, four conditions must hold simultaneously:
1. **K3 decay PASS** — predictor-rank stability cross-window. **HOLDS** (decay 0.99991 ≫ 0.5).
2. **K1 OOS strict FAIL** — DSR_OOS << 0.95. **HOLDS** (DSR 0.206 << 0.95).
3. **Realized PnL distribution worse OOS than IS** — sharpe_mean drops, hit_rate drops, profit_factor drops, max_drawdown rises. **HOLDS** (all four worsen OOS).
4. **Realized PnL distribution sub-deployable OOS** — sharpe negative, hit sub-50%, profit_factor sub-1. **HOLDS** (sharpe -0.053, hit 0.472, pf 0.929; ALL three sub-deployable).

The N8.2 evidence satisfies ALL FOUR conditions cleanly. The diagnostic `costed_out_edge_oos_confirmed_K3_passed` is the strongest possible empirical signature.

### §5.3 What this means strategically

- **Predictor IS valid:** rank-correlation between `-intraday_flow_direction` (entry-time) and `ret_forward_to_17:55_pts` (label) IS robust cross-window. The H1 alternative thesis (end-of-day inventory unwind in WDO produces fade reversion to 17:55 close) IS OOS-confirmed at the rank level.
- **Strategy IS NOT deployable:** under realistic costs (cost atlas SHA `bbe1ddf7…` per ESC-012 R6) + slippage (latency_dma2 profile) + triple-barrier early exits + RLP filter (tradeType=13 excluded per Nova spec), the realized PnL distribution OOS is materially worse than IS and below break-even at trade level (hit_rate 0.472 < 0.5; pf 0.929 < 1).
- **Path A (cost reduction R&D) is BLOCKED per user constraint:** ESC-013 §5(c) explicitly notes "cost reduction R&D path (BLOCKED by user constraint Path A OFF)". No retuning of cost atlas / triple-barrier / latency_dma2 / entry-window is authorized as forward research.
- **Path C (T002 retire ceremony) auto-trigger:** ESC-013 §5(c) prescribes "T002 retire with refined diagnostic". Pax T002.7 close trigger §10 below.

### §5.4 Comparison to Round 2 IS reading

Round 2 §5.3 noted: "the thesis Q4 kill triplet does NOT refute T002 hypothesis. The Mira Gate 4b spec §1 strict bar (DSR > 0.95) is the deployment gate, NOT the thesis falsification gate." Round 3.1 refines this:

- **Round 2 reading:** thesis NOT falsified; deployment gate FAILED IS; Phase G holdout unlock as OOS confirmation step.
- **Round 3.1 reading:** thesis NOT falsified at the predictor-rank-stability level (K3 PASS COMPUTED OOS confirms); deployment gate FAILED OOS strict (DSR 0.206 < 0.95 STRICT FAIL OOS); cost-foreclosure hypothesis IS-and-OOS confirmed; T002 retire with refined diagnostic per ESC-013 §5(c).

The thesis is **OOS-confirmed-as-valid-but-undeployable**. This is the strongest possible diagnostic the spec §0 falsifiability mandate could yield short of clean K3 collapse (Bucket B falsification) or surprise PASS (ESC-014 trigger).

---

## §6 3-bucket attribution: `strategy_edge` sub `costed_out_edge_oos_confirmed_K3_passed` (refined from Round 2 same bucket)

Per Mira Gate 4b spec v1.2.0 §7 + Riven 3-bucket framework + ESC-013 §5(c) outcome decision tree:

### §6.1 Bucket attribution

| Bucket | Definition | Round 1 attribution | Round 2 attribution | **Round 3.1 attribution** |
|---|---|---|---|---|
| **A — data_quality** | Data-quality issues (RLP-gap, circuit-breaker contamination, <70% session coverage, rollover-gap, IC pipeline wiring gap) preclude clean strategy verdict | `data_quality` (IC=0 wiring gap) | CLOSED (IC pipeline wired Round 2) | CLOSED (Round 2 + N8.2 evidence preserves close) |
| **B — strategy_edge** | Failure attributable to the strategy itself (no economic edge in the WDO end-of-day fade hypothesis under realistic costs+latency) | n/a (closed by A) | `strategy_edge` sub `costed_out_edge` (provisional pending Phase G OOS) | **`strategy_edge` sub `costed_out_edge_oos_confirmed_K3_passed`** (OOS-confirmed) |
| **C — both** | Both data-quality AND strategy-edge issues; disambiguation requires further iteration | n/a | n/a | n/a |

### §6.2 Sub-classification refinement chain

| Round | Sub-classification | Evidence basis |
|---|---|---|
| Round 1 | `data_quality_ic_pipeline_wiring_gap` | IC=0 + CI95=[0,0] (wiring gap signature) |
| Round 2 | `costed_out_edge` (provisional) | F2 IS IC=0.866 + DSR=0.767 strict FAIL + hit_rate=0.497 inconsistent with leakage |
| **Round 3.1** | **`costed_out_edge_oos_confirmed_K3_passed`** | N8.2 OOS IC=0.866 (K3 decay PASS COMPUTED 0.99991 > 0.5) + DSR_OOS=0.206 strict FAIL + sharpe_mean -0.053 + hit_rate 0.472 + profit_factor 0.929 + max_drawdown 1.274 |

### §6.3 Riven F2-T6.1 Round 3.1 reclassify trigger

Riven post-mortem `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` requires a Round 3.1 ledger entry per Mira Gate 4b spec v1.2.0 §15.13.8 disposition rule + ESC-013 §5(c) explicit outcome:

- **Bucket A `IC_pipeline_wiring_gap`** — CLOSED carry-forward (Round 2 close preserved; N8.2 wiring contract test R20 preserves enforcement).
- **Bucket B `strategy_edge` sub `costed_out_edge_oos_confirmed_K3_passed`** — NEW Round 3.1 entry. Refines Round 2 `costed_out_edge` provisional with OOS-confirmed K3-PASS evidence.
- **Bucket C n/a** — not triggered.

### §6.4 What the K3-passed qualifier signals to forward research

The `_K3_passed` qualifier in the sub-classification carries forward the Round 3.1 evidence that K3 decay test PASSED at OOS measurement under PROPER Phase G unlock. This signals to forward research that:

1. **Predictor↔label rank-stability is structural, not regime-dependent.** The `-intraday_flow_direction → ret_forward_to_17:55_pts` rank correlation IS preserved across IS+OOS windows. Future T-series strategies that use this predictor can rely on its rank-stability as a structural property (subject to monitoring; per spec §15.13 protocol).
2. **The fail mode is exclusively at the realized-PnL/cost layer.** Future forward-research at the cost-side (cost atlas tuning, triple-barrier widening, latency profile, entry-window narrowing, RLP filter tuning) MIGHT recover deployable Sharpe — BUT this path is BLOCKED by user constraint Path A OFF per ESC-013 §5(c).
3. **The bucket B closure is clean at the predictor-validity level**, not falsifying-clean (which would require K3 decay collapse, ESC-013 §5(b), Bucket B falsification with `strategy_edge_oos_K3_falsified` sub-classification). T002 retires with the predictor IP retained in the research-log; T-series successors may borrow.

---

## §7 R10 footer Gate 5 fence verbatim

Per Mira Gate 4b spec v1.2.0 §10.2 footer + ESC-011 R5 + R6 + ESC-012 R10 + ESC-013 R10-carry-forward (verbatim from spec §10):

> **Footer (R5/R6 mandatory per ESC-011):** Gate 4b PASS does NOT pre-disarm Gate 5 alone. Gate 5 capital ramp dual-sign requires Gate 4a (HARNESS_PASS, DONE) AND Gate 4b (GATE_4_PASS, this artifact pending future). Riven §9 HOLD #2 Gate 5 disarm authority preserved.

**Strengthening clause for THIS Round 3.1 verdict:** Gate 4b verdict here is `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed`, NOT `GATE_4_PASS`. Therefore the Gate 5 conjunction `Gate 4a HARNESS_PASS ∧ Gate 4b GATE_4_PASS ∧ paper-mode audit` is **NOT SATISFIED**. Gate 5 capital ramp authority **REMAINS LOCKED**. Riven §9 HOLD #2 Gate 5 disarm ledger MUST NOT mark Gate 4b as cleared on the basis of this artifact.

The R10 fence is **reinforced**, not relaxed, by this STRICT FAIL verdict at K1 OOS threshold + by the K3 COMPUTED-PASS at OOS measurement. Anti-Article-IV Guard #4 UNMOVABLE thresholds are preserved verbatim Round 3.1: DSR > 0.95 / PBO < 0.5 / IC > 0 thresholds were applied AS-IS despite (a) strong K3 OOS evidence (K3 PASS COMPUTED would be a salvage path under a relaxed reading); (b) N8 surprise DSR=0.965 near-miss artifact (would have been a salvage path under a non-strict reading). Round 3.1 does NOT relax K1 strict bar in light of K3 OOS PASS — the spec design requires JOINT conjunction; partial pass does not promote.

ESC-013 §5(a) outcome explicitly requires that even K1+K2+K3 ALL COMPUTED PASS scenario routes to ESC-014 council convene to adjudicate K1 strict bar interpretation under OOS-PROPER surprise PASS — and that "Gate 5 still requires paper-mode Phase G/H per R10 fence carry-forward". The R10 fence binds even in the surprise-PASS hypothetical. Round 3.1 outcome is FAIL strict; the fence is binding a fortiori.

**No Gate 5 progression Round 3.1.** Gate 4b verdict FAIL strict; capital ramp LOCKED; ESC-014 NOT triggered; Path C activate per §10 below.

---

## §8 T002 disposition: RETIRE per spec §0 + Path C (ESC-012 fallback) auto-trigger

### §8.1 Spec §0 falsifiability mandate application

Per Mira Gate 4b spec v1.2.0 §0 falsifiability mandate: "the data MUST be allowed to refute the H1 alternative thesis". Round 3.1 evidence:

- **H1 alternative ("`IC_spearman ≥ 0.05` in 45 paths CPCV with p < 0.01 post-Bonferroni n_trials=5"):** STRONGLY satisfied OOS at IC=0.866 (17× above 0.05 threshold; CI95_lower 0.865 also far above; OOS-confirmed under K3 decay PASS COMPUTED).
- **Q4 K1 (`DSR<0`):** observed OOS `0.206 > 0` — **NOT triggered** at the kill criterion (kill criterion is DSR strictly negative; 0.206 is finite-positive).
- **Q4 K2 (`PBO>0.4`):** observed OOS `0.060 < 0.4` — **NOT triggered**.
- **Q4 K3 (`IC_holdout < 50% IC_in_sample`):** observed OOS decay 0.99991 ≫ 0.5 — **NOT triggered** (K3 PASS COMPUTED).
- **Q4 K4 (`DD > 3σ budget Riven em paper`):** Phase H paper-mode; cannot test (downstream of Gate 4b, NOT this spec).

The thesis Q4 kill triplet does NOT refute T002 hypothesis at the falsification level. **Round 3.1 reading: thesis is OOS-confirmed-as-valid-but-undeployable.** The Mira Gate 4b spec §1 strict bar `DSR > 0.95` is the **deployment gate**, NOT the **thesis falsification gate**. Under spec §0 falsifiability mandate:

- Thesis falsified ⇒ retire T002 with `strategy_edge_oos_K3_falsified` (Bucket B clean falsification) — **NOT applicable** (K3 PASS).
- Deployment gate failed ⇒ T002 cannot deploy at this iteration; partial-edge story is OOS-confirmed; cost-side parameter exploration MIGHT salvage — **APPLICABLE but BLOCKED by user constraint Path A OFF**.

### §8.2 Path C (ESC-012 §5 fallback) auto-trigger

ESC-012 §5 fallback: "Path C — T002 retire ceremony with refined diagnostic; falsifiability mandate honored at deployment-gate level even when thesis-falsification level shows partial-edge survival". Path C auto-triggers per ESC-013 §5(c) prescription verbatim: "T002 retire with refined diagnostic; cost reduction R&D path (BLOCKED by user constraint Path A OFF)".

**T002 retire ceremony components:**
1. Mira F2-T9.1 Round 3.1 sign-off (THIS artifact) — closes the Gate 4b adjudication chain.
2. Riven F2-T6.1 Round 3.1 reclassify (`docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md`) — Bucket B sub `costed_out_edge_oos_confirmed_K3_passed` ledger entry per §6 above.
3. Pax T002.7 close (story status DONE | ARCHIVED) — closes the story per §10 below.
4. ESC-013 §6 closure conditions row 6 `Pax T002.7 close per outcome decision tree §5` — checked off.
5. ESC-013 §7 closure conditions met → ESC-013 RESOLVED status.
6. Predictor IP retained in `docs/ml/research-log.md` for T-series successors per §6.4 above.

### §8.3 What does NOT happen Round 3.1

- **NO Gate 4b PASS** (strict K1 OOS FAIL pre-empts).
- **NO Gate 5 progression** (R10 fence preserved verbatim per §7).
- **NO ESC-014 escalation** (K1 strict OOS FAIL pre-empts surprise PASS scenario per ESC-013 §5(a); INCONCLUSIVE_again §5(d) NOT applicable because verdict computed not deferred).
- **NO threshold relaxation** (K1 strict bar 0.95 applied AS-IS; Anti-Article-IV Guard #4 honored).
- **NO source-code modification** (this verdict is write-only at QA-gate layer; no code mutation; no spec yaml mutation; no spec markdown mutation; no hold-out re-touch; no Round 1/2/3 sign-off mutation — Round 3.1 supersedes via append-only revision discipline).
- **NO Path A activation** (cost reduction R&D BLOCKED by user constraint per ESC-013 §5(c)).
- **NO re-run after N8.2** (ESC-013 R19 strict re-run discipline: "No re-run after N8.1 regardless of outcome").

---

## §9 Riven F2-T6.1 Round 3.1 reclassification trigger

Riven (@risk-manager) is triggered by THIS Round 3.1 verdict to update `docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md` per Mira Gate 4b spec v1.2.0 §15.13.8 disposition rule + ESC-013 §5(c) explicit outcome. Required ledger entry:

```yaml
- id: F2-T6.1-Round3.1
  timestamp_brt: 2026-05-01
  source_authority: Mira F2-T9.1 Round 3.1 verdict (this artifact)
  bucket: B (strategy_edge)
  sub_classification: costed_out_edge_oos_confirmed_K3_passed
  refines: F2-T6 Round 2 (costed_out_edge — provisional pending Phase G OOS)
  closes: bucket_B_phase_G_oos_confirmation_ladder
  evidence:
    - n8_2_oos_dsr: 0.205731  # K1 strict FAIL << 0.95
    - n8_2_oos_pbo: 0.059524  # K2 PASS << 0.5
    - n8_2_oos_ic_in_sample: 0.865933  # K3 in-sample binding PASS
    - n8_2_oos_ic_holdout_decay_ratio: 0.99991  # K3 decay PASS COMPUTED >> 0.5
    - n8_2_oos_sharpe_mean: -0.053056  # NEGATIVE OOS
    - n8_2_oos_hit_rate: 0.472377  # SUB-50% trade-level
    - n8_2_oos_profit_factor: 0.929058  # BELOW 1.0
    - n8_2_oos_max_drawdown: 1.273941  # 28% worse vs IS 0.995
  esc_013_outcome_branch: c  # K1_FAIL_OOS_marginal + K3 PASS
  disposition: T002 retire with refined diagnostic
  forward_research_path: Path A BLOCKED (user constraint); Path C auto-trigger (ESC-012 §5 fallback)
```

Riven authority basis: ESC-013 §5(c) prescription + Mira Gate 4b spec v1.2.0 §15.13.8 + Round 3.1 verdict (this artifact) issued.

---

## §10 Pax T002.7 close trigger

Pax (@po) is triggered by THIS Round 3.1 verdict to close T002.7 per ESC-013 §6 closure conditions row 6 + ESC-013 §5(c) prescription. Required actions:

1. **Story status transition:** T002.7 status `In Review` → `Done` (or `Done | ARCHIVED`); Pax 10-point validate per Pax authority.
2. **Epic status:** EPIC-T002 status update — strategy retire ceremony complete; predictor IP retained in research-log; T-series successor scope authorized.
3. **ESC-013 closure update:** check-off ESC-013 §6 row 6 `Pax T002.7 close per outcome decision tree §5` → marks ESC-013 RESOLVED.
4. **ESC-012 closure update:** Path C auto-trigger from ESC-012 §5 fallback acknowledged in ESC-012 closure ledger.
5. **Spec versioning:** Mira Gate 4b spec v1.2.0 status update — Round 3.1 FINAL appended to §14.4 cosign ledger; no v1.3.0 amendment needed (Round 3.1 is verdict-layer, not spec-layer).
6. **Riven ledger update:** invoke F2-T6.1 Round 3.1 entry per §9 above.
7. **Research log update:** `docs/ml/research-log.md` Round 3.1 entry — predictor IP retained for T-series; cost-foreclosure diagnostic OOS-confirmed; bucket B closure clean at predictor-validity level.

Pax authority basis: ESC-013 §5(c) prescription + ESC-013 §6 closure conditions + PRR-20260430-1 §3-§4 disposition rule + Round 3.1 verdict (this artifact) issued.

---

## §11 ESC-014 NOT triggered (DSR strict FAIL pre-empts surprise PASS scenario)

ESC-014 trigger conditions per ESC-013 §5(a) and §5(d):

- **§5(a) surprise PASS:** K1+K2+K3 ALL COMPUTED PASS (DSR>0.95 + PBO<0.5 + IC_holdout > 0.5×IC_in_sample) → ESC-014 council convene to adjudicate K1 strict bar interpretation under OOS-PROPER surprise PASS.
- **§5(d) INCONCLUSIVE_again:** procedural double-failure (Round 3 INCONCLUSIVE → re-run produces another INCONCLUSIVE) → ESC-014 escalation.

**Round 3.1 evidence vs ESC-014 trigger:**

| Trigger | Round 3.1 status | Match? |
|---|---|---|
| §5(a) K1+K2+K3 ALL COMPUTED PASS | K1 FAIL (DSR=0.206 < 0.95); K2 PASS; K3 PASS | **NO** (K1 strict OOS FAIL pre-empts surprise PASS) |
| §5(d) INCONCLUSIVE_again | Verdict computed (verdict.reasons=[] empty; ic_holdout_status='computed'; OBS-1 short-circuit DID NOT fire) | **NO** (verdict computed not deferred) |

**ESC-014 NOT triggered Round 3.1.** No council convene needed. Path C auto-trigger per ESC-013 §5(c) is the binding disposition.

---

## §12 Anti-Article-IV self-audit (8 guards)

| Guard | Description | Round 3.1 status |
|---|---|---|
| **#1** | Pre-registered hypothesis (H1 alternative + Q4 kill criteria) | PRESERVED — F2 IS + N8.2 OOS evidence applied AGAINST pre-registered thresholds AS-IS; no post-hoc relaxation |
| **#2** | Threshold UNMOVABILITY (parent spec yaml v0.2.3 §kill_criteria L207-209 + Mira spec §1 + §15.13.5) | PRESERVED — DSR>0.95 / PBO<0.5 / IC>0 thresholds applied verbatim Round 3.1; no relaxation under K3 OOS PASS evidence; no relaxation under N8 surprise DSR=0.965 near-miss artifact (Round 3 INCONCLUSIVE held strict reading; vindicated by Round 3.1) |
| **#3** | Holdout-window lock (no IS measurement on OOS data outside Phase G unlock chain) | PRESERVED — Phase G unlock chain ratified by ESC-012 5/6 SUPERMAJORITY APPROVE_PATH_B + ESC-013 5/5 UNANIMOUS APPROVE_PATH_IV; N8.2 PROPER unlock with --phase G CLI flag + holdout_locked=False propagated per ESC-013 R18 Dex wiring; one-shot K3 measurement consumed cleanly; no further OOS re-runs per ESC-013 R19 |
| **#4** | K1 strict bar UNMOVABLE (DSR > 0.95 strict; Bailey-LdP 2014 §3 Φ-CDF; n_trials Bonferroni n=5 carry-forward) | PRESERVED — Round 3.1 STRICT FAIL emitted at DSR_OOS=0.206 << 0.95; no relaxation under K3 PASS evidence; ESC-013 §5(c) outcome explicitly preserves K1 strict bar UNMOVABLE; R10 fence reinforced |
| **#5** | Single point-of-decision (one verdict per round; no multi-axis aggregation) | PRESERVED — Round 3.1 verdict label `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed` is single-axis (Gate 4b clearance gate); sub-classification is qualitative refinement not multi-axis aggregation |
| **#6** | NO enforce Gate 5 disarm without Gate 4a + Gate 4b BOTH | PRESERVED — §7 footer + §8.1 explicit — Gate 5 LOCKED; Round 3.1 does NOT pre-disarm; R10 fence reinforced under K1 OOS strict FAIL |
| **#7** | Falsifiability mandate (data MUST be allowed to refute H1 alternative) | PRESERVED — N8.2 OOS evidence COULD have refuted H1 (would have been observed K3 decay collapse < 0.5; not observed; thesis OOS-confirmed at predictor-rank-stability level); falsifiability honored AT data level even though thesis Q4 kill triplet did NOT refute |
| **#8** | `*_status` provenance enforcement + verdict-vs-reason invariant (R20 contract test post-ESC-013) | PRESERVED — N8.2 ic_holdout_status='computed' propagated; verdict.reasons=[] empty (R20 contract test PASS — verdict-PASS implies empty reasons or computed-numbers reasons, NOT sentinel strings); Anti-Article-IV Guard #8 fully operational under Phase G PROPER protocol |

**Self-audit result:** ALL 8 GUARDS HONORED ROUND 3.1. No Article IV violation; no spec mutation; no threshold relaxation; no OOS re-touch; no salvage-path interpretation of K3 OOS PASS evidence.

---

## §13 Article IV trace

Every clause in §1-§12 traces to a verifiable source. NO INVENTION. NO threshold relaxation Round 3.1 (K1 strict 0.95 applied AS-IS despite K3 OOS PASS COMPUTED evidence). NO subsample. NO Gate 5 pre-disarm. NO source-code modification (this verdict is write-only at QA-gate layer; no code mutation; no spec yaml mutation; no spec markdown mutation; no hold-out re-touch; no Round 1/2/3 sign-off mutation — Round 3.1 supersedes via append-only revision discipline).

| Clause | Source |
|---|---|
| Verdict label `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed` | ESC-013 §5(c) outcome decision tree verbatim + Mira Gate 4b spec v1.2.0 §9 verdict-label discipline + §15.13.8 disposition rule |
| K1 strict FAIL DSR_OOS=0.206 | `data/baseline-run/cpcv-dryrun-auto-20260501-3fce65dab8f8/full_report.json:8` |
| K2 PASS PBO_OOS=0.060 | `full_report.json:9` |
| K3 in-sample PASS IC_OOS=0.866 + CI95_lower=0.865 | `full_report.json:3-7` |
| K3 decay PASS COMPUTED ratio 0.99991 | F2 N7-prime IC_in_sample=0.866010 (`docs/backtest/T002-beckett-n7-prime-2026-04-30.md` §5.1) + N8.2 IC_holdout=0.865933 (`full_report.json:3`); decay = 0.865933/0.866010 = 0.99991 ≫ 0.5 threshold |
| verdict.reasons=[] empty (R20 contract test PASS) | `full_report.json:256` |
| ic_holdout_status='computed' propagation | inference from verdict.reasons empty + kill_decision.k3_ic_decay_passed=true + ESC-013 R18 Dex wiring contract; verbatim per Mira spec §15.13.2 unlock mechanism |
| ESC-013 §5(c) outcome match | `docs/councils/COUNCIL-2026-04-30-ESC-013-resolution.md` §5 row 3 verbatim |
| sharpe_mean -0.053 + hit_rate 0.472 + profit_factor 0.929 + max_drawdown 1.274 | `full_report.json:237, 245, 244, 243` |
| Anti-Article-IV Guard #4 UNMOVABLE | parent spec yaml `T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml` v0.2.3 §kill_criteria L207-209 + Mira spec §1 + ESC-011 R14 + ESC-012 R3 + ESC-013 R20 |
| Gate 5 R10 fence preservation | Mira Gate 4b spec v1.2.0 §10.2 footer + ESC-011 R5 + R6 + ESC-012 R10 + ESC-013 §5(a) carry-forward + Riven §9 HOLD #2 |
| Path C auto-trigger | ESC-012 §5 fallback + ESC-013 §5(c) prescription verbatim |
| ESC-014 NOT triggered | ESC-013 §5(a) trigger (K1+K2+K3 ALL COMPUTED PASS) NOT met because K1 OOS strict FAIL; ESC-013 §5(d) trigger (INCONCLUSIVE_again) NOT met because verdict computed not deferred |
| 3-bucket attribution `strategy_edge` sub `costed_out_edge_oos_confirmed_K3_passed` | Mira Gate 4b spec v1.2.0 §7 + Riven 3-bucket framework + ESC-013 §5(c) outcome explicit sub-classification |

---

## §14 Mira cosign 2026-05-01 BRT — F2-T9.1 authoritative Round 3.1 FINAL re-clearance

**Cosign:** Mira (@ml-researcher) 2026-05-01 BRT — F2-T9.1 authoritative Round 3.1 FINAL re-clearance.

**Verdict label (Round 3.1 FINAL):** `GATE_4_FAIL_strategy_edge_costed_out_edge_oos_confirmed_K3_passed`.

**ESC-013 §5 outcome match:** (c) K1_FAIL_OOS_marginal + K3 PASS → costed_out_edge_oos_confirmed_K3_passed (refined from Round 2).

**Authority basis:**
- Mira (@ml-researcher) per ESC-013 R12 (F2-T9.1 re-clearance authority post-PROPER-Phase-G).
- Mira Gate 4b spec v1.2.0 §1 + §9 + §10 + §15.13 + §15.13.5 + §15.13.7 + §15.13.8.
- PRR-20260430-1 §3.1-§3.4 4-branch disposition rule.
- ESC-013 §5 outcome decision tree.
- Anti-Article-IV Guards #1-#8 ALL HONORED (§12).

**Supersedes:** Round 3 (`signoff-round3.md`), Round 2 (`signoff-round2.md`), Round 1 (`signoff.md`). All prior rounds preserved per spec §15 append-only revision discipline. Round 3.1 FINAL.

**Closure triggers issued:**
1. Riven F2-T6.1 Round 3.1 reclassify (`docs/risk/post-mortems/T002-synthetic-vs-real-tape-attribution.md`) per §9.
2. Pax T002.7 close per §10 + ESC-013 §6 closure conditions row 6.
3. ESC-013 RESOLVED status (post §9 + §10 closure events).
4. T002 retire ceremony auto-trigger per ESC-013 §5(c) + Path C ESC-012 §5 fallback.

**No Gate 5 progression Round 3.1.** R10 fence preserved verbatim per §7. Capital ramp authority LOCKED. Anti-Article-IV Guard #4 honored.

**No further re-runs Round 3.1.** ESC-013 R19 strict re-run discipline ("No re-run after N8.1 regardless of outcome") binding. Hold-out one-shot K3 measurement consumed cleanly under PROPER Phase G unlock; no protocol re-do authorized.

**No spec mutation Round 3.1.** Mira Gate 4b spec v1.2.0 status preserved; this verdict is write-only at QA-gate layer; v1.3.0 amendment not needed (Round 3.1 is verdict-layer disposition not spec-layer threshold/protocol).

**No source-code mutation Round 3.1.** R18 Dex caller wiring landed pre-N8.2; N8.2 protocol-corrected run executed cleanly; no further code changes authorized post-N8.2 per ESC-013 R18 close.

— Mira, mapeando o sinal 🗺️
