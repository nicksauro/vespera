# T002.4 QA Gate — CONCERNS

**Reviewer:** Quinn (@qa)
**Date:** 2026-04-24 BRT
**Story:** T002.4 — Child-side peak telemetry in `run_materialize_with_ceiling.py` (ADR-4 §14.5.2 B1)
**Commit under review:** `80805ac5b44ad3314ca2a4b2c068db7d97d037e4` on branch `t002-refactor-next-run` (NOT pushed)
**Spec:** `docs/architecture/pre-cache-layer-spec.md` §14.5.2 B1
**Governance:** `docs/architecture/memory-budget.md` §RISK-DISPOSITION-20260423-1 Q6b=(i) — dual signature Quinn + Riven mandatory per AC-F
**Verdict:** **CONCERNS** (PASS for the telemetry surface; CONCERNS on scope inflation in the same commit — see FINDING-01)

---

## 7-Check Summary

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Requirements traceability | PASS | All 10 in-Dex-scope ACs (A–E, G–K) map to code/tests (matrix below). AC-F intentionally left unchecked — unlocks Riven co-sign dispatch. |
| 2 | Test execution | PASS | `pytest tests/core/test_run_with_ceiling.py -v` → **49 passed in 0.51s** (45 pre-existing + 4 new). `pytest tests/` → **263 passed, 1 skipped in 15.50s** (skip is pre-existing — `test_parquet_speed.py::test_hot_loop_cpcv_1path_250_sessions_under_budget`, blocked on T002.0a). Ruff on the 3 touched source/test files → "All checks passed!". Mypy on `scripts/run_materialize_with_ceiling.py` + `scripts/materialize_parquet.py` → 5 errors, 4 pre-existing psutil-stubs class + 1 new psutil-stubs at `materialize_parquet.py:982` (same class — install `types-psutil` resolves), and 2 `arg-type` errors at `materialize_parquet.py:889` that are **not** telemetry-scope (see FINDING-01). No NEW telemetry-scope mypy error introduced. |
| 3 | Security/correctness review | PASS | Telemetry emission wrapped in outer `try/finally` at `materialize_parquet.py:1070-1094` — covers success + `HoldoutLockError` + generic `Exception` branches (AC-A). psutil primary with `getattr(mem, "peak_pagefile", None)` + `getattr(mem, "peak_wset", None)` defensive lookup at `:986-988`; ctypes `GetProcessMemoryInfo` fallback at `:999-1032` with `_PROCESS_MEMORY_COUNTERS_EX` struct; emit itself also wrapped in `try/except` (:1086-1094) so an emission exception can't mask the real exit code. Wrapper parser `_parse_child_peak_line` uses a strict regex (`^TELEMETRY_CHILD_PEAK_EXIT\s+commit=(\d+)...$`) so malformed lines return `None` rather than propagating exceptions. `_extract_child_peak_from_log` tolerates missing log (`OSError` caught). `_augment_summary_with_child_peak` uses atomic `os.replace` pattern mirroring `write_summary`. **No eval/exec, no path traversal, no hardcoded secrets, no shell=True**. BRT-naive preserved — `datetime.now().isoformat(timespec="seconds")` used throughout; anti-UTC grep clean (0 matches for `tz_convert|utcnow|pytz.UTC` on both scripts). |
| 4 | Telemetry schema consistency | PASS | `core/telemetry_schema.py` git diff EMPTY — `SUMMARY_JSON_FIELDS` and `TELEMETRY_CSV_COLUMNS` tuples unchanged (AC-I). New JSON keys (`peak_commit_bytes_child_self_reported`, `peak_wset_bytes_child_self_reported`, `child_pid`, `child_peak_timestamp_brt`, `child_peak_telemetry_missing`, `child_peak_error`) are distinct from parent-polled `peak_commit` / `peak_rss` — no name collision. Augmented JSON is additive only; `test_wrapper_parses_and_surfaces_child_peak_in_summary` asserts every SUMMARY_JSON_FIELDS key remains present after augmentation. CSV columns untouched (consumers unaffected). |
| 5 | Regression integrity | PASS | `core/run_with_ceiling.py` git diff EMPTY (AC-D — R5 gating byte-identical, parent-polling pathway preserved). 45/45 pre-existing tests in `test_run_with_ceiling.py` pass alongside the 4 new ones. New tests cover: (a) normal-exit emission via real subprocess `--dry-run`, asserting psutil primary path yields positive `peak_commit` + `peak_wset` + `pid > 0`; (b) parent `_extract_child_peak_from_log` + `_augment_summary_with_child_peak` round-trip, asserting SUMMARY_JSON_FIELDS preservation and exact new-key values; (c) missing-line fallback — log without `TELEMETRY_CHILD_PEAK_EXIT`, wrapper records `child_peak_telemetry_missing=True` without inventing peak values; (d) degraded line with `error=<reason>` suffix, parser preserves `commit=0 wset=0` + error string. |
| 6 | Performance impact | PASS | Emission runs ONCE in `main()`'s outer `finally{}` — single `psutil.Process().memory_info()` call + one `print(line, flush=True)`. No new loops, no per-tick cost, no memory pressure. Wrapper parser scans last 200 log lines via `readlines()[-200:]` — O(tail_lines), negligible for production log sizes. `os.replace` atomic JSON rewrite is single syscall. End-to-end overhead <1 ms. Zero impact on child runtime or parent R5 gating cadence. |
| 7 | Spec compliance (Article IV — No Invention) | CONCERNS | **Telemetry surface: PASS.** Every telemetry LOC traces to ADR-4 §14.5.2 B1 + AC-A..K. `data/manifest.csv` sha verified byte-identical `75e72f2c1185eb795a5db6c5a127706e1b90c30906216a72ea79c443d5391641` (AC-H). `core/memory_budget.py` git diff EMPTY (AC-G). `core/telemetry_schema.py` git diff EMPTY (AC-I contract preservation). `core/run_with_ceiling.py` git diff EMPTY (AC-D). **However — scope inflation detected in the commit:** see FINDING-01. The commit title + body + story scope describe a ~+125/+110/+135 LOC telemetry patch; the actual numstat is `scripts/materialize_parquet.py +541/-26`, because the diff folds in previously-uncommitted ADR-4 §10.3 cache-dispatch (`--source`, `--cache-dir`, `--cache-manifest`, `_fetch_month_from_cache`) + MWF-20260422-1 manifest-mode flags (`--no-manifest`, `--manifest-path`). These are NOT invented — they trace to shipped work (commits `85664f7`, `8c217bf`, `63a9a3a` landed the parallel wrapper-side + test-side changes), so no "invention" violation — but the fact the child-side counterparts landed only in THIS commit creates an atypical Article-IV audit surface Dex did not disclose in the story File List or commit body. |

---

## AC Coverage Matrix

| AC | Covered? | Evidence |
|----|----------|----------|
| AC-A | YES | `scripts/materialize_parquet.py:1066-1094` — `main()` wraps `run()` in outer `try/finally` with inner `except HoldoutLockError` + `except Exception` branches. `_emit_child_peak_telemetry()` called unconditionally in `finally{}`. `test_child_peak_telemetry_emitted_on_normal_exit` asserts exactly one line on real subprocess `--dry-run`. |
| AC-B | YES | `scripts/run_materialize_with_ceiling.py:367-375` — post-`run_with_ceiling` call, `_extract_child_peak_from_log` + `_augment_summary_with_child_peak` merge peak payload into summary JSON. Halt-report `child_self_peak` print at `:390-414` (MISSING / DEGRADED / normal paths). `test_wrapper_parses_and_surfaces_child_peak_in_summary` asserts 5 new fields + all pre-existing SUMMARY_JSON_FIELDS preserved. |
| AC-C | YES | `_collect_child_peak_memory` at `materialize_parquet.py:967-1032` — psutil primary (`peak_pagefile` / `peak_wset` via `getattr` defensive lookup); ctypes `GetProcessMemoryInfo` fallback with full `_PROCESS_MEMORY_COUNTERS_EX` struct; structured error string on complete failure. `test_wrapper_parses_degraded_line_with_error_field` covers the error-suffix parse path. Live psutil primary path exercised by `test_child_peak_telemetry_emitted_on_normal_exit` (asserts positive peak values — proves psutil branch executed on psutil 7.2.2). |
| AC-D | YES | `core/run_with_ceiling.py` git diff EMPTY (verified `git diff 80805ac^ 80805ac -- core/run_with_ceiling.py` → no output). All 45 pre-existing tests in `test_run_with_ceiling.py` still green. Parent-side polling at `--poll-seconds` cadence untouched — R5 WARN@0.85 / KILL@0.95 gating behavior preserved byte-identical. |
| AC-E | CONCERNS (accepted) | Emission is in `main()`'s outer `finally{}` which is shared by both `--source=cache` and `--source=sentinel` dispatch branches — no branch-specific telemetry code exists (by-design satisfaction). The real-subprocess test uses `--dry-run` which bypasses dispatch. Dex deferred T5 (live cache+sentinel integration test) citing DB + cache-fixture requirements out of CI scope. **I accept the deferral** — the `finally{}` location makes the dispatch-independence structurally provable from the source, and a mock-dispatch unit test would add little over what `test_wrapper_parses_and_surfaces_child_peak_in_summary` already proves. If Riven requires live-dispatch evidence for co-sign, it can be added without touching production code. **Recorded as FINDING-02 informational.** |
| AC-F | **UNCHECKED** | **PENDING Riven co-sign.** Quinn gate verdict (this document) unlocks Riven's co-sign dispatch per §Q6b B6. This gate does NOT satisfy AC-F alone — Riven must file the co-sign record at `docs/qa/gates/T002.4-riven-cosign.md` (or equivalent path per Riven's pattern) before @devops merges. |
| AC-G | YES | `git diff 80805ac^ 80805ac -- core/memory_budget.py` → EMPTY. `CAP_ABSOLUTE_v3`, R4/R5 constants byte-identical. |
| AC-H | YES | `sha256sum data/manifest.csv` at review time → `75e72f2c1185eb795a5db6c5a127706e1b90c30906216a72ea79c443d5391641` (byte-identical to pin). |
| AC-I | YES | `core/telemetry_schema.py` git diff EMPTY. New JSON keys additive-only; distinct names (no collision with `peak_commit` / `peak_rss`). `test_wrapper_parses_and_surfaces_child_peak_in_summary` asserts `for field in SUMMARY_JSON_FIELDS: assert field in augmented`. CSV columns untouched. |
| AC-J | YES | 45/45 pre-existing tests in `test_run_with_ceiling.py` green alongside the 4 new ones (49/49 total). No existing-test modifications (diff inspected). |
| AC-K | YES | 4 new tests added (exceeds the ≥3 budget): (i) `test_child_peak_telemetry_emitted_on_normal_exit` — subprocess psutil primary path; (ii) `test_wrapper_parses_and_surfaces_child_peak_in_summary` — parent parse + summary merge + SUMMARY_JSON_FIELDS preservation; (iii) `test_wrapper_handles_missing_child_peak_without_crash` — missing-line fallback records `child_peak_telemetry_missing=True` without inventing values; (iv) `test_wrapper_parses_degraded_line_with_error_field` — AC-C error-suffix parse defense. |

---

## Findings

- **FINDING-01 (CONCERNS, non-blocking for Quinn gate — flagged for Riven awareness):** The commit's git numstat is `scripts/materialize_parquet.py +541/-26`, but the story's File List claims `~+125 LOC net` + "child-side peak telemetry" scope only. The diff folds in ADR-4 §10.3 cache-dispatch (`--source`, `--cache-dir`, `--cache-manifest`, `_fetch_month_from_cache`, cache-manifest alias guard) + MWF-20260422-1 manifest-write-mode flags (`--no-manifest`, `--manifest-path`). These changes are **not invented** — they trace to shipped work on the branch (`85664f7 feat(wrapper): passthrough --source/--cache-dir/--cache-manifest (RA-20260426-1 P5)`, `8c217bf feat(build-cache): §13.1 streaming parquet writer for raw_trades pre-cache`, `63a9a3a test(build-cache): repoint monkeypatches to _stream_month_to_parquet (CONCERNS-01)`) where the wrapper-side + test-side counterparts landed first, leaving the child-side CLI surface to land in this commit. The ACs for those prior stories are already discharged; the child-side flags are their logical completion. **No Article-IV invention violation** (every folded-in piece traces to prior governance + ACs). **However:** (a) Dex did not disclose the scope expansion in the commit body or story File List; (b) a reviewer focused on T002.4 alone will not find the trace without reading the whole branch. **Recommendation:** Riven should spot-check that the folded-in LOC aligns with the prior stories' merged artifacts before co-signing. Not a gate FAIL — the telemetry surface is clean in isolation and the AC coverage is complete. *Classification: CONCERNS (informational for Riven).*
- **FINDING-02 (informational, non-blocking):** T4 (separate isolation file `tests/unit/test_materialize_child_peak_telemetry.py`) and T5 (live cache+sentinel integration file) are deferred. I accept both deferrals — `test_child_peak_telemetry_emitted_on_normal_exit` exercises child-side emission via real subprocess (psutil primary path live), and the `finally{}` sharing of both dispatch branches makes AC-E structurally satisfied. If Riven requires split files for §14.5.2 literal compliance, Dex can extract in a ≤10-LOC follow-up. *Classification: informational.*
- **FINDING-03 (informational, non-blocking):** CodeRabbit CLI not installed in the WSL environment Dex runs on (`which coderabbit` → not found). Dex substituted ruff + mypy + pytest self-review chain per T6. Given ruff "All checks passed!", mypy introduces zero new telemetry-scope errors, and pytest 49/49 + 263/1s, I accept the substitute. Once Gage's side provisions the CodeRabbit CLI, a retroactive pre-commit run against `80805ac` would close the governance loop. Not a gate blocker. *Classification: informational.*
- **FINDING-04 (informational, pre-existing):** mypy reports `arg-type` errors at `scripts/materialize_parquet.py:889` (2× `Path | None` passed to a `Path`-only parameter of `_fetch_month_from_cache`). Runtime is safe because `parse_args` guarantees `cache_dir` + `cache_manifest` are non-None on the `--source=cache` branch (explicit `parser.error` otherwise), but a `cast` or conditional-narrowing pattern would clean the mypy surface. These errors live inside the folded-in ADR-4 §10.3 cache-dispatch code (FINDING-01), NOT in the telemetry surface. Not a telemetry-scope gate finding. *Classification: informational — recommended follow-up in a future cache-dispatch clean-up story.*
- **FINDING-05 (positive):** The structured line + regex contract is well-designed. Anchored regex (`^TELEMETRY_CHILD_PEAK_EXIT\s+...\s*$`), optional `error=<reason>` capture group, reverse-scan tail semantics, and atomic summary-JSON rewrite via `os.replace` collectively give the mechanism a clean recovery posture against malformed output, missing log files, and wrapper/child desync. `child_peak_telemetry_missing` as a tri-state signal (True / False / absent) correctly distinguishes "child never ran" from "child ran but emission lost" from "normal". This is RA-20260428-1-ready. *Classification: positive.*

No FAIL-class findings. The single CONCERNS-class finding (FINDING-01) is scope-disclosure hygiene, not a correctness or safety defect.

---

## Canonical sha verification

Pre-review: `sha256sum data/manifest.csv` → `75e72f2c1185eb795a5db6c5a127706e1b90c30906216a72ea79c443d5391641` ✅ (byte-identical to pin, matches Dex's post-implementation assertion).

## Regression test count confirmation

- `tests/core/test_run_with_ceiling.py -v` → **49 passed** (45 pre-existing + 4 new) ✅
- `tests/` full suite → **263 passed, 1 skipped** (pre-existing skip: `test_hot_loop_cpcv_1path_250_sessions_under_budget`, T002.0a blocker) ✅
- Ruff on `scripts/run_materialize_with_ceiling.py scripts/materialize_parquet.py tests/core/test_run_with_ceiling.py` → "All checks passed!" ✅

---

## Decision rationale

The telemetry surface itself — the whole reason this story exists — is implemented cleanly. The child emits a structured stdout line on every exit path (success, `HoldoutLockError`, generic `Exception`) wrapped in an outer `finally{}` that also swallows emission failures so the real exit code is never masked. The psutil 7.2.2 primary path is exercised live by a real-subprocess test that asserts positive `peak_commit` + `peak_wset` + `pid > 0`, proving the kernel-maintained counters are reaching the line. The ctypes `GetProcessMemoryInfo` fallback is structurally present with the full `PROCESS_MEMORY_COUNTERS_EX` struct and is covered by a degraded-line parser test. The wrapper parses the log tail via an anchored regex, records the new fields in the summary JSON via an atomic `os.replace` rewrite, handles missing/malformed lines without crashing, and surfaces the values in a human-readable halt-report section. `core/run_with_ceiling.py`, `core/memory_budget.py`, `core/telemetry_schema.py`, and `data/manifest.csv` are all byte-identical to pre-patch — AC-D, AC-G, AC-H, AC-I hard constraints held. Anti-UTC grep clean, BRT-naive preserved. Regression intact (263/264).

The single concern (FINDING-01) is that the same commit folds in substantial ADR-4 §10.3 cache-dispatch + MWF-20260422-1 manifest-mode code that was not disclosed in the story's File List and is not part of T002.4 scope. Every piece of that folded-in code traces to shipped work on the branch (commits `85664f7`, `8c217bf`, `63a9a3a`), so there is no Article-IV invention violation — but the disclosure gap is real, and a reviewer focused only on T002.4 would miss it. This is why the verdict is **CONCERNS**, not PASS.

I recommend Riven (a) cross-check that the folded-in LOC aligns with the prior stories' ACs (should be mechanical — those stories already signed off on their halves), and (b) co-sign on the telemetry surface per Q6b B6. If Riven is satisfied that the folded-in work is consistent with prior signoffs, this gate clears on CONCERNS and Gage may merge after Riven's co-sign. If Riven wants the folded-in LOC split out into a separate commit for audit-trail cleanliness, Dex can rebase trivially — the telemetry hunk is the outermost changes in `main()` + the entirety of `_collect_child_peak_memory` / `_emit_child_peak_telemetry` + `TELEMETRY_CHILD_PEAK_TAG` constant.

**Gate: CONCERNS — clears to Riven co-sign dispatch.** AC-F remains unchecked pending Riven's signature. Gage must NOT merge without both signatures.

---

## Recommendations

1. **Riven co-sign (immediate):** per §Q6b B6, file Riven's co-sign record addressing (a) R5 gating byte-identical, (b) ADR-2 contract compatibly extended, (c) wrapper-surface scope contained, (d) B4 commit hash `80805ac` recorded for RA-20260428-1. Additionally spot-check FINDING-01 against prior stories' signoffs.
2. **Commit discipline (future stories):** when a telemetry story's diff incidentally folds in other-scope work, disclose in the commit body + story File List. The current body's File List implicitly scopes to "telemetry in materialize_parquet.py / run_materialize_with_ceiling.py", not to the full 541-LOC delta.
3. **Follow-up (low priority):** clean up the 2 mypy `arg-type` errors at `materialize_parquet.py:889` with a `cast(Path, ...)` or explicit assertion once mypy stub posture is normalized. Not a T002.4 concern.
4. **CodeRabbit retro-run (when CLI provisioned):** run `coderabbit --prompt-only -t commit=80805ac` once Gage provisions the CLI; attach output to this gate file for full T6 closure.
5. **RA-20260428-1 consumer:** when Riven drafts RA-20260428-1, ensure the audit-YAML schema `peak_commit_bytes_child_self_reported` field binds to the exact JSON key name used here (`peak_commit_bytes_child_self_reported`) and that step-7 derivation consumes THAT key, not `peak_commit`. Document the key-name contract in the RA template.
