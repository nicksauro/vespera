# QA Gate — T002.0e Cost Atlas Wire (engine-config + BacktestCosts.from_engine_config)

**Story:** [docs/stories/T002.0e.story.md](../../stories/T002.0e.story.md)
**Owner:** Beckett (@backtester)
**QA:** Quinn (@qa)
**Date:** 2026-04-25 BRT
**Verdict:** **PASS**

---

## Scope

Verificação de qualidade da materialização do snippet AC9 (Beckett-consume-signoff 2026-04-22) em arquivo canônico `docs/backtest/engine-config.yaml` v1.0.0 + loader testado `BacktestCosts.from_engine_config()`. Atlas v1.0.0 (Nova) já APPROVED_WITH_CONDITIONS por Sable em T002.0e original; este gate cobre apenas o wire incremental T9 (Beckett follow-up 2026-04-25).

Files in scope:
- `docs/backtest/engine-config.yaml` (NEW v1.0.0)
- `packages/t002_eod_unwind/adapters/exec_backtest.py` (`BacktestCosts.from_engine_config` classmethod + 3 custom exceptions + 5 helpers)
- `tests/t002_eod_unwind/adapters/test_exec_backtest_engine_config.py` (NEW 11 tests)
- `tests/t002_eod_unwind/adapters/test_exec_backtest.py` (12 legacy tests — back-compat must remain)

---

## Quality Checks

### Check 1 — `pytest tests/t002_eod_unwind/adapters/ -v`

**Result:** PASS — **35/35** tests pass in 1.14s.

Coverage breakdown:
- `test_exec_backtest_engine_config.py` — 11/11 (happy path + CRLF + sha drift + version drift + missing fields + back-compat + real-atlas integration)
- `test_exec_backtest.py` — 12/12 (legacy execute/force_exit/pnl + determinism + custom_costs + WDO multiplier)
- `test_feed_cache.py` — 8/8 (no regression)
- `test_feed_historical.py` — 4/4 (no regression)

### Check 2 — `ruff check packages/t002_eod_unwind/adapters/`

**Result:** PASS — `All checks passed!` Clean.

### Check 3 — SHA normalization LF/CRLF

**Result:** PASS.

- `_sha256_lf` helper (exec_backtest.py L196-205) reads atlas as bytes, normalizes `\r\n` → `\n`, hashes the result. This matches the canonical git blob hash (LF) regardless of Windows checkout EOL setting.
- `test_loads_engine_config_handles_crlf_atlas` writes atlas with CRLF endings explicitly via `body.replace("\n", "\r\n")` and verifies that `from_engine_config` still loads successfully and returns canonical Nova values.
- End-to-end validation against committed atlas: `python -c "BacktestCosts.from_engine_config('docs/backtest/engine-config.yaml')"` → returns `brokerage=0.00, exchange=1.23, roll_half=0.5, extra_ticks=1`. Atlas SHA `acf449415a3c9f5dce0571c307dc891d00488860132093c40b2f00c2434a5126` validated against engine-config lock.
- `test_loads_real_engine_config_against_real_atlas` is the gate that proves the committed engine-config matches the committed atlas in the repo.
- `policy_on_sha_mismatch: fail_fast` documented in engine-config L42 with rationale at L44-49.

### Check 4 — `[TO-VERIFY]` markers preserved (F-02/F-03/F-04 + slippage extra_ticks)

**Result:** PASS.

Found 6 distinct markers in engine-config.yaml:
- L88: `liquidacao_brl` `# 0.00 [TO-VERIFY 2026-04-22]` — Sable F-04 condition
- L143-145: `slippage_extra_ticks` `[TO-VERIFY 2026-04-25]` — Beckett worst-case cushion pending Tiago book/calibration

`audit_provenance.open_audit_conditions_tracked` (L185-205) explicitly tracks all 4 open conditions:
- F-02 (BTG source aging) — `blocks_phase_E: false`
- F-03 (Portal Tributário tax source weak) — `blocks_phase_E: false`
- F-04 (3 sources HTTP 403) — `blocks_phase_E: false`
- atlas.to_verify[0] (liquidacao_brl) — `blocks_phase_E: false`

All 4 deferred to atlas v1.0.1 / future sprints; none gate Phase E.

### Check 5 — Article IV traceability

**Result:** PASS.

Each numerical value in engine-config.yaml is traceable to one of three sources:
- (a) Atlas Nova v1.0.0 lookup via dotted `from:` path (no duplication of value at engine-config layer; comments document expected for code review).
- (b) Deterministic Beckett simulator parametrization (slippage_model.roll_spread_half_points=0.5, slippage_extra_ticks=1) — documented in `slippage_model.rationale` L136-141.
- (c) `[TO-VERIFY YYYY-MM-DD]` carrying open audit condition.

Verified mappings:
| Field                     | atlas path                                          | atlas value | engine-config target                  |
|---------------------------|-----------------------------------------------------|-------------|---------------------------------------|
| `brokerage.from`          | `costs.brokerage.per_contract_one_way`              | 0.00        | `brokerage_per_contract_side_rs`      |
| `exchange_fees.from`      | `costs.exchange_fees.per_contract_one_way`          | 1.23        | `exchange_fees_per_contract_side_rs`  |
| `ir_day_trade.from`       | `costs.tax_day_trade`                               | post-hoc    | (post-hoc, not per-fill)              |
| `wdo_contract.tick_size`  | `product.tick_size_points`                          | 0.5         | `WDO_TICK_SIZE` constant assertion    |
| `wdo_contract.multiplier` | `product.contract_multiplier_brl_per_point`         | 10.00       | `WDO_MULTIPLIER` constant assertion   |

Loader (`from_engine_config`) does NOT bypass atlas lookups — `_resolve_atlas_path_lookup` raises `EngineConfigInvalid` if any dotted path is broken. Single-source authority preserved.

### Check 6 — Back-compat legacy constructor

**Result:** PASS.

- `BacktestCosts(brokerage=0.25, exchange=0.35, ...)` direct constructor preserved (exec_backtest.py L68-72).
- Defaults marked `# DEFAULT — override via nova-cost-atlas.yaml v>=1.0.0; [TO-VERIFY] until wired in engine-config` (L68).
- Docstring marked `[DEPRECATED — use from_engine_config]` (L62).
- `test_back_compat_default_constructor_still_works` — verifies legacy defaults (R$ 0.25 brokerage, R$ 0.35 exchange) still construct.
- `test_back_compat_explicit_overrides_still_work` — verifies kwargs path intact for existing test suite.
- All 12 legacy `test_exec_backtest.py` tests pass unchanged → `BacktestBroker.execute()` / `force_exit()` / `pnl_contracts()` API not modified.
- `Fill` dataclass unchanged → AC14 contract preserved (T002.0c `TradeRecord.entry_fill: Fill` parity intact).

### Check 7 — AC9 (engine-config wire) + AC10 (exec_backtest comment)

**Result:** PASS.

- AC9 promoted from comment-in-story to canonical file at `docs/backtest/engine-config.yaml` v1.0.0. Snippet from Beckett-consume-signoff 2026-04-22 materialized + extended with `cost_atlas_ref`, `cost_components`, `slippage_model`, `wdo_contract`, `audit_provenance`, `consumer_contract`, `changelog` sections.
- AC10 preserved: `exec_backtest.py` L68 `# DEFAULT — override via nova-cost-atlas.yaml v>=1.0.0; [TO-VERIFY] until wired in engine-config` matches story spec exactly. `grep "nova-cost-atlas" exec_backtest.py` finds matches in docstring (L4, L64) and in default comment (L68).
- Story checkboxes T6, T7, T8, T9 marked `[x]`. AC1-AC10 marked `[x]`.

---

## Findings

### CRITICAL: 0
None.

### HIGH: 0
None.

### MEDIUM: 0
None.

### LOW: 2
- **L1 — Story checkboxes for AC1-AC10 already pre-marked `[x]`** in story file. AC1-AC8 belong to Nova/Sable (delivered 2026-04-22, audited APPROVED_WITH_CONDITIONS); AC9-AC10 belong to Beckett (delivered 2026-04-25). All evidence in story logs is consistent — no fabrication detected. Cosmetic only. Pax may reconcile lifecycle in `*validate-story-draft`.
- **L2 — `audit_provenance.open_audit_conditions_tracked` lists 4 items** referencing Sable findings F-02/F-03/F-04 + atlas.to_verify[0] for liquidacao_brl. All marked `blocks_phase_E: false`. Tracked correctly per Sable verdict APPROVED_WITH_CONDITIONS; deferred to atlas v1.0.1 sprint. Not a defect.

### Notes (informational)
- IR day-trade NOT injected into `BacktestCosts` (which operates per-fill); `applies_at: metrics_aggregation_monthly` documented in engine-config L112. Correct architectural separation — confirmed by Riven persona ownership for production live calibration.
- IRRF 1% explicitly ignored in backtest as it is compensable, not a terminal cost. Documented for audit at L120.
- `slippage_model` carries explicit `to_verify` block for `slippage_extra_ticks` cushion — defers to Tiago calibration. Article IV satisfied via `rationale` field.

---

## Dependencies & Cross-Refs

- Atlas Nova v1.0.0 sha lock `acf449415a3c9f5dce0571c307dc891d00488860132093c40b2f00c2434a5126` matches `git show 59e03fe:docs/backtest/nova-cost-atlas.yaml | sha256sum` (Beckett verification log).
- `BacktestBroker.execute()` / `force_exit()` / `pnl_contracts()`: NOT modified — AC14 Fill parity from T002.0c preserved.
- T002.0c `TradeRecord.entry_fill: Fill` continues to import `Fill` from this module (`packages/vespera_cpcv/result.py` L28) — coherence verified.

---

## Verdict Rationale

All 7 Quinn checks PASS. SHA normalization handles Windows CRLF. Fail-fast policy on sha + version drift verified. Article IV preserved (no value duplication; atlas single-source authority). Back-compat constructor intact (12/12 legacy tests pass). Real-atlas integration test (`test_loads_real_engine_config_against_real_atlas`) exercises end-to-end against committed atlas — gate against future drift. `[TO-VERIFY]` markers correctly preserved.

**No CRITICAL/HIGH/MEDIUM findings.** Two LOW (cosmetic checkbox lifecycle + tracked-conditions accounting) — non-blocking.

Story T002.0e is **PASS** for merge. Approved for Gage push (R12). Soft-gate of Phase E GO/NO-GO satisfied.

---

## Next Handoff

`@devops` (Gage) — `*push` for T002.0c + T002.0e together. Both gates PASS; clean ruff; full test pass; AFML byte-exact + atlas SHA-locked.

— Quinn, guardião da qualidade 🛡️
