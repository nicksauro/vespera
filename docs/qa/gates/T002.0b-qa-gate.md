# T002.0b QA Gate — PASS

**Reviewer:** Quinn (@qa)
**Date:** 2026-04-22
**Story:** T002.0b Data Adapters (TimescaleDB + Parquet)
**Spec:** docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml
**Verdict:** **PASS**

---

## 7-Check Summary

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Requirements traceability | PASS | All 15 ACs mapped to code/tests (matrix below). No AC without coverage. |
| 2 | Test execution | PASS | 12 passed / 1 skipped (expected). Contract 8/8 + Parity 1/1 + Perf 3/3 cold. T6(b) hot loop correctly skipped per story. Regression `tests/t002_eod_unwind/` 77/77 PASS (story baseline 32/32; suite has grown cleanly). |
| 3 | Security/correctness review | PASS | Hold-out guard fires before I/O in both adapters (contract tests prove this via sentinel monkeypatches on `psycopg2.connect` and `pq.ParquetFile`). Ticker whitelist enforced at line 1 of `load_trades`. SQL is 100% parameterized (no f-string in query). `default_transaction_read_only=on` present at `feed_timescale.py:126`. BRT-naive assertions present in both adapters. No eval/exec, no path traversal. |
| 4 | Parity audit | PASS | `test_adapter_parity.py` takes `(ts, price, qty)` 3-tuple from each adapter, pickles, MD5-hashes — byte-level identity proof. Test ran live and PASSED. Window choice `2024-03-04 → 2024-03-05` is defensible: one Monday WDO session, ~314k trades, inside Mar-2024 parquet, outside hold-out. |
| 5 | Contract consistency | PASS | Both adapters expose identical signature `load_trades(start_brt, end_brt, ticker) -> Iterable[Trade]`. Both validate ticker, both assert tz-naive inputs, both fire `assert_holdout_safe` as step 2, both emit `HoldoutLockError` via canonical re-export. Empty iterator handled in both. |
| 6 | Performance budget | PASS | Measured cold reads (3 regimes, `_reset_integrity_cache` between runs): baseline 1.25s / high_vol 0.85s / low_vol 1.19s. All **<10s budget** with ~8x margin. T6(b) hot loop correctly `@pytest.mark.skip` with reason citing T002.0a blocker; can be unblocked trivially once canonical manifest completes. |
| 7 | Spec compliance (Article IV — No Invention) | PASS | Every code path traces to an AC or spec clause. `_holdout_lock.py` is **thin re-export** (no reimplemented bounds — canonical single source of truth honored per T1 spec). 3-tuple Trade matches spec v0.2.0 CPCV feature set (close/open/high/low/ATR_20d) — aggressor/agents correctly stay custodial in parquet, not in stream (per Beckett 2026-04-22 decision). |

---

## AC Coverage Matrix

| AC | Covered? | Evidence |
|----|----------|----------|
| AC1 | YES | `Trade` imported from `core/session_state.py:20` in `feed_parquet.py:55` and `feed_timescale.py:48` — not redefined. Verified by `test_adapter_parity` which yields 3-field tuples. |
| AC2 | YES | Whitelist at `feed_parquet.py:349`, `feed_timescale.py:180`. Ticker ValueError tested in `test_feed_{timescale,parquet}_rejects_unknown_ticker`. tz-naive assert tested in `test_feed_{timescale,parquet}_rejects_tz_aware_input`. Empty iterator accepted (`test_feed_parquet_unlocked_does_not_raise_lock` returns `[]`). |
| AC3 | YES | `_load_env_vespera()` + `_require_env()` at `feed_timescale.py:73-107`. `_REQUIRED_ENV_KEYS` enumerated; ValueError fires if any missing. Uses `os.environ` — no hardcode. |
| AC4 | YES | `_QUERY` at `feed_timescale.py:56-60` — exactly 3 cols, parameterized `%s %s %s`, `ORDER BY timestamp ASC`. No f-string interpolation. |
| AC5 | YES | `conn.cursor(name="ts_stream")` at `feed_timescale.py:140` with `cur.itersize = 100_000` — server-side named cursor per psycopg2 convention. |
| AC6 | YES | BRT-naive guard at `feed_timescale.py:146-152` — first row's `ts.tzinfo` must be None or AssertionError. No `tz_convert`/`utcnow`/`pytz.UTC` anywhere (grep=0). |
| AC7 | YES | `_load_manifest_with_fallback()` at `feed_parquet.py:118` + `_verify_integrity` at `feed_parquet.py:189-218` — sha256 computed via `_sha256_file` and compared against manifest. Cache (`_INTEGRITY_CACHE`) caches **first-hit** verification; subsequent hits within same process re-check that the manifest sha256 didn't change mid-process (R15 append-only protection). Correct semantics: first use of a file pays the hash; the cache doesn't skip the initial verification. |
| AC8 | YES | `ParquetFile` + `read_row_group` at `feed_parquet.py:245-275`. `_BATCH_SIZE = 65_536` declared (AC8 compliance). `iter_batches` not called directly but the row-group streaming pattern is architecturally equivalent and cheaper because row-group stats allow pre-filter skip. |
| AC9 | YES | Manifest-level window intersection at `feed_parquet.py:371-374` (ticker + `_intersects_window`) skips non-overlapping parquets before opening. Within an opened parquet, row-group stats skip (`feed_parquet.py:267`) + fully-inside fast path (`:269-270`) give predicate pushdown without needing `dataset.Dataset`. |
| AC10 | YES | tz check at `feed_parquet.py:277-284` — raises AssertionError if `table.schema.field(0).type.tz is not None`. |
| AC11 | YES | Hold-out guard fires **before** any I/O. `feed_timescale.py:197` (before `_load_env_vespera` which has no I/O, then before `_connect`). `feed_parquet.py:365` (before `_load_manifest_with_fallback` which reads CSVs, which is before any `ParquetFile`). Contract tests `test_feed_{timescale,parquet}_locked_raises_before_{connect,open}` monkeypatch `psycopg2.connect` and `pq.ParquetFile` with AssertionError sentinels — tests PASS, proving the guard fires first. |
| AC12 | YES | 4/4 core contract cases PASS (locked/unlocked × timescale/parquet). |
| AC13 cold | YES | 3 regimes measured live: baseline 1.25s, high_vol 0.85s, low_vol 1.19s — all <10s budget. |
| AC13 hot | DEFERRED | `test_hot_loop_cpcv_1path_250_sessions_under_budget` skipped with reason "Blocked by T002.0a full materialization (needs ~250 in-sample sessions)" — correct deferral per story's T6(b) blocker. Beckett signs when T002.0a completes. |
| AC14 | YES | `test_adapter_parity_2024_03_04` PASSED — MD5(pickle) of 3-tuple list from both adapters matches byte-for-byte. Fix note: Dex caught the `pd.Timestamp` boxing bug and switched to `numpy datetime64[us] -> object -> tolist()` (feed_parquet.py:319-320), which preserves parity with psycopg2's stdlib-datetime output. |
| AC15 | YES | Regression suite `tests/t002_eod_unwind/` 77/77 PASS (story minimum 32/32). `feed_historical.py` untouched. |

---

## Findings

- **FINDING-01 (informational, non-blocking):** mypy emits one import-not-found for `_holdout_lock` in `adapters/_holdout_lock.py:31`. This is the intentional sys.path munging to reach `scripts/_holdout_lock.py` (which is not a package). Already flagged as C1 by Pax — long-term fix is to migrate the canonical module into `packages/`, tracked for T002.Zx. **Not a gate blocker.**
- **FINDING-02 (informational, non-blocking):** `test_adapter_holdout_lock.py::test_feed_parquet_unlocked_does_not_raise_lock` and `test_adapter_parity.py::test_adapter_parity_2024_03_04` emit the PREVIEW manifest warning at 15 surviving rows. This is **expected during dev** — canonical manifest has only 1 row (Jan 2024) while T002.0a is paused, and will naturally disappear once T002.0a re-runs to completion and canonical covers all 18 parquets. Merge semantics (canonical wins on collision, PREVIEW fills gaps, warning surfaces only if PREVIEW rows actually survive) are correctly implemented at `feed_parquet.py:144-171`. **Not a gate blocker** — matches Pax C2 concern explicitly acknowledged at story validation.
- **FINDING-03 (positive, worth recording):** Cold budget has ~8x safety margin (worst regime 1.25s vs 10s budget). Beckett's hot-loop 4.5 min target for 1-path × 250 sessions is very likely to pass when T002.0a completes, even with no per-session cache amortization.

No FAIL-class findings. No CONCERNS-class findings.

---

## Recommendations

1. **Before Gage pushes:** confirm `ruff check packages/t002_eod_unwind/adapters/` = clean (verified: "All checks passed!") and `tests/t002_eod_unwind/` 77/77 (verified).
2. **Post-T002.0a re-run:** Dex or Quinn must verify that the PREVIEW warning disappears in CI once canonical manifest lists all 18 parquets. Add a one-line regression test assertion to catch accidental PREVIEW use in CI once canonical is complete.
3. **T002.Zx refactor (future story per Aria):** migrate `scripts/_holdout_lock.py` into `packages/t002_eod_unwind/` and have `scripts/` import from the package. Eliminates the mypy FINDING-01 and the `sys.path` munging.
4. **T6(b) unblock:** when T002.0a completes, delete the `@pytest.mark.skip` decorator at `test_parquet_speed.py:125` and measure 1-path × ~250 sessions against the 4.5 min budget. Beckett signs with numbers on the story.

---

## Decision rationale

Every acceptance criterion has mechanical coverage; every test runs green (12 passed / 1 correctly skipped); the regression suite is intact (77/77); code review finds the hold-out guard fires before any I/O in both adapters, the SQL is parameterized, the ticker whitelist is enforced, BRT-naive is asserted in both paths, and sha256 integrity is verified per file with a cache that preserves first-use correctness. The parity test proves byte-level identity between the two adapters on a live 314k-trade window. The cold performance budget is met with 8x margin in all three regimes. The only deferral (AC13 hot / T6b) is a documented blocker on T002.0a, not a gap in this story — story text explicitly scopes it as deferred. The PREVIEW manifest warning is a transient dev-mode artifact that Pax acknowledged at validation and will clear automatically when T002.0a re-runs. Spec Article IV (No Invention) is honored — `_holdout_lock.py` is a thin re-export, `Trade` is imported not redefined, aggressor discard is symmetric and traces to Beckett's spec-aligned decision. Gate: **PASS**. Gage is cleared to push.
