# T002.6 — Beckett Consumer Sign-off (T0c) on Mira Gate 4b Spec v1.0.0

**Author:** Beckett (@backtester) — Backtester & Execution Simulator authority; consumer of Mira spec / co-author of latency profile.
**Date (BRT):** 2026-04-29
**Branch:** `t002-2-phase-f-real-tape`
**Stage:** T0c — third sign-off in Mira spec-finalize chain (T0a Mira ✅ → T0b Aria ✅ APPROVE_WITH_CONDITIONS → **T0c Beckett (THIS DOC)** → T0d Riven ⏳ → T0e Pax ⏳ → T1 Dex gated).
**Source spec under sign-off:** `docs/ml/specs/T002-gate-4b-real-tape-clearance.md` v1.0.0 (Mira T0a finalize, 14 §§).
**Predecessors consumed:**
- Aria T0b archi review: `docs/architecture/T002.6-aria-archi-review.md` (APPROVE_WITH_CONDITIONS, 7 binding C-A1..C-A7).
- Beckett latency spec (self-authored, consumed verbatim by Mira §5): `docs/backtest/latency-dma2-profile-spec.md` v1.0 PROPOSED.
- Nova RLP/rollover spec (consumed verbatim by Mira §4): `docs/backtest/T002.6-nova-rlp-rollover-spec.md` v1.0.0.
- Beckett N6 report (synthetic baseline — perimeter contrast): `docs/backtest/T002-beckett-n6-2026-04-29.md`.
- T002.6 story Status Ready (Pax 10/10): `docs/stories/T002.6.story.md`.

---

## §1 Spec executable verdict — **PASS**

Mira spec v1.0.0 is **mechanically executable** by the Beckett N7+ real-tape harness. Verdict on the four executor-relevant interfaces:

### §1.1 Real-tape data interface (Mira §3) — PASS

| Item | Mira spec | Beckett executor view |
|---|---|---|
| Tape source single path | `D:\sentinel_data\historical\` per-session per-contract (§3.1) | Confirmed canonical per Beckett `historical_data_reality.location` persona block. WDO coverage 571 sessions; window 2024-08-22..2026-03-20 contains the in-sample lock 2024-08-22..2025-06-30 (Mira §3.1). Hold-out 2025-07-01..2026-04-21 also in coverage, NOT touched per Anti-Article-IV Guard #3. **Executable.** |
| Schema columns | 5 columns: `ts`, `price`, `qty`, `aggressor`, `tradeType` (§3.2) | Beckett `historical_data_reality.schema` block matches `timestamp`, `price`, `qty`, `aggressor`, `trade_type` (BUY/SELL/NONE; the 13-value Nelo enum ABSENT — §3.2 row 5 + §4.3). Mira correctly flags Live-vs-Historic gap. **Executable; gap is acknowledged-not-invented.** |
| Replacement of `_walk_session_path` / `_walk_to_exit` | Synthetic walk REMOVED; new `_replay_session_tape_real` + `_walk_real_tape_to_barrier` (§3.3) | Interface contract `(entry_event, session_tape, atr_hora, latency_model) -> ExitEvent` matches Beckett `fill_rules_trades_only` (market order step_1..step_4 + limit-order rules) — minus book queue logic which is N/A in trades-only regime per Beckett persona block. **Executable** — the helper signature aligns with the trades-only fill_engine. |
| Anti-leakage guard | "Barrier-hit detection at-tape; fill price reconstruction within latency window uses ONLY tape data ≤ `t_decision + latency_ms`" (§3.3) | Beckett `fill_rules_trades_only.market_order` step_3 mandates `fill_price = next observed trade after t + L_ida + slippage_model`. No look-ahead path possible. **Executable.** |
| Cost atlas wiring | Atlas v1.0.0 dual-hash, F1 patch `9997f14`, `cost_atlas_sha256` + `rollover_calendar_sha256` in `determinism_stamp.json` (§3.5) | N6 baseline already proved the closure-capture pattern works (`engine_config_sha256` populated). N6+ post-F1 patch lifted `cost_atlas_sha256` from null → populated. **Executable; carry-forward from N6+.** |

### §1.2 Strategy logic preservation (Mira §2) — PASS

T002.1.bis APPROVE_OPTION_B factory pattern + per-fold P126 D-1 invariant + Bonferroni n_trials=5 are preserved verbatim per Aria T0b §2 PASS. From the executor side — N6 already proved `_resolve_trial_params` T1..T5 + `_build_daily_metrics_from_train_events` (Aria §I.5) per-fold rebuild. Phase F mutates only the price source inside the closure body. **No new executor risk.**

### §1.3 ADR-1 v3 6 GiB RSS cap defensibility — PASS WITH PROJECTED BUDGETS

ADR-1 v3 RSS ≤ 6 GiB applies. N6 baseline observed:
- **Python-process RSS peak:** 0.158 GiB (`telemetry.csv rss_mb max`)
- **OS working-set peak:** 0.597 GiB (`peak_wset_bytes max`)

Real-tape replay loads parquet trade tapes per session. WDO daily parquet typical size: 5-50 MB per session. In-sample window 2024-08-22..2025-06-30 ≈ 220 sessions. Per-fold loading model:

| Loading strategy | Working-set delta | Defensibility vs 6 GiB |
|---|---|---|
| **A — Lazy per-session** (recommended): load 1-3 sessions in memory at a time, GC between | +50-150 MB over N6 baseline → projected peak ≈ 0.7-1.0 GiB OS-WS | 6× headroom — **DEFENSIBLE** |
| **B — Per-fold materialization** (alternative): load entire fold (~7-8 sessions × 5 trials × 45 paths) — but per-fold is small | +200-500 MB over N6 → projected peak ≈ 1.0-1.5 GiB OS-WS | 4× headroom — **DEFENSIBLE** |
| **C — Eager whole-window**: load all 220 sessions × 50 MB upper bound = 11 GiB | OUT-OF-BUDGET | **REJECTED** — exceeds cap |

Beckett executor commits to Strategy A (lazy per-session) by default. Strategy B reserved as fallback if per-session loading creates wall-time bottleneck (Mira §6 sample-size floor is 150-250 events total, distributed across 220 sessions × 5 trials). **Projected RSS budget: ≤ 1.5 GiB OS-WS, 4× headroom under 6 GiB cap.** AC7 ADR-1 v3 IMMUTABLE — preserved.

### §1.4 Wall-time budget projection — PASS WITH PROPOSED CEILING

Mira spec does NOT specify a wall-time budget; Aria T0b §6.3 cites "wall-time budget projection from Beckett N7+ smoke phase" as deferred. Beckett-as-executor proposes the budget here per Mira §0.2 Scope IN dimension 6 (T002.6 story AC7).

**Synthetic N6 baseline:** 27.4 s (full phase, 3800 events, 5 trials × 45 paths = 225 sharpe paths). Per the synthetic walk's pre-baked tape (instantaneous events), N6 was IO-trivial.

**Real-tape escalation factor estimation:**

| Cost factor | Synthetic N6 | Real-tape N7+ projection | Multiplier |
|---|---|---|---|
| Event count | 3800 events | ~3800 events (parametrically same; AC2 carry-forward of `build_events_dataframe`) | 1× |
| Per-event tape walk | constant 8 ticks × 32 bars synthetic walk = O(1) | per-event walk on real tape: median ~50-200 trades from entry to barrier hit (15-min holding window × ~10-20 trades/min on WDO afternoon) | ~50× per event |
| Tape IO | none (in-memory synthetic) | parquet load per session lazy: ~10-50 ms per session × 220 sessions × 5 trials (cached per fold) = ~10-30 s amortized | additive ~10-30 s |
| Latency model draw | not applied (synthetic skip per `enabled_for_phase: ["F"]`) | per-fill blake2b_64 + lognormal inverse CDF: ~1 µs per draw × ~7600 fills (entry+exit × 3800 events) = ~10 ms total | negligible |
| Slippage formula | constant cost atlas | constant cost atlas + additive `slippage_latency_pts` term (Mira §5.3) | negligible |

**Total projection:** 27.4 s × 50 (per-event walk multiplier, dominant) ≈ 1370 s ≈ **23 min**. Conservative pessimist add 2× safety margin for first-run debug + lazy-load IO surprises = **45 min ceiling**.

**Beckett proposed wall-time ceilings:**

| Phase | Proposed ceiling | Rationale |
|---|---|---|
| **Smoke** (1-month window 2025-05-31..2025-06-30 ≈ 21 sessions × 5 trials, ~360 events per N6 smoke baseline) | **15 min** (900 s) | 27 s × 50 ≈ 23 min synthetic-equivalent; smoke has 1/10 events vs full → ~2.3 min projected, 6× headroom for debug |
| **Full** (in-sample 2024-08-22..2025-06-30 ≈ 220 sessions × 5 trials, ~3800 events) | **45 min** (2700 s) | Per projection above; 2× pessimist margin |
| **Hard cap (early-abort)** | **60 min** (3600 s) | If wall exceeded, Beckett early-abort → mini-council escalation (R-9 wall-time runaway risk per T002.6 story). |

These are **PROPOSED** budgets, NOT yet baked into the spec. Recommend Mira / Pax T0e cosign these values into Mira §6 (sample-size mandate) or §8 (Beckett N7+ pre-conditions) per appropriate locus.

### §1.5 Verdict §1 — PASS

Mira spec v1.0.0 is mechanically executable. Real-tape interface clean; ADR-1 v3 RSS budget defensible (4× headroom proj.); wall-time ceilings proposed (15 min smoke / 45 min full / 60 min hard cap). One **CONDITION** (C-B1 below) on §1.4 wall-time ceiling baking; one **CONDITION** (C-B2) on lazy-load implementation strategy. No NEEDS_REVISION.

---

## §2 Latency model applicability — PASS (self-authored)

Beckett is **author** of latency spec consumed verbatim by Mira §5. Self-confirmation:

| Aria T0b condition | Beckett response |
|---|---|
| **C-A1** — `_walk_real_tape_to_barrier` call site MUST be inside closure body, NOT factory builder | **CONFIRMED.** Latency model draw is per-event, per-fill — must execute at fill materialization time inside the closure (not at factory build time which is per-fold). Aligns with Beckett `fill_engine` description: "decide if/when/qual preço uma ordem é executada" — fill-level, inside closure. |
| **C-A2** — Slippage integration at fill-event materialization time, not PnL aggregation, to anchor seed `(session, order_id, trial_id, component_name)` deterministically | **CONFIRMED.** Beckett latency spec §2.4 verbatim: `seed_event = hash((session, order_id, trial_id, component_name))`. Per-fill API REQUIRED. Helper `LatencyModel.from_engine_config(...)` exposes `.draw_latency_ms(seed_inputs)` per call — Dex T1 to wire. |
| **C-A3** — `enabled_for_phase` gate unit-tested for F-enabled and F-disabled paths | **CONFIRMED.** F-disabled (synthetic regime) returns `slippage_latency_pts == 0` deterministically because `enabled_for_phase: ["F"]` short-circuits before lognormal draw. F-enabled returns log-normal draw bounded by `(P50, P95)` anchors. **Test surface aligned.** |

### §2.1 `enabled_for_phase: ["F"]` gating — confirmed

Phase F only (real tape). Phase E synthetic walk skipped per Mira §5.2 row 1: "synthetic walk N6 events were instantaneous; latency physically n/a." Beckett confirms: synthetic walk's `_walk_session_path` synthesizes prices on-demand at decision time → `mid_at_decision == mid_at_fill_after_latency` by construction → `slippage_latency_pts = 0` trivially. The gate is enforced at the closure body via `if latency_model.enabled_for_phase contains current_phase: draw_latency() else: 0`. **Factory dispatch consistent.**

### §2.2 Slippage formula composition — no double-counting confirmed

```
total_slippage_pts = roll_spread_half_points          # immediate spread cushion (existing)
                   + slippage_extra_ticks × tick_size # static extra-ticks override (existing)
                   + slippage_latency_pts             # NEW Phase F: temporal mid drift during round-trip
```

Three components are **temporally orthogonal**:
- `roll_spread_half_points` is the **half-spread** at the fill instant → spatial cost (cross the book once).
- `slippage_extra_ticks × tick_size` is a **static** override / safety margin → constant-tick conservative add.
- `slippage_latency_pts = sign × (mid_at_decision − mid_at_fill_after_latency)` is the **mid-drift during latency window** → temporal cost.

No two of these terms measure the same physical phenomenon. **No double-counting.** Engine-config v1.0.0 §slippage_model preserved verbatim; v1.1.0 ADDS `latency_model:` block (Beckett spec §4); cosign re-locked at Beckett N7+ smoke run.

### §2.3 Determinism — seed anchoring confirmed

Per-event seed = `blake2b_64( (session, order_id, trial_id, component_name) )`. Cross-platform stable; bit-identical replay. Aria T0b §4 row 3 confirms zero RNG-state-leak. Beckett re-confirms: at no point does the latency draw mutate `numpy.random` global state — it uses a per-call `numpy.random.default_rng(seed)` instance scoped to the fill event. **R7 hold-out lock spirit + ADR-1 v3 reproducibility ledger preserved.**

### §2.4 Verdict §2 — PASS (self-authored)

Beckett confirms own spec. No condition; no findings.

---

## §3 Toy benchmark real-tape proposal — Beckett implementer's plan

Mira §8(f) mandates re-running Bailey-LdP 2014 toy benchmark on real-tape harness with seeds `[42, 1337, 271828, 314159, 161803]` UNMOVABLE, asserting **Δ DSR > 0.10 across 5 seeds preserved**. Beckett-as-implementer proposes:

### §3.1 Strategy A no-edge / Strategy B small-edge over real tape

Per Aria C-A6 (structural sibling, NOT fork): import the same fixture pattern from `tests/cpcv_harness/test_toy_benchmark_bailey_lopez_de_prado_2014.py` which encodes Bailey-LdP 2014 §3 + Table 2 toy benchmark. Strategy contracts:

| Strategy | Synthetic version (Phase E) | Real-tape version (Phase F) |
|---|---|---|
| **A — no-edge** | Random labels uniform on `{−1, 0, +1}`, seed-driven | Random entry direction `{long, short}` chosen via `numpy.random.default_rng(seed_external_to_tape)` at session-day seed; entries fired at the standard 4 windows (16:55/17:10/17:25/17:40 BRT); barriers triple-barrier per Mira §2.1 over the **real WDO tape**. By construction, expected edge = 0 because direction is random; non-zero variance because real tape has volatility. |
| **B — small-edge** | Synthetic noise + small +0.1σ drift for true label sign | Real WDO tape + tiny edge injection: bias direction probability 55%/45% in favor of the empirically-observed end-of-day fade direction (negative sign on `intraday_flow_magnitude > P-upper`, positive when < P-lower per parent thesis) → injects ~0.10 standardized edge per Bailey-LdP 2014 Table 2. Barriers triple-barrier real tape. |

**Discriminator power claim:** Δ DSR = DSR(Strategy B) − DSR(Strategy A) > 0.10 across all 5 seeds, replicating the Bailey-LdP harness witness on real tape source.

### §3.2 Why this preserves the harness witness

The harness measures **DSR sensitivity to label-sign signal-to-noise**. Mathematically, Strategy A's PnL distribution is symmetric around 0 (random direction × realized return); Strategy B's is shifted right by ~0.10σ × realized-return-volatility. Bailey-LdP 2014 §3 shows DSR distinguishes the two with statistical power ≥ 0.50 at sample size ≥ 30-50 events per trial (matches Mira §6 mandate).

**Real-tape consideration:** real WDO afternoon tape has time-varying volatility (heteroscedasticity). The harness must be robust to this — but the theoretical argument holds because Δ DSR depends on the difference of two means over the **same** noise realization, and heteroscedasticity affects both equally. Empirically: if Δ DSR collapses below 0.10 on real-tape, the **harness is broken**, not the strategy → Mira §7 Step 2 routes to `data_quality` bucket. This is by design.

### §3.3 Seeds [42, 1337, 271828, 314159, 161803] UNMOVABLE — confirmed

Beckett accepts. These are post-spec-finalize Mira authority (Mira §8(f) verbatim). Three observations:
1. The 5 seeds span deterministic-different starting points; Bailey-LdP 2014 §3 pseudo-replicate convention.
2. NumPy `default_rng(seed)` with these seeds produces independent draws across the 5 fixture instances.
3. Beckett N7+ harness run will execute the toy benchmark **before** the strategy benchmark — a "smoke test on the harness" preceding the "smoke test of the strategy." Pass/fail of toy benchmark is GATE for proceeding to Strategy benchmark per Mira §7 Step 2.

### §3.4 Verdict §3 — IMPLEMENTABLE; no conditions

Beckett implementer commits to executing toy benchmark per §3.1-§3.3 above. No additional conditions beyond Aria C-A6.

---

## §4 3-bucket attribution executability — PASS WITH SCOPE CLARIFICATION

Mira §7 decision tree mandates 5-step attribution. Beckett-as-executor commits to producing the inputs; **Mira retains adjudication authority** for bucket assignment. Beckett produces tech-evidence; Mira produces verdict. Aligns with Beckett persona "tech enabler" stance.

### §4.1 Inputs Beckett produces from N7+ run

| Input | How Beckett computes from parquet | Mira §7 Step |
|---|---|---|
| `total_events` | Count rows in `events_dataframe` post-fold partition; sum across 5 trials | Step 1 (sample-size floor) |
| `session_coverage_%` | (sessions with ≥1 fill event in N7+ output) / (sessions in window with ≥1 valid entry-window candidate per parent thesis) × 100 | Step 3 (data_quality vs strategy_edge) |
| `circuit_breaker_count` | Count sessions where `circuit_breaker_fired: bool == True` per Nova §4.1 detection signal (continuous `aggressor=NONE` gap >30 min during 09:30-17:55) | Step 3 threshold ≥3 → data_quality bucket |
| `rlp_gap_count` | Per Nova §1.3: historic parquet has NO RLP discriminator. Gap is **structural** (not sporadic). Beckett records `data_quality.rlp_flag_available_historic: false` in determinism stamp — counts not as "gaps" but as "regime constant" | Step 3 — see §4.3 below |
| `toy_benchmark_delta_DSR_per_seed` | Run §3 toy benchmark with 5 seeds, compute DSR(Strategy B) − DSR(Strategy A) per seed | Step 2 |
| `triplet_metrics` | DSR / PBO / IC per Mira §1 thresholds | Step 3 |

Beckett emits these into `gate_4b_attribution_inputs.json` alongside the N7+ standard artifacts (`determinism_stamp.json`, `events_metadata.json`, `full_report.json`, `full_report.md`, `telemetry.csv`).

### §4.2 RLP gap clarification per Nova §1.3

Mira §7 row "data_quality (a) … `≥2 sessions with RLP-detection-gap`" reads literally as if RLP gaps are **session-by-session** sporadic events. Per Nova §1.3 historic regime: **the RLP discriminator is ABSENT for the entire historic parquet** (not session-by-session). Two readings:

| Reading | Interpretation |
|---|---|
| **Literal** | "RLP gap count = 220" (every session has the gap) → Step 3 threshold ≥2 trivially TRUE → automatic data_quality bucket → Mira FAIL by data_quality regardless of triplet outcome. Cannot be Mira's intent because it would forbid Gate 4b PASS forever in historic regime. |
| **Pragmatic (Beckett reading)** | "RLP gap count" = sporadic-event count, NOT structural-regime count. Structural regime (Nova §1.3 historic ABSENT) is **already handled** by `data_quality.rlp_flag_available_historic: false` determinism stamp. Sporadic events would be e.g., a malformed parquet day. |

Beckett **proposes** Mira clarify (suggested addendum to Mira §7): "RLP-detection-gap count = sporadic per-session anomalies, NOT the structural Nova §1.3 historic-regime constant. The latter is recorded as `data_quality.rlp_flag_available_historic: false` and does NOT trigger Step 3 threshold." This is a CONDITION C-B3 below.

### §4.3 Beckett bucket-assignment authority — NO

Beckett produces evidence; Mira adjudicates. This is consistent with:
- Aria T0b §6 — Riven authoring authority for ledger entries; Aria sets format.
- Mira ML/statistical authority over Gate 4b clearance per ESC-011 R11.
- Beckett persona Article IV trace: bucket assignment requires statistical reasoning (which IC/DSR pattern → which bucket), Mira jurisdiction.

**Beckett confirms NON-AUTHORITY for bucket assignment.** Beckett produces inputs only.

### §4.4 Verdict §4 — PASS (with C-B3 clarification request)

3-bucket attribution scaffolding is executable. Beckett produces the 6 inputs (§4.1). Mira retains bucket adjudication. C-B3 requests Mira clarify the RLP gap reading per §4.2 (sporadic vs structural).

---

## §5 Beckett N7+ run plan + budget proposals

### §5.1 Run plan (N7+ standard pattern preserved from N6+)

| Phase | Window | Expected events | Sample artifacts | SHA-stamped |
|---|---|---|---|---|
| **Smoke (T1..T5)** | 1-month: 2025-05-31..2025-06-30 (~21 sessions × 5 trials = ~105 sample-events; per N6 smoke ~360 events but N7+ may differ post-real-tape walk barriers) | ~100-400 events | 5: `determinism_stamp.json`, `events_metadata.json`, `full_report.json`, `full_report.md`, `gate_4b_attribution_inputs.json` (NEW Phase F) | YES |
| **Full (T1..T5)** | In-sample: 2024-08-22..2025-06-30 (~220 sessions × 5 trials, ~3800 events per N6 full) | **≥150-250 floor per Mira §6 R9** | 5: same set + telemetry.csv | YES |
| **Total artifacts** | | | **10** (5 smoke + 5 full) consistent with N6/N6+ pattern; `gate_4b_attribution_inputs.json` is NEW Phase F addition (replaces nothing in N6) | YES |

### §5.2 Sample-size R9 ≥ 150-250 events floor — feasibility check

In-sample window 2024-08-22..2025-06-30:
- ~220 sessions (BR holidays excluded)
- 4 entry windows per session (16:55 / 17:10 / 17:25 / 17:40 BRT)
- 5 trials T1..T5 per parent spec yaml v0.2.3 §1.2

**Maximum theoretical events** = 220 × 4 × 5 = **4400** events. N6 baseline yielded 3800 events full phase (~86% of max). Real-tape regime may reduce slightly due to:
- Sessions excluded if `circuit_breaker_fired = True` for downstream attribution (kept in sample per Nova Option B; no exclusion)
- Sessions excluded if rollover D-3..D-1 inflates noise (kept per Nova §2.4 Option B; flagged not excluded)
- Sessions in hold-out (NOT in in-sample by definition; UNMOVABLE per Anti-Article-IV Guard #3)

**Projected real-tape events:** 3000-4000 events (15-30% from 4400 max due to entry-rule filtering by `intraday_flow_magnitude` percentile cutoff). **Comfortably above floor 150-250 per Mira §6 (12-25× headroom).** AC9 flag `marginal_sample_size` will not fire.

### §5.3 Wall-time + RSS budgets summary (proposed §1.3-§1.4 baked)

| Resource | Smoke | Full | Hard cap |
|---|---|---|---|
| Wall-time | 15 min | 45 min | 60 min (early-abort) |
| Python-process RSS | ≤ 0.5 GiB | ≤ 1.0 GiB | 6 GiB ADR-1 v3 IMMUTABLE |
| OS WS RSS | ≤ 1.0 GiB | ≤ 1.5 GiB | 6 GiB ADR-1 v3 IMMUTABLE |

**Headroom:** wall 6× (smoke) / 1.3× (full); RSS 4-6× across phases. Conservative.

### §5.4 Determinism stamp populates `cost_atlas_sha256` + `rollover_calendar_sha256` (R16 carry-forward — BLOCKING)

Per N6+ §6 R16/C1 closure, F1 patch (commit `9997f14`) lifted both fields from `null` (N6 baseline gap C1) to populated. **Beckett N7+ inherits this wiring** — no source change needed; both SHAs auto-stamped via `_compute_dataset_sha256` analog helpers reading from `docs/backtest/nova-cost-atlas.yaml` and `config/calendar/2024-2027.yaml` at run start.

**Verification at smoke phase:** Beckett asserts both fields non-null in `determinism_stamp.json` BEFORE proceeding to full phase. If null, **abort + R16 violation** flagged for Riven T0d.

### §5.5 Verdict §5 — PASS

N7+ run plan is concrete: 10 SHA-stamped artifacts, sample-size floor 12-25× headroom, wall+RSS budgets proposed with rationale, R16 carry-forward verified at smoke. **Executable.**

---

## §6 Findings table

| # | Finding | Severity | Section | Bound to (Beckett condition ID) |
|---|---|---|---|---|
| F1 | Mira spec v1.0.0 mechanically executable; real-tape interface clean; ADR-1 v3 RSS budget defensible (4× headroom proj.) | INFORMATIONAL | §1 | — |
| F2 | Wall-time budget MISSING from Mira spec — Aria T0b §6.3 deferred to Beckett N7+ smoke. Beckett proposes 15 min smoke / 45 min full / 60 min hard cap — needs T0e Pax cosign baking into spec or story DoD | MEDIUM | §1.4 | **C-B1** — T0e Pax + T0a addendum (or T002.6 story Spec ref update) |
| F3 | Lazy per-session loading strategy is Beckett executor commitment — needs Aria/Dex confirmation T1 to align engine.py orchestration | LOW | §1.3 | **C-B2** — T1 Dex |
| F4 | Mira §7 Step 3 "RLP-detection-gap count ≥2" reading is ambiguous per Nova §1.3 historic-regime structural ABSENCE. Beckett proposes Mira clarify "sporadic anomaly count" not "structural regime constant" | MEDIUM | §4.2 | **C-B3** — Mira clarification (footnote / §7 addendum) |
| F5 | Beckett latency spec self-confirmed; Aria C-A1, C-A2, C-A3 all align with Beckett persona authority | INFORMATIONAL | §2 | — |
| F6 | 3-bucket attribution Beckett produces 6 inputs; Mira retains bucket adjudication authority | INFORMATIONAL | §4.3 | — |
| F7 | Sample-size floor R9 ≥150-250 satisfied with 12-25× headroom on in-sample 220-session × 5-trial × 4-window window | INFORMATIONAL | §5.2 | — |
| F8 | R16 cost_atlas_sha256 + rollover_calendar_sha256 carry-forward from N6+ F1 patch — verified at N7+ smoke before full | INFORMATIONAL (pending smoke verification) | §5.4 | — |

No CRITICAL findings. No NEEDS_REVISION blocks.

---

## §7 Decision: **APPROVE_WITH_CONDITIONS**

**Beckett T0c sign-off: APPROVE_WITH_CONDITIONS.**

Mira Gate 4b spec v1.0.0 is **executor-side sound** for Phase F real-tape replay. Real-tape data interface, latency model integration (self-authored), 3-bucket attribution scaffolding, and Beckett N7+ run plan are all PASS. Three engineering / clarification conditions (C-B1, C-B2, C-B3) propagate to T0e Pax, T1 Dex, and Mira (footnote-level addendum). No spec body mutation required; conditions can be addressed via T0e Pax baking proposed budgets into story DoD or via Mira footnote.

---

## §8 Conditions Beckett binds for downstream

| ID | Condition | Stage bound | Severity |
|---|---|---|---|
| **C-B1** | Wall-time ceilings (smoke 15 min / full 45 min / hard cap 60 min) MUST be baked into Mira spec §6 / §8 OR T002.6 story Spec ref + AC7 before Beckett N7+ smoke executes. Without explicit ceiling, AC7 "wall-time < TBD-Mira-spec-T0a" is unenforceable (T002.6 story DoD #4) | T0e Pax cosign + Mira footnote addendum (or River story update) | MEDIUM |
| **C-B2** | Lazy per-session parquet loading (Strategy A per §1.3) MUST be the Dex T1 implementation default. Engine orchestration MUST NOT eager-load full window. Aria-confirmed at T0b implicitly (factory closure body); Dex explicit at T1 | T1 Dex | LOW |
| **C-B3** | Mira clarify §7 Step 3 "RLP-detection-gap ≥2 sessions" reading: sporadic per-session anomalies vs Nova §1.3 structural-regime ABSENCE. Suggested addendum at §7.1 or §7.2 footnote — Nova historic gap recorded as `data_quality.rlp_flag_available_historic: false` is **structural** (not gap-count contributor) | Mira footnote in spec OR Beckett N7+ report explicit interpretation | MEDIUM |

All three conditions are clarification / placement mandates. None require Mira spec rewrite; thresholds DSR>0.95 / PBO<0.5 / IC UNMOVABLE per Anti-Article-IV Guard #4 — UNTOUCHED.

---

## §9 Article IV self-audit

| Claim in this sign-off | Source anchor (verified) |
|---|---|
| Mira spec v1.0.0 14 §§ executable | `docs/ml/specs/T002-gate-4b-real-tape-clearance.md` read in full (624 LoC) |
| Aria T0b APPROVE_WITH_CONDITIONS, 7 conditions C-A1..C-A7 | `docs/architecture/T002.6-aria-archi-review.md` §9 + §10 condition table |
| Beckett latency spec consumed verbatim into Mira §5.1 | Beckett spec `docs/backtest/latency-dma2-profile-spec.md` §4 yaml block ↔ Mira §5.1 — exact match (Aria T0b §11 verified line-for-line) |
| Nova RLP/rollover spec consumed verbatim into Mira §4.1 | Nova spec `docs/backtest/T002.6-nova-rlp-rollover-spec.md` §6 yaml block ↔ Mira §4.1 — exact match (Aria T0b §11 verified) |
| Beckett N6 baseline 27.4 s wall + 0.158 GiB Py-RSS / 0.597 GiB OS-WS | `docs/backtest/T002-beckett-n6-2026-04-29.md` §3 + §4 sub-criteria 2-3 |
| WDO parquet path `D:\sentinel_data\historical\` 571 sessions | Beckett persona `historical_data_reality.location` + `coverage_atual` |
| Schema BUY/SELL/NONE aggressor only, NO 13-value tradeType | Beckett persona `historical_data_reality.schema` + Nova §1.3 + Mira §3.2 row 5 + §4.3 |
| Hold-out lock 2025-07-01..2026-04-21 UNMOVABLE | Mira §3.1 + Anti-Article-IV Guard #3 + T002.6 story Spec ref |
| ADR-1 v3 6 GiB RSS IMMUTABLE | T002.6 story Spec ref + AC7 + ADR-1 v3 cited |
| In-sample 220 sessions × 4 windows × 5 trials = 4400 max events | Parent spec yaml v0.2.3 §1.2 + entry-window literals 16:55/17:10/17:25/17:40 |
| Sample-size floor 150-250 per Bailey-LdP 2014 §3 | Mira §6 + Bailey & López de Prado 2014 *JPM* 40(5) §3 |
| Toy benchmark seeds [42, 1337, 271828, 314159, 161803] UNMOVABLE | Mira §8(f) verbatim |
| Bonferroni n_trials=5 NON-NEGOTIABLE | Mira §1 + ESC-011 R9 + parent spec yaml v0.2.3 §1.2 |
| F1 patch commit `9997f14` populates `cost_atlas_sha256` + `rollover_calendar_sha256` | Beckett N6 §3 + §6 R16/C1 closure + Mira §3.5 |
| Aria T0b conditions C-A1, C-A2, C-A3 all align with Beckett spec §3 + §4 (slippage formula + per-event seed) | Aria T0b §10 + Beckett spec `latency-dma2-profile-spec.md` §3.1 + §2.4 |
| Synthetic walk N6 IC=0.0 by construction (strategy-logic-neutral) | N6 report §6 + §11.1 |

**Article IV self-audit verdict:** every claim in this sign-off traces. NO INVENTION. Mira spec body NOT MUTED (separate file). Beckett latency spec self-confirmed. Wall-time and RSS budget projections grounded in N6 baseline + escalation factors with explicit rationale (no inventions of standalone numbers). RLP gap clarification request is Mira-jurisdiction footnote, NOT Mira spec rewrite. Bucket-assignment authority confirmed Mira-side; Beckett produces evidence inputs only.

**Anchor count:** 16 source anchors verified above (Mira spec, Aria review, Beckett spec, Nova spec, Beckett N6 report, T002.6 story, parent thesis, parent spec yaml, Bailey-LdP 2014, ESC-011 R9, ADR-1 v3, Beckett persona historical_data_reality, F1 patch, etc.).

---

## §10 Beckett cosign

```
Authority: Beckett (@backtester) — Backtester & Execution Simulator authority
Council provenance: ESC-011 5/5 UNANIMOUS APPROVE_OPTION_C
                    (Beckett + Mira + Riven + Aria + Pax)
Predecessor specs consumed:
  - Mira spec finalize T0a v1.0.0
  - Aria archi review T0b APPROVE_WITH_CONDITIONS (7 conditions C-A1..C-A7)
  - Beckett latency spec self-authored (consumed verbatim by Mira §5)
  - Nova RLP/rollover spec self-consumed verbatim by Mira §4
  - Beckett N6 synthetic baseline (perimeter contrast for projection)
Article II (Gage push exclusivity): preserved — this sign-off is write-only,
  no push initiated; no `git add` / `commit` performed by Beckett
Article IV (No Invention): preserved — every clause traces in §9 self-audit;
  conditions C-B1, C-B2, C-B3 bound to source anchors;
  wall-time + RSS projections grounded in N6 baseline + escalation factors
Anti-Article-IV Guards (preserved unchanged):
  #1 (Dex impl gated em Mira spec PASS) — this T0c sign-off advances chain T0a → T0b → T0c → T0d → T0e → T1
  #3 (NO touch hold-out lock) — projections respect 2024-08-22..2025-06-30 in-sample only
  #4 (Gate 4 thresholds UNMOVABLE) — explicit non-touch in §8 condition table footer
  #5 (NO subsample backtest run) — N7+ run plan is full in-sample × 5 trials, no fold-skip
  #6 (NO enforce Gate 5 disarm sem Gate 4a + Gate 4b BOTH) — explicit reaffirmation in §1 + §2 + §3
  #7 (NO push) — preserved
Boundary respected:
  NÃO mute Mira spec (separate file by Mira-permitted alternative; Mira authority retained over §-bodies)
  NÃO mute spec yaml v0.2.3 thresholds (UNMOVABLE per Anti-Article-IV Guard #4)
  NÃO mute Aria T0b conditions (C-A1..C-A7 acknowledged + cross-referenced)
  NÃO pre-empt Mira bucket-assignment authority (§4.3 confirmed Beckett produces evidence only)
  NÃO pre-empt Riven Gate 5 authority (§10 conjunction reaffirmed implicit)
  NÃO commit / NÃO push (Gage @devops authority preserved for any future commit)
Decision: APPROVE_WITH_CONDITIONS (3 engineering/clarification bindings C-B1, C-B2, C-B3)
Cosign: Beckett @backtester 2026-04-29 BRT — T002.6 T0c consumer sign-off (Gate 4b spec v1.0.0 Phase F real-tape clearance executable per executor view)
```

— Beckett, reencenando o passado com fidelidade pessimista para Phase F.

---

## §T0c Beckett Consumer Audit Phase F2 IC Wiring (2026-04-30 BRT)

> **Author:** Beckett (@backtester) — Backtester & Execution Simulator authority; consumer audit on Mira T5 verdict `GATE_4_FAIL_data_quality` and IC pipeline wiring gap.
> **Trigger:** Mira T5 sign-off `docs/qa/gates/T002.6-mira-gate-4b-signoff.md` v `GATE_4_FAIL_data_quality` (provisional pending Phase F2 re-run after IC pipeline fix). T002 hypothesis NOT yet falsified.
> **Audit scope:** consumer-side operational impact of IC gap on Round 1 N7 full evidence (validity carry-forward) and N7-prime re-run scope/budget.
> **Authority boundary:** Beckett does NOT override Mira ML semantics (IC formula / predictor-label pairing definition is Mira authority) NOR Aria architecture (BacktestResult shape / closure interface change is Aria authority). Beckett surfaces operational implications and recommends; Aria T0b adjudicates re-run vs re-compute.

### §1 N7 full evidence validity post IC gap

The IC pipeline gap (Mira §4 finding: `ReportConfig.ic_in_sample = 0.0` default propagated; no upstream IC computation site wired to dry-run path) does NOT contaminate the metrics produced by the well-wired branches of `compute_full_report`. Beckett partial-validity carry-forward table:

| N7 full evidence component | Validity post IC gap | Justification |
|---|---|---|
| Distribution PnL real-tape (σ=0.185, sharpe range [-0.546, +0.614], 225/225 unique values, 3800 events × 5 trials) | **VALID** | DSR/PBO/sharpe pipeline reads `BacktestResult.metrics.sharpe_daily` per fold (`report.py:_extract_path_sharpes`); independent of `ReportConfig.ic_in_sample`. Distribution non-degeneracy is a real-tape empirical observation. |
| **DSR = 0.766987** (K1 measured) | **VALID** | Bailey-LdP 2014 §3 Φ-CDF over per-trial mean-Sharpe distribution computed at `report.py:481-514` (DSR pipeline); fully wired; consumes `sharpe_per_path` array, NOT `ic_in_sample` field. K1 is honestly measured. |
| **PBO = 0.337302** (K2 measured) | **VALID** | BBLZ 2014 CSCV (T,N) matrix at `report.py:_build_pbo_matrix`; consumes per-trial × per-path sharpe matrix; independent of IC field. K2 is honestly measured. |
| **IC = 0.000000 with CI95 [0,0]** (K3 not measured) | **INVALID** | `ReportConfig` defaults propagated verbatim per Mira §4 audit (line 185-189 `report.py` + line 1070 `run_cpcv_dry_run.py`). Zero-width CI is the implementation-artifact signature, not a measurement. K3 was never honestly computed. |
| **KillDecision NO_GO via K3** | **INVALID** (verdict-misleading) | Verdict reason string `IC_in_sample=0.000000 non-positive — no edge` is mechanically correct given the (default-zero) input but semantically void because K3 axis was not measured. Kill decision logic itself is wired correctly; the input it consumed is the artifact. |
| Pipeline integrity (R16 stamps, latency model active, microstructure flags, Bonferroni n=5 carry-forward, 5 SHA-stamped artifacts, determinism stamps populated) | **VALID** | All Phase F integrity invariants honored per N7 full report §3 + §4 + §10 + §8. The pipeline is sound except for the IC computation site. |
| Wall-time observation 181 min 50s (3× over Beckett T0c hard cap) | **VALID** (operational observation; not affected by IC gap) | Telemetry-derived; concern C1' carry-forward independent of statistical wiring. |
| Real-tape vs synthetic σ contraction (4.354 smoke → 0.185 full) | **VALID** | Empirical, confirms strategy logic alive on the real-tape distribution; orthogonal to IC. |

**Conclusion §1:** Round 1 N7 full evidence is **partially reusable** — the distribution shape, K1 (DSR), K2 (PBO), pipeline integrity, microstructure-flag diagnostics, and operational telemetry are AUTHORITATIVE and carry forward to Phase F2. K3 (IC) measurement and the conjunction `K1 ∧ K2 ∧ K3` Gate 4b verdict are NOT conclusive. T002 hypothesis remains in-scope per Mira §1 disposition `provisional_fail_pending_phase_F2`.

### §2 Re-run vs re-compute hipóteses (Aria T0b decides)

User-supplied three hipotheses on what the IC fix entails. Beckett operational opinion below; Aria T0b architecture authority adjudicates the BacktestResult-shape question.

| Hipótese | Description | Beckett operational opinion |
|---|---|---|
| **(a) Post-fanout aggregation over per-fold path-PnL** | IC computed entirely from existing N7 artifacts (e.g. `path_pnl` rank vs path-id rank, or some inter-trial / inter-path rank correlation) → re-compute only, no engine re-run. | **UNLIKELY to satisfy K3 ML semantics.** K3 per Mira parent thesis §5 is a **predictor-vs-label** rank correlation: predictor at decision time (e.g. signed `intraday_flow_magnitude` percentile rank) vs realized forward-return label at barrier exit. Path-PnL rank vs path-id rank carries no economic predictor signal — would be a degenerate "self-correlation" placeholder, not the K3 statistic Mira spec §5 requires. **If Aria + Mira agree the path-PnL pair-rank IS the operational K3 surrogate**, then re-compute path is feasible from existing `cpcv_results[trial][i].metrics.*` carried in the run dir parquet/jsonl. |
| **(b) BacktestResult shape modification (per-fold IC fields added)** | `BacktestResult.metrics` gains `predictor_per_event[]`, `label_per_event[]` arrays serialized per-fold; downstream IC computation runs at compute_full_report time (post-fanout). | **PLAUSIBLE if minimal-change.** Each closure already produces per-event (predictor, label) pairs internally for the strategy signal computation (entry-time `intraday_flow_magnitude` and exit-time realized return); exposing them in BacktestResult metrics is a serialization addition, not a strategy-logic mutation. Per-fold storage cost: ~5KB × 225 paths ≈ ~1MB additional per run — negligible RSS. **Re-run necessário** because the closure must emit the new fields (existing artifacts do not carry them); HOWEVER, the closure body strategy logic is UNCHANGED — only the metrics record schema expands. |
| **(c) Closure-level instrumentation** | Closure body emits per-event (predictor, label) records into a new column on the events output AND/OR into BacktestResult metrics; downstream `compute_full_report` aggregates. | **MOST LIKELY** the architecturally clean shape. Closure already has both quantities at decision time + exit time (it must, to compute PnL and apply triple-barrier). Wire is: closure → events_dataframe (or BacktestResult.metrics) → `ReportConfig` constructor in `run_cpcv_dry_run.py:1070` → `compute_full_report` → `info_coef.ic_spearman` over aggregated arrays. **Re-run necessário** for the same reason as (b); the closure body change is the location of the wiring. |

**Beckett operational call:** **(b) or (c) — re-run necessário.** Hipótese (a) is operationally cheap but requires Mira concession that the K3 statistic can be derived from path-level aggregates without economic predictor-label pairing — Mira §4.3 evidence (zero-width CI95 over 3800 events as signature of "field not wired" not "field measured zero") suggests Mira sees IC as a real predictor-vs-label statistic, which (a) would not produce. Final architecture call belongs to Aria T0b post-Mira F2-T0 spec amendment.

**If Aria validates (b)/(c) → re-run mandatory.** If Aria validates (a) → re-compute path opens (see §3 budget).

### §3 Wall-time + RSS budget projection N7-prime

Two paths, two budgets:

#### §3.1 Re-run path (hipótese b/c likely)

Same parquet window 2024-08-22 → 2025-06-30 (canonical in-sample, UNMOVABLE per Anti-Article-IV Guard #3 + R8 Mira authority). Same engine config v1.1.0 (UNCHANGED). Same Bonferroni n_trials=5 (UNCHANGED). Same seed=42.

| Metric | N7 full observed | N7-prime projection | Verdict |
|---|---|---|---|
| Wall-time | 181 min 50s | **180-200 min** (≈ same; closure adds ~µs per event for predictor/label record append; latency draws unchanged; aggregations near-zero overhead post-fanout) | **3× over Beckett T0c hard cap (60 min) — concern C1' CARRY-FORWARD** |
| Peak Python RSS | ~0.6 GiB observed | **~0.6-0.7 GiB** (extra per-event arrays: 3800 events × 8 bytes × 2 fields × 5 trials = ~300 KB; negligible) | PASS (10× headroom under 6 GiB ADR-1 v3 cap) |
| OS WS RSS | ~1.0 GiB projected (N6+ baseline + real-tape escalation) | **~1.0-1.5 GiB** | PASS (4-6× headroom) |
| Sample-size | 3800 events × 5 trials × 45 paths | UNCHANGED | PASS (76× headroom over R9 floor 250) |

**C1' wall-time concern carry-forward — RECOMMENDATION:** Beckett strongly recommends Beckett engine-config v1.2.0 OR Dex perf-optimization round (parquet pre-aggregation OR session caching across folds) BEFORE Phase F2-T4 N7-prime dispatch. Rationale:
- Round 1 N7 full burned 3 hours; Round 2 (F2-T4 N7-prime) burning 3 hours again on the same compute is operationally wasteful.
- If F2 re-clearance fails again (data_quality recurrence OR strategy_edge clean negative) and Round 3 triggers, compounding 3× over hard cap is unsustainable for council bandwidth.
- Perf round is **orthogonal** to the IC fix — does NOT affect strategy logic, threshold UNMOVABILITY, or Bonferroni invariant. Can run in parallel to F2-T0 Mira spec amendment + F2-T1 Dex impl.

If Aria accepts the C1' carry-forward and pre-Phase-F2 perf round, re-run wall-time projection becomes **45-90 min** (within original Beckett T0c 60 min hard cap with margin OR 50% over — uncertain pending perf-round profiling). **Non-blocking for F2 re-clearance** (operational, not statistical) but **strongly recommended** to disarm wall-budget concern before F2-T4.

#### §3.2 Re-compute path (hipótese a, only if Aria + Mira validate post-fanout aggregation suffices)

| Metric | Projection | Verdict |
|---|---|---|
| Wall-time | **~5 min** (read N7 artifacts → compute IC over per-fold path-PnL aggregates → bootstrap CI 10000 resamples → emit IC value + CI to amended `full_report.json` with new `compute_ic_post_hoc.py` script) | TRIVIAL |
| Peak RSS | **< 100 MB** (load 5 × 45 path-PnL vectors + bootstrap resampling) | TRIVIAL |
| Re-run penalty | ZERO (no engine call) | — |
| C1' wall-budget concern | DISARMED (no re-run) | — |

**Beckett operational caveat:** re-compute is operationally cheap BUT Beckett does not endorse it as the K3 measurement unless Aria + Mira jointly confirm the path-PnL rank pairing satisfies K3 economic semantics. Mira §4 audit framing (predictor-vs-label IC as the K3 statistic) suggests path-PnL pairing would not satisfy. **If Aria + Mira say re-compute is valid → great. If not → §3.1 re-run path applies.**

### §4 Determinism stamp considerations N7-prime

| Stamp field | N7 full | N7-prime expected | Why |
|---|---|---|---|
| `seed` | 42 | **42** UNCHANGED | Seed canonical |
| `simulator_version` | `cpcv-dry-run-T002.1.bis-T1` | **bumped to `cpcv-dry-run-T002.6-F2-T1`** OR equivalent semver | Code change in closure / report config wiring constitutes new simulator semver |
| `dataset_sha256` | `ff9b5058…` | **may be UNCHANGED if events_dataframe schema is unchanged**, OR change if new columns emitted (predictor/label per-event) | Aria T0b decides events_dataframe shape change |
| `spec_sha256` | `9985a6dc…` | **UNCHANGED** | Spec yaml v0.2.3 thresholds UNMOVABLE per Anti-Article-IV Guard #4; F2 amendment is a separate Mira F2-spec doc, not a yaml mutation |
| `spec_version` | `T002-v0.2.3` | **UNCHANGED** | Same yaml |
| `engine_config_sha256` | `ccfb575a…` | **UNCHANGED** | engine-config.yaml v1.1.0 (latency_model + microstructure_flags) is sufficient for F2; no engine-config bump required for IC wiring fix UNLESS perf round (§3.1) fires v1.2.0. |
| `rollover_calendar_sha256` | `c6174922…` | **UNCHANGED** | Calendar canonical |
| `cost_atlas_sha256` | `bbe1ddf7…` | **UNCHANGED** | Atlas v1.0.0 SHA-locked, still authoritative |
| `cpcv_config_sha256` | `d2ea61f2…` | **UNCHANGED** | CPCV (10,2) configuration UNMOVED |
| HEAD commit SHA (R17) | `8855a25` | **NEW commit SHA** post-F2-T1 Dex impl + F2-T2/T3 Quinn QA passes + Gage commit | F2 cycle commit SHA reflects new code; stamp will reflect new HEAD per R17 carry-forward |
| `run_id` | `951241732d33…` | **NEW UUID** | UUID per run; orthogonal to determinism content |

**Critical determinism property preserved:** same seed + same parquet window + same engine config + same spec → bit-identical PnL distribution. The IC fix changes only the **measurement output channel**, not the **strategy logic execution**. Therefore all sharpe/DSR/PBO numbers in N7-prime should reproduce N7 full WITHIN reproducibility tolerance (modulo any incidental floating-point ordering changes from new code paths). If they do NOT reproduce, that is a Quinn QA regression flag.

### §5 Cost atlas + rollover preservation confirm

| Invariant | N7 full | N7-prime | Status |
|---|---|---|---|
| Cost atlas v1.0.0 `bbe1ddf7898e79a7b8dadf4408c854fa010bd7444e2c2f3b9fcb006c24b84b6d` | locked | locked | **PRESERVED** (R16 carry-forward; engine-config `cost_atlas_ref.atlas_sha256_lock` UNCHANGED) |
| Rollover calendar SHA `c6174922dea303a34cec3a17ddd37933d5a0dabef0e0f6c5f9f01bc40063fcc2` | locked | locked | **PRESERVED** |
| Latency model DMA2 (Beckett spec §4 lognormal seeded) | active | active | **PRESERVED** (no parameter mutation; latency physics unchanged) |
| Microstructure flags (RLP / rollover / auction / circuit_breaker / cross_trade) | active | active | **PRESERVED** (Nova spec §6 verbatim) |
| Phase gating (`enabled_for_phase: ["F"]`) | F | F | **PRESERVED** |
| Spec yaml v0.2.3 §kill_criteria L207-209 (DSR>0.95, PBO<0.5, IC>0) | UNMOVABLE | UNMOVABLE | **PRESERVED** (Anti-Article-IV Guard #4) |
| Hold-out lock 2025-07-01 → 2026-04-21 | UNTOUCHED | UNTOUCHED | **PRESERVED** (Anti-Article-IV Guard #3) |
| Bonferroni n_trials=5 ([T1..T5]) | preserved | preserved | **PRESERVED** (ESC-011 R9 NON-NEGOTIABLE) |

All Phase F invariants intact across the F2 cycle.

### §6 Seven Anti-Article-IV guards preservation N7-prime

| Guard | Mandate | F2 cycle preservation |
|---|---|---|
| **#1** Mira spec authority gates Dex impl | F2-T1 Dex impl gated em F2-T0 Mira F2-spec amendment PASS | **PRESERVED** — Beckett does NOT pre-empt Mira authority over IC computation site/pairing definition |
| **#2** Quinn QA gates dispatch | F2-T2/T3 Quinn QA gates F2-T4 Beckett dispatch | **PRESERVED** — Beckett does NOT dispatch N7-prime without Quinn QA PASS on F2-T1 impl |
| **#3** Hold-out lock UNTOUCHED | hold-out [2025-07-01, 2026-04-21] read-zero by F2 cycle | **PRESERVED** — same in-sample window 2024-08-22..2025-06-30; no hold-out touch |
| **#4** Gate 4 thresholds UNMOVABLE | DSR>0.95, PBO<0.5, IC>0 verbatim from spec yaml v0.2.3 | **PRESERVED** — F2 amendment governs IC computation site, NOT thresholds; spec yaml UNMUTATED |
| **#5** NO subsample backtest run | full in-sample × 5 trials × 45 paths | **PRESERVED** — same window, same fanout |
| **#6** NO Gate 5 disarm without Gate 4a + Gate 4b BOTH PASS | Gate 5 LOCKED until joint conjunction | **PRESERVED** — Mira T5 verdict explicit (§5 footer): Gate 5 LOCKED; F2-T5 re-clearance is the only path to unlock |
| **#7** NO push by Beckett | Gage @devops EXCLUSIVE | **PRESERVED** — this audit is a write-only artifact; no `git add` / `commit` / `push` performed |

All seven guards preserved.

### §7 Recommendation: prefer re-compute (hipótese a) IF Aria validates BacktestResult shape sufficient for post-hoc IC

**Beckett conditional recommendation:**

1. **IF Aria T0b confirms** `BacktestResult.metrics.*` already carries (or can be made to carry post-hoc from existing serialized fields) the predictor-vs-label pairing economically equivalent to K3 ML semantics under Mira spec §5 → **re-compute path (§3.2) preferred**: ~5 min wall, < 100 MB RSS, zero re-run penalty, C1' wall-budget concern DISARMED. F2 cycle compresses to F2-T0 (Mira amendment) + F2-T1 (re-compute script wiring) + F2-T2/T3 (Quinn QA) + F2-T5 (Mira re-clearance) — no Beckett N7-prime fanout. Operational ideal.

2. **IF Aria T0b OR Mira does NOT confirm** post-hoc reconstruction satisfies K3 → **re-run path (§3.1) mandatory**: 180-200 min wall, 0.6-1.5 GiB RSS, full F2-T4 dispatch. **STRONGLY RECOMMEND** Beckett engine-config v1.2.0 OR Dex perf-round before F2-T4 to disarm C1' (parquet pre-aggregation OR session caching). Per N7 full report §7 recommendation already on the table; F2 cycle is the natural moment to fire it.

3. **REGARDLESS of path:** Mira F2-spec amendment must define explicit (predictor, label) pairing semantics (per-event? per-fold? per-trial mean? path-level?) and bootstrap CI95 protocol — Beckett operational position is that the choice DRIVES the path:
   - Per-event predictor (e.g. `intraday_flow_magnitude` z-score at decision time) vs per-event realized forward-return label at barrier exit → **re-run path** (closure must emit per-event records).
   - Per-fold mean-Sharpe rank vs per-fold mean-PnL rank → **re-compute path** (existing fields).
   - Per-trial mean-IC over 45 paths aggregated → **re-compute path** (existing fields).

Beckett does NOT pre-empt Mira F2-T0 amendment authority on this choice; surfaces operational consequence so Mira + Aria can choose with full information.

### §8 Sample-size R9 floor confirm (3800 events × 5 trials sample sufficient para IC measurement com bootstrap CI)

Per Mira Gate 4b spec §6 R9 mandate: ≥ 30-50 events/trial × 5 trials = 150-250 events floor for stable IC/DSR/PBO. N7 full delivered 3800 events × 5 trials = **19,000 trial-events (76× over floor)**.

For the Phase F2 IC measurement specifically:
- **Spearman IC** computed over 3800 (predictor, label) pairs per trial → 3800 paired observations is **massively above** any IC stability threshold (Bailey-LdP 2014 §3 minimum-N satisfied, AFML Ch.8 §8.4.1 IC Spearman convergence bound respected).
- **Bootstrap CI95** via `info_coef.bootstrap_ci(sample, statistic=..., n_resamples=10_000, confidence=0.95, seed=42)` over 3800 paired samples → **CI95 width will be tight and non-degenerate** (compare: a truly-measured Spearman over 3800 random-but-non-trivial pairs produces CI95 width on the order of ±0.02-0.05 typically; collapse to [0,0] is the implementation-artifact signature Mira flagged §4.3).
- **Per-fold/per-path IC variance:** 45 paths × ~84 events/path on average → per-path IC less stable but per-trial mean-IC over 45 paths is robust.
- **Inter-trial Bonferroni adjustment:** n=5 trials, Bonferroni-α applied per ESC-011 R9 NON-NEGOTIABLE; α/5 per trial → individual K3 PASS threshold tightened, joint K1∧K2∧K3 conjunction unaffected at family level.

**Sample-size floor R9 PASS for Phase F2 IC measurement.** No INCONCLUSIVE_pending_more_data risk on the IC axis specifically.

### §9 Article IV self-audit

| Claim in this audit section | Source anchor (verified) |
|---|---|
| `ReportConfig.ic_in_sample = 0.0` default propagated; no upstream IC computation site wired to dry-run path | Mira T5 sign-off `docs/qa/gates/T002.6-mira-gate-4b-signoff.md` §4.1-§4.4 + direct inspection `packages/vespera_metrics/report.py` lines 185-189 + line 527-528 + line 554-555 + `scripts/run_cpcv_dry_run.py` line 1070 |
| `info_coef.ic_spearman` + `bootstrap_ci` correct functions exist as orphan code from dry-run path | Direct inspection `packages/vespera_metrics/info_coef.py` lines 18-69 + lines 72-110 + Mira §4.2 grep evidence |
| N7 full sharpe σ=0.185, range [-0.546, +0.614], 225/225 unique | Beckett N7 full report `docs/backtest/T002-beckett-n7-2026-04-30.md` §5 + N7 full `full_report.json` lines 10-100 sample inspection |
| K1 DSR=0.766987 honestly measured (Bailey-LdP 2014 §3 Φ-CDF over per-trial mean-Sharpe) | `report.py` lines 481-514 (DSR pipeline) + Mira T5 §2 + Beckett N7 §5 |
| K2 PBO=0.337302 honestly measured (BBLZ 2014 CSCV (T,N) matrix) | `report.py:_build_pbo_matrix` + Mira T5 §2 + Beckett N7 §5 |
| Wall-time 181 min 50s observed, 3× over Beckett T0c 60 min hard cap | Beckett N7 full report §3 + §7 + telemetry.csv |
| Peak Python RSS ~0.6 GiB observed | Beckett N7 full report §7 |
| Cost atlas v1.0.0 SHA-locked `bbe1ddf7…` UNCHANGED | engine-config.yaml `cost_atlas_ref.atlas_sha256_lock` line 51 + N7 full determinism stamp + N7 smoke determinism stamp cross-match |
| Rollover calendar SHA `c6174922…` UNCHANGED | N7 full + N7 smoke determinism stamp cross-match |
| In-sample window 2024-08-22..2025-06-30 canonical | Mira parent spec yaml v0.2.3 + Anti-Article-IV Guard #3 + N7 full events_metadata |
| Hold-out [2025-07-01, 2026-04-21] UNTOUCHED | Anti-Article-IV Guard #3 + Mira spec §3.1 verbatim |
| Bonferroni n_trials=5 NON-NEGOTIABLE | ESC-011 R9 verbatim + Mira spec §1 + parent spec yaml v0.2.3 §1.2 |
| 7 Anti-Article-IV guards (§6) | T002.6 story + Mira Gate 4b spec §11.2 + this T0c original sign-off §10 cosign block |
| C1' wall-budget concern carry-forward | N7 full report §7 §11 |
| ADR-1 v3 6 GiB RSS IMMUTABLE cap | T002.6 story Spec ref + AC7 + ADR-1 v3 |
| Bucket-assignment authority Mira-side; Beckett produces evidence inputs only | Beckett T0c original sign-off §4.3 + Mira T5 §4 (Mira authoritative call) |
| Three hipotheses (a)/(b)/(c) for IC fix scope | User-supplied audit prompt; Beckett operational analysis grounded in `report.py` + `info_coef.py` + `run_cpcv_dry_run.py` direct code inspection |
| Sample-size 3800 × 5 = 19,000 (76× R9 floor) | Beckett N7 full report §3 + Mira T5 §2 |
| Determinism stamps reflect new commit SHA per R17 | R17 housekeeping ledger + Beckett N7 full §4 |

**Self-audit verdict:** every clause in this audit traces. NO INVENTION. NO Mira ML semantic override (re-run-vs-re-compute decision deferred to Aria T0b architecture authority + Mira F2-T0 spec amendment). NO Aria architecture override (Beckett surfaces operational consequence; Aria adjudicates). NO threshold mutation. NO subsample. NO Gate 5 pre-disarm (Gate 5 LOCKED reaffirmed). NO push (Article II preserved). Bucket-assignment authority Mira-side (T0c original §4.3 reaffirmed). Wall-time + RSS projections grounded in N7 full empirical baseline + IC-fix-specific incremental cost modeling.

### §10 Beckett cosign 2026-04-30 BRT — Phase F2 consumer audit

```
Authority: Beckett (@backtester) — Backtester & Execution Simulator authority;
           consumer audit on Mira T5 GATE_4_FAIL_data_quality verdict and
           IC pipeline wiring gap.
Council provenance: ESC-011 5/5 UNANIMOUS APPROVE_OPTION_C carry-forward
                    (Beckett + Mira + Riven + Aria + Pax) preserved.
Predecessor signoffs consumed:
  - Mira T5 sign-off (THIS verdict's trigger): docs/qa/gates/T002.6-mira-gate-4b-signoff.md
    verdict GATE_4_FAIL_data_quality (provisional pending Phase F2)
  - Beckett N7 full report: docs/backtest/T002-beckett-n7-2026-04-30.md
    (KillDecision NO_GO via K3, distribution authoritative, IC=0 implementation artifact)
  - Beckett N7 smoke report: docs/backtest/T002-beckett-n7-smoke-2026-04-29.md
    (pipeline integrity strict-literal PASS)
  - Beckett T0c original sign-off (above this section): APPROVE_WITH_CONDITIONS
    (C-B1, C-B2, C-B3 all closed by 4-sign-off chain CLOSED 2026-04-29)
  - Engine config v1.1.0: docs/backtest/engine-config.yaml (UNCHANGED for F2)
  - Latency spec v1.0 PROPOSED → v1.0 LOCKED in engine-config v1.1.0 (UNCHANGED for F2)
Article II (Gage push exclusivity): preserved — this audit is write-only;
  no `git add` / `commit` / `push` initiated by Beckett
Article IV (No Invention): preserved — every clause traces in §9 self-audit;
  re-run-vs-re-compute hipotheses grounded in direct code inspection
  (report.py / info_coef.py / run_cpcv_dry_run.py); operational projections
  grounded in N7 full empirical wall+RSS baseline; bucket-assignment
  authority deferred to Mira (T5 + F2-T0 amendment)
Anti-Article-IV Guards (preserved unchanged):
  #1 (Mira spec authority gates Dex impl) — F2-T1 gated em F2-T0 Mira amendment PASS
  #2 (Quinn QA gates dispatch) — F2-T4 Beckett dispatch gated em F2-T2/T3 Quinn QA PASS
  #3 (NO touch hold-out lock) — same in-sample window; no hold-out touch in F2 cycle
  #4 (Gate 4 thresholds UNMOVABLE) — F2 amendment governs IC site, NOT thresholds
  #5 (NO subsample backtest run) — F2-T4 dispatch is full in-sample × 5 trials
  #6 (NO enforce Gate 5 disarm without Gate 4a + Gate 4b BOTH) — Gate 5 LOCKED
  #7 (NO push) — preserved
Boundary respected:
  NÃO override Mira ML semantics (predictor-vs-label IC pairing definition is Mira F2-T0 authority)
  NÃO override Aria architecture (BacktestResult shape / closure interface change is Aria T0b authority)
  NÃO mute prior content of T002.6-beckett-consumer-signoff.md (this section is append-only)
  NÃO mute Mira spec yaml v0.2.3 thresholds (UNMOVABLE per Anti-Article-IV Guard #4)
  NÃO pre-empt Pax F2 story-creation authority (R10 — Pax @po authoring F2 story under Phase F2 cycle)
  NÃO pre-empt Riven T6 reclassification authority (Riven post-mortem ledger update post-Mira-T5 verdict)
  NÃO commit / NÃO push (Gage @devops authority preserved for any future commit)
Decision: COSIGN — Phase F2 consumer audit complete; recommendation conditional
  on Aria T0b architecture verdict (re-compute path §3.2 preferred IF Aria + Mira
  validate BacktestResult shape sufficient for post-hoc IC; otherwise re-run
  path §3.1 mandatory with C1' wall-budget perf-round STRONGLY RECOMMENDED).
  Round 1 N7 full evidence partial-validity preserved (DSR, PBO, distribution,
  pipeline integrity) for F2 verdict adjudication.
Cosign: Beckett @backtester 2026-04-30 BRT — Phase F2 consumer audit under
  autonomous mode + branch t002-6-phase-f2-ic-wiring.
```

— Beckett, reencenando o passado com fidelidade pessimista para Phase F2.
