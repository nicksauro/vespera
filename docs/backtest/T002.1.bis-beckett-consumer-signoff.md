# T002.1.bis Consumer Sign-off — Beckett (@backtester)

**Date (BRT):** 2026-04-28
**Source spec:** `docs/ml/specs/T002.1.bis-make-backtest-fn-spec.md` (Mira authority — T0a complete)
**Reviewer:** Beckett (@backtester) — consumer authority + N6 empirical anchor
**Authority basis:** §9 sign-off chain T0c (per spec §9 + story Anti-Article-IV Guard #1)
**Spec sha256 (this review):** computed at consumer-side pre-impl handshake — read-only review of `docs/ml/specs/T002.1.bis-make-backtest-fn-spec.md`
**Cost atlas reference:** `docs/backtest/nova-cost-atlas.yaml` v1.0.0 (Nova compiled, Sable APPROVED_WITH_CONDITIONS)
**N5 baseline anchor:** `docs/backtest/T002-beckett-t11-bis-smoke-report-N5-2026-04-28.md` (run_id `cpcv-dryrun-auto-20260428-c5d5a708acda`, wall 4.381 s, peak RSS 0.140 GiB, 5/5 artifacts sha-stamped)

---

## §1 Signal-to-trade — operationalizable?

**Verdict: PASS.**

The closure contract in §1.1 maps cleanly onto the existing dry-run pipeline at `scripts/run_cpcv_dry_run.py:695-731`:

- `build_events_dataframe(...)` already produces `(t_start, session, trial_id, entry_window)` rows (line 695-701) — this is the exact input row vocabulary §1.1 prescribes.
- `make_backtest_fn(costs, calendar, p126)` is invoked per-fold in `_run_phase` (line 711), and the returned closure is consumed by `run_5_trial_fanout(events, backtest_fn, runner)` (line 726). The §1.1 step ordering (resolve trial → compute features → compute_signal → emit/skip) fits inside that closure body without altering the orchestrator signature.
- §1.2 trial table T1..T5 maps verbatim to spec yaml L307-321 — the linear interpolation derivations for T2 (P50 between P20/P60) and T3 (P70 between P60/P80) are **deterministic, pure, and Bonferroni-honest** (engineering refactor, not new family-wise hypothesis test). N5 already runs `n_trials=5` from `docs/ml/research-log.md@359844528d7768b9ea7642708d32d13fc08ab9ba` (determinism stamp `n_trials_source` field) — the post-impl invocation will inherit that anchor unchanged.
- §1.3 fade direction is consistent with `signal_rule.py:65-67` FLAT-on-zero handling already coded.
- §1.4 active windows `["16:55", "17:10", "17:25", "17:40"]` are spec-locked; the closure's defensive assertion on `t_start.time()` is appropriate (raise on out-of-window event = pipeline integrity violation, never seen in N5 run because `events_df` already filters upstream).

**Operationalizable in `scripts/run_cpcv_dry_run.py` post-Dex impl:** YES. No script-side changes required for §1; the closure body change at `cpcv_harness.py:334-450` is internal to `make_backtest_fn`.

**Beckett T11.bis canonical execution pattern alignment (§1.4):** the entry windows match the Nova canonical execution pattern that drove N5's `n_events=3800` count. No drift.

---

## §2 Triple-barrier — execution semantics clear?

**Verdict: PASS.**

§2.1 three-barrier table is unambiguous — PT/SL/vertical with explicit formulas, side-aware trigger conditions, and tick-level evaluation. Maps directly onto the `BacktestBroker.execute(...)` + `force_exit(...)` contract already coded in `exec_backtest.py` (referenced spec §0 building block #3).

**§2.2 `ATR_hora` ruling:** Mira's resolution (`mean(true_range_15min[s, t-4..t])` = ~4 bars/hour rolling) is computable from trade-tape alone (no book required) — consistent with Beckett's historical-data reality (parquet trades-only, see Beckett `expertise.historical_data_reality.consequence_for_engine`). The defensive fallback (DO NOT TRADE if `< 4 bars`) is exactly the kind of fail-closed posture Beckett's anti-Article-IV invariants demand.

**§2.3 precedence SL > PT > vertical:** Mira anchors this to Lopez de Prado AFML 2018 §3.4 canonical ordering. This is the risk-management-invariant-preserving choice (stop-loss before profit-target on simultaneous tick). No Beckett objection.

**§2.4 timestamping + anti-leak invariant:** `exit_ts ∈ [entry_ts, 17:55]` is bound inside the closure — closure NEVER consults ticks beyond `t_vertical = 17:55 BRT same session as entry`. This is the single most important invariant for Beckett's `*fill-audit` post-N6: any exit_ts outside this window = lookahead violation = run discarded. Spec is explicit. No ambiguity.

**One observation (NON-BLOCKING):** §2.2 says "computability against historical-availability... PASS — true range from 15-min OHLC is computable from trade tape alone." This is correct for the WDO trades-only parquet schema (timestamp, price, vol, qty, aggressor, trade_type per Beckett `historical_data_reality.schema`). Beckett confirms — no new captura-de-book is required to materialize ATR_hora.

---

## §3 P60 / P20 / P80 filters — N6 verification path?

**Verdict: PASS.**

§3.1 filter calculation aligns with `Percentiles126dBuilder.build(...)` already coded in `percentiles_126d_builder.py:91-136`. Critical: §3.1 mandates `as_of_date = first_test_session_of_fold` (per-fold), NOT global in_sample start — this **closes the DEFERRED-T11 M1 leakage path** documented in `cpcv_harness.py:287-307` and is Beckett's primary anti-leak concern from N4 §4.2.

§3.2 activation triggers map verbatim to `signal_rule.py:75-85` (already coded, read-only consumer per spec §0).

§3.4 D-1 invariant (`m.day < as_of_date`) is preserved per `percentiles_126d_builder.py:131-136` — N5 stamp confirms `warmup_gate_as_of: "2024-08-22"` enforces this at the gate level.

**N6 verification path (Beckett-side):**
1. `*fill-audit` over a sample of 100 fills will check `entry_ts.date() < bands.as_of_date` (D-1 invariant) and `exit_ts ≤ entry_ts + (17:55 - entry_ts.time())` (no-leak vertical bound).
2. Cross-fold isolation check: distinct `(fold_id → percentiles_state.as_of_date)` mapping in determinism stamp; no two folds share `as_of_date`.
3. P60/P50/P70/P80 numerics for T1..T5 reported in N6 §X (per-trial PnL line items) — anchored to fold-specific bands, NOT global bands.

§3.3 tie-break determinism (numpy type-7 linear interp) is what `Percentiles126dBuilder._percentile` (line 139-153) already does — N5 reproduces bit-equivalent metrics across runs (confirmed at N5 §6 vs N4 DIAGNOSTIC: PBO=0.5, DSR=0.5, IC=0 identical with seed=42).

---

## §4 Position sizing — N6 PnL reproducibility?

**Verdict: PASS.**

§4.1 fixed `n_contracts = 1` is the Bonferroni-honest baseline. Beckett endorses for three reasons:
1. **Riven custodial gate (§9 HOLD #2 Gate 5)** is the correct authority for sizing-magnitude clearance, NOT Mira and NOT Beckett — out of T002.1.bis scope per Article IV trace.
2. **Reproducibility:** fixed sizing eliminates a non-deterministic axis (no leverage path, no path-dependent volatility-targeting). N6 PnL line items will be **purely a function of entry/exit prices + costs + N=1**, identical given identical inputs.
3. **Comparability with N5 baseline:** N5 stub also returned identity-zero PnL with implicit `n_contracts=1`. Sizing-magnitude swap is a **separate, downstream** Mira+Riven exercise.

§4.2 per-fold rebuild integration: closure CONSUMES `percentiles_state` rebuilt by the engine (per Aria T0b decision). This is the right separation of concerns — closure does NOT rebuild bands internally. Beckett note: regardless of which Aria option (A/B/C) is canonical, the **closure-side invariant** (`percentiles_state.as_of_date == first_test_session_of_fold`) is the same — Beckett's `*fill-audit` and N6 verification path do not depend on Aria's architectural choice.

**N6 PnL reproducibility checklist (closure-internal):**
- Same seed (42) + same dataset_sha256 + same spec_sha256 + same engine_config_sha256 → bit-equivalent per-fold PnL. PASS by construction (§4.1 fixed sizing + §3.3 deterministic ties + §2.4 anti-leak invariant).

---

## §5 Cost atlas wiring — line items reportable?

**Verdict: PASS.**

§5.1 per-trade cost components map cleanly onto the atlas v1.0.0 fields verified above:

| §5.1 component | Atlas field | Atlas value | N6 line item reportable? |
|---|---|---|---|
| Brokerage | `costs.brokerage.per_contract_one_way` | 0.00 BRL ([WEB-CONFIRMED 2026-04-22], 5 retail sources) | YES — closure threads via `BacktestCosts.brokerage_per_contract_side_rs` |
| Exchange fees | `costs.exchange_fees.per_contract_one_way` | 1.23 BRL ([WEB-CONFIRMED 2026-04-22], CM Capital + B3 PDF + MQL5 cross-ref) | YES — closure threads via `BacktestCosts.exchange_fees_per_contract_side_rs` |
| Slippage | `slippage_model` engine-config block | Roll-half=0.5 + 1 tick worst-case (spec §5.1 says "Beckett T0e" — already wired in `BacktestBroker._slippage()` at exec_backtest.py:218-222) | YES — applied to fill price; N6 reports `signed_slippage_ticks` per trade |
| IR DARF (day-trade) | `costs.tax_day_trade.ir_rate` 0.20 + `irrf_daily_rate` 0.01 | Post-hoc on monthly net gain (atlas L208-225, `treatment_choice: post_hoc_on_monthly_net_gain`) | YES — gross PnL in closure; **separate post-hoc table** in N6 §X for monthly-aggregated IR-net PnL |

**§5.2 IR DARF post-hoc treatment is the correct call.** Mira's ruling that DSR/PBO are computed on **gross PnL** (research convention, Bailey-LdP 2014 §3) preserves the closed-form null distribution. Beckett endorses — applying IR per-trade would non-linearly distort the per-trade return distribution and corrupt the Sharpe ratio's null-hypothesis comparability.

**§5.3 PnL formula:**
```
gross_pnl_rs[i] = (exit_price - entry_price) * sign(direction) * n_contracts * WDO_MULTIPLIER
                  - 2 * n_contracts * (brokerage_per_contract_side_rs + exchange_fees_per_contract_side_rs)
```
With `WDO_MULTIPLIER = 10.00` BRL/point (DOMAIN_GLOSSARY single-source per fix G005, atlas product spec confirms). With `n_contracts = 1`, brokerage = 0.00, exchange = 1.23: per-trade fixed cost = `2 × 1 × (0.00 + 1.23) = 2.46 BRL` round-trip. Slippage already baked into entry/exit via `_slippage()`. Reportable as 4 separate line items in N6 §X.

**§5.4 atlas SHA + version locks:** `from_engine_config(...)` (exec_backtest.py:114-130) validates atlas SHA256 (LF-normalized) + `atlas_version` at config-load time. Closure inherits validated `BacktestCosts`, no atlas-side validation inside closure body — correct separation.

**N6 line items reportable (cost atlas wiring breakdown):**
1. `gross_pnl_rs` per trade (entry, exit, side, direction, n_contracts, WDO_MULTIPLIER) — YES
2. `brokerage_cost_rs` per trade (2 sides × per_contract_one_way) — YES
3. `exchange_fees_cost_rs` per trade (2 sides × per_contract_one_way) — YES
4. `slippage_cost_signed_ticks` per trade (Roll model + 1 tick worst-case) — YES via `BacktestBroker._slippage()` already coded
5. `monthly_aggregated_gross_pnl_rs` + `monthly_ir_net_pnl_rs` (post-hoc IR DARF table) — YES, separate aggregation layer

**Cost atlas SHA lock visible in determinism stamp:** N5 stamp shows `engine_config_sha256: 9a97e8f8734cbb8cd35362d7a336fe876a5b16257a170b9436d9e868ad946c81` — the cost atlas SHA is currently **subsumed** under engine_config_sha256 (per N5 §5 limitation note). Beckett records this as an **N6 housekeeping refinement** (separate `cost_atlas_sha256` field in stamp) — non-blocking, but tracked.

---

## §6 Per-fold rebuild — Aria T0b decision binding (defer)

**Status: NOTED — Aria authority decides among Options A / B / C.**

Beckett position (from consumer perspective, NON-BINDING on Aria):

- **Option B (factory pattern, Mira preferred)** is most consistent with Beckett's `expertise.simulator_architecture.event_loop` orchestrator-owned-lifecycle mental model. Each fold's closure is scoped to a single split, identical to how Beckett's CPCV literature (Lopez de Prado AFML §7 + §12) describes the canonical pattern.
- **Option A (provider injected)** introduces a closure that is no longer pure (computes percentiles internally). Beckett's `*fill-audit` becomes harder because the closure has implicit train-events capture per call — auditable, but more brittle.
- **Option C (caller-managed)** loses bulk parallelization potential; future N7+ runs that span multiple in-sample windows or holdout simulations would need to duplicate the orchestration.

**Beckett N6 verification works for ALL THREE options** — the closure-side invariant (`percentiles_state.as_of_date == first_test_session_of_fold`) is the same regardless of how the engine wires the rebuild. This sign-off is therefore **architecture-agnostic**.

Aria T0b binding is recorded; Beckett does not gate on which option is chosen.

---

## §7 Toy benchmark — test contract executable?

**Verdict: PASS.**

§7.1 Toy strategy A (pure noise, μ=0, σ=1, N=45×20, n_trials=5) and §7.2 Toy strategy B (small edge, μ=0.20, σ=1) are anchored to Bailey-LdP 2014 §3 + Table 2 (DSR primary literature). Expected metrics:

| Toy | DSR | PBO | Tolerance |
|---|---|---|---|
| A (no edge) | ≈ 0.5 | ≈ 0.5 | ±0.05 |
| B (small edge) | > 0.95 | < 0.25 | ±0.05 |

§7.3 test contract at `tests/cpcv_harness/test_toy_benchmark_bailey_lopez_de_prado_2014.py` (NEW file) is **executable and well-defined**:
- Synthetic returns drawn iid from `np.random.default_rng(seed=42).normal(...)` — deterministic.
- Same `compute_full_report` aggregation layer (already wired in `scripts/run_cpcv_dry_run.py:743`) — same DSR/PBO formulas as N5.
- `assert |DSR - 0.5| < 0.05` for toy A; `assert DSR > 0.95` for toy B — clean acceptance gates.

**Why this matters for Beckett (False-NO_GO protection):** N5 reported `DSR=0.5, PBO=0.5, IC=0` — those are the **exact same numbers Bailey-LdP 2014 predicts under H0 (no skill)**. The N5 stub-degenerate signature is mathematically indistinguishable from "real strategy with no edge" without running toy A first. Toy A executed BEFORE real-data N6 confirms harness correctness independent of strategy edge — protects against the inverse failure mode (real edge masked by harness bug).

Mira's pre-flight sanity ordering (toy passes → THEN compute DSR/PBO on real data) is the right discipline. Beckett supports this gate ordering.

**Test contract executable as specified:** YES. Dex T1 implementation can write the test file as part of the impl PR (story T002.1.bis acceptance criteria reference `tests/cpcv_harness/test_toy_benchmark_bailey_lopez_de_prado_2014.py` — Beckett expects this test to be in the PR diff alongside the closure body change).

---

## N6 reproducibility checklist

| Check | Status | Notes |
|---|---|---|
| Determinism stamp (seed 42 + sha-stamped) feasible? | **YES** | N5 stamp confirms `seed=42, spec_sha256=72261326..., dataset_sha256=e3962eb8..., engine_config_sha256=9a97e8f8..., cpcv_config_sha256=d2ea61f2...`. Real `make_backtest_fn` does NOT change this surface — same fields, same Python/numpy/pandas versions pinned. |
| Cost atlas SHA lock visible in determinism stamp? | **PARTIAL** | Currently subsumed under `engine_config_sha256` (N5 §5 limitation note). Beckett N6 housekeeping recommendation: emit separate `cost_atlas_sha256` field in stamp (non-blocking for T002.1.bis sign-off; tracked for follow-up). |
| Bonferroni n_trials=5 enforced? | **YES** | `n_trials_source: docs/ml/research-log.md@359844528d7768b9ea7642708d32d13fc08ab9ba` — N5 stamp confirms; T002.1.bis spec §1.2 preserves `n_trials=5` with engineering-refactor research-log `n_trials: 0` (no new family-wise hypothesis test). |
| Wall-time projection (vs N5 baseline 4.381 s)? | **PROJECTED 6-15 s** | Real `make_backtest_fn` traverses tick tape per event for triple-barrier evaluation. N5 stub returns identity-zero in O(1) per call. Conservative estimate: 2-4× wall-time inflation for real closure (still <<300 s budget = 60-100× headroom). Beckett will instrument N6 with phase telemetry (`fanout_duration_ms`) to confirm. |
| Peak RSS projection (vs N5 baseline 0.140 GiB)? | **PROJECTED 0.20-0.40 GiB** | Closure adds intermediate per-trial state (Features dataclass + Signal + barrier-eval scratch). Conservative: 1.5-3× memory inflation. Far below 6 GiB soft cap (15-30× headroom). |

**N6 reproducibility verdict: ACHIEVABLE.** The four reproducibility footprint axes (seed, spec sha, dataset sha, engine config sha) are unchanged by §1-§7. Wall-time and peak RSS will inflate but remain orders of magnitude below the hard caps.

---

## Anti-Article-IV self-audit (consumer-side)

| Guard | Status |
|---|---|
| No spec modification | PASS — review-only access to `docs/ml/specs/T002.1.bis-make-backtest-fn-spec.md` |
| No source code modification | PASS — read-only review of `cpcv_harness.py`, `signal_rule.py`, `feature_computer.py`, `exec_backtest.py`, `percentiles_126d_builder.py`, `scripts/run_cpcv_dry_run.py` |
| No git push | PASS — Article II Gage exclusive (Article IV trace: agent-authority.md) |
| Sign-off scope: consumer authority only | PASS — Beckett does NOT pre-empt Aria T0b architecture decision (§6 deferred); Beckett does NOT pre-empt Riven T0d cost-atlas custody verification (§5 wiring only); Beckett does NOT pre-empt Pax T0e 10-point story validation |
| No invented thresholds | PASS — every numeric in this sign-off traces to spec §X / atlas L## / N5 §X / Lopez de Prado primary lit |

---

## Verdict

**HANDSHAKE_OK_FOR_DEX_START** (T0c consumer authority sign-off complete).

Conditional on T0b (Aria architecture choice §6) + T0d (Riven cost-atlas custody §5) + T0e (Pax 10-point story validate) being recorded GO before Dex begins T1 impl, per story Anti-Article-IV Guard #1 ("Dex T1 impl is **strictly forbidden** until all four T0b-T0e sign-offs are recorded").

**No HOLD concerns.** Two non-blocking housekeeping notes:
1. N6 housekeeping: separate `cost_atlas_sha256` field in determinism stamp (non-blocking, tracked).
2. N6 instrumentation: phase telemetry for `fanout_duration_ms` already wired (line 727-731 of `scripts/run_cpcv_dry_run.py`); Beckett will emit projected vs actual wall-time + peak RSS comparison in N6 report.

**Sections covered:** §1 (signal-to-trade) PASS · §2 (triple-barrier) PASS · §3 (P60/P20/P80 filters) PASS · §4 (position sizing) PASS · §5 (cost atlas wiring) PASS · §6 (per-fold rebuild) NOTED — Aria T0b decides · §7 (toy benchmark) PASS.

**N6 anchor commitment:** post-Dex T1 impl, Beckett re-runs `scripts/run_cpcv_dry_run.py` with identical CLI as N5 (`--spec docs/ml/specs/T002-end-of-day-inventory-unwind-wdo-v0.2.0.yaml --dry-run --in-sample-start 2024-08-22 --in-sample-end 2025-06-30`) and produces N6 smoke report at `docs/backtest/T002-beckett-t11-bis-smoke-report-N6-YYYY-MM-DD.md` with: (a) toy A/B benchmark results from §7 test contract, (b) real-PnL line items per §5 (gross, brokerage, exchange, slippage, IR-net post-hoc), (c) determinism stamp diff vs N5 confirming reproducibility, (d) wall-time + peak RSS deltas, (e) `*fill-audit` over sample of 100 fills confirming §2.4 anti-leak invariant + §3.4 D-1 invariant.

---

## Beckett signature

```
Reviewer:        Beckett (@backtester) — The Simulator
Authority:       consumer authority + N6 empirical anchor + simulation realism
Spec reviewed:   docs/ml/specs/T002.1.bis-make-backtest-fn-spec.md (Mira T0a)
Atlas reviewed:  docs/backtest/nova-cost-atlas.yaml v1.0.0 (Nova compiled, Sable APPROVED_WITH_CONDITIONS)
N5 baseline:     docs/backtest/T002-beckett-t11-bis-smoke-report-N5-2026-04-28.md (run_id cpcv-dryrun-auto-20260428-c5d5a708acda)
Date (BRT):      2026-04-28
Status:          HANDSHAKE_OK_FOR_DEX_START — T0c sign-off recorded
Article IV:      NO INVENTION — every clause traced to spec §X / atlas L## / N5 §X / primary lit
Anti-leak:       §2.4 vertical bound (exit_ts ∈ [entry_ts, 17:55]) + §3.4 D-1 invariant verified consumer-side
Reproducibility: seed=42 + 4-axis SHA stamping unchanged by §1-§7; wall-time and peak RSS within hard caps
Architecture:    §6 Option A/B/C decision deferred to Aria T0b — Beckett architecture-agnostic
Sizing:          §4.1 fixed n_contracts=1 endorsed (Riven T0d Gate 5 owns sizing-magnitude clearance, NOT this story)
Tax:             §5.2 gross PnL in closure + post-hoc monthly IR table — Mira ruling endorsed
Toy benchmark:   §7 Bailey-LdP 2014 §3 + Table 2 anchor — False-NO_GO protection ordering endorsed
Next handoff:    T0d Riven (@risk-manager) cost-atlas custody confirmation + T0e Pax (@po) 10-point validate
```

— Beckett, reencenando o passado 🎞️
